# ── Required ──────────────────────────────────────────────────────────
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# ── Dev Mode (remove before production) ──────────────────────────────
# Set to "true" to bypass security friction during development:
# - Turnstile CAPTCHA skipped (dummy token)
# - Reauth dialog auto-verifies
# - Email verification gate skipped
# - Inactivity timeout extended to 24h
# - Password minimum reduced to 6 chars
# ⚠️ MUST be removed before production (see preproduction-checklist.md PP-006)
# VITE_DEV_MODE="true"

# ── Cloudflare Turnstile ─────────────────────────────────────────────
# Always-pass test key for development (no Cloudflare account needed):
#   VITE_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
# Replace with real site key for production.
VITE_TURNSTILE_SITE_KEY="your-turnstile-site-key"

# ── Optional ─────────────────────────────────────────────────────────
VITE_SENTRY_DSN="your-sentry-dsn"

# ── Quick Setup ──────────────────────────────────────────────────────
# 1. cp .env.example .env
# 2. Fill in VITE_SUPABASE_* values from your Supabase dashboard
# 3. Uncomment VITE_DEV_MODE="true" for dev bypass
# 4. npm run dev

# ── Alpaca Paper Trading (FP-006 sub-step 6.7) ───────────────────────
# Generate at https://app.alpaca.markets/paper/dashboard/overview
# Used by: src/features/longshort/services/broker/alpaca/
# Consumed via Deno.env in fetcher implementations + connection-test CLI.
ALPACA_PAPER_KEY=
ALPACA_PAPER_SECRET=
