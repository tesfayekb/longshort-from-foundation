/**
 * Page wrapper for /trading/overshoot/universe (FP-069 W4.g, ACT-465.g).
 * T1: imports ONLY from the strategy façade.
 */
import { OvershootUniversePage } from '@/features/overshoot';
export default OvershootUniversePage;