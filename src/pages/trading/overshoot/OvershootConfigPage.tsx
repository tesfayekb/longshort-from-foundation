/**
 * Page wrapper for /trading/overshoot/config (FP-069 W4.f, ACT-465.f).
 * T1: imports ONLY from the strategy façade.
 */
import { OvershootConfigPage } from '@/features/overshoot';
export default OvershootConfigPage;