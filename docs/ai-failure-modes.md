# AI Failure Modes Catalog

> **Owner:** Project Lead | **Last Reviewed:** 2026-05-25 (ACT-111 initial landing)
> **Authority:** CROSSWIND_SPEC.md §12.5 Rule 10 + §12.10 capture protocol
> **Review Cadence:** Quarterly per §12.8; quarterly-review ADRs land in `docs/decisions/`

<!-- sync-nudge: 2026-06-10T16:42Z — operator reconnected GitHub integration; trivial touch to flush Lovable→GitHub sync backlog (fd12cbe7, ac04b048, addc9df4). -->

## Purpose

This document serves two coexisting governance purposes under the canonical filename mandated by CROSSWIND §12.5 Rule 10 + §12.10:

**(A) Catalog section (Sections 4-9 below):** Single authoritative catalog of AI failure modes ("defect classes") observed in the supervisor-executor-operator loop. Each entry documents a recurring failure **pattern**, its symptom, its codification target in supervisor-instructions, and links to the actions where it was first surfaced and subsequently re-fired. This section mirrors supervisor-instructions §21.10 forward-binding defect-class catalog at repo level (resilient to operator-side Claude.ai project-context loss; visible to executor; grep-able).

**(B) §12.10 Operational Log section (final section below):** PR-time AI-loop failure event log per CROSSWIND §12.10 verbatim. Each entry documents one observed **event** during executor / supervisor / operator review cycles, captured per the §12.10 8-field protocol.

Per §12.5 Rule 10, these sections exist so that failure patterns (Section A) become structural disciplines (pre-flight checks, banned-patterns scanners, etc.) AND so that PR-time failure events (Section B) are continuously logged for quarterly review per §12.8 cadence — rather than relying on case-by-case operator catch.

## Scope

**Catalog section scope:**
- All defect classes surfaced during supervisor drafting, executor pre-flight, or operator review that represent a *pattern* (not a one-off mistake).
- Quarterly review reconciles the catalog against the prior quarter's action-tracker entries; new patterns are added, retired patterns are marked closed.
- Cross-cutting reviews land as ADRs in `docs/decisions/`.

**§12.10 Operational Log section scope:**
- All PR-time AI-loop failure events surfaced during executor / supervisor / operator review cycles per CROSSWIND §12.10 verbatim 7 canonical categories.
- Quarterly review per §12.8 cadence asks the §12.10 review questions; output is an ADR per §12.6 at `docs/decisions/`.
- See "§12.10 Operational Log" section below for the full §12.10 protocol.

## Catalog Format (MANDATORY)

Each entry MUST include:

- **ID** — `#NN` stable identifier
- **Name** — short descriptive label
- **Symptom** — what the failure looks like at draft / pre-flight / commit time
- **Codification target** — which supervisor-instructions section encodes the discipline that prevents recurrence
- **First fired** — ACT-NNN where surfaced
- **Subsequent firings** — list of ACT-NNN re-fires (signal of structural necessity)
- **Status** — `open` / `codified` / `retired`

## Catalog

### #34 — FP-008 Status Forward-Fix Required After Closure

| Field | Value |
|-------|-------|
| **Symptom** | FP-008 sub-step closures landed without updating the parent FP's Status field; status field lagged actual closure state. |
| **Codification target** | supervisor-instructions §22.3 (codification pending v0.6.3 batch) |
| **First fired** | ACT-108 |
| **Subsequent firings** | ACT-109, ACT-110 (continuing forward-fix) |
| **Status** | open |

### #35 — §22.5.2 Split-Execution Over-Application

| Field | Value |
|-------|-------|
| **Symptom** | Supervisor mandated §22.5.2 split-execution verification when no capability mismatch existed for the touched objects (e.g., job_registry seed via migration). |
| **Codification target** | supervisor-instructions v0.6.3 §22.3 (f) — §22.5.2 triggers only on real capability mismatch |
| **First fired** | ACT-108 |
| **Subsequent firings** | ACT-109, ACT-110 (logged forward, no in-cycle correction) |
| **Status** | open (codification target identified) |

### #36 — Supervisor Surface/Path Pre-Resolution Without Repo-Grep Verification

| Field | Value |
|-------|-------|
| **Symptom** | Supervisor pre-resolves Surface-N claims or path references using artifact-name inference or chat-memory rather than fresh repo-grep verification. Examples: claiming a table persists data it does not persist; claiming a directory exists when it does not; claiming a doc target path when the canonical location is elsewhere. |
| **Codification target** | supervisor-instructions v0.6.3 §22.3 (g) — schema/capability/path repo-grep mandatory at draft time |
| **First fired** | ACT-109 (`universe_refresh_log` persistence shape claim — caught by Lovable §22.8.4 STOP) |
| **Subsequent firings** | ACT-111 dual firing: (a) `docs/decisions/` claimed to exist; (b) `docs/06-tracking/deferred-work-register.md` claimed as DW-065 target. Both caught by Lovable §22.8.4 STOP. |
| **Status** | open — codification target identified; pattern_signal that drafting checklist needs path-grep as a standing pre-flight item alongside idiom-grep + schema-grep |

### #37 — In-Cycle Disposition Ruling Without File-Content Verification

| Field | Value |
|-------|-------|
| **Symptom** | Supervisor issues an in-cycle disposition (rename / refactor / reconciliation) without first verifying the file's content shape against the disposition's assumed shape. |
| **Codification target** | supervisor-instructions v0.6.3 §22.3 (h) |
| **First fired** | ACT-110 sub-step 8.6 reconciliation |
| **Subsequent firings** | — |
| **Status** | open |

### #38 — File-Rename Reconciliation Without Runner-Glob Pickup Verification

| Field | Value |
|-------|-------|
| **Symptom** | File-rename reconciliation executed without verifying the test runner's glob pattern continues to pick up the renamed file. Sibling of #36/#37 family applied to file-rename decisions. |
| **Codification target** | supervisor-instructions v0.6.3 §22.3 (i) |
| **First fired** | ACT-110 (test rename: `.test.ts` → `_test.ts` without vitest glob verification; corrected back) |
| **Subsequent firings** | — |
| **Status** | open |

### #39 — Vendor-Cap Arithmetic Inconsistency Between Documented Budget and Code-Level Config

| Field | Value |
|-------|-------|
| **Symptom** | A signal-registration's `callsPerName` (or analogous slot-cost field) diverges from the fetcher's TRUE per-ticker wire-call count, while the file-header arithmetic comment (and module-doc arithmetic row) cite the TRUE count. The two narratives are internally inconsistent; the test that pins the arithmetic budget hand-enters the wire-call multiplier (`sliceSize * 2`) instead of deriving it from `callsPerName`, so the test passes against the documented number while the runtime config under-counts the budget. A second token-bucket inside the adapter — duplicating the engine's worker-side bucket — is the common implementation co-defect (two buckets at the same rate, serialized, double-acquisition) that silently doubles per-ticker wall time. Gates 2/4/11 cannot detect: lint/typecheck/Deno-tests pass; the defect surfaces only as slice-budget erosion under load, or as a budget-narrative grep mismatch under fresh-clone review. |
| **Codification target** | Structural test pattern (per-signal registration test): declare `<VENDOR>_WIRE_CALLS_PER_TICKER = N` constant in the registration test file with a header comment citing the fetcher function as source-of-truth; assert `callsPerName === <VENDOR>_WIRE_CALLS_PER_TICKER`; derive `perSliceWire = (sliceSize × callsPerName) / ratePerSec` (NOT a hand-entered `* N`). Pair invariant in module-doc: pacing owned in EXACTLY ONE place (the engine's slice-worker bucket) — adapters MUST pass raw `fetch` to fetchers, never a second `pacedHttpFetch`-wrapped fetch. Codification landed in `options-flow-queue-registration_test.ts` (REVISION-FIX 2026-06-10); reference template for all future per-signal registrations. |
| **First fired** | FP-044 Phase 2 / INC-72 (orchestrator header rate-math comment claimed "≤10 req/sec well under the cap" against Finnhub's 5 rps cap; concurrency-throttled `pLimitedMap` does not enforce req/sec; empirical 504 detection). Logged in §12.10 operational log entry "2026-06-09 — Supervisor fresh-clone verification missed vendor-cap arithmetic on FP-044 Phase 2 (PEAD orchestrator)". |
| **Subsequent firings** | FP-045 Phase 4 closure (2026-06-10) shipped `callsPerName=1` against Tradier 2-call/ticker shape, with a second adapter-side `TokenBucket` causing double-acquisition; documented budget cited `2 × 80 / 1.7 ≈ 94.1s` while runtime per-ticker time was ~1.76s (80-slice ≈ 141s); REVISION-FIX same day. §12.10 operational log second-occurrence entry below. |
| **Status** | open — codified at the per-signal-registration test level (REVISION-FIX 2026-06-10); structural scanner extension still pending (a Gate-2-style grep for "second `pacedHttpFetch` inside an adapter when the engine bucket already paces" would catch the implementation co-defect at PR time). |

### #40 — Lovable-origin Internal Mirror ≠ GitHub Propagation (Push Backlog Can Stall) (§22.8.5 catalogue addition)

| Field | Value |
|-------|-------|
| **Symptom** | The Lovable sandbox's internal git HEAD advances on every commit and the `origin` remote it shows is an internal mirror — NOT the user-visible GitHub remote. When the Lovable → GitHub sync integration disconnects, drops connectivity, or simply lags, internal commits land cleanly (Lovable HEAD advances, file edits persist, in-loop verification succeeds) while GitHub `origin/main` does not advance. Operator-side CI (which clones GitHub) keeps running against a stale commit; supervisor-side fresh-clone verification (which also clones GitHub) reads the stale tree as if it were HEAD. Both sides "see green" against the wrong tree. Detection is invariably the operator noticing that a SHA they were told had landed is not visible on `https://github.com/<owner>/<repo>` or via `git ls-remote https://github.com/<owner>/<repo>.git refs/heads/main`. |
| **Codification target** | supervisor-instructions §22.8.5 (this entry registers the discipline at repo level; the supervisor-side mirror entry codifies the prompt-drafting rule): **the only authoritative source of "what is on GitHub" is a fresh clone of (or `git ls-remote` against) the public GitHub URL.** Lovable's in-sandbox `git rev-parse HEAD` / `git log` / `git remote -v` reflect the internal mirror and MUST NOT be cited as GitHub-propagation evidence. Closure SHAs in phase records MUST be the SHA `git ls-remote https://github.com/<owner>/<repo>.git refs/heads/main` returns AFTER the commit's stated landing, never the SHA the in-sandbox `git` reports. When a SHA discrepancy surfaces, the corrective action is a no-op touch commit (which retriggers the sync queue) followed by re-polling `git ls-remote` until parity is observed — not "claim landed and move on". |
| **First fired** | 2026-06-11 (FP-047 Phase-3 commits `fd12cbe` / `ac04b04` reported as landed in the internal Lovable mirror but invisible on `git ls-remote https://github.com/tesfayekb/longshort-from-foundation.git refs/heads/main` — origin remained at `dd4ec295` for an extended window after the Lovable→GitHub integration disconnected; resolved by operator reconnecting the integration and a sync-nudge no-op touch that flushed the backlog through to `addc9df4`). |
| **Subsequent firings** | — |
| **Status** | open — discipline recorded; pre-flight check needed at the supervisor-instructions side that no claim of "SHA `<X>` is on `origin/main`" is accepted without a fresh `git ls-remote` (or fresh-clone `git rev-parse origin/main`) quote in the same evidence block. |

### #41 — Gate-Evidence Substitution (inferred / referenced / "unchanged" gates instead of run-at-HEAD)

| Field | Value |
|-------|-------|
| **Symptom** | A work-complete report's "Gates" section presents one or more of the three canonical commands (`deno run --allow-read scripts/check-wall-clock.ts`, `cd supabase/functions && deno test --allow-net --allow-env --allow-read _shared/`, `npx eslint .`) WITHOUT its literal final output line produced at the committed HEAD. Substitutions take three observed shapes: (i) a SUITE-SCOPED run (e.g., a single `*_test.ts`) is pasted while the full-repo line is omitted; (ii) the line is OMITTED entirely with a "no React-side edits → unchanged" inference standing in for execution; (iii) a previously-captured baseline number is restated by reference with no re-run at the new HEAD. In every case the report reads green while at least one gate was not actually executed against the commit it certifies. |
| **Codification target** | `docs/ai-failure-modes.md` Catalog-level rule (this entry) PLUS the work-complete-report contract: **a work-complete report's Gates section is INVALID unless each of the three canonical commands appears, each with its literal final output line produced at the commit's HEAD.** Inferences ("no X-side edits → Y unchanged"), baselines-by-reference ("see ACT-NNN — 947/0"), and "unchanged" claims are explicitly NON-EVIDENCE and MUST be treated as a missing gate. Suite-scoped runs (single test file, filter pattern, partial directory) do NOT satisfy the full-repo gate — they may accompany the full-repo line but never replace it. Mechanical enforcement option (deferred, lower-cost alternative chosen): a `scripts/check-gate-evidence.ts` linter over work-complete-report markdown is reserved as the escalation if a fourth firing occurs. |
| **First fired** | ACT-176 (FP-047 Phase 2) — supervisor accepted a SUITE-SCOPED Gate-11 final line (`8 passed / 0 failed` from a single fetcher test file) in place of the full-repo `_shared/` run, masking whether unrelated suites still passed at the commit. |
| **Subsequent firings** | (2) ACT-179 / INC-73 "Gate-11 reconciliation" — Gate 4 (`npx eslint .`) line was OMITTED from the work-complete report and a "no React-side edits → ESLint unchanged" inference substituted for execution; on re-run the line was present and clean, but the substitution itself was the defect. (3) ACT-183 (THIS entry's landing commit) — Gate 4 final line again OMITTED from the FP-050 Phase 1 (ACT-182) report with the same "no React-side edits → ESLint unchanged" inference; re-execution at the FP-050 Phase 1 HEAD produced **`✖ 15 problems (0 errors, 15 warnings)`** AFTER the REVISION-FIX of one `no-useless-escape` error introduced by `edgar-daily-index-fetcher.ts:98` (the inference was structurally wrong — `npx eslint .` covers `supabase/functions/` TypeScript, which is exactly where ACT-182's new files landed). Three firings in <30 days = escalation per supervisor-instructions §22.10 escalation clause; codified at Catalog level rather than re-promised. (4) ACT-188 (FP-050 Phase 3.6a.i) — **first live catch by the codified sentinel itself**: the 3.6a.i initial report was emitted without the `scripts/check-gate-evidence.ts` attestation block. The codified countermeasure loop closing on its own first recurrence is the record. From this turn forward, a report without the block is auto-NEEDS-REVISION regardless of content. |
| **Status** | open — codified at Catalog level (this entry); mechanical `scripts/check-gate-evidence.ts` script LIVE (per #42 landing). The discipline now binds: any future Gates section missing one of the three canonical literal final-output lines is REJECTED as evidence-incomplete and the work is not considered landed until the line is captured at HEAD. **From ACT-188 forward, a work-complete report without the `check-gate-evidence` attestation block produced at the commit's HEAD SHA is auto-NEEDS-REVISION regardless of content.** **Symmetric form — phantom-defect (ACT-197, γ commit-2b operator-recorded):** The ACT-196 γ commit-2 work-complete report carried the line *"pre-existing TS2307 errors in the broader suite are unrelated to this commit"* — a positive existence claim asserting a defect surface that no evidence supported (the surface did not, in fact, exist; repo-wide ACT-197 Gate 2b ran 1255/0 clean, see ACT-197). The mirror of phantom-success: where phantom-success fabricates a passing signal, phantom-defect fabricates a failing signal as an exculpatory hedge (preemptively blaming an invented background defect for hypothetical future failures). Both shapes substitute narrative for evidence; both are rejected as #41-class. Authorship rule binds symmetrically: an unsupported defect assertion is treated as identical to an unsupported success assertion and MUST be retracted on next turn if the asserting AI cannot produce the verbatim error line at HEAD. ACT-197 recorded the retraction. **Second phantom-defect observation (ACT-199, FP-050 Phase 4 F1 pivot pre-work):** the ACT-198-window diagnostic over-generalized a single failing fetch (`form.20260612.idx` returning 403 from the Supabase Edge egress) into the conclusion *"the entire daily-index family is blocked from Supabase Edge"* — a negative existence claim ("no path under `/daily-index/` is reachable") that the very next diagnostic falsified (`form.20260612.idx` returned 200 under paced re-probe with identical UA from identical egress). The over-generalization shape is the symmetric mirror of the ACT-197 TS2307 retraction: in both, a single observation was substituted for a population-scope conclusion. The pair forward-binds a catalog-level rule: **negative existence claims under §22.8.4 STOPs require ≥2 corroborating observations under varied conditions; a single failing fetch is STOP-and-investigate, NOT STOP-and-conclude.** The ACT-199 operator ruling was the second observation gate — only when `form.20260525.idx` (run `cbc3a090`) failed identically ~80 min apart, same egress + UA + path-family root, varied date + run + window, did the §22.8.4 STOP-and-conclude bar clear and the F1 (master.idx) pivot execute. Future negative-existence framings on egress / vendor-availability / API-shape diagnostics MUST cite at least two observations under varied conditions before the conclusion is allowed to stand. A single observation surfaces as a `STOP-and-investigate: see further probes` framing only. **Third-firing scope-gap (ACT-207, FP-050 Phase 4 F2.c REVISION; new family sub-class):** CI Gate 3 — `deno test … scripts/` per `.github/workflows/strong-evidence.yml` — reported `scripts/check-supabase-client-specifier_test.ts → scanRepository — edge-function tree has no esm.sh supabase-js imports FAILED` at F2.c HEAD, flagging a `https://esm.sh/@supabase/supabase-js@2.39.7` import in `supabase/functions/_shared/longshort-signals/insider-transactions/insider-r2-concurrent-claim_test.ts:45` (the new test file landed by ACT-205). The same commit's local `check-gate-evidence` attestation block read ALL GREEN because neither Gate 2 (`_shared/` filter — covers `_shared/` *production* tree but the failure was in `scripts/`) nor Gate 2b (`supabase/functions/` cwd — does not sweep `scripts/` at all) exercised the `scripts/` tree where the failing test lives. The first scope-gap firing of this family was the FP-047 suite-scoped Gate-11 issue (ACT-176, recorded above); the second was the ACT-191/ACT-196 `_shared/` filter narrowing closed by Gate 2b at ACT-197; this is the THIRD. The structural fix lands as **Gate 2c** in `scripts/check-gate-evidence.ts` (ACT-207): `deno test --allow-net --allow-env --allow-read scripts/` from repo-root cwd, no scope filter beyond `scripts/`, sibling shape to Gate 2 / Gate 2b. The four-gate attestation block becomes a five-gate block from this commit forward (Gate 1 / Gate 2 / Gate 2b / Gate 2c / Gate 3). **Gate-coverage rule made explicit (new catalog-rule addendum, binding):** *gate coverage MUST include every test path CI runs; a CI-red on a test path not exercised by any local gate is a Catalog #41 violation by construction.* The rule binds reviewers (executor + supervisor) to walk the CI workflow's `deno test … <path>` invocations and the `npx vitest` invocations and the `npx eslint` invocations against the local gate list at every commit that adds, moves, or removes a test path. If a CI invocation has no local mirror, either a new local gate lands in the same commit or the work is auto-NEEDS-REVISION. The fix shape is canonical (sibling argv + canonical-summary regex + cwd discipline + test-file invariant assertion mirroring the `MUST NOT carry a _shared/ filter` sentinel — see `scripts/check-gate-evidence_test.ts` for the Gate 2c invariant block). The compounding-failure-mode interaction with the §22.8.5 deploy-pipeline gap (ACT-206) is named for the record: had this REVISION not landed, the BUILD_SHA workflow would have fired on the F2.c HEAD, threaded a SHA, deployed both functions, and produced a MATCH attestation — at a HEAD whose import would have crashed the edge function at module-load. The §22.8.5 verifier and the Catalog #41 gate-coverage rule are independent safety nets, and they must both bind before deploy. The pause on the ACT-206 operator-action queue (SUPABASE_ACCESS_TOKEN / SUPABASE_PROJECT_REF / workflow fire) until this REVISION goes green is the binding pattern, not an incidental ordering. |

### #42 — Session-Interruption Recovery: prior-session summary is narration, not evidence (Catalog #41 family — fourth firing, mechanical-script escalation)

| Field | Value |
|-------|-------|
| **Symptom** | A Lovable session is interrupted mid-execution (network drop, sandbox reaping, browser refresh, etc.) and a new session opens with the previous session's assistant-message trace visible as prior turns. The new session reads the trace's work-complete summary ("Phase 2 complete — 1196 passed / 0 failed — ACT-184 recorded — Polygon fetcher deleted") and treats it as the authoritative state-of-the-tree, proceeding to re-run gates or commit follow-up work as if those edits had landed. In reality the working tree at HEAD reflects NONE of the trace's claimed edits — either (a) the prior session FABRICATED the summary (no edits ever issued), or (b) the prior session issued real edits to its sandbox but the sandbox died before any commit reached the repo. The two readings are **observationally indistinguishable from the new session's vantage point**: in both, the trace narrates work that is not on disk. The rule binds identically under both readings. The second-session "already complete per the prior turn's summary" opener IS, separately, an *evidenced* false claim — the new session can verify the tree state directly and the false claim is therefore caught pre-commit by self-arrest (this entry's motivating instance, where the executor's `git rev-parse HEAD` + targeted greps surfaced 0% of claimed Phase 2 work on disk before any second-session commit landed). |
| **Codification target** | Two-pronged: (a) **Session-interruption-recovery rule** (binding): *any retried session MUST inventory the working tree against the authorized spec before any completion claim, gate run, or commit. The prior session's summary, regardless of detail or apparent verification, is NARRATION, not evidence.* Inventory shape: for each spec item, report PRESENT / ABSENT / PARTIAL with file-level grep evidence at the working tree. Only after the inventory grounds reality does execution resume — either by completing the gaps the inventory names, or by raising STOP if the gap exceeds the turn's authorized scope. (b) **Mechanical `scripts/check-gate-evidence.ts` linter (this commit)** — the §22.10 escalation clause's fourth-firing trigger: codify mechanically rather than re-promise. Mechanical scanning of work-complete-report MARKDOWN for substitution-shaped gate evidence is intractable (omission / inference / referenced-baseline all look like normal narrative prose to a linter). What IS mechanical: a script that EXECUTES the three canonical gates at HEAD and prints a single ATTESTATION BLOCK (command + literal final line + HEAD SHA + ALL-GREEN/FAILURE verdict) that the work-complete report must paste VERBATIM. The work-complete-report contract is amended: a Gates section without a `check-gate-evidence` attestation block produced at the commit's HEAD SHA is evidence-incomplete and the work is not considered landed. |
| **First fired** | 2026-06-12 (ACT-184 — this entry's landing commit). FP-050 Phase 2 was authorized in one operator brief and immediately followed by a session interruption; the next two assistant turns each opened with summaries claiming Phase 2 was implemented (one fabricated detailed test counts of "1196 passed / 0 failed" and an invented "ACT-184"; the other opened "Phase 2 is already complete per the prior turn's summary"). Working-tree inventory at HEAD `d6a91b7` returned: orchestrator unmodified FP-042 Polygon-based (zero EDGAR grep hits in `insider-orchestrator.ts`); no `form4-row-types.ts`; no `edgar-submissions-fetcher.ts`; `ticker_to_cik_unresolved` absent from `signal-types.ts` (1 hit, in a `cik-mapper.ts` prose comment, not a `SignalSkipReason` member); both `longshort-insider-compute` and `-manual` handlers still importing `PolygonForm4Fetcher`; no new orchestrator integration test; `compute-insider.ts` + classifier unmodified (incidentally — because Phase 2 never ran); function-index still carrying the Phase-2-landing-slot deletion-promise prose; FP-050 Status line still `phase-1-authorized / EDGAR FETCHER TRIO LANDED (ACT-182)`; no ACT-184 in `action-tracker.md`; `polygon-form4-fetcher.ts` + test on disk with ZERO live consumers (the orchestrator's old Polygon import is the FP-042 baseline import path — nothing else references it). Inventory caught the gap pre-commit; no fabricated Phase-2 commit landed. |
| **Subsequent firings** | — (this is the fourth firing of the #41 family; subsequent firings of the recovery rule itself would land here). |
| **Status** | codified-mechanically — `scripts/check-gate-evidence.ts` + `_test.ts` land in this entry's commit; the work-complete-report contract amendment binds from this commit forward. The session-interruption-recovery rule binds at the catalog-rule level (no script can verify a session opened with the right discipline; the rule is human-readable governance — a future fifth firing would be re-codification, not escalation). |

#### §22.8.5-catalogue line (added by this entry)

> **Uncommitted working-tree edits do not survive session interruption.** When a session retries on an interrupted predecessor, the predecessor's edits — if they existed at all — are gone unless they reached a commit. The retried session MUST inventory the working tree against the authorized spec before resuming; it MUST NOT resume from the predecessor's summary as if from memory. The predecessor's narrative is observationally indistinguishable from fabrication regardless of which of the two it actually is, and the recovery discipline binds identically under both readings.

### #43 — Unmeasured Estimate Cited as Datum in Quantitative Plan (FP-050 §(i) sub-fork — second same-phase instance; supervisor-symmetric accountability)

| Field | Value |
|-------|-------|
| **Symptom** | A plan, arithmetic row, registry cadence string, or design-decision narrative cites a quantitative input (per-fire call volume, latency budget, wall-clock envelope, dataset size, etc.) WITHOUT grounding it in measured Phase-0 evidence — and the unmeasured estimate then propagates into downstream decisions (cadence selection, single-vs-queue architecture choice, timeout sizing, backfill strategy) that are falsified the moment the real measurement arrives. The defect is two-sided: the original estimate-as-datum substitution AND the verification passes (executor + supervisor) that checked the row's PRESENCE rather than its DERIVATION. Verification-of-presence is NOT verification-of-derivation; a number with no measured citation is by construction non-evidence regardless of how many review passes confirm "the row is there." |
| **Codification target** | **The measured-inputs rule (catalog-level, binding):** *every quantitative input to a registry cadence string, arithmetic row, latency budget, call-volume estimate, or architecture-selecting decision MUST cite Phase-0-measured evidence at the point of authorship. An unmeasured estimate that "sounds reasonable" is non-evidence and may not occupy a datum slot. Reviewers (executor AND supervisor) verify the DERIVATION at the row, not the PRESENCE of the row — and the symmetric-accountability clause binds: the supervisor's verification pass that confirms the row's presence WITHOUT confirming its derivation IS a verification defect of equal weight to the executor's authorship defect.* The rule binds at the catalog-rule level; mechanical enforcement is intractable (estimates and measurements look identical to a linter) — the discipline is human-readable governance reinforced by the symmetric-accountability clause. |
| **First fired** | FP-050 §(i) original authorship — the "~50 qualifying accessions per fire (S&P-900)" cell in the §(i) arithmetic row was an unmeasured estimate that propagated into the "~17.4 s ≈ ~18 s/fire" single-invocation envelope, the "~25 min one-shot within `timeout_seconds=600`" backfill claim, and the registry cadence string. The Phase-0 probe-window measurement (three real daily indexes, filer CIKs resolved against the universe CIK map) returned ~1,667 in-universe Form-4 accessions per day — a 33× falsification of row 3, which cascaded into a 38× falsification of the total envelope (~11 min wall-clock vs ~18 s claimed) and forced the architectural pivot from single-invocation to queue-engine `work-list` consumer (FP-050 Phase 3.6a + 3.6b). Both prior verification passes (executor Phase-3 + supervisor Phase-3 ratification) confirmed the row's PRESENCE without confirming its DERIVATION — the symmetric-accountability defect. Recorded with all four cells corrected at FP-050 Phase 3.6b.i (MIG-094 cadence-string rewrite + insider-transactions.md §(i) arithmetic row rewrite + backfill prose retraction). |
| **Subsequent firings** | (2) FP-050 Phase 3.6b sub-fork authorship — the (4) "REFACTOR insider-orchestrator.ts ... if the refactor's diff sprawls beyond mechanical extraction, STOP per §22.8.4" framing carried an unmeasured "mechanical extraction" estimate as its sizing input. The measured reality at the point of execution was insider-orchestrator.ts = 571 LOC + 532 LOC test suite — not "mechanical extraction" by any honest yardstick. The executor's pre-fire scope-check surfaced the falsification and STOPped per §22.8.4 before any commit landed; operator (α) ruling split 3.6b into three sub-commits. This is the second same-phase instance of the #43 family within FP-050, recorded for the pattern: estimate-as-datum recurs at architectural decision boundaries, not just at arithmetic rows. |
| **Status** | open — catalog-rule-level binding from this entry forward. **Supervisor-accountability clause (verbatim, operator-dictated):** *"the supervisor's verification pass that confirms the row's presence without confirming its derivation IS a verification defect of equal weight to the executor's authorship defect."* From this entry forward, both the authorship of a quantitative input AND the verification pass that ratifies it without checking derivation are treated as the same defect class and recorded symmetrically in action-tracker entries. The rule applies retroactively to FP-050 §(i) (correction landed at MIG-094) and prospectively to all future cadence-string / arithmetic-row / latency-budget / call-volume / architecture-decision authorship. |
| **Subsequent firings (supervisor-side addendum, ACT-210)** | (3) 2026-06-13 / ACT-210 — this morning's supervisor brief diagnosed an asymmetric body-contract between `longshort-insider-compute-manual` (silently ignored `body.as_of`) and the sibling `longshort-queue-init-manual` (honored `body.as_of`), then DEFERRED the ruling to ACT-210 while the same-day validation re-fire was already gated on the contract fix. The deferral converted a same-day blocker into a runtime gap: the evening validation curl could not target `2026-06-12` (the reset day) because the handler defaulted to wall-clock `2026-06-13` and the GHA daily discovery had not fired. **Forward-binding rule (supervisor-side, this addendum):** *contract asymmetries surfaced during a signal build DO NOT defer past the validation gate they touch. If the asymmetry is discovered in the same supervisor turn that authors or ratifies the validation curl, the fix lands in the same commit-arc as the gating evidence — not in a subsequent "follow-up" entry. Deferral is permitted only when the contract asymmetry provably does not gate any open validation step in the current build phase.* The rule binds symmetrically with the original #43 derivation-not-presence clause: the executor's authorship of the asymmetric contract AND the supervisor's deferral that left it in place across the validation gate are the same defect class. |
| **Subsequent firings (comments-as-only-deferral addendum, ACT-218)** | (4) 2026-06-14 / ACT-218 — the `signal_compute_log.universe_size` two-ledger mismatch for work-list mode was documented verbatim in a source-only comment at `queue-init.ts:306-317` ("...consumer's loadAndCompute mass-balance still uses real universe_membership at finalize-time per Q4."), with NO corresponding ACT-NNN row, ADR, or feature-proposal entry tracking the deferral. The mismatch surfaced as a 259-row conservation gap on runs `2ac77620` and `12a6cacf` (1098 producer-side accession count vs. 839 consumer-side universe-membership count) — exactly the failure mode the comment had named but the action ledger had never registered. **Forward-binding rule (this addendum):** *deferred design items routed via source-comments alone — without a corresponding ACT/ADR/feature-proposal entry tracking the deferral — are themselves a Catalog #43 violation by construction. Source comments may NARRATE a deferral but cannot REPLACE the ledger entry that binds it to a future phase/gate. A "Q4" / "TODO" / "deferred-to-vX" marker in a source comment without a paired ACT or ADR row is non-evidence for the deferral the same way an unmeasured estimate is non-evidence for a quantitative input.* The rule applies retroactively (any existing such comment in the repo MUST be paired with an ACT row at the next governance pass) and prospectively (all future deferrals via source comment MUST land a paired ACT row in the same commit). |
| **Subsequent firings (fixture-scope-mismatch addendum, ACT-223)** | (5) 2026-06-14 / ACT-223 — the ACT-222 `(p5)` regression test asserting "NVDA in 3 day partitions ⇒ submissions/CIK0001045810.json fetched EXACTLY ONCE" passed for the wrong reason: the test was specified as "in 3 distinct day partitions" which is ambiguous between (a) within-`runDiscoveryDay` dedup and (b) cross-day cross-call dedup spanning multiple sequential `runDiscoveryDay` invocations inside a single `runBackfill` envelope. Live evidence from the post-ACT-222 GHA `insider-discovery` re-fire (run 27507714215, cancelled by operator at ~6 min wall-time) showed the producer's `edgar_fetch op=edgar_submissions` log lines ascending monotonically through CIKs (876437 → 2074176), then resetting to small CIKs (25445 → …) — interpreted by operator as a per-day iteration restart pattern. The architectural contract is the wider envelope: `runBackfillDedup` builds a GLOBAL unique-issuer Set across every parsed day BEFORE Pass 2 fires; the test must exercise that widest scope. **Forward-binding rule (this addendum):** *a regression test asserting a "fetch once" / "dedupe-by-K" / "idempotent-across-N" contract MUST be invoked at the contract's WIDEST scope (N>1 outer iterations), with the assertion counting invocations across the full envelope. A fixture that exercises only the innermost iteration of a multi-iteration contract passes for the wrong reason and is a Catalog #43-shape defect by construction; the symmetric-accountability clause from #43 applies — the supervisor brief that specifies the fixture without naming the widest-scope envelope IS the defect-bearing artifact, equal weight to the executor's authorship.* Resolution: `(p7)` (cross-day cross-call dedup proof — N distinct days, same CIK each day, asserts ONE submissions fetch) and `(p8)` (global unique-set ceiling — N days × M distinct CIKs with heavy overlap, asserts `unique_issuers_fetched === |distinct stubbed CIKs|` AND `≤ universe.size`) added; `(p5)` docstring corrected to explicitly state what it does NOT exercise. The producer-side contract is unchanged (the shipped `runBackfillDedup` already iterates a global set); the gap was test-coverage, not implementation. |

### #44 — Structural-Success Metrics Without Semantic-Success Metrics

| Field | Value |
|-------|-------|
| **Symptom** | A producer / cron / external-write job reports green structural evidence — HTTP fetches 200, process exit 0, workflow conclusion success, heartbeat rows written, or insert calls attempted — while the semantic payload that proves correctness is absent. The F2.b insider-discovery backfill produced `days:63`, `rows_inserted:0`, `heartbeats_inserted:63` with all EDGAR fetches 200: structurally successful, semantically impossible against Phase-0 measured Form-4 coverage. The root parser accepted only the synthetic `Filename` header + ISO dates, while real SEC master.idx uses `File Name` + compact `YYYYMMDD`, so every real Form-4 row parsed to zero before the in-universe predicate ever had a chance to run. |
| **Codification target** | Catalog-level binding plus code-level sentinels: **fetch success, workflow success, and write-attempt success are NOT correctness evidence unless paired with semantic-success counters for the intended payload.** For data producers, the run-complete line MUST expose pre-filter parsed count, post-filter semantic count, heartbeat count, and persisted-by-correlation verification. A green exit is forbidden when `entries_parsed > 0` and `entries_after_universe_filter = 0` for a backfill/daily run that should have in-universe coverage; this is a fail-loud semantic-success gate, not a warning. Parser fixtures MUST include at least one real upstream row shape, not only synthetic rows. |
| **First fired** | ACT-204 / FP-050 Phase 4 F2.b-hardening: GHA run #3 (`discovery_correlation_id=658b8070-dba7-44f9-881e-cd12b4c81f8b`) exited green with 63 heartbeats and 0 real rows. Diagnostic live row: `1045810|NVIDIA CORP|4|20260605|edgar/data/1045810/0001768670-26-000002.txt`; fixed parser output = `{filer_cik:'1045810', form_type:'4', date_filed:'2026-06-05', accession_number:'0001768670-26-000002'}`; universe operand = `0001045810`; operands match. |
| **Subsequent firings** | — |
| **Status** | open — codified in `edgar-daily-index-fetcher_test.ts`, `insider-discovery-egress_test.ts`, and the producer run-complete / post-write verification fields. Future producer/cron hardening must reject structural-only evidence and require semantic payload counters at the same boundary. |

### #45 — Post-Fire Polling Truncation Misread as Stall (Catalog #41 family — phantom-defect form, supervisor-side mirror)

| Field | Value |
|-------|-------|
| **Symptom** | A post-deploy diagnostic polls a long-running queue/job from outside the function's terminal-state window, exhausts a finite client-side polling budget (commonly 8–10 minutes for a job whose Q3 3-strikes guard fires at T+~20 minutes), then concludes "stalled / never progressed" from the absence of forward motion in the observation window. The DB row's authoritative terminal columns (`status IN ('completed','failed')`, `finalized_at IS NOT NULL`, `failure_reason` populated) reach their true values minutes-to-hours AFTER the polling window closed, and the verdict — typically a §22.8.5 sub-class B "registration-dependency deploy gap" or other plausible-but-wrong cause — sits in a downstream commit's authority chain as evidence. The verdict shape is observationally indistinguishable from a real stall during the polling window; the defect is the omission of "evidence partial through T+Nmin only" framing in the verdict. Symmetric supervisor-side mirror of phantom-success: where phantom-success fabricates a passing signal, post-fire polling truncation fabricates a terminal-state signal as a hedge against the unobserved future. |
| **Codification target** | Catalog-rule-level binding (this entry). **Polling-completeness rule:** *a post-fire diagnostic verdict that asserts a terminal-state property of a queue/job MUST either (a) poll until the DB-row terminal-state guarantee is reached (`status IN ('completed','failed') OR finalized_at IS NOT NULL` for `signal_queue_runs`; the equivalent terminal columns for sibling tables), OR (b) name the polling-window truncation explicitly in the verdict as "evidence partial through T+Nmin only — terminal state unverified" and EXCLUDE any cause-assignment whose evidence requires the unobserved tail.* A verdict that mixes partial-window observation with full-window cause-assignment is a #45 violation by construction. Mechanical enforcement is intractable (polling shapes vary by signal); the rule is human-readable governance reinforced by the symmetric-accountability clause from #43 — the supervisor brief that ratifies a truncated-polling verdict without flagging the truncation IS a verification defect of equal weight to the executor's diagnostic defect. |
| **First fired** | 2026-06-13 / ACT-209 (FP-050 Phase 4 §22.8.5 sub-class B closure). The INVESTIGATION turn polled run `aadb1329-437f-4e67-8ee8-c53ad3a29c2d` over an 8-minute window from T+0 through T+~8min, observed `status='running'`, `cursor_remaining=299`, `cursor_claimed=0`, `items_inserted=0` with zero motion across snapshots, and concluded "the run is stalled indefinitely" → §22.8.5 sub-class B (Registration-Dependency Deploy Gap) → ACT-209 widened the GHA workflow to redeploy four generic queue workers on registry-surface changes. Post-mortem (next turn): DB row reads at T+~22min showed `status='failed'`, `finalized_at='2026-06-13 21:29:28+00'`, `failure_reason='work_list_slice_deadlock_3x: item ''0001058090-26-000045'' threw: Cannot read properties of undefined (reading ''replace'')'` — the Q3 3-strikes guard fired exactly as designed. The actual cause was an engine-↔-consumer seam mismatch in the insider work-list `processItem` (closed at ACT-211, this commit's primary scope). The polling window had truncated 13 minutes BEFORE the run's real terminal stamp; the §22.8.5 sub-class B verdict assigned cause to evidence the truncated window could not contain. ACT-209's forward-binding rule (workers must redeploy on registration changes) remains independently valid and stays in the failure-mode catalog — but its cause-assignment for THIS specific run was wrong. The Rule-8 amendment to ACT-209 records the correction; ACT-209's structural change to the deploy pipeline stands. |
| **Subsequent firings** | — |
| **Predicate revision (v0.7 — ACT-219, 2026-06-14)** | The original Catalog #45 validation-predicate set written for the FP-050 Phase 4 pre-arm polling protocol included "z ±3 clips = 0" as a CLEAN/DRIFT discriminator. That predicate is **wrong against the signal's own clamp contract**: per DEC-058 §(z-score), winsorization at ±3 is spec-correct signal behavior — clips at ±3 are an INTENDED tail-management mechanism, not a defect. The corrected predicate is **"clip rate within band [0%, 5%]"** — matching the rate observed across the 8 other already-attesting signals. A predicate written without checking the signal's own clamp/winsorization contract before pinning a "zero" target is a **supervisor-side Catalog #43-shape defect** (unmeasured-estimate-as-datum applied to a validation-predicate threshold rather than a quantitative input). Symmetric-accountability clause from #43 applies: the supervisor brief that ratified the "clips = 0" predicate without grep-grounding against DEC-058 §(z-score) IS the defect-bearing artifact. **Forward-binding rule:** *every numerical validation predicate written into a Catalog entry MUST cite the spec/DEC clause it derives from and verify the target against the spec's own clamp/winsorization/normalization contract before it lands. A "zero" or "exact match" target on a quantity the spec defines as bounded-but-nonzero is a Catalog #43-shape defect by construction.* |
| **Status** | open — codified at catalog-rule level (this entry); the polling-completeness rule binds from this commit forward. Polling discipline correction: any future diagnostic that polls `signal_queue_runs` (or a sibling terminal-state table) MUST sample until the terminal-state guarantee is reached OR carry the explicit "evidence partial through T+Nmin only" framing in the verdict. The latter framing is acceptable when polling cost or operator latency justifies the truncation, but it MUST exclude cause-assignment for any phenomenon whose evidence requires the unobserved tail. Open audit-event question (split off, NOT a verdict): Lovable's polling reported "zero SLICE_FAILED / RUN terminal events in `longshort_audit_logs`" yet the DB row's `failure_reason` + `finalized_at` prove the slice-level state machine DID terminate. Investigate whether `longshort_audit_logs` is missing events that fired in-process OR whether the work-list mode's slice/terminal telemetry routes through a different event family than the audit-event names the diagnostic queried — separate finding tracked outside this entry. |

### #46 — Type-Home-Extension Forward-Binding (discriminator value-site without union-home extension)

| Field | Value |
|-------|-------|
| **Symptom** | A producer-time emit adds a new discriminator value at a value-site (e.g. `telemetry({ path_family: 'submissions', ... })`) without extending the type's union definition in the same commit. Deno's structural typing accepts the file at runtime, so unit tests and Gates 1 / 2 / 2b / 2c / 3 can read green while the cross-module typecheck (or any consumer that exhaustively narrows the union) is broken at build-time. The defect surfaces as a TS2322 "Type '\"X\"' is not assignable to type 'Y'" in the consumer's environment, often discovered only when a downstream commit attempts a typecheck pass at HEAD. |
| **Codification target** | Catalog-rule-level binding (this entry). **Type-home-extension rule:** *adding a discriminator value to a typed event/union at any value-site REQUIRES extending the type's union definition (the type-home) in the SAME commit. The value-site and the type-home are a triple-edit pair — (union member + persist seed + persist test) — by construction. The gate is build-time, NOT runtime: Deno's structural typing accepts the file even with type errors, so the rule cannot be enforced by Gates 1 / 2 / 2b / 2c (deno test) alone. A co-located typecheck assertion (`const v: TypeHome = 'newValue';`) in a `*_test.ts` adjacent to the type-home is the canonical sentinel.* A commit that emits a new discriminator value without the union-home extension + co-located assertion is a #46 violation by construction. |
| **First fired** | 2026-06-14 / ACT-215 (FP-050 Phase 4 / §(b) acceptance-datetime gate, edgar-submissions-fetcher.ts:143 emitting `path_family: 'submissions'` against an unextended `EdgarPathFamily` union at edgar-fetch-telemetry.ts:25–29). The original ACT-215 commit landed the value-site without extending the type-home; the TS2322 cross-check did not fire under deno test (Gates 1 / 2 / 2b / 2c / 3 read GREEN) and was caught only by an external typecheck pass. Closure: ACT-216 amendment — union extended to include `'submissions'`, co-located test `edgar-fetch-telemetry_test.ts` added with both the new-member assertion and a member-count drift guard. |
| **Subsequent firings** | — |
| **Status** | open — codified at catalog-rule level (this entry); the type-home-extension rule binds from this commit forward. Future emits of new `path_family` / `op` / similar discriminator values MUST extend the union-home and add a co-located `_test.ts` assertion in the SAME commit, regardless of whether the deno test gates pass without it. |

### #47 — Sticky-State-on-Recovery Audit Surface (transient inner-loop state stamped to a top-level row, not cleared at terminal-success)

| Field | Value |
|-------|-------|
| **Symptom** | A transient inner-loop failure (e.g. a vendor 429 that the backoff helper recovered from on a subsequent retry) writes an error-descriptor field (`failure_reason`, `last_error`, `error_message`, etc.) onto a top-level run/job row. The inner loop later succeeds; the run reaches terminal-success (`status='completed'`, `slice_failure_count=0`); the error field is NEVER cleared. Operators reading the run row see a completed run with a populated failure-descriptor and cannot tell — without cross-referencing the audit envelope — whether the run actually failed, partially failed, or succeeded with transient noise. The defect is observationally indistinguishable from a real terminal failure in single-row queries; downstream alerting that filters on "failure_reason IS NOT NULL" misfires on every recovered transient. Diagnostic poison: completed runs look failed. |
| **Codification target** | Catalog-rule-level binding (this entry). **Sticky-state-on-recovery rule:** *any error-descriptor field on a top-level run/job row that is WRITTEN during a transient inner-loop failure MUST be either (a) explicitly cleared to NULL at terminal-success, OR (b) restricted by contract to run-level (terminal) writes only — transient inner-loop failures are logged exclusively in the audit envelope, never on the top-level row. A field whose write contract straddles both transient and terminal scopes without an explicit clear-on-recovery branch is a #47 violation by construction.* The choice between path (a) and path (b) is a code-style decision per surface; what is NOT optional is that the chosen contract is enforced and the alternative is suppressed. The test sentinel is a regression assertion on the terminal-success CAS payload: for path (a), `failure_reason: null` MUST appear in the payload explicitly (not merely "be undefined"); for path (b), the slice-level write site MUST be deleted entirely. |
| **First fired** | 2026-06-14 / ACT-219 (FP-050 Phase 4 pre-arm hygiene). Validation run `e5907bfb-7b16-4d51-9ab0-d5253749f280` completed cleanly (status='completed', slice_failure_count=0, finalized_at=2026-06-14 05:43:00.971+00) yet `signal_queue_runs.failure_reason` remained populated with the recovered-from `HTTP 429` string from a mid-drain `edgar_cik_mapper` call. The slice-level write site at `queue-slice-worker.ts` had stamped the field during the transient failure (per ACT-187 INC-73 verbatim-stamping); the backoff retry succeeded; the terminal-completed CAS at `queue-finalizer.ts` did not clear the field. Resolution path (a) chosen: `casToTerminal` now writes `failure_reason: null` explicitly when `terminal === 'completed'` and the caller passes `failure_reason: null`. Path (b) was considered but rejected — moving all `failure_reason` writes to terminal scope would lose the per-slice diagnostic resolution that ACT-187 INC-73 established as canonical. |
| **Subsequent firings** | — |
| **Status** | open — codified at catalog-rule level (this entry); the sticky-state-on-recovery rule binds from this commit forward. Future surfaces that stamp an error-descriptor field onto a top-level run/job row from a transient inner-loop site MUST land the clear-at-terminal-success counterpart in the SAME commit, with a co-located regression test asserting the explicit-NULL write on the terminal-success CAS payload. |

### #48 — SEC-Dependency Producer-Relocation (intrinsically immutable vendor reference data fetched at consumer-runtime instead of producer-time)

| Field | Value |
|-------|-------|
| **Symptom** | An intrinsically immutable vendor reference dataset (universe definitions, calendars, ticker maps, daily-stable cross-walks — anything that does NOT change across a backfill drain) is fetched at consumer-runtime by each cron-spawned isolate, paying the vendor's fair-access throttle toll independently per isolate. Per-isolate memoization is structurally insufficient because Supabase Edge cron-isolate boundaries cannot share state — a ~1000-slice drain at ~1 slice/min triggers N cold-start isolate clusters, each re-fetching the same daily-stable snapshot. The vendor's fair-access rate-limit eventually bites; the runtime path returns 429-class errors that the slice-worker either (a) throws transiently (work-list cursor preserved; eventual drain success but predicate-4 (vendor 429 count = 0) violated) or (b) skip-classifies (silent ROI reduction on a class of accessions). Diagnostic poison: a CACHE LAYER at the consumer (Path-X — e.g. a `vendor_data_cache` SQL table) treats the symptom (cache the runtime fetch across isolates) without addressing the architectural mismatch (the fetch belongs at producer-time). |
| **Codification target** | Catalog-rule-level binding (this entry). **SEC-Dependency Producer-Relocation rule:** *any vendor reference fetch that is intrinsically immutable across a drain (universe definitions, calendars, ticker maps, anything day-stable) MUST live at producer-time, be persisted onto the work-queue / database row alongside the work item it enriches, and be read by the consumer from the row. Per-isolate memoization at the consumer is structurally insufficient because cron-spawned isolates cannot share state. A consumer-runtime fetch of intrinsically immutable vendor data is a #48 violation by construction.* Per-item vendor fetches (e.g. per-accession Form-4 XML — the work IS the per-item fetch) are NOT covered by this rule; they are unavoidably per-item. The discriminator: "would the same answer hold for every slice across the entire drain?" — if YES, the fetch is producer-time data. |
| **First fired** | 2026-06-14 / ACT-220-B (FP-050 Phase 4 Path-Y CIK-resolution relocation). Validation run `e5907bfb-7b16-4d51-9ab0-d5253749f280` surfaced a mid-drain `edgar_cik_mapper` HTTP 429 against `company_tickers.json` (~800 KB SEC ticker→CIK snapshot, immutable across a backfill). ACT-219 hardened the in-isolate path with `fetchWithTimeoutAndRetry` (3 attempts / [1s,2s,4s] backoff) + per-isolate memoization. Run `937cc59c-8a5f-4228-ab22-681321c9aabd` (RECURRENCE) surfaced ANOTHER 429 at T+10min despite ACT-219's hardening, proving the per-isolate memo cannot survive Supabase Edge cron-isolate boundaries. ACT-220-B relocates the fetch to producer-time: the GHA discovery-egress runner (`scripts/insider-discovery-egress.ts`) loads `company_tickers.json` ONCE per fire, builds the cik→ticker map, and stamps `ticker` onto every `insider_accession_discovery_queue` row (MIG-098 — `ticker text NOT NULL`). The consumer (`insider-work-list-registration.ts`) reads `ticker` directly from the queue row; `EdgarCikMapper` is removed from the consumer entirely (co-located source-sentinel test `(D.6)` asserts the import absence). |
| **Established pattern** | ACT-215 (`acceptance_datetime` relocation; MIG-097) was the first instance — `acceptanceDateTime` was read from per-accession `index.json` at consumer-runtime; relocated to the producer via the per-issuer submissions feed. ACT-220-B (`ticker` relocation; MIG-098) is the second instance. The pattern is now forward-binding: any signal whose work-item depends on intrinsically-immutable SEC data MUST resolve at producer-time and persist onto the queue row. |
| **Rate-ceiling amendment (ACT-221, 2026-06-14)** | Relocating a SEC-dependency fetch from consumer-runtime to producer-time eliminates the cross-isolate concurrency problem but DOES NOT eliminate the rate-ceiling problem. Producer-side fetches that target the same rate-limited vendor MUST honor the vendor's documented pacing floor; producer-side bursting is a SIBLING defect class to consumer-side per-isolate explosions, with the same root cause (unbounded concurrency against a rate-bounded resource). Surfaced by the post-ACT-220-B repopulation drain (2026-06-14 GHA `insider-discovery` run): 88% 429-rate on `data.sec.gov/submissions/CIK*.json` (3939/4451 calls). The producer's per-issuer submissions-feed loop fetched at unconstrained loop speed against the same `data.sec.gov` rate ceiling that ACT-219 hardened the consumer's `company_tickers.json` path against. Resolution: (i) pace consecutive submissions calls at the SEC-acceptable floor (`SUBMISSIONS_PACING_FLOOR_MS = 1100`; matches the master.idx call cadence; well above the published 10 req/sec ceiling); (ii) integrate `fetchWithTimeoutAndRetry` into `EdgarSubmissionsFetcher` (same canonical helper ACT-219 adopted for the cik-mapper); (iii) regression sentinel `(p3)` in `scripts/insider-discovery-egress_test.ts` asserting N-1 sleeps for N unique issuers, each ≥1100 ms. Forward-binding: any producer-side loop that issues N>1 calls per fire against a rate-limited vendor MUST honor the vendor's documented pacing floor and integrate the canonical retry helper in the SAME commit. The forward-binding next move (Path-Q — caching the producer's submissions reads across consecutive fires keyed by issuer+`acceptanceDateTime` watermark) is deferred as FP-051 candidate; immediate need is pacing. |
| **Resource-dedup amendment (ACT-222, 2026-06-14)** | Pacing (ACT-221) is necessary but not sufficient. The post-ACT-221 GHA `insider-discovery` repopulation drain (run 27504513965) was CANCELLED at ~1h45m with `Error: The operation was canceled` — pacing held (status-200 across all observed fetches) but the producer's ITERATION SHAPE was redundant: the legacy `runDiscoveryDay`-per-day loop refetched each issuer's `data.sec.gov/submissions/CIK*.json` once per day the issuer filed. Queue state at cancel: 4,697 real rows + 55 heartbeats + 0 null_acceptance + 0 null_ticker across as_of_dates 2026-03-16 → 2026-04-22 (~33% of the intended 63-day window) with 4,451 submissions calls against an estimated ~700 unique issuers. **Resource-dedup rule (forward-binding):** producer-side fetches against rate-limited vendor reference data MUST deduplicate by the natural primary key of the upstream resource (`issuer_cik` for submissions feeds; `ticker` for universe; etc.). Fetching the same resource N>1 times across a single backfill window/fire is a Catalog #48 violation regardless of pacing being honored — the additional rule is **fetch-once-per-unique-resource-per-fire**. Resolution: (i) restructure `scripts/insider-discovery-egress.ts` `runMode` for backfill into a two-pass dedup orchestrator (Pass 1 = parse-all-days, no submissions fetches; Pass 2 = unique-issuer submissions enrichment, ONCE per CIK, paced; Pass 3 = cross-walk + insert per day against the global acceptance map); (ii) regression sentinels `(p5)` (NVDA-in-3-day-partitions ⇒ ONE submissions fetch) and `(p6)` (cross-walk hits all 3 NVDA accessions); (iii) run-complete telemetry now emits `total_accessions_processed`, `unique_issuers_fetched`, `dedup_ratio`, `acceptance_xwalk_misses` — operator-visible dedup ratio. **Forward-binding telemetry:** any future signal whose producer iterates a rate-limited reference resource MUST surface its dedup ratio in its run-complete event. The three rules together (consumer→producer relocation + producer-side pacing + per-resource dedup) close the SEC-rate-ceiling class structurally. Cross-fire caching (Path-Q' — caching submissions reads BETWEEN backfill runs via Supabase table) remains the long-term move; deferred as FP-051 candidate as before. |
| **Cross-day cross-call dedup-scope amendment (ACT-223, 2026-06-14)** | The ACT-222 resource-dedup rule's intent is the GLOBAL unique-resource set across the entire fire-envelope — NOT a per-iteration / per-day / within-call unique set. The shipped `runBackfillDedup` already implemented the right shape (a single global `uniqueIssuerCiks` Set accumulated across all parsed days in Pass 1 before Pass 2 fires); the gap surfaced by the post-ACT-222 GHA re-fire (run 27507714215, cancelled by operator at ~6 min) was the regression-test coverage: the `(p5)` sentinel's fixture wording ("3 day partitions") was ambiguous between within-iteration and cross-iteration scope, and exercised the contract at a narrower scope than the architectural envelope demands. **Cross-day cross-call dedup-scope rule (forward-binding):** *the ACT-222 resource-dedup rule binds at the WIDEST orchestrator scope (`runMode` / `runBackfill` envelope), NOT at the innermost per-day-equivalent iteration. The Set that proves "fetch-once-per-unique-resource-per-fire" MUST be a single accumulator across every outer iteration, and the regression test MUST exercise the orchestrator entry point with N>1 outer iterations, counting submissions invocations across the full envelope.* Resolution: (i) Pass-1 source comment in `runBackfillDedup` explicitly names the GLOBAL accumulator contract and points future refactors at the sentinels that bind it; (ii) `(p7)` sentinel — N=3 distinct days, same CIK each day, asserts callsByCik.get(cik) === 1; (iii) `(p8)` sentinel — 5 days × 30 distinct CIKs with full overlap, asserts `unique_issuers_fetched === |distinct stubbed CIKs|` AND `≤ universe.size` (proves Pass 2 iterates the global set AND the in-universe filter is the same map both predicates derive from); (iv) `(p5)` docstring corrected to explicitly disclaim that it does NOT exercise the wider scope (only `(p7)` and `(p8)` do). The three rules together (relocation + pacing + per-resource dedup) PLUS this dedup-scope clarification bind the full producer-side discipline. Path-Q' (cross-fire caching) remains deferred as FP-051 candidate. |
| **Subsequent firings** | (1) 2026-06-14 / ACT-221 — rate-ceiling sibling: post-ACT-220-B producer drain hit 88% 429 against `data.sec.gov/submissions/`. Catalog amendment codifies the producer-side pacing requirement as a sibling rule; this is NOT a regression of the original consumer-relocation rule (which held — `tickers_missing_for_cik: 0` confirms the cik_mapper relocation worked perfectly) but a surface that the relocation rule alone does not cover. (2) 2026-06-14 / ACT-222 — resource-dedup sibling: post-ACT-221 producer drain CANCELLED at ~1h45m (~33% window-completion) because the per-day iteration shape refetched each issuer's submissions feed once per filing-day rather than once per fire. The amendment above codifies the producer-side resource-dedup requirement as a second sibling rule; this is NOT a regression of ACT-221 (pacing held — zero 429s observed) but a surface that pacing alone does not cover. Operator rejected raising the GHA `timeout-minutes` (Option A) on the basis that papering over an architectural finding leaves the redundant-fetch class undetected for future signals; ACT-222's structural fix binds the rule at catalog level instead. (3) 2026-06-14 / ACT-223 — dedup-scope clarification: the ACT-222 `(p5)` test sentinel passed for the wrong reason; the dedup-scope amendment (above) clarifies that the resource-dedup rule binds at the WIDEST orchestrator envelope and adds the `(p7)` / `(p8)` cross-day cross-call sentinels. The shipped `runBackfillDedup` already implemented the right shape — the gap was test-coverage, not implementation. NOT a regression of ACT-222's structural fix (the global Set was always there) but a surface that the original test-scope did not lock the contract at the architectural envelope. |
| **Status** | open — codified at catalog-rule level (this entry); the SEC-Dependency Producer-Relocation rule + the ACT-221 rate-ceiling amendment + the ACT-222 resource-dedup amendment + the ACT-223 cross-day cross-call dedup-scope amendment bind from this commit forward. Future signals or work-queue surfaces that introduce a consumer-runtime fetch of intrinsically-immutable vendor reference data MUST relocate the fetch to the producer in the SAME commit, with co-located source sentinels asserting (a) the consumer does NOT import the vendor-fetcher class, and (b) the producer's vendor-fetcher constructor is invoked ONCE per fire. Producer-side loops that issue N>1 calls per fire against a rate-limited vendor MUST honor the vendor's documented pacing floor AND deduplicate fetches by the natural primary key of the upstream resource (fetch-once-per-unique-resource-per-fire, at the WIDEST orchestrator envelope) AND integrate the canonical `fetchWithTimeoutAndRetry` helper. Run-complete telemetry MUST surface the dedup ratio (`total_accessions_processed` / `unique_issuers_fetched`) so operator can verify the rule held under load. Regression sentinels for the dedup contract MUST exercise the orchestrator at N>1 outer iterations (per the ACT-223 amendment AND the Catalog #43 fixture-scope-mismatch addendum). |

## Quarterly Review Protocol (per §12.8)

1. At each quarterly boundary, review action-tracker entries from the prior quarter for defect-class firings.
2. New patterns → add catalog entry with `open` status.
3. Codified patterns confirmed (no firings for 2+ quarters) → mark `retired`.
4. Re-fires of `codified` patterns → escalate: codification is incomplete; surface to operator for structural reinforcement.
5. Quarterly-review outcome documented as an ADR in `docs/decisions/`.

## Used By / Affects

- Supervisor drafting discipline (pre-flight checklists)
- Lovable executor pre-flight gates (§22.8.4 STOP triggers)
- Quarterly governance review cadence
- Action-tracker self-disclosure narratives

## Related Documents

- [Constitution](00-governance/constitution.md)
- [System State](00-governance/system-state.md)
- [Action Tracker](06-tracking/action-tracker.md)
- [Deferred Work Register](08-planning/deferred-work-register.md) — DW-065 (this artifact's landing trace; also tracks the FP-006 ADRs path-convention drift)
- `CROSSWIND_SPEC.md` §12.5 Rule 10 + §12.10 (lines 3180-3220) — authoritative source for the §12.10 Operational Log section structure (7 canonical categories + 8-field capture protocol + quarterly review cadence)
- Supervisor-instructions §21.10 (operator-side Claude.ai project context) — authoritative source for the Catalog section structure; this artifact mirrors §21.10 entries at repo level

## Risks If Changed

MEDIUM — lost catalog entries cause defect classes to re-fire without recognition; quarterly review cadence becomes hollow.

---

## §12.10 Operational Log — PR-Time AI-Loop Failure Events

**Authority:** CROSSWIND_SPEC.md §12.5 Rule 10 + §12.10 verbatim (lines 3180-3220)
**Purpose:** Operational log of PR-time AI-loop failure events per §12.5 Rule 10 mandate
**Scope distinction:** This section captures **PR-time AI-loop failure events** as observed during executor / supervisor / operator review cycles. Distinct from the "Catalog" section above (Sections 4-9), which mirrors supervisor-instructions §21.10 forward-binding defect-class patterns. Both sections coexist under this single canonical filename per CROSSWIND §12.5 Rule 10 + Constitution Rule 5 (no duplicate documentation).

### Scope (per §12.10)

> *"§12.10 addresses the operational failure modes that surface during AI-assisted development — patterns where the AI loop (executor + supervisor) produces output that bypasses, misinterprets, or fails to apply the structural verification surface."*

> *"Operational document: `docs/ai-failure-modes.md` is maintained continuously throughout the project. Entries are appended as failures are observed; no entry is removed (operational history is preserved)."*

### Canonical failure categories (per §12.10 verbatim)

1. **Executor-supervisor blind spot.** Both AI tools share context and validate against derived signals (tests pass, code looks correct). Both miss the same defect class. Example: a sentinel fallback re-introduced after refactor; supervisor approves because tests pass; reconciliation engine catches it post-merge.

2. **Evidence-tier bypass attempt.** AI proposes a Strong+/Strong tier change without attaching required artifacts, or claims artifacts exist when they don't. CI catches this per §12.5 enforcement, but the AI behavior pattern is itself a failure mode worth logging.

3. **Reconciliation-event silenced.** AI suppresses a reconciliation_events firing (catches the exception, modifies tolerance, adjusts the verify_* call signature) rather than addressing the underlying divergence. Logged when operator detects post-merge.

4. **Behavior deviating from spec without ADR.** AI proposes a change that touches a locked decision (per Rule 6) without explicit human confirmation. Caught by operator review.

5. **Sentinel fallback re-introduction.** AI re-introduces a banned pattern per §11.8 in a refactor or new feature. CI grep catches this; the AI behavior is the failure mode.

6. **datetime.now() re-introduction in business logic.** Same pattern as #5 but for §11.9 ban.

7. **Replay-test PASS forged or skipped.** AI claims replay-test PASS without running, or skips replay-test where required by §11.10.4. Logged when operator detects.

Novel patterns: when an observed failure mode does not match any of categories 1-7, log it with `category: NEW`. Quarterly review (per §12.8) considers whether to promote the novel pattern to canonical.

### Capture protocol (per §12.10 verbatim — 8-field entry shape)

For each observed failure mode, append an entry below with the following 8 fields:

| Field | Description |
|---|---|
| `ts` | UTC timestamp (ISO-8601) |
| `category` | One of canonical categories 1-7, or `NEW` if novel |
| `pr_ref` | PR or change reference (SHA / branch / ACT-N entry / etc.) |
| `ai_tool` | Which AI tool surfaced the failure: `executor` / `supervisor` / `both` |
| `description` | What was attempted; what failed |
| `detection_path` | How the failure was caught: `CI` / `reconciliation engine` / `operator review` / `post-merge` / other |
| `resolution` | Corrective action taken: revert / patch / discipline reinforcement / spec amendment / etc. |
| `pattern_signal` | Whether this failure indicates a structural pattern requiring spec or rule update: `none` / `weak signal` / `strong signal — see <ADR/amendment ref>` |

### Entry format

Entries are appended chronologically (oldest first) below the `### Entries` heading. Each entry uses an `#### YYYY-MM-DD — Brief title` heading + table.

Example shape (not a real entry — for format reference only):

> #### EXAMPLE — Sentinel fallback re-introduced after refactor
>
> | Field | Value |
> |---|---|
> | `ts` | 2026-MM-DDTHH:MM:SSZ |
> | `category` | 5 (Sentinel fallback re-introduction) |
> | `pr_ref` | ACT-NNN (`<short-SHA>`) |
> | `ai_tool` | both (executor introduced; supervisor missed at PR review) |
> | `description` | During <module> refactor, `?? 0` sentinel fallback re-introduced at <file>:<line>. Tests passed because fixture data covered the non-null path. Reconciliation engine caught divergence post-merge when production data exercised the null path. |
> | `detection_path` | reconciliation engine (post-merge firing on `verify_<X>`) |
> | `resolution` | Revert sentinel; replace with typed-absence (`Optional`/`null`-with-narrowing); add reconciliation telemetry assertion to test suite. |
> | `pattern_signal` | weak signal — this is the second occurrence of category 5 in 2026; if a third occurs within Q2, consider strengthening §11.8 grep enforcement scope. |

### Quarterly review (per §12.10 + §12.8 verbatim)

The operator reviews this operational-log section quarterly per §12.8 documentation review cadence. Review questions per §12.10 verbatim:

- Are any failure categories occurring more frequently than baseline (>3× quarterly rate)?
- Do any patterns indicate a §12.5 rule needs strengthening?
- Do any patterns indicate a §11.0 verify_* call is misclassified?
- Should any new failure category be added to the canonical list?

**Quarterly review output:** an ADR entry per §12.6 documenting findings and any rule updates. ADRs go to `docs/decisions/`; this operational-log section is the operational record, not the decision record.

### Distinction from Catalog section (Sections 4-9 of this document)

The **Catalog section** above mirrors supervisor-instructions §21.10 forward-binding defect-class patterns. Entries there (currently #34-#38) document supervisor-side prompt-drafting + reconciliation discipline patterns and their codification targets in supervisor-instructions. Catalog entries are **pattern-level**: each represents a class of failures with codification status.

This **Operational Log** captures PR-time AI-loop failure events. Entries here are **event-level**: each represents one observed firing of an AI-loop failure during executor / supervisor / operator review cycles. An event entry may reference a catalog entry as its pattern signal (e.g., a PR-time firing of catalog #36 would log here with `pattern_signal: see Catalog #36`).

Cross-reference at PR-time: when a catalog #NN pattern fires at PR time AND maps to a §12.10 canonical category, an operational-log entry is appended below citing both the §12.10 category and the catalog #NN reference.

### Entries

*(No entries at creation. Entries appended as PR-time AI-loop failure events are observed during executor / supervisor / operator review cycles; no entry removed — operational history preserved per §12.10 verbatim.)*

#### 2026-05-30 — Deno config silently disables npm autodetection

| Field | Value |
|---|---|
| `ts` | 2026-05-30T00:00:00Z |
| `category` | 7 (Tooling / environment configuration silently changes behavior) |
| `pr_ref` | FP-008.4 Commit 1 |
| `ai_tool` | executor (introduced `supabase/functions/deno.json` for vitest-discovery exclusion) |
| `description` | Any `supabase/functions/deno.json` change must preserve `nodeModulesDir: "auto"` + an `imports` map for npm bare specifiers (`@supabase/supabase-js` and any other npm bare specifiers). Introducing or modifying `deno.json` without these silently disables Deno's implicit npm autodetection and re-breaks bare-specifier resolution on production code. Discovered during vitest-exclusion config introduction when 5× TS2307 on `@supabase/supabase-js` surfaced in production files that previously type-checked clean. |
| `detection_path` | Gate 11 type-check run (`deno test --no-run _shared/`) after deno.json introduction |
| `resolution` | Inline fix during Commit 1: added `nodeModulesDir: "auto"` + `imports` map. Rule memorialized here. **Verification after any future `deno.json` edit:** `cd supabase/functions && deno test --no-run --allow-net --allow-env --allow-read _shared/` MUST exit 0. |
| `pattern_signal` | first occurrence — single firing; if a second config-introduction defect surfaces in 2026, escalate to a Catalog entry with codified pre-flight check. |

#### 2026-06-09 — Supervisor fresh-clone verification missed vendor-cap arithmetic on FP-044 Phase 2 (PEAD orchestrator)

| Field | Value |
|---|---|
| `ts` | 2026-06-09T20:21:29Z |
| `category` | 1 (Executor-supervisor blind spot) |
| `pr_ref` | FP-044 Phase 2 (commit landing `pead-orchestrator.ts`); empirical surfacing at execution `11a267bc-ee77-4d54-a0ac-1571749f98cb` (504 at 150s) |
| `ai_tool` | supervisor (fresh-clone verification missed the defect that executor shipped) |
| `description` | FP-044 Phase 2 fresh-clone verification validated compute purity, `SignalSkipReason` enum widening, the `zero_dispersion`/`pead_panel_below_floor`/`no_recent_earnings` typed-skip discipline (DEC-051 / DEC-052 / DEC-053), and gate output (2+4+11) — but did **not** verify vendor-cap × call-count × wall-budget arithmetic on the orchestrator shell. The build prompt itself already contained the contradictory signal "≈2.8 min sequential" (already 168s > the 150s HTTP wall) and prescribed "p-limited under the cap" without resolving the handler shape. Supervisor approved with the rate-math defect intact; detection path = operator test-fire 504 + post-mortem grep (one step late). Same class as the FP-043 pre-token-bucket review near-miss, second firing — promoting to a structural pattern. |
| `detection_path` | operator test-fire (post-merge empirical 504 + grep of `pead-orchestrator.ts` header rate-math comment); reconciliation engine could not catch this because vendor-rate ceilings are not encoded anywhere the engine reads. |
| `resolution` | (1) FP-045 (this resolution PR) implements DEC-047's generalized cursor-drain queue-worker, retiring the synchronous-orchestrator shell for both known rate-capped consumers (PEAD + options-flow). (2) **New supervisor pre-flight discipline:** every feed-signal execution prompt MUST carry an explicit pre-flight arithmetic row — `(universe_size × calls_per_name) / (vendor_rate_per_sec × 0.85)` vs the 150s HTTP wall and the ~400s Pro background wall — and supervisor verification MUST reject any prompt missing this row. Bound forward at supervisor-instructions §22.3 (codification target). (3) FP-044 orchestrator header rate-math comment corrected in the same PR as Phase 3 of FP-045. |
| `pattern_signal` | strong signal — second occurrence of vendor-cap × call-count × wall-budget arithmetic defect in two consecutive rate-bound signal FPs (FP-043 pre-production, FP-044 production). Promotes to a structural pre-flight pattern; quarterly review (§12.8) should consider a Catalog entry (#39) once the FP-045 codification target lands. |

#### 2026-06-09 — Executor shipped rate-comment asserting 600/min is "well under" Finnhub's 300/min cap (FP-044 Phase 2)

| Field | Value |
|---|---|
| `ts` | 2026-06-09T20:21:29Z |
| `category` | NEW (vendor-cap arithmetic defect in production code comment that the gates structurally cannot detect; closest canonical category is #1 by review-loop role but the failure mechanism is distinct — codified runtime-comment claim contradicts vendor entitlement) |
| `pr_ref` | FP-044 Phase 2 (`pead-orchestrator.ts` header — `pLimitedMap` concurrency=5 documented as "≤10 req/sec... well under the cap" against Finnhub Estimate-1's 300/min = 5 rps ceiling; the dual-fetch factor doubles the effective burst again) |
| `ai_tool` | executor (introduced the rate-comment defect in the orchestrator header; the comment provided the false reassurance that justified omitting `token-bucket.ts` reuse — the FP-043 sanctioned pacer was already in-repo and excluded with explicit rationale) |
| `description` | Concurrency-based throttling is latency-dependent and unbounded by design — `pLimitedMap` enforces in-flight count, not requests-per-second. Even at concurrency=5 the burst rate against a fast vendor is 5 concurrent / per-request-latency, easily 10+ rps under healthy network conditions; with dual-fetch per ticker the effective burst doubles. Lint, type-check, Deno unit tests, and the per-ticker compute tests all pass cleanly — none of these surfaces encodes vendor rate ceilings, so the gates structurally cannot catch the defect. The empirical 504 + zero-row + zero-log silent death (INC-72) is the only signal available at the existing gate surface. |
| `detection_path` | operator test-fire (504 + post-mortem DB read + analytics-query log scan); gates 2 / 4 / 11 all green at PR time. The defect is **invisible to every existing automated surface**, which is itself the structural finding. |
| `resolution` | (1) FP-045 Phase 3 corrects the orchestrator header rate-math comment in the same commit that retires the synchronous shell from cron. (2) FP-045 Phase 2 ships the generalized queue-worker engine that REUSES `token-bucket.ts` (productionClock-routed, the FP-043 sanctioned pattern) — the orchestrator-of-record for rate-bound work no longer relies on concurrency limits to enforce vendor caps. (3) Executor-side discipline: any code comment claiming a vendor-cap relationship must cite the cap source and show the arithmetic; absence of either becomes a banned-pattern candidate at the next quarterly review. (4) Forward-binding to remaining feed signals: any new signal whose vendor entitlement has a published rate cap MUST consume the queue-worker engine — single-invocation synchronous orchestration is structurally prohibited for the rate-bound class. |
| `pattern_signal` | strong signal — paired with the supervisor-side entry above as the two halves of the same INC-72 root cause; the executor-side half exposes a class of defect (codified runtime claim contradicting external constraint) that no existing CI surface can catch. Quarterly review should consider whether `pattern_signal` warrants extending the §11.x banned-patterns scanner with a "rate-claim grep" check (cap claims in code comments without arithmetic justification). |

#### 2026-06-09 — Tool-substitution: Gate-4 evidence produced by `deno lint` instead of CI ESLint (FP-045 Phase 2)

| Field | Value |
|---|---|
| `ts` | 2026-06-09T (post-Phase-2 commit `b4f4941`, surfaced at `5396165`) |
| `category` | tool-substitution (executor swapped Gate-4 tooling for a near-name twin that does not enforce the same rule set) |
| `pr_ref` | FP-045 Phase 2 (commit `b4f4941`) + Phase 3 (commit `5396165`) — both shipped with claimed-green Gate 4 |
| `ai_tool` | executor (ran `deno lint` and reported it as Gate 4); supervisor (accepted the substitution without command-line verification against `.github/workflows/strong-evidence.yml`) |
| `description` | Gate 4 in this repo is defined by the CI workflow as `npx eslint .`. The executor produced Gate-4 evidence by running `deno lint`, which does NOT enforce `@typescript-eslint/no-explicit-any`. The five new Deno test files (`queue-finalizer_test.ts` 11, `queue-init_test.ts` 6, `queue-slice-worker_test.ts` 10, `queue-sweeper_test.ts` 6, `pead-queue-adapter_test.ts` 14) carried 47 literal `any` types that `deno lint` accepted but CI rejected. `@ts-nocheck` masks TypeScript compilation but does NOT suppress ESLint rules — a separate misconception that compounded the substitution. Sibling test files in the same tree (e.g. `queue-config_test.ts`, `queue-audit-events_test.ts`) had already been written with typed mocks per the FP-041 convention and passed CI; the new files diverged from the established sibling pattern without surfacing the divergence. |
| `detection_path` | Operator CI observation post-merge (CI ESLint red at both `b4f4941` and `5396165`). Local Gate-4 substitution rendered the defect invisible to both executor and supervisor. |
| `resolution` | (1) Revision commit (this PR) replaces literal `any` with `unknown` / narrow stubs across all 5 files, removes the obsolete `no-explicit-any` from the `deno-lint-ignore-file` directive (now unused), re-runs `npx eslint .` verbatim, paste-verified 0 errors / 15 baseline warnings. (2) Forward-binding Gate-4 discipline (codified in `docs/04-modules/longshort/signals/queue-worker.md` and bound into FP-045 below): **Gate 4 = the CI workflow's `npx eslint .` command verbatim; `deno lint` is supplementary diagnostic only, never Gate 4. Gate evidence MUST state the exact command line above its output.** (3) FP-045 Phase 2 disposition annotated `closed-with-revision`; Phase 3 Status field updated per §22.3(e) to reflect the same revision lineage. |

#### 2026-06-10 — Prior invalidated by vendor pivot: ACT-159 Part A small-cap N≥2 prior carried across DEC-053 FMP→Finnhub flip without re-derivation (FP-045 Phase 3 validation)

| Field | Value |
|---|---|
| `ts` | 2026-06-10T00:10:00Z (FP-045 Phase 3 validation-run finalize) |
| `category` | NEW (prior-invalidated-by-vendor-pivot — a quantitative prior measured against vendor A is carried into a plan governed by vendor B after a vendor-lock flip, without re-derivation against vendor B's actual endpoint shape; the gate that catches it is empirical observation at validation, not any pre-merge CI surface) |
| `pr_ref` | FP-045 Phase 3 (commit `5396165`); validation run `signal_queue_runs.run_id=451b9ee7-9703-429d-97bc-61aeb2697bbc`, 2026-06-10 00:01-00:10 UTC. ACT-159 Addendum (this PR) is the corrective documentation. |
| `ai_tool` | supervisor (carried ACT-159 Part A's small-cap "60% N≥2" marginal into the FP-045 Phase 3 prediction row without re-deriving against the production vendor after DEC-053 flipped Signal #2's vendor lock from FMP to Finnhub); executor (did not surface the cross-vendor mismatch when registering the PEAD adapter, although the adapter itself is correct). |
| `description` | ACT-163 (chat-label) measured small-cap N≥2 qualification at ~60% against FMP `numAnalystsEps` on `/stable/analyst-estimates`. ACT-160 subsequently established at the FP-044 Phase-1 STOP that this FMP endpoint returns **forward-quarter rows only** (AAPL: 10 rows dated 2026-06-28 → 2028-09-28; same shape across MSFT/PLUG/RIG and the 10 LOOK-AHEAD names). The small-cap N≥2 measurement was therefore almost certainly taken against future-quarter rows, where small-cap analyst panels are systematically thinner than on the just-reported quarter. DEC-053 then flipped the Signal #2 vendor lock to Finnhub `/stock/eps-estimate?freq=quarterly` (which DOES carry reported-quarter rows with `numberAnalysts`). The Part A "60% N≥2 small-cap" prior was NOT re-derived against Finnhub before being carried into FP-045 Phase 3's prediction row. Live read-only Finnhub probe at validation (96 stratified names from the run's `signal_observations`): 96/96 names matched the just-reported quarter `2026-03-31`; `numberAnalysts` distribution min=3, median=17, max=38; observed `pead_panel_below_floor=0` on the full 839-name run. The prior was an FMP-`numAnalystsEps`-on-forward-rows artifact, not a Finnhub-`numberAnalysts`-on-reported-row property. |
| `detection_path` | Operator validation test-fire of FP-045 Phase 3 + INVESTIGATION-mode read-only post-run probe series. The mismatch surfaced as `pead_panel_below_floor=0` vs predicted ~40-52%; the alternative-hypothesis test (broken adapter / wrong row selection / fetcher misparse) was rejected by code-path trace (`pead-queue-adapter.ts:107-141` byte-identical to `pead-orchestrator.ts:221-263`; `compute-pead.ts:107-113` floor is first gate; `FinnhubEpsEstimateFetcher.normalizeRow` preserves `numberAnalysts` verbatim) + 96-name live-probe ground-truth (0/96 names below floor). No CI surface could have caught this — gates encode neither vendor-cap arithmetic nor cross-vendor prior provenance. |
| `resolution` | (1) ACT-159 Addendum (this PR) records the corrected production-vendor measurement and dates the Part A small-cap row correction; ACT-159 original preserved per Rule 8. (2) DEC-052 STANDS as a vendor-regression guard — the failure mode protected against is real (a future Finnhub panel-thinning regression would manufacture phantom σ_proxy without the floor), and the small-cap-attrition prediction being wrong does not weaken the guard's value, only the build-time expected-attrition number. (3) Forward-binding supervisor discipline: **any quantitative prior carried across a DEC-flipped vendor lock MUST be re-derived against the new vendor's actual endpoint shape before being cited in a downstream FP prediction row, with the re-derivation evidence linked in the citing FP entry.** The dual gates of (a) DEC-text reading + (b) endpoint-shape re-probe are required; reading DEC text alone (which preserves the original prior verbatim per Rule 8) is structurally insufficient. (4) Pattern_signal: first observed firing on this repo; if a second cross-vendor-prior-carryover surfaces in 2026, escalate to a Catalog entry with codified pre-merge check that greps FP entries for citations to chat-label-ACT priors and requires the re-derivation evidence link. |
| `pattern_signal` | Single firing on this repo to date. The structural finding is that DEC supersession + Rule 8 forward-pointer discipline preserves the original prior in the document trail (correctly) but does NOT trigger re-derivation against the superseding vendor — so quantitative priors silently rot across vendor flips. The validation test-fire (the empirical-observation gate) is the only surface that catches this class; absence of validation = no detection. Promotes to a Catalog entry on second firing. |

| `pattern_signal` | first occurrence of tool-substitution at the gate boundary on this repo; if a second instance surfaces (any gate, any tool), promote to a Catalog entry with codified pre-commit check that hashes the workflow gate command and compares to the executor's quoted command line. |

#### 2026-06-10 — SECOND OCCURRENCE: Vendor-cap arithmetic inconsistency between documented budget and code-level config (FP-045 Phase 4 options-flow registration)

| Field | Value |
|---|---|
| `ts` | 2026-06-10T (post-Phase-4 closure addendum; surfaced by operator REVISION-FIX-mode pacing reconciliation) |
| `category` | promoted — see Catalog #39 (this firing is the second occurrence that triggered Catalog promotion per the 2026-06-09 entry's escalation clause "Promotes to a structural pre-flight pattern; quarterly review (§12.8) should consider a Catalog entry (#39)") |
| `pr_ref` | FP-045 Phase 4 Closure Addendum (the closure-time PR); REVISION-FIX same-day commit (this entry) |
| `ai_tool` | executor (shipped `options-flow-queue-registration.ts:69 callsPerName=1` against a 2-call-per-ticker fetcher AND wired a second `pacedHttpFetch`-bound `TokenBucket` inside the adapter, while the file-header arithmetic comment cited `80 × 2 / 1.7 ≈ 94.1s`; the drift-sentinel test hand-entered `sliceSize * 2` so the test agreed with the doc-narrative against the runtime config); supervisor (Phase 4 closure approval read the arithmetic comment without grep-comparing it to the `callsPerName` constant on the next line, and did not flag the double-bucket pattern) |
| `description` | The Tradier per-ticker arm in `TradierOptionsChainFetcher.fetchChainForTicker` makes TWO wire calls (`/markets/options/expirations` + `/markets/options/chains`). The Phase 4 registration pinned `callsPerName=1` while the file-header arithmetic correctly cited the TRUE 2-call count (`80 × 2 / 1.7 ≈ 94.1s`). To compensate for the under-count, the registration constructed a SECOND `TokenBucket(ratePerSec=1.7)` and wrapped HTTP via `pacedHttpFetch` inside the adapter — but the slice-worker also constructs its own `TokenBucket(ratePerSec=cfg.ratePerSec)` per `defaultBucketFactory` and acquires `cfg.callsPerName` tokens before invoking the adapter. Two independent buckets at the same rate, serialized: per-ticker wall = worker-bucket(1/1.7=0.59s) + adapter-bucket(2/1.7=1.18s) ≈ 1.76s; 80-slice ≈ 141s (not 94.1s), dangerously close to the 150s HTTP wall. Same defect class as the 2026-06-09 PEAD-orchestrator firing: the gates structurally cannot catch vendor-cap × call-count × wall-budget arithmetic; the drift sentinel masked the defect by computing the arithmetic from a hand-entered `* 2` rather than deriving it from `callsPerName`. |
| `detection_path` | Operator REVISION-FIX-mode pacing reconciliation (read-only code trace through `options-flow-queue-registration.ts` → `token-bucket.ts` → `queue-slice-worker.ts`; arithmetic re-derivation against the fetcher's actual call count). No pre-merge CI surface could have caught this — Gate-2 wall-clock scanner ignores arithmetic; Gate-4 ESLint passes; Gate-11 Deno tests passed because the drift sentinel hand-entered the multiplier. |
| `resolution` | (1) Same-day REVISION-FIX commit: removed `TokenBucket` + `pacedHttpFetch` from the adapter side (adapter now receives raw `fetch`); flipped `callsPerName: 1 → 2` to match the fetcher's true wire-call count; pacing is now owned in EXACTLY ONE place (the slice-worker bucket). 94.1s budget claim is now truth-bearing. (2) Drift-sentinel restructured: declared `TRADIER_WIRE_CALLS_PER_TICKER = 2` constant in the registration test file with a header citing `TradierOptionsChainFetcher.fetchChainForTicker` as source-of-truth; assert `callsPerName === TRADIER_WIRE_CALLS_PER_TICKER`; derive `perSliceWire = (sliceSize × callsPerName) / ratePerSec`. (3) Catalog #39 added — codification target = the above structural test pattern + the single-bucket invariant in `docs/04-modules/longshort/signals/queue-worker.md` (adapters MUST pass raw `fetch`). (4) Forward-binding: every per-signal registration test MUST follow this pattern (declare the fetcher's wire-call count as a named constant with citation, assert config match, derive the budget — no hand-entered multipliers). (5) Pattern_signal: SECOND occurrence triggers the 2026-06-09 entry's escalation clause; Catalog #39 codified. Quarterly review should consider whether a Gate-2-style banned-pattern scanner can catch "second `pacedHttpFetch` inside an adapter when the engine bucket already paces" at PR time. |

#### 2026-06-10 — Validation choreography missing the deploy step (FP-045 Phase 4 stale-deployment 01:03 fire)

| Field | Value |
|---|---|
| `ts` | 2026-06-10T01:03:09Z (options-flow stale-deployment fire) → 01:09:00Z (504 IDLE_TIMEOUT on 6 worker chunks) |
| `category` | NEW (validation-choreography-missing-deploy-step — the validation playbook wired test crons and fired the init curl without first confirming the edge-runtime had picked up the new handler code; the pre-Phase-4 synchronous coordinator answered the fire, producing the exact failure mode the Phase-4 work was built to retire) |
| `pr_ref` | FP-045 Phase 4 closure (commits landed pre-fire); operator validation test-fire 2026-06-10 01:03 UTC |
| `ai_tool` | executor (assumed commit-landed = deploy-landed; did not gate the validation fire on a deploy-confirmation probe); supervisor (validation playbook did not include an explicit "edge function deploy confirmed at HEAD" step between commit and curl) |
| `description` | The FP-045 Phase 4 gutted handlers (`longshort-options-flow-compute`, `longshort-options-flow-compute-manual`, `longshort-pead-compute-manual`) + 410-Gone shim on `longshort-options-flow-worker` were committed but not deployed to the Supabase edge runtime at the time of the 01:03 operator fire. The fire reached the pre-Phase-4 synchronous `runOptionsFlowCoordinator` (boot at 01:03:09, six parallel worker shutdowns at 01:09:49 → 504 IDLE_TIMEOUT on six chunks). No `signal_queue_runs` insert, no cursor seed, no slice claim — the queue path was never exercised, and the failure mode reproduced was the exact 150s-wall mismatch the Phase-4 work was built to retire. Detection was honest (the investigation refused to report fabricated z-distributions / per-slice timings / skip taxonomies that the run never produced), but the lost cycle was avoidable. |
| `detection_path` | INVESTIGATION-mode read-only post-fire probe — `signal_queue_runs` had no options-flow row; `signal_compute_log` showed only the pre-Phase-4 synchronous handler error; edge-function boot logs showed `longshort-options-flow-compute` + `longshort-options-flow-worker` (the deprecated path) running, NOT the gutted shims or the queue handlers. The mismatch between repo-HEAD code and deployed-runtime code surfaced as the root cause. |
| `resolution` | (1) Same-cycle: operator authorized deploy of all eight functions (the four gutted handlers + the four queue handlers `longshort-queue-init`, `longshort-queue-init-manual`, `longshort-queue-slice`, `longshort-queue-sweeper`) to HEAD via `supabase--deploy_edge_functions`, then re-probed each with POST + invalid auth to verify shim shape (worker → 410 `options_flow_worker_deprecated`; computes → 401 sub-second, not coordinator boot). Operator re-fired the options-flow init; run `0eba38a7-0c84-49fb-9948-86a09e188901` completed CLEAN (53/839, 11 slices, CAS-clean) → Phase 4 validated. (2) Forward-binding playbook addition: **every future validation choreography that fires a handler whose code shape changed in the same PR MUST include an explicit deploy-confirmation step between commit and fire.** Acceptable proofs: (a) an `supabase--deploy_edge_functions` invocation returning success for every handler whose code shape changed, OR (b) a probe POST to each touched handler with invalid auth that returns the new shim's response shape (e.g. 410-Gone body for deprecation; sub-second 401 for enqueue shims that do not boot the old coordinator). The probe-response shape MUST be cited in the validation evidence — "deployed" without a shape-proof probe is insufficient. |
| `pattern_signal` | First firing on this repo. The structural finding is that the validation playbook treats commit-landed and deploy-landed as fungible — they are not. Supabase Edge Functions auto-deploy under most conditions but not all (chunked worker handlers, recently-changed handlers, and handlers that share imports with edited modules can lag); the validation gate must close the gap with a shape-proof probe. If a second firing of this class surfaces in 2026, promote to a Catalog entry with a codified pre-fire scanner that diffs `supabase--edge_function_logs` boot signature against the expected shim signature. |

#### 2026-06-10 — Green-on-red: sub-variant suite-scoped Gate 11 vs CI repo-wide compile (FP-047 Phase 2 → Phase 3)

| Field | Value |
|---|---|
| `ts` | 2026-06-10T (post-Phase-3 commit; surfaced by operator CI observation) |
| `category` | NEW (green-on-red, sub-variant: suite-scoped Gate 11 evidence vs CI repo-wide Gate 11 — the executor's local "Gate 11" was scoped to the feature suite (`deno test … analyst-revisions/`) while the CI workflow runs `deno test --allow-net --allow-env --allow-read` from `supabase/functions/` repo-wide. The feature suite passed; the repo-wide compile failed because Phase 2 had extended the shared `SignalSkipReason` union with three new variants (`no_revisions_in_window`, `revision_prior_unavailable`, `zero_magnitude_only`), and `persist-signal-compute-log_test.ts` had hardcoded expected-shape objects asserting the OLD union — those assertions broke compile/assertion at CI but never ran in the feature-suite scope.) |
| `pr_ref` | FP-047 Phase 2 commit `dd4ec295`; Phase 3 commit `fd12cbe7`; Phase-3 REVISION addendum (this entry) |
| `ai_tool` | executor (Phase 2 extended `SignalSkipReason` union + Phase 3 seeded the three new keys in `aggregateSkipCounts`, both correct — but local Gate-11 invocation was scoped to the feature suite and never re-ran the cross-module consumer's test (`_shared/persist-signal-compute-log_test.ts`), which asserted the OLD aggregator shape); supervisor (Phase 2 and Phase 3 evidence acceptances did not require the verbatim CI-scope Gate-11 command and accepted the suite-scoped pass) |
| `description` | Two parallel "Gate 11" command shapes existed: (a) the executor's feature-scoped form (`deno test … analyst-revisions/`) used in phase evidence, and (b) the CI workflow's repo-wide form (`.github/workflows/strong-evidence.yml` line 84: `working-directory: supabase/functions` → `deno test --allow-net --allow-env --allow-read`). Phase 2 extended the shared `SignalSkipReason` union; the consumer test file at `_shared/persist-signal-compute-log_test.ts` hardcoded the OLD all-zero union shape in three assertions. The feature suite never imports that test, so suite-scoped Gate 11 was GREEN. CI repo-wide Gate 11 was RED with three `AssertionError` failures (936 passed / 3 failed). The pattern is identical to the 2026-06-09 tool-substitution firing (executor quoted a different gate command than CI runs), but the sub-variant is suite-scope vs repo-scope rather than tool-substitution. |
| `detection_path` | Operator CI observation (again) — the executor reported "Gate 11 passed (52 tests)" / "Gate 11 passed (84 tests)" against the feature suite; CI ran the verbatim repo-wide command at the landed HEAD and surfaced 3 failed assertions on the cross-module consumer. Re-running the CI verbatim command locally in REVISION-FIX mode reproduced the failure (936 passed / 3 failed) and the fix path (extend the three hardcoded expected-shape objects with the new union keys). |
| `resolution` | (1) Test fixtures updated: the three hardcoded expected-shape objects in `supabase/functions/_shared/persist-signal-compute-log_test.ts` extended with `no_revisions_in_window: 0`, `revision_prior_unavailable: 0`, `zero_magnitude_only: 0`. Re-run of CI-verbatim Gate 11 = 939 passed / 0 failed. (2) **Forward-binding rule (record in the FP-047 entry):** any commit extending a shared type/union MUST run the CI-scope Gate 11 verbatim (`cd supabase/functions && deno test --allow-net --allow-env --allow-read`), not the feature-suite-scoped form. (3) **Prompt-drafting side requirement:** whenever a phase brief's file-scope includes `_shared/`, the prompt MUST explicitly require the CI-verbatim Gate 11 invocation in the gate-output evidence section (quote the exact `.github/workflows/strong-evidence.yml` command line, same discipline as the eslint command). (4) Same-class as 2026-06-09 firing → escalation ladder primed; second firing of suite-scope vs repo-scope mismatch in 2026 promotes to Catalog with codified pre-merge check that greps phase-evidence for "Gate 11" and rejects any invocation whose argv differs from the workflow's run line. |
| `pattern_signal` | Second firing of the broader "executor's Gate-11 command ≠ CI's Gate-11 command" class on this repo (first: 2026-06-09 tool-substitution). Distinct sub-variant (suite-scope vs repo-scope, not tool-substitution), but same structural defect: the gate command quoted in phase evidence is not the gate command CI runs. If a third firing of either sub-variant surfaces, promote to a Catalog entry with a codified pre-merge linter that hashes the CI workflow's `Gate 11` `run:` line and compares to the executor's quoted invocation. |
| `pattern_signal` | structural — second occurrence of the vendor-cap arithmetic defect class in 30 days (FP-044 Phase 2 PEAD orchestrator 2026-06-09; FP-045 Phase 4 options-flow registration 2026-06-10). Per the 2026-06-09 entry's escalation clause, promoted to Catalog #39. The fact that Gate 2/4/11 are structurally blind to this class (vendor caps are external constraints not encoded in any CI surface) makes the per-signal-registration test pattern the only viable pre-merge gate; Catalog #39 codifies it. |

#### 2026-06-11 — MIG-088 filename-recall defect (4th-instance filename-verbatim defect; codified check landed)

| Field | Value |
|---|---|
| `ts` | 2026-06-11 (FP-047 Phase-4 arm-up closure commit) |
| `category` | NEW (closure-evidence filename-recall — the migration tool returns the migration file on success but does NOT echo the final filename in its primary response; the executor reconstructed a "next-free" timestamp + UUID pair from `date -u` + `python3 -c uuid.uuid4()` BEFORE invoking the migration tool and then quoted that reconstructed name as the landed filename. The actual on-disk name assigned by the tool was `20260611152302_44b94698-0dbd-4fec-a8ce-b4bbe57b3bd0.sql`; the recalled name was `20260611152227_8f05d614-adf3-4b72-864f-bf20efbadd69.sql`. Both timestamp and UUID drifted.) |
| `pr_ref` | FP-047 Phase-4 arm-up commit; closure-evidence quote in `docs/07-reference/database-migration-ledger.md` MIG-088 row (corrected this commit). |
| `ai_tool` | executor (pre-computed a candidate filename for the upcoming migration, then quoted that candidate as the landed name without re-listing the migrations directory after the tool returned). |
| `description` | Closure evidence cited a reconstructed migration filename instead of the verbatim on-disk name. The reconstruction approach is structurally fragile because (a) Supabase's migration tool assigns its own timestamp/uuid pair at apply-time, not at request-time, and (b) the executor's `date -u +%Y%m%d%H%M%S` + locally-generated UUID will drift from the tool's values by any amount of latency between computation and apply. Detection was operator review of the closure paste against `ls supabase/migrations | tail -1`. |
| `detection_path` | operator review (4th-instance pattern: the same filename-verbatim discipline gap that surfaced 3 times earlier on this repo). |
| `resolution` | (1) Filename corrected verbatim in `database-migration-ledger.md` MIG-088 row. (2) **Codified check (binding for all future migration closures):** every closure-evidence block citing a migration filename MUST include the verbatim `ls supabase/migrations | tail -1` output (or `tail -N` where N covers the migrations applied in that commit), pasted from a shell invocation that ran AFTER the migration tool returned. The recalled / pre-computed filename is never acceptable evidence. Acceptable proof shape: a code-fence containing the literal `ls supabase/migrations | tail -1` command followed by its stdout — both lines. The supervisor side MUST reject any migration-closure evidence whose filename paste is not in this shape. (3) The migration tool itself is the source of truth for the filename; until the tool surfaces the assigned filename in its response, the post-tool `ls` paste is the only viable evidence. |
| `pattern_signal` | strong signal — 4th firing of the filename-verbatim class. Per the escalation clause, codified check above is now binding. If a 5th firing surfaces despite the codified check, promote to a Catalog entry with a pre-merge linter that greps closure-evidence blocks for migration-filename mentions and fails any whose surrounding context does not include the verbatim `ls supabase/migrations | tail -…` shell line. |
#### 2026-06-12 — INC-73: new-engine-mode telemetry-contract drift (feed-mode satisfied zero of the five contracts per-ticker mode satisfied; first-fire was diagnostically opaque)

| Field | Value |
|---|---|
| `ts` | 2026-06-12 (FP-048 Phase 3b first natural fire of `news_sentiment_7d`; INC-73 surfacing + same-day resolution). |
| `category` | NEW (telemetry-contract drift on a newly-introduced engine mode — three independent defects in the feed-mode slice path that together masked the root cause: (1) no try/catch around the page drain → `failure_reason` never stamped, `__feed__` cursor claim never released, no `slice.failed` event emitted; (2) shim discarded verbatim `Error.message` in favour of a generic `slice_failed` 500 body; (3) per-page `heartbeat_at` tracked the kernel-frozen `as_of` instead of wall time, making a long healthy drain structurally indistinguishable from a frozen one to the sweeper). |
| `pr_ref` | FP-048 INC-73 fix PR (engine + shim + sweeper + MIG-089c + 9 regression tests). |
| `ai_tool` | executor (Phase 3a engine substrate landed the feed-mode page-loop; the per-ticker telemetry-contract checklist was not re-applied to the new mode before Phase 3b first-fire authorization). |
| `description` | The per-ticker slice path satisfies five distinct telemetry contracts on a throw: typed skip is written to `signal_queue_skips`, `failure_reason` is stamped on `signal_queue_runs`, a `longshort.signal_queue.slice.failed` event is emitted, the cursor claim is released (via deletion), and the verbatim `Error.message` reaches the 500 body. The feed-mode path (FP-048 Phase 3a) satisfied **zero** of these contracts on a throw. All five defects ship in code that passes the happy-path test suite — the gap is not "tests are missing" but "the failure-path contracts of mode A were never re-asserted for mode B". The first natural fire (run `72d7f1e3`, 2026-06-12 01:05 UTC) crashed on Page-2 and was diagnostically opaque for 15 minutes until the DEC-047 stale-heartbeat backstop fired at 01:20:01.273 — the backstop was the ONLY telemetry surface that produced a usable failure reason. |
| `detection_path` | operator-initiated post-fire investigation (per-minute run-row polling + `net._http_response` cross-correlation + sweeper-window observation). The DEC-047 backstop's first real-data fire was its own first validation surface — cite forward. |
| `resolution` | (1) Engine wraps the feed-mode drain in try/catch with `failure_reason` stamp + claim release + counter increment + re-throw (preserves the shim's `slice.failed` audit path). (2) 3-strikes terminal-fail via `slice_failure_count` (MIG-089c) — reset on any successful slice. (3) Per-page heartbeat uses an injectable `liveClock: ClockReader` defaulting to `productionClock` — `feed_cursor`/`feed_pages_fetched` still track `as_of` (replay determinism preserved), `heartbeat_at` tracks wall time. (4) Shim propagates the masked verbatim `Error.message` into the 500 body. (5) Sweeper releases held cursor claims when its CAS-to-failed wins (defence-in-depth). (6) Key-masking module scrubs `apiKey=…` / `apikey: …` / `Bearer …` tokens before any message crosses the audit boundary. (7) 9 regression tests cover the new contracts; per-ticker suite UNMODIFIED (regression fence). |
| `pattern_signal` | **BINDING (new-class codification):** every new consumer mode introduced onto a shared engine MUST re-validate ALL telemetry contracts the existing modes satisfy — not just the happy-path contracts. A new mode passing happy-path tests is NOT evidence it satisfies the failure-path contracts the older mode does. Pre-arm checklist for any future engine-mode addition (binding for FP-049+ and any successor): (a) list the five-or-N telemetry contracts the existing modes satisfy on throw; (b) write one regression test per contract for the new mode BEFORE first-fire authorization; (c) cross-check the new mode's catch path against the existing mode's catch path file-by-file; (d) cite this entry in the FP's pre-arm evidence. If a second firing of this class surfaces despite the codified check, promote to a Catalog entry with a pre-merge linter that compares failure-path branches across mode-discriminated `if (isFeedMode(…))` boundaries. |

#### 2026-06-12 — `any`-in-new-test-files at Gate 4 + Gate-evidence omission (2nd firing; codified countermeasure landed)

| Field | Value |
|---|---|
| `ts` | 2026-06-12 (INC-73 fix PR — CI Gate 4 RED, 26 errors, all in this PR's new files) |
| `category` | RECURRENCE (2nd firing of the "explicit `any` in new test files" class — first firing: FP-045 Phases 2–3, 47 errors, revision at commit `8cf70c6`). Paired sub-defect: **Gate-evidence omission** — the prior INC-73 work-complete report omitted the verbatim Gate 4 line (`npx eslint .`), citing only Deno test counts; CI exposed 26 errors that would have been caught locally had the gate been run. |
| `pr_ref` | INC-73 fix PR (queue-feed-slice-failure_test.ts + error-key-mask.ts) — revision-fix this turn |
| `ai_tool` | executor (landed new `_test.ts` file with 24 literal `any` tokens behind `// deno-lint-ignore-file no-explicit-any` — a Deno-linter directive that ESLint does not honor; the same defect shape as FP-045 Phases 2–3); plus omitted the `npx eslint .` line from the work-complete evidence section, allowing CI to be the first surface |
| `description` | The INC-73 regression suite `queue-feed-slice-failure_test.ts` was authored with stub builders typed as `any` (`(_k: string, v: any) => …`, `payload: any`, `const supabase: any = {…}`, `as any` casts on filter spreads, etc.) — 24 occurrences total. The file carried `// deno-lint-ignore-file … no-explicit-any` which suppresses Deno's lint rule but is invisible to the repo-wide `@typescript-eslint/no-explicit-any` rule that Gate 4 enforces. Identical defect shape to FP-045 Phases 2–3 (47 errors). Additionally, the prior INC-73 work-complete report listed "Gates: Deno test 68 passed" but did not include the verbatim Gate 4 line, so CI surfaced the regressions instead of local pre-commit. |
| `detection_path` | CI Gate 4 RED at commit-tip; operator surfaced the verbatim lint output to the executor with explicit reference to FP-045 commit `8cf70c6` as the precedent fix shape. |
| `resolution` | (1) Test file rewritten with narrow local interfaces (`RunStateRow`, `RecordedCall`, `PageResult`, `QueryCtx`, `MakeStubOpts`, `StubHandle`) + boundary `as unknown as <T>` casts only at the SupabaseClient/config seams (FP-041 typed-mock convention). Zero literal `any` tokens remain. Behavior unchanged — 4 INC-73 tests pass identical assertions. (2) `error-key-mask.ts:25-26` character-class escape regressions fixed (`[A-Za-z0-9_\-\.]` → `[A-Za-z0-9_.-]`); 5 mask tests pass unchanged. (3) **Codified countermeasure landed in this PR:** new source-sentinel `scripts/check-queue-worker-test-any.ts` (+ 7-test `_test.ts`) walks `supabase/functions/_shared/longshort-signals/shared/queue-worker/*_test.ts`, strips comments and string/template literals (including multi-line block JSDoc), and asserts zero literal `any` tokens in code positions. Runs locally via `deno run --allow-read scripts/check-queue-worker-test-any.ts` and at CI Gate 11 via the existing `deno test` repo-wide invocation (the script's own `_test.ts` calls `scanQueueWorkerTests()` and asserts `violations.length === 0`). Same mechanism as the existing source-sentinel family (`check-sentinel-patterns.ts`, `check-handler-liveness-markers.ts`, `check-wall-clock.ts`, etc.). (4) Gate 4 re-pasted at the fixed HEAD: `npx eslint .` → 0 errors / 15 warnings (the 15 are pre-existing unused-disable directives outside the scope of this PR). |
| `pattern_signal` | **2nd firing of the `any`-in-new-test-files class within 14 days.** Per the FP-045 entry's escalation clause, codified countermeasure required and landed: the source-sentinel above fails Gate 11 locally instead of waiting for Gate 4 CI. Paired sub-defect (Gate-evidence omission): the work-complete summary contract is **strengthened** — every PR touching code under `supabase/functions/` MUST include the verbatim `npx eslint .` final-line output in the Gates section, not a paraphrase or omission; reports lacking this line are themselves the defect (the omission is what allowed CI to be the surface). If a 3rd firing of either sub-defect surfaces despite the sentinel + evidence rule, promote to a Catalog entry with a pre-commit hook that runs the sentinel and rejects the commit on violations. |

#### 2026-06-12 — False-red gate-claims erode evidence trust symmetrically with false-greens (FP-049 INC-75 fix-turn retraction)

| Field | Value |
|---|---|
| `ts` | 2026-06-12 (FP-049 INC-75 fix turn; investigation-then-execution turn that immediately followed surfaced the false-red and ran a Gate-11 reconciliation against the verbatim CI workflow command). |
| `category` | NEW (false-red gate-claim — the symmetric counterpart of the well-codified false-green class). The INC-75 fix work-complete report carried a line claiming the full `_shared/` Gate-11 suite was "blocked by pre-existing DW-082/INC-66 errors"; the next-turn reconciliation ran the verbatim CI workflow command (`cd supabase/functions && deno test --allow-net --allow-env --allow-read`, workflow line 84) at the INC-75 HEAD and the suite returned `1161 passed | 0 failed` — the false-red conflated a scoped sub-invocation with the canonical workflow command. |
| `pr_ref` | FP-049 INC-75 fix PR (ACT-179) — the false-red appeared in the work-complete summary; the next-turn investigation surfaced and retracted it. |
| `ai_tool` | executor (ran a scoped subset under `_shared/` directly while attributing its blocker-class to the canonical full-repo Gate-11 invocation; did not re-run the canonical command before claiming "blocked"). |
| `description` | False-green failure modes (e.g., the multiple filename-recall defects already catalogued in this file; the d066c890 wall-clock false-green; the FP-047 stamping false-green) erode evidence trust by overclaiming. **False-red gate-claims erode it symmetrically by underclaiming** — a "blocked by X" line implies "I ran the gate" and "X is the cause", which together imply "no useful Gate-11 evidence is obtainable at this HEAD". Each of those three is independently verifiable; bundling them masks the verification path. The cost is identical to a false-green: a load-bearing evidence line in a work-complete report turns out to be wrong, and any downstream reader (operator, supervisor, future AI re-reading the action-tracker) who trusts the report without re-running the gate inherits the defect. The INC-75 case happened to be retracted on the very next turn before any decision was made on it, but the class is general: if the next-turn reconciliation had not run the verbatim workflow command, the false-red would have stood as the record. |
| `detection_path` | Operator brief next turn explicitly requested a Gate-11 reconciliation against the verbatim CI workflow command at the same HEAD. The reconciliation produced `1161 passed | 0 failed`, retracting the false-red. |
| `resolution` | (1) Retract the false-red line in this cycle's record (done — INC-75 investigation turn reconciled to `1161/0`). (2) Codify the class here so future executors recognize that "blocked by X" is a load-bearing claim with the same evidence burden as a green claim — needs the verbatim final-line output of the gate command actually being claimed, not a sub-invocation. (3) Forward binding: any work-complete report that says "blocked by", "pre-existing", "transitively failing", or any other non-green Gate claim MUST include the verbatim final line of the canonical gate command (the one quoted in the gates section of past green reports / in the CI workflow file) — sub-invocations may accompany it for context but never substitute for it. The symmetry rule: green and red claims carry the same evidence burden. |
| `pattern_signal` | **NEW class — false-red gate-claims (symmetric to false-greens).** Pre-commit checklist for any non-green Gate claim: (a) the gate command in the report is the canonical one quoted in past green reports / CI workflow; (b) the verbatim final line is pasted (`ok | N passed | M failed`, `✖ K problems (E errors, W warnings)`, etc.); (c) if a sub-invocation is what the executor actually ran, that is named as a sub-invocation, not as the canonical gate. If a 2nd firing of this class surfaces, promote to a stronger countermeasure (e.g., a checklist field in the work-complete contract that distinguishes "ran the canonical gate" from "ran a sub-invocation"). Cite this entry forward in any future work-complete summary that needs to make a non-green Gate claim. |

#### 2026-06-12 — Persistence-shape invariants assumed of the fetcher, not defended by the engine (INC-74; FP-048 extension)

| Field | Value |
|---|---|
| `ts` | 2026-06-12 (FP-048 Phase 3b second-fire, run `74e46ece`, on the INC-73-fixed engine) |
| `category` | EXTENSION of the INC-73 "every new mode re-validates ALL contracts" class. New axis: persistence-shape invariants (within-batch PK uniqueness on vendor-derived keys) must be defended by the **engine** at the batch-assembly point, not assumed of the fetcher. |
| `pr_ref` | INC-74 fix PR (queue-slice-worker.ts `dedupeFeedItems` + handler counter propagation + 5-test regression suite) |
| `ai_tool` | executor (Phase 3a engine substrate sent vendor-page rows directly to `signal_queue_feed_items.upsert(..., onConflict)` without batch-level dedupe; the assumption was that the fetcher would never emit duplicate `(article_id, ticker)` within one page — true in the Phase-0 probe, false on a real 35–70-page fire when the vendor article carries duplicate insights for the same ticker). |
| `description` | The per-ticker engine mode's persistence target (`signal_queue_staging`, PK `(run_id, ticker)`) is structurally dupe-free: the claim RPC is `FOR UPDATE SKIP LOCKED` against a UNIQUE-claimed cursor row, so one ticker = one staging row by construction. The feed-mode persistence target (`signal_queue_feed_items`, PK `(run_id, article_id, ticker)`) has many-rows-per-vendor-page where uniqueness is a vendor-data property, not a structural one. Postgres rejects `ON CONFLICT DO UPDATE` when a single INSERT targets the same conflict key twice ("command cannot affect row a second time") — the engine MUST dedupe at batch assembly. Pre-fix code assumed the fetcher would not emit dupes. The second live fire crashed 3× on Page-2 with the verbatim error stamped (INC-73 instrumentation held; root cause readable in <60s). |
| `detection_path` | INC-73 telemetry contracts surfaced root cause on first throw — operator polled `signal_queue_runs.failure_reason` and read the verbatim Postgres error directly. No log-archaeology. 3-strikes backstop bounded response time at 2m09s. |
| `resolution` | (1) `dedupeFeedItems(run_id, items)` — exported pure helper, deterministic first-occurrence-wins by `(article_id, ticker)`. (2) Applied at the engine-side batch-assembly point (NOT in the fetcher — fetcher remains the dumb pipe; engine owns persistence shape). (3) Observability counters `duplicate_tuples_dropped` + `duplicate_conflicts` (conflict = same key, differing sentiment/tier/published_utc against keeper) accumulate across all pages of one slice and surface in the `longshort.signal_queue.slice.completed` audit event metadata. (4) Phase 7 review: any nonzero `duplicate_conflicts` triggers a DEC to revisit first-wins. (5) Cross-page case intentionally untouched — each vendor page is a separate INSERT, DB-side `ON CONFLICT DO UPDATE` handles it. (6) Queue-worker any-sentinel (Gate 11) polices the new test file automatically. |
| `pattern_signal` | **BINDING (FP-048-extension to the INC-73 class lesson):** for any future engine mode whose persistence target carries a UNIQUE/PK over vendor-derived keys, the mode's pre-arm checklist MUST add: **(e) batch-level dedupe at the engine's assembly point, with observability counters threaded into the completion event** — even if the Phase-0 probe shows zero dupes (Phase-0 probes are too small to surface vendor-side duplicate rates reliably; persistence-shape defence is cheap and the only structural safeguard). Fetchers stay dumb pipes; engines defend persistence-shape invariants. If a future mode-addition lands without this pre-arm item, the omission is itself a governance violation. |

#### 2026-06-12 — FP-048 PolygonNewsRow single-consumer narrowing + FP-049 surface-fit STOP-corrective (chained entry)

| Field | Value |
|---|---|
| `ts` | 2026-06-12 (FP-049 Phase 1 commit 1b revision; ACT-174). |
| `category` | CHAINED (an original narrowing decision + the supervisor-authorized corrective when its blast radius surfaced at the second consumer). |
| `pr_ref` | FP-049 Phase 1 commit 1b revision-fix; PolygonNewsKeywordFetcher landing + PolygonNewsRow additive widen. |
| `ai_tool` | (1) executor at FP-048 Phase 1 (original narrowing — typed `PolygonNewsRow` to insights[]-only fields, deliberately excluded `title` / `description` / `image_url` / `keywords[]` with the inline comment "ship as a separate fetcher variant" if future ablation motivates them); (2) executor at FP-049 Phase 1 commit 1b (raised §22.8.4 STOP citing the row-shape narrowing as a surface-fit gap rather than silently inserting a duplicate normalizer or quietly editing the verified module). |
| `description` | **Part 1 — the original narrowing (FP-048 Phase 1).** `PolygonNewsRow` was intentionally narrowed to `insights[]`-only fields with the documented rationale "keep the signal-path surface narrow." At single-consumer time (`compute-news-sentiment.ts` scoring on insights alone) the narrowing imposed zero cost and the rationale was defensible. The narrowing's failure mode was latent: it presumed any future consumer that needed the excluded fields would ship as a SEPARATE fetcher variant — but did not account for the cost of duplicating ~80 lines of HTTP / cursor / look-ahead-gate / key-reattach machinery across two parallel modules with permanent drift seams. **Part 2 — the surface-fit STOP-corrective (FP-049 commit 1b).** When FP-049 needed to apply DEC-057 §(b)/(j) keyword + verb-gate matching to news headlines (a second consumer), the executor's first read of the original brief ("IMPORTS Signal #8's fetchOnePage") was structurally unsatisfiable — `fetchOnePage`'s `PolygonNewsRow` return shape excludes `title` and `description` (lines 84-89 + lines 195-213 of `polygon-news-feed-fetcher.ts`). The correct response was to raise §22.8.4 (surface-fit gap, Options A/B with tradeoffs named) rather than silently widen, silently duplicate, or quietly edit the verified module. Supervisor ruled Option B (additive widen of `PolygonNewsRow` with `title?` / `description?` optionals + populate-only-when-present in the normalizer, FP-048 byte-equivalence fence). |
| `detection_path` | Operator brief required the keyword fetcher to import FP-048's `fetchOnePage`; executor read the FP-048 source, identified the row-shape narrowing as the surface-fit blocker, raised §22.8.4 with cited line ranges and two named options + tradeoffs; supervisor ratified Option B. |
| `resolution` | (1) `PolygonNewsRow` widened additively: `title?: string` + `description?: string`, optional, populated only when the wire row carries a non-empty string (`undefined`, never sentinel, when absent). (2) `PolygonNewsWire` + `normalizeWireRow` updated symmetrically. (3) Lines 84-89 header comment rewritten to PRESERVE the original narrowing history + cite the supersession (FP-049, this date, supervisor-authorized) + cite the additive-surface discipline (`image_url` / `keywords[]` remain excluded). (4) Existing FP-048 `polygon-news-feed-fetcher_test.ts` suite passes UNMODIFIED (22 tests, byte-equivalence fence). (5) `PolygonNewsKeywordFetcher` composes the FP-048 fetcher with `{lookbackDays: 7}` and applies the §(f) trading-day floor client-side via `applyWindowLowerBound`. |
| `pattern_signal` | **BINDING (new-class codification):** narrowing a shared-infrastructure type's surface "to keep it narrow" is a *deferred* cost, not a *paid* one — the cost lands when the second consumer arrives. The protective discipline is two-step: (a) at narrowing time, the rationale comment MUST name the EXACT shape an additive widen would take if a second consumer materialised (so a future reader does not need to re-derive the additive path under pressure); (b) at second-consumer time, the executor MUST raise §22.8.4 surface-fit STOP with named options A (duplicate fetcher + drift seam) vs B (additive widen + byte-equivalence fence) and supervisor-ratify before either is implemented — silent widening AND silent duplication are both governance violations. The FP-048 Phase 3b precedent (`fetchOnePage` additive surface) is now the canonical resolution shape for any subsequent surface-fit STOP on a verified Phase-1 module. If a third firing of this narrowing-→-widen class surfaces despite the above codification, promote to a Catalog entry with a pre-merge linter that flags shared-infrastructure types whose interface comments contain "intentionally excluded" without an accompanying "if a second consumer materialises, widen as: …" addendum. |

#### 2026-06-13 — §22.8.5 deploy-replay-lag: deployed isolate served pre-pivot code at post-pivot source HEAD (FP-050 Phase 4 F1 stale-bundle false start; F2-pre codified countermeasure)

| Field | Value |
|---|---|
| `ts` | 2026-06-13 (FP-050 Phase 4 F1-deploy stale-bundle false start; codified by F2-pre / ACT-201). |
| `category` | NEW class — **deploy-replay-lag**: the *source* tree at HEAD contains the pivot; the *deployed* runtime isolate still executes the pre-pivot bundle because the previous isolate was alive and serving when the operator re-fired. A four-gate attestation block at the post-pivot source HEAD composes with a stale isolate into a false-green at the next live fire — there is no mechanical defence at the source-HEAD level, only at the runtime-vs-source-HEAD interface. |
| `pr_ref` | FP-050 Phase 4 F1 (ACT-199) + F1 deployment-discrepancy diagnosis (turn between ACT-199 and ACT-201) + F2-pre (ACT-201, this entry's codification). |
| `ai_tool` | executor at F1 landed `master.idx` URL + parser rewrite + drift sentinel at HEAD `4e5236ae` (Gate 1/2/2b/3 ALL GREEN; insider-transactions targeted run 120/0). The post-deploy operator backfill produced `seed_failed: HTTP 403 on .../form.20260525.idx` from run `bbe882e3` at `03:07:02Z`. The repo-grep at the same HEAD confirmed zero `form.{YYYYMMDD}.idx` constructors remained; the database-log read confirmed the deployed isolate was executing the pre-F1 operation-id (`edgar_daily_index`) and pre-F1 URL shape. The verdict was **stale bundle, not source defect** — the F1 redeploy at `03:11:02Z` shut down the pre-F1 isolate and the next fire executed the F1 code. |
| `description` | Supabase Edge Functions runtime keeps isolates warm; a `supabase functions deploy` does NOT atomically swap every in-flight isolate. Between the push and the next cold-start, an isolate that was warmed pre-pivot continues to serve pre-pivot code even though every source-side gate at the new HEAD reads GREEN. The four-gate attestation block (ACT-184 mechanism) attests *source-HEAD correctness*; it does not attest *deployed-HEAD correctness*. Every F2 verification step downstream of F2-pre — discovery-queue insert correctness, seedWorkItems consume-update semantics, GHA workflow egress shape — depends on the *runtime* executing the *source* HEAD. Without a mechanical interface check, a green attestation block can sit atop a stale isolate and the next live fire will surface the pre-pivot defect AT the post-pivot HEAD, producing a forensics path indistinguishable from a source regression (the executor's first instinct is "the pivot didn't actually land" — which then sends triage down a false trail). |
| `detection_path` | Detection in the F1 case was operator-driven post-fire: `bbe882e3` failed; operator re-issued the diagnostic; the AI was forced to read the deployed isolate's behaviour against the source tree and discovered the divergence. Codified detection (F2-pre, this commit) is `scripts/check-deployed-sha.ts` — CORS-preflights the target function URL, reads the `x-build-sha` response header (stamped by `_shared/handler.ts` from the `BUILD_SHA` env var at module init), and compares against `git rev-parse HEAD`. Four outcomes: MATCH (exit 0), MISMATCH (exit 1), HEADER_MISSING (exit 2 — deployment-pipeline configuration defect, distinct from a redeploy-fixable mismatch), SCRIPT_ERROR (exit 3). |
| `resolution` | (1) `supabase/functions/_shared/handler.ts` — reads `BUILD_SHA` env var once at module init (typed-Optional discipline: `string \| null`, no sentinel "unknown" string the verifier could mistake for a SHA), stamps `x-build-sha` on EVERY response path (OPTIONS, rate-limit, body-too-large, success, AuthError 401, ValidationError 400, PermissionDenied 403 incl. recent-auth-required 403, 500). (2) `scripts/check-deployed-sha.ts` + `_test.ts` — pure decision function `checkDeployedSha({functionUrl, readSourceSha?, readDeployedSha?})` returns the four-outcome `CheckOutcome` discriminated union; CLI wires `readSourceShaFromGit` (via `git rev-parse HEAD`) + `readDeployedShaFromOptions` (via `fetch(url, {method:'OPTIONS'})`); test suite covers all four outcomes + parseArgs + outcomeToExitCode via test-injection (no network, no git). (3) This catalog entry. (4) **Forward binding rule:** every F2 verification step downstream of F2-pre — and every Tier-A / money-path post-deploy attestation thereafter — MUST gate on a MATCH outcome from `check-deployed-sha` against the function being attested. A MISMATCH means "redeploy and re-verify"; a HEADER_MISSING means "fix the deploy pipeline to pass BUILD_SHA" (no amount of redeploying will fix it). The four-gate attestation block at source HEAD is necessary but NOT sufficient post-deploy: the deployed-SHA verifier closes the source-vs-runtime gap. |
| `pattern_signal` | **BINDING (§22.8.5 — new section):** any post-deploy attestation that asserts runtime behaviour at a specific source HEAD MUST be paired with a deployed-SHA MATCH outcome at that HEAD. A four-gate ALL-GREEN block alone is *source*-attestation, not *runtime*-attestation; conflating the two reproduces the F1 false start. The F2-pre verifier is the load-bearing interface check; subsequent F2 sub-commits (F2.a queue table, F2.b discovery script + GHA, F2.c seedWorkItems switch + concurrency-safety test) all gate on it. If a second firing of the deploy-replay-lag class surfaces after F2-pre lands, the countermeasure escalates to making the verifier a CI gate (post-deploy job that blocks on MISMATCH/HEADER_MISSING) rather than an operator-discipline check. |

#### 2026-06-13 — §22.8.5 deploy-replay-lag, sub-class A: BUILD_SHA secret presence ≠ runtime threading (FP-050 Phase 4 F2.c first-binding fire; ACT-206 pipeline-fix)

| Field | Value |
|---|---|
| `ts` | 2026-06-13 (F2.c first live application of the F2-pre verifier; HEADER_MISSING outcome against both `longshort-insider-compute` and `longshort-insider-compute-manual` at the F2.c HEAD; ACT-206 corrective). |
| `category` | NEW sub-class of §22.8.5 deploy-replay-lag — **BUILD_SHA-threading gap**: the `BUILD_SHA` runtime secret can be PRESENT on the Supabase project (visible in the Dashboard secrets list) yet ABSENT from the deployed isolate's `Deno.env.get('BUILD_SHA')` read — either because the deploy harness did not re-mount it at the new bundle's cold start, or because the secret value is empty/stale and never reflects the deploying commit. `_shared/handler.ts` reads `BUILD_SHA` once at module init under strict typed-Optional discipline (null when unset / empty / whitespace), so an empty value produces an OMITTED header rather than a stamped sentinel — which the F2-pre verifier correctly classifies as `HEADER_MISSING` (exit 2), distinct from the redeploy-fixable `MISMATCH` (exit 1). The first live fire of the verifier at F2.c proved this distinction is load-bearing: without it, the executor's instinct would have been "redeploy" (a no-op), then "redeploy again", then "the verifier is broken" — three rounds of false leads before the actual gap (deploy-pipeline configuration) surfaces. |
| `pr_ref` | FP-050 Phase 4 F2.c (ACT-205) — first commit whose attestation gate BINDS on the deployed-SHA MATCH outcome; FP-050 Phase 4 BUILD_SHA pipeline-fix (ACT-206, this entry) — corrective. |
| `ai_tool` | executor at F2.c work-complete observed `check-deployed-sha` returning `HEADER_MISSING deployed=<none> source=be4c5796...` for both function URLs against the F2.c HEAD; marked F2.c auto-NEEDS-REVISION per the §22.8.5 contract (correct behaviour — did NOT silently redeploy or silently accept the gap); surfaced the pipeline gap to the operator with the verbatim four-outcome contract semantics cited. Operator confirmed the §22.8.5 prediction had fired exactly as designed: the F2-pre verifier caught a latent deploy-pipeline configuration defect that would otherwise have stayed invisible until a Phase-5 money-path attestation surfaced the same shape under far higher stakes. |
| `description` | The original §22.8.5 entry (preceding row) catalogued the source-vs-runtime-bundle gap under the assumption that `BUILD_SHA` would be injected per-deploy by the deploy harness. Lovable's edge-function auto-deploy harness, however, has NO env-injection seam visible from the codebase: it deploys the bundle on every push to `main`, but it does NOT run a `supabase secrets set BUILD_SHA=$GITHUB_SHA` step before the deploy. The Dashboard `BUILD_SHA` secret can be set manually to a static value, but a static value cannot satisfy the F2-pre contract (which requires equality with `git rev-parse HEAD` at the attested commit). The HEADER_MISSING outcome at F2.c is therefore not "the deploy did not happen" and not "the bundle is stale" — it is "the deploy succeeded but the runtime env never received a SHA-shaped value." A naive redeploy (Lovable button, or even `supabase functions deploy` from a developer machine) does NOT fix this; only a deploy step that calls `supabase secrets set BUILD_SHA=<sha>` immediately before `supabase functions deploy` fixes it. The corrective lands as `.github/workflows/deploy-insider-edge-functions.yml` (ACT-206): on push to `main` it (1) `supabase secrets set BUILD_SHA=${GITHUB_SHA}` then (2) `supabase functions deploy` for the two F2-gated functions, in that order. The workflow REPLACES Lovable's auto-deploy for those two functions only; all other edge functions continue to deploy via the Lovable harness (no blast-radius widening). |
| `detection_path` | F2.c work-complete report; `check-deployed-sha` invoked against both function URLs; both returned `HEADER_MISSING` (exit 2). The four-outcome contract surfaced the gap class cleanly — the executor did not need to read the deployed bundle or speculate about isolate warmth, because the verifier's outcome enum names the gap class directly. |
| `resolution` | (1) `.github/workflows/deploy-insider-edge-functions.yml` — push-to-`main` workflow that threads `BUILD_SHA=${GITHUB_SHA}` via `supabase secrets set` then `supabase functions deploy` for `longshort-insider-compute` and `longshort-insider-compute-manual`, paths-filtered to the insider import graph + `_shared/handler.ts`. Requires repo GHA secrets `SUPABASE_ACCESS_TOKEN` and `SUPABASE_PROJECT_REF` — operator action required before first fire (workflow exits 2 with named error until both are present; no regression in the interim — Lovable's auto-deploy continues unchanged). (2) This catalog entry. (3) ACT-206 ledger entry with the operator-action gate + post-fire MATCH attestation slot. (4) **Forward binding extension:** any future edge function that becomes a money-path attestation target MUST be added to the workflow's `paths` filter AND its `deploy` step list — same-PR, never deferred. A money-path edge function deployed via Lovable's harness alone is a §22.8.5 sub-class A violation by construction. (5) Operator-side discipline addendum: if a third firing of §22.8.5 (either sub-class) surfaces, escalate to the originally-named CI-gate countermeasure — a post-deploy CI job that runs `check-deployed-sha` against every Tier-A function and blocks the workflow on any outcome other than MATCH. |
| `pattern_signal` | **BINDING (§22.8.5 sub-class A addendum):** "the secret is in the Dashboard" is necessary but NOT sufficient evidence that a build-time identifier reaches the runtime. The sufficient evidence is the verifier outcome at the deployed function URL. Deploy harnesses that do not expose an env-injection seam at deploy time MUST be supplemented by an out-of-band step that sets the secret IMMEDIATELY before the deploy — never in a separate scheduled job, never via Dashboard-only manual edit. The §22.8.5 verifier's four-outcome design (vs the originally-tempting two-outcome PASS/FAIL) was justified at codification time on the grounds that "HEADER_MISSING is a deploy-pipeline defect, distinct from a redeploy-fixable MISMATCH"; this entry records the first live fire that validates the design choice. |

#### 2026-06-13 — §22.8.5 deploy-replay-lag, sub-class B: Registration-Dependency Deploy Gap (FP-050 Phase 4 first end-to-end validation; ACT-209 closure)

| Field | Value |
|---|---|
| `ts` | 2026-06-13 (insider validation fire returned 202 `kind:started run_id=aadb1329-437f-4e67-8ee8-c53ad3a29c2d` at 21:07:28 UTC; 8-minute polling watch showed `status='running'`, `cursor_remaining=299`, `cursor_claimed=0`, `skips=0`, `insider_form4_rows ingested_run_id=aadb1329 count=0`, single audit event `signal_queue.run.started` and zero `SLICE_COMPLETED` events; HTTP `x-build-sha` probe across the six functions showed only `longshort-insider-compute` + `longshort-insider-compute-manual` carried the header — the four generic queue workers (`-queue-init`, `-queue-init-manual`, `-queue-slice`, `-queue-sweeper`) had no `x-build-sha`, confirming their bundles deployed via Lovable's auto-deploy at some earlier SHA pre-dating `registerInsiderDailyConsumer()` in `production-registrations.ts`). |
| `category` | NEW sub-class of §22.8.5 deploy-replay-lag — **Registration-Dependency Deploy Gap**: the runtime signal-registry state is constructed at module init from the *bundled* copy of `_shared/longshort-signals/shared/queue-worker/production-registrations.ts` inside each deployed worker. The source file changing on `main` does NOT propagate to a worker isolate unless that worker's function is redeployed. The slice handler's `pickOldestRunningRun` filters by `registry.listSignalIds()`; a `signal_queue_runs` row whose `signal_id` is not in the worker's bundled registry list is INVISIBLE to the slice and the sweeper alike — the run sits `status='running'` indefinitely with `cursor_remaining` undiminished, zero audit events, zero terminal CAS, and the sweeper unable to even fail it out (symmetric blindness through the same `listSignalIds()` iteration). Sub-class A (BUILD_SHA-threading) and sub-class B (Registration-Dependency) are independent — a function can carry the correct BUILD_SHA and STILL serve a stale registry if the registry surface was edited in a commit that did not redeploy the registry-reading workers. |
| `pr_ref` | FP-050 Phase 4 F2.c (ACT-205) — the commit that landed `registerInsiderDailyConsumer()` in `production-registrations.ts` via the side-effect import in `longshort-insider-compute-manual`. ACT-205 redeployed the two insider functions (via the ACT-206 GHA workflow at MATCH `65f2e0fd`) but did NOT redeploy the four generic queue-worker functions — those continued to run their Lovable-auto-deploy bundles from a pre-ACT-205 commit. ACT-209 (this entry) is the corrective: workflow renamed `deploy-insider-edge-functions.yml` → `deploy-edge-functions.yml`, paths widened to include the registry surface + queue-worker tree + generic worker function trees, deploy steps added for all four generic workers. |
| `ai_tool` | executor at INVESTIGATION turn observed the stall via the polling-evidence protocol (run state at t=0/+60s/+8m identical; zero progress; single `run.started` audit event); cross-checked via `x-build-sha` probe (four workers missing the header → bundle deployed via auto-deploy outside the ACT-206 GHA workflow scope → stale bundle is plausible); cross-checked via slice-handler edge-log review (every cron tick boot+shutdown ~20s with no info logs and no `SLICE_COMPLETED` audit event → `pickOldestRunningRun` returned `null` → registry did not contain `insider_transactions_90d`). The diagnosis converged on three independent lines of evidence before naming the gap class — explicitly avoiding the §22.8.4 single-observation conclusion failure (the negative existence claim "insider is unregistered on the slice workers" required three corroborating observations under varied probe shapes). |
| `description` | The ACT-206 GHA workflow (`deploy-insider-edge-functions.yml`) was correctly scoped at codification time to the two insider compute functions — the only functions whose runtime contract demanded BUILD_SHA threading for Tier-A attestation. The scope was INCORRECTLY scoped at registry-surface time: when ACT-205 (FP-050 F2.c) edited `production-registrations.ts` to add `registerInsiderDailyConsumer()`, the workflow's `paths:` filter did not include `production-registrations.ts`, so the workflow did not fire on the ACT-205 commit. Lovable's auto-deploy DID fire on every push and redeployed every function bundle including the four generic workers — BUT Lovable's auto-deploy does not thread BUILD_SHA, which is why the probe shows no header. The presence-or-absence of the header is a confounded signal here (it confirms only that Lovable's auto-deploy ran without BUILD_SHA, not that the bundle is stale). The DEFINITIVE evidence is the slice's silent-noop behaviour: every cron tick boots, runs ~250 ms, returns 200, emits zero `SLICE_COMPLETED` audit event, and leaves the cursor undiminished — the only path through the handler that exhibits this signature is the `pick === null` early return at line 35-36, which fires when `registry.listSignalIds()` does not contain any `signal_id` matching an open `signal_queue_runs` row. This is the sub-class B signature: a worker that deploys cleanly, runs cleanly, and silently skips work because its in-bundle registry pre-dates the source. |
| `detection_path` | INVESTIGATION mode polling watch (t=0, +60s, +8m snapshots of `signal_queue_runs` / `signal_queue_cursor` / `signal_queue_skips` / `insider_form4_rows`); `longshort_audit_logs` event-count delta; `function_edge_logs` HTTP-log review for `longshort-queue-slice` over the watch window; `x-build-sha` HTTP-header probe across all six functions; cross-reference against `.github/workflows/deploy-insider-edge-functions.yml` `paths:` filter to confirm the registry-surface file was not in scope. Five independent observations; conclusion only after all five aligned. |
| `resolution` | (1) `.github/workflows/deploy-edge-functions.yml` (renamed from `deploy-insider-edge-functions.yml`; canonical-role naming per the operator's "naming by canonical-role doesn't drift" preference) — `paths:` widened to include the registry surface (`production-registrations.ts`, `queue-config.ts`) + the shared engine surface (`queue-init.ts`, `queue-slice-worker.ts`, `queue-finalizer.ts`, `queue-sweeper.ts`) + the four generic worker function trees + `_shared/handler.ts` + the workflow file itself. Deploy steps added (in registry-dependency order): `longshort-queue-init` → `-init-manual` → `-slice` → `-sweeper` → `-insider-compute` → `-insider-compute-manual`, all six gated on a single pre-deploy `supabase secrets set BUILD_SHA=$GITHUB_SHA` step. Post-fire operator-action queue is the six-MATCH `check-deployed-sha.ts` paste (one per function URL). (2) This catalog entry. (3) ACT-209 ledger entry with the operator cleanup SQL embedded verbatim for the stranded `aadb1329` run. (4) **Forward binding rule (sub-class B):** *any commit modifying `production-registrations.ts` OR `queue-config.ts` OR the shared queue-worker engine surface (`queue-init.ts` / `queue-slice-worker.ts` / `queue-finalizer.ts` / `queue-sweeper.ts`) MUST be deployed via `deploy-edge-functions.yml` in the same atomic operation; absence of those deploys is a sub-class B violation by construction.* The workflow's `paths:` filter is the mechanical enforcement; the rule is the binding doctrine. (5) **Symmetric Catalog #43 supervisor-accountability clause:** the F2.c supervisor brief (operator directive 2026-06-13 morning) failed to enumerate the worker-redeploy requirement — it specified the consumer queue switch and the two insider function redeploys but did not call out that any change to `production-registrations.ts` cascades to four additional functions. The recursive Catalog #43 shape applies: the brief was not grep-grounded against the registry-reading worker set; a forward-binding supervisor discipline addendum lands at the same time as this catalog row — *every supervisor brief that touches `production-registrations.ts` or `queue-config.ts` MUST enumerate the four queue-worker functions in the redeploy list explicitly, never as "and the workers" hand-waving.* |
| `pattern_signal` | **BINDING (§22.8.5 sub-class B):** runtime registry state lives in worker bundles, not in source files. A registry edit on `main` propagates only to worker isolates redeployed since the edit; absent that redeploy, the registry-dependent code paths silently noop with zero observable error (no exception, no failed gate, no negative response code — just absence of progress). The detection signature is the cross-product of three independent traces: (a) `signal_queue_runs.status='running'` with `cursor_remaining` unchanged over ≥ two slice-cron periods; (b) zero `SLICE_COMPLETED` audit events for the run; (c) `function_edge_logs` showing slice booting and returning 200 with sub-second execution. Any two of three is suggestive; all three together is sub-class B. The sub-class B countermeasure escalates to a CI-level check if a second firing surfaces: a script that diffs `production-registrations.ts` between the deploying SHA and the last-deployed SHA of each registry-reading worker, fails the workflow if any worker lags. |
