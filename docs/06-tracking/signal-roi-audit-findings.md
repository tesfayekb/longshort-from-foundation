# Signal ROI Audit — Findings Ledger

> **Owner:** longshort strategy module | **Scope:** rolling per-signal audit of the 9 CROSSWIND signals for ROI-optimality, edge-survival, and construction correctness. | **Started:** 2026-06-30 (signals #6 + #2). | **Status:** in-progress. | **Authority:** operator-directed audit ("excellent ROI per signal, avoid any losing signal"). | **Discipline:** literature-cited only; NO fabricated edge/Sharpe numbers on our universe (Phase-7/FP-058 measures); NO signal-compute change made under audit — promotable findings charter as Phase-7 DWs (see `docs/08-planning/deferred-work-register.md`). | **Audit order (strongest-edge-first):** #6 momentum → #2 PEAD → #5 short-interest → #7 reversal → #1 analyst → #4 insider → #8 news → #9 catalyst → #3 options-flow.

## Audit conventions

- One audit pass per signal, 5 lenses: (1) economic hypothesis, (2) code construction, (3) decay / cadence, (4) failure path / typed-absence, (5) ROI risks.
- Per-signal verdict is one of: **spec-correct (no now-change)** / **spec-correct + Phase-7 ablation chartered** / **construction-bug → fix FP authored** / **edge-dead → cut FP authored**.
- All Phase-7 ablation findings home under FP-058 (Phase-7 measure-and-lock); see register entries DW-169 … DW-172 as the precedent template.
- Anti-fabrication: any claim of edge size on OUR universe is forbidden. Literature claims cite paper + year; their results are on their universes, not ours.

## #6 Cross-sectional momentum (12-1) — audited 2026-06-30 (HEAD `1ce89c85`)

**Verdict:** spec-correct baseline (NO now-change to `compute-momentum.ts`) + TWO Phase-7 ablations chartered (one risk-layer, one signal-level).

**One-liner:** raw 12-1 is the spec-correct §4.4.1 baseline; the real ROI levers are (i) a portfolio-vol-target risk-layer overlay [STRONG literature consensus — Barroso/Santa-Clara 2015, Daniel/Moskowitz 2016] and (ii) a residual-momentum signal-level Phase-7 ablation [MODERATE literature — Blitz/Huij/Martens]; per-name σ-divide is the WEAK signal-level candidate and likely double-counts with the regime feature + the vol overlay. Not a now-change — momentum is a CRITICAL §4.3.5 signal and the changes are Phase-7 ablations or risk-layer overlays, not code edits today.

**Charters:**
- **DW-169** — Momentum risk-layer portfolio-vol-target overlay (the strong-conviction win; risk-layer, NOT signal-change; double-counting axis is `market_realized_vol_6m` regime feature per DEC-066 §c).
- **DW-170** — Momentum signal-level construction ablation (Phase-7 shadow variants: raw 12-1 [keep as baseline] vs σ-divide [weak] vs residual [moderate, stronger candidate]); sequence AFTER DW-169 to size residual benefit.

**What was NOT chartered (and why):**
- A now-change to `compute-momentum.ts`: STOP — § ROI guardrails forbid silent signal mutation; §4.4.1 is CRITICAL; promotion requires a DEC and Phase-7 measurement.
- A fabricated Sharpe number on our universe: STOP — anti-fabrication discipline.
- A "cut momentum" verdict: STOP — momentum is the spec-correct backbone; the question is whether to LAYER on it, not whether to keep it.

## #2 PEAD (SUE × exp(−t_d/20)) — audited 2026-06-30 (HEAD `1ce89c85`)

**Verdict:** spec-correct + TWO Phase-7 ablations chartered (one σ_proxy standardization, one consensus-snapshot capture as zero-cost forward action).

**One-liner:** PEAD's STANDALONE edge in a liquid universe is, on the public record, close to dead (Chordia/Goyal/Sadka/Sadka/Shivakumar 2009 *FAJ* — "PEAD occurs mainly in highly illiquid stocks"; ~0.04%/month in the most-liquid quintile; secular decline). PEAD earns its seat as a COMBINER FEATURE / short-side risk-gate, NOT a standalone strategy. The highest-leverage correctable issue is the σ_proxy standardization shape (current analyst-dispersion-range from Finnhub epsHigh/epsLow vs the canonical Bernard-Thomas own-firm-time-series SUE std — the classic and stronger denominator; Cheong-Thomas 2011 documents the range variant's poor cross-firm-size scaling). The walk-down dampening (using T-0 frozen consensus that has already walked toward the true number) is second-order; mitigation requires our own T-0 snapshot series, which doesn't exist today — zero-cost to start storing now.

**Charters:**
- **DW-171** — PEAD σ_proxy standardization ablation (two-way ONLY: range vs own-firm-time-series — anti-phantom CORRECTION: a cross-sectional-σ "cheap fix" is NOT available; Finnhub returns ONLY epsAvg/epsHigh/epsLow/numberAnalysts, NOT individual estimates, so a true panel std cannot be computed — the range IS the best-available dispersion proxy; future investigators MUST confirm the endpoint contract before chartering a third "cross-sectional std" variant — it would be a phantom).
- **DW-172** — PEAD T-0 consensus snapshot capture (zero-cost forward action; LOW priority; build-now-vs-defer is operator's call; if not started now, Phase-7 walk-down ablation is delayed ~3 months while snapshots accrue).

**What was NOT chartered (and why):**
- A now-change to `compute-pead.ts`: STOP — §4.4.6 is CRITICAL; promotion requires a DEC and Phase-7 measurement.
- A cross-sectional-σ "cheap fix" using individual analyst estimates: STOP — the data does not exist (Finnhub panel-summary endpoint, not individual estimates); chartering it would assert against a phantom field (same anti-phantom class as the SI `si_pct_float` phantom CORRECTED at FP-041 addendum).
- A "cut PEAD" verdict: STOP — PEAD's combiner-feature value is a Phase-7 measurement question (FP-058); do NOT cut pre-measurement; do NOT expect standalone weight.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication.

## #5 Short-interest-change (ΔSI, 30d) — audited 2026-06-30 (HEAD `635c1924`)

**Verdict:** spec-correct baseline (NO now-change to `compute-short-interest-change.ts`) + THREE Phase-7 charters (one of them — the shadow stand-up — is **WEEKEND-BUILDABLE** to start the measurement clock now on free data).

**One-liner:** the SI literature locates the predictive edge primarily in the LEVEL (Asquith/Pathak/Ritter 2005 *JFE* ≈ −2.15%/mo top-decile-shorted-low-IO; Boehmer/Jones/Zhang 2008 *JF* ≈ 1.16%/mo shorting-flow level) and ESPECIALLY in days-to-cover (Hong/Li/Ni/Scheinkman/Yan 2016 *JFE* canonical DTC > SI% result, ≈ 1.2%/mo), NOT in the change; our #5 is built on the weaker, more lag-sensitive CHANGE dimension; the stronger alpha (DTC) is already computed in our system but spent ONLY on risk (the DW-165 squeeze screen); DW-173 stands up the level/DTC shadow variant THIS WEEKEND via the §6.5 harness so it accrues free comparison data now (promotion decision Phase-7-gated under FP-058 — no model / no labels yet); the squeeze stack composes correctly (alpha proposes, screen disposes; no path lets alpha override the screen — a GME-class name is ranked-short by #5 but hard-excluded by DW-165); the daily-SI vendor procurement (DW-174) is deferred until the free ablation runs (no point de-lagging the CHANGE dimension if level/DTC dominates it).

**Charters:**
- **DW-173** — SI level / DTC as-alpha SHADOW variant. **WEEKEND-BUILDABLE shadow stand-up** (cheap: existing fetcher + existing DTC column from MIG-131 + existing §6.5 harness; ZERO change to live #5 / live ranker / squeeze screen). PROMOTION (replace ΔSI / stack / keep) stays Phase-7-gated with the ex-ante rule pinned now to avoid p-hacking: level/DTC beats ΔSI beyond NDCG@25 noise band → replace; both carry independent edge → stack; else keep ΔSI.
- **DW-174** — Daily-SI vendor procurement charter (S3 / Ortex / S&P-Astec). Sister to DW-166. SEQUENCED AFTER DW-173 (the lag damages the CHANGE dimension most; if level/DTC dominates, de-lagging the CHANGE is the wrong spend). Pre-live nice-to-have, NOT a blocker.
- **DW-175** — Phase-7 orthogonality (rank correlation ΔSI vs level/DTC → informs combiner regularization + stack-vs-replace shape) + DTC = 7 squeeze-gate sensitivity probe (alpha is continuous, gate is binary; informs soft-penalty vs hard-exclude — MEASUREMENT ONLY, does NOT loosen the screen).

**Squeeze-composition confirmation (no fix required):** the composition between Signal #5 alpha and the DW-165 DTC ≥ 7 squeeze screen is CORRECT — alpha proposes (ranks), risk disposes (PreflightComposer hard planner-reject AFTER the rank); risk wins on its axis; the screen is a HARD gate, not a soft penalty. The DTC = 7 binary-vs-continuous seam → DW-175 sensitivity probe (Phase-7 measurement, not a now-change).

**What was NOT chartered (and why):**
- A now-change to `compute-short-interest-change.ts`: STOP — §4.4 Signal #5 is CRITICAL §4.3.5; promoting level/DTC pre-data is the fabricate-a-candidate anti-pattern FP-057 Sub-step 6 closed against.
- A "cut #5" verdict: STOP — ΔSI may still carry independent edge as a stacked combiner feature; the FP-058 ablation answers this; do NOT cut pre-measurement.
- Loosening the DW-165 DTC = 7 squeeze screen: STOP — the composition is correct; only the gate-shape sensitivity is a Phase-7 PROBE (DW-175), not a now-change.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication.

## #7 Short-term reversal (1-week) — audited 2026-06-30 (HEAD `adac9c0d`)

**Verdict:** the audit's FIRST now-fix — a §4.4.2 CRITICAL-signal mutation chartered as **DEC-071** (PROPOSED, not yet built; rides the consolidated weekend-build PR). Departure from the #6/#2/#5 Phase-7-only pattern, justified by an UNMITIGATED + LIVE failure mode + UNAMBIGUOUS literature + a CHEAP seam on existing data. The signal stays in spec; the pure-compute layer stays untouched; the gate is added at the orchestrator emit path.

**One-liner:** Signal #7's `-1 × ((P[T-1]/P[T-6]) - 1)` blindly FADES every recent move (the `-1 ×` is the load-bearing negation that distinguishes it from short-window momentum, per `compute-reversal_test.ts`); this creates a vicious LEFT TAIL — falling-knife on the long side, short-into-strength / breakout-squeeze on the short side. The failure mode is REGIME-DEPENDENT (Nagel 2012: reversal is a liquidity-provision premium that CRASHES in vol-spike regimes — the 2008 / COVID-March / GME / SVB class). VERIFIED LOAD-BEARING FACT: the §6.4 count-normalized fallback is the ACTIVE ranker (model loader = `model_active_artifact_loader_not_wired_pending_3_3b_ii`, not wired) and does ADDITIVE / EQUAL-WEIGHT catalog-order summation (`ranker.ts:156`) with NO cross-signal interaction veto — so the failure mode is GENUINELY UNMITIGATED today (only DW-165 DTC ≥ 7 short-side + DW-149 ex-post −15% stop exist; the trained combiner that could veto is months away under FP-058). DEC-071 closes the two literature-documented portions: (i) a **news/catalyst gate** (Tetlock 2010 / Savor 2012: reversal is concentrated in no-news days; news-day moves are information not overshoot) — reversal emits typed-absence `gated_by_news` / `gated_by_catalyst` when #8 or #9 is present at the same `as_of`, NOT a fabricated zero per §9; and (ii) a **magnitude cap** (Da-Liu-Schaumburg 2014: extreme moves are TREND, non-monotone) — reversal emits typed-absence `gated_by_magnitude` when `|R_5d| > 3σ(trailing-60d daily return)`, with the 3σ threshold LITERATURE-ANCHORED (Nagel 2012's 2-3σ framing) and PINNED in the DEC to prevent pre-data tuning (= p-hacking on a CRITICAL signal). The gate lives at the ORCHESTRATOR EMIT PATH (`reversal-orchestrator.ts`), NOT the planner — the pathology is a CROSS-SIGNAL INTERACTION and belongs at the signal-emit layer (a planner-gate would re-read 3 signals at planning time; distinct shape from DW-165 which is a SINGLE-SIGNAL exclusion at the planner). Honest scope: the gate does NOT eliminate the failure mode — news-absent sub-3σ moves can still trend; the residual tail goes to the eventual trained combiner (FP-058) + the DW-169 portfolio-vol-target overlay; DW-149 / DW-165 continue to fire orthogonally. No overclaiming.

**Charters:**
- **DEC-071** — "Signal #7 reversal cross-signal gate + magnitude cap" (PROPOSED, not yet ratified/built). Two keystone clauses (news/catalyst gate at same `as_of` + 3σ trailing-60d magnitude cap); seam = orchestrator emit path (`compute-reversal.ts` PURE/unchanged); `as_of` discipline pins no T+1 look-ahead; clause (e) stands up the ungated shadow variant; clause (f) acknowledges the residual tail; clause (g) defers two weaker fixes (wait-for-turn + volume gate) to Phase-7 ablation. Build authorized in the consolidated weekend PR.
- **DW-176** — Reversal ungated shadow variant (rides with the DEC-071 build; the only retrospective check on OVER-gating; Phase-7 reads the gated-vs-ungated comparator under FP-058).
- **DW-177** — Phase-7 ablation of the 3σ threshold + the news/catalyst lookback window + the two weaker fixes deferred from DEC-071 clause (g); measurement-only; informs any DEC-071 amendment FP.

**Why now-fix and not Phase-7 deferral (the asymmetry):** the shadow variant catches OVER-gating retrospectively, but NOTHING catches an unbounded short loss retrospectively. The trained combiner that could veto is months away. The literature verdict is unambiguous on both halves (Tetlock/Savor for explained moves; Da-Liu-Schaumburg for violent moves). The seam is cheap (cross-signal read in existing orchestrator; existing `signal_observations` table; existing typed-absence plumbing). Each ingredient on its own is insufficient justification for a critical-signal mutation; together they cross the threshold.

**What was NOT chartered (and why):**
- A silent edit to `compute-reversal.ts`: STOP — §4.4.2 is CRITICAL §4.3.5; mutating the pure-compute layer is a spec change and the wrong seam regardless; DEC-071 explicitly pins the orchestrator-only seam.
- A planner-side gate: STOP — cross-signal interaction belongs at the signal-emit layer (clause c rationale); DW-165's planner-gate is a SINGLE-SIGNAL exclusion, a different shape.
- A fabricated-zero suppression: STOP — §9 SENTINEL violation; typed-absence (`gated_by_news` / `gated_by_catalyst` / `gated_by_magnitude`) per existing `SignalSkip` discipline.
- A pre-data σ threshold TUNE (e.g., setting the cap at 2.5σ or 3.5σ on intuition): STOP — DEC-071 clause (b) pins 3σ trailing-60d as the literature-anchored default; Phase-7 (DW-177) ablates with measured data; pre-data tuning of a CRITICAL signal is p-hacking.
- A T+1 look-ahead on #8/#9: STOP — clause (d) pins same-`as_of` semantics; the build PR adds an explicit replay-determinism guard.
- Folding the two weaker fixes (wait-for-turn, volume gate) into the now-fix: STOP — weaker literature support; deferred to DW-177 Phase-7 ablation to measure marginal contribution beyond the DEC-071 baseline.
- Loosening DW-149 (−15% stop) or DW-165 (DTC ≥ 7 screen): STOP — orthogonal safeties; DEC-071 is additive ex-ante alpha discipline, not ex-post risk relaxation.
- A "cut #7" verdict: STOP — the signal is spec-correct as an alpha hypothesis (Lehmann 1990, Jegadeesh 1990); the construction is correct; the gate addresses the failure mode at the cheapest seam.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication; literature citations only.

## #1 Analyst-revision drift — audited 2026-06-30 (HEAD `3c259f07`)

**Verdict:** the BEST-CONSTRUCTED signal of the audit so far — the §4.4 spec construction includes the true-revision-delta sign rule (DEC-055 §c, the NKE $62→$50-on-$44-spot catch — exactly the silent sign-inversion a naive `sign(target − spot)` implementation would get wrong; verified at `compute-analyst-revision.ts` and `compute-analyst-revision_test.ts`) — but carries the audit's RICHEST improvement menu: one charter-deferred near-term construction lever (DEC-072 tier-weight, modest), one TIME-SENSITIVE forward-data-accrual build candidate (DW-178 capture step, the only item in this cluster where waiting has a permanent cost), one LARGE Phase-7 lever blocked on that capture (DW-179 per-analyst accuracy weight), one revision-type Phase-7 ablation (DW-180 EPS-estimate parallel stream + rating-change third stream — we are on the WEAKER of three available types per FMP), one vendor-gated long-run lever (DW-181 II All-America rankings), and one decay-horizon Phase-7 ablation (DW-182).

**One-liner:** the uniform `ANALYST_CREDIBILITY_WEIGHT = 1.0` (DEC-055 §a) is an explicit v1 placeholder; the literature-supported replacement is per-analyst accuracy weighting (Loh-Stulz 2011 "influential" ~12% subset; Clement 1999 top-quintile; Fang-Yasuda 2009 ranking persistence), but that LARGE lever needs an accuracy-history capture (DW-178) that does NOT currently accrue (the orchestrator persists the Stage-4 within-sector z-scored signal to `signal_observations`, NOT per-revision `(analystName, analystCompany, ticker, direction, target-delta, timestamp)` rows joinable to forward returns); the brokerage-tier weight (DEC-072) is the only buildable-now substitute but is a proxy-for-a-proxy (firm-tier ≈ resourced ≈ skilled, edge leaking at each ≈) carrying recurring tier-map maintenance, so the operator (Option 3) chartered it and deferred the build decision to the post-audit weekend-slate ranking; we are confirmably on the WEAKER of three available FMP revision types (we use `fmp_price_target_feed` / `/price-target-news`; `/analyst-estimates` and `/grades` are AVAILABLE BUT UNUSED — Gleason-Lee 2003 and the IBES Earnings-Revisions factor literature place the strongest documented edge on EPS-estimate revisions, with Brav-Lehavy 2003 documenting price-target accuracy at only ~38%), so DW-180 stands up EPS-estimate and rating-change as PARALLEL streams (not replacement) for Phase-7 measurement; the `exp(−age/5)` decay decays revisions to ~nothing within ~14 days while documented analyst-revision drift persists weeks-to-months (Gleason-Lee ≥ 3mo, Womack ~ 1–6mo) and is also inconsistent with PEAD's 20-day half-life for a similar underreaction phenomenon, but guess-tuning 5d → 20d would be the "silent ROI change" the guardrails forbid, so DW-182 charters the Phase-7 ablation (5d / 10d / 15d / 20d / 30d shadow variants).

**THE TIME-SENSITIVE ITEM (the flag worth pulling out of the menu):** **DW-178 per-revision-outcome CAPTURE step**. Every day without capture is a day of analyst-track-record accrual PERMANENTLY lost (FMP /price-target-news is a forward stream; reliable per-analyst-granularity historical backfill is not available). The CAPTURE itself is cheap (one alongside-INSERT per revision, ≈ 200–500 rows/day, no change to live signal/ranker/PnL). The WEIGHTING that uses it (DW-179) is Phase-7 — but Phase-7 weights need 6–12 months of accrued labels, so deferring the capture delays the LARGE lever by the full accrual window AFTER FP-058 starts. Sister anti-pattern to DW-172 (PEAD T-0 consensus capture). This is the one #1-audit item where the operator's "review all first, build together" framing has a real cost; flagged distinctly in the weekend build list above.

**Charters:**
- **DEC-072** — "Signal #1 analyst credibility-weight v1.1 (brokerage-tier proxy)" PROPOSED-CHARTER, BUILD-DEFERRED to the post-audit weekend-slate ranking (operator Option 3). Pins the SHAPE (4 tiers, single-multiplier-per-tier, applied at `compute-analyst-revision.ts:208`), the PROXY CHOICE (firm-tier, not analyst identity), and the literature-anchored multiplier DEFAULTS `{ BULGE: 1.3, MID: 1.0, BOUTIQUE: 0.7, UNKNOWN: 1.0 }` to prevent build-time drift. Two outcomes: authorized in the consolidated post-audit weekend PR → PROPOSED → RATIFIED; dropped from the slate in favor of larger levers elsewhere → stays PROPOSED indefinitely, v1 placeholder remains acknowledged, DW-179 remains the LARGE lever.
- **DW-178** — "Analyst per-revision-outcome CAPTURE step." TIME-SENSITIVE NEAR-TERM BUILD CANDIDATE (NOT Phase-7). Persist `(analystName, analystCompany, ticker, prior_target, new_target, direction, magnitude, published_at, as_of_date)` per revision into a new `analyst_revision_events` table; alongside-INSERT in `analyst-revision-orchestrator.ts`; no change to z-scored aggregate; idempotent UPSERT on `(analyst_normalized, ticker, published_at_microseconds)`. Confirm at build whether an existing per-revision audit row already exists upstream.
- **DW-179** — "Analyst per-analyst rolling accuracy-weight." The LARGE skill lever (Loh-Stulz / Clement / Stickel). Phase-7 + accrual-gated; DEPENDS-ON DW-178 having accrued ≥ 6–12 months of revision-outcome triples.
- **DW-180** — "Analyst EPS-estimate revision parallel stream + rating-change third stream." Phase-7 revision-type ablation. The PARALLEL stand-up itself is part of the post-audit weekend slate (ranked); promotion (replace / stack / drop) is Phase-7-gated under FP-058. Caveat: FMP `/analyst-estimates` is consensus-snapshot, not per-analyst-delta — building per-revision rows requires diffing consecutive snapshots (gold-standard IBES feed covered by DW-181).
- **DW-181** — "Analyst II All-America ranking weight." VENDOR-GATED, pre-live nice-to-have. Sister to DW-166 (borrow-rate) and DW-174 (daily-SI). Sequenced AFTER DW-179 evaluation so the marginal value of II is measurable, not assumed.
- **DW-182** — "Analyst decay-horizon ablation." Phase-7 (5d / 10d / 15d / 20d / 30d shadow variants). Do NOT guess-tune now (silent ROI change forbidden).

**Why DEC-072 is charter-deferred and not now-built (the operator-decided trade-off):** the tier-weight is a PROXY-FOR-A-PROXY (Fang-Yasuda's persistence result is about INDIVIDUAL analyst skill, not firm tier; firm-tier captures a fraction of the LARGE accuracy-weight lever DW-179). Per the ROI guardrails, ROI-changing construction changes should be MEASURED, not GUESSED — the tier-weight (and its multipliers) are hand-curated GUESSES. Shipping it pre-measurement would FORECLOSE clean Phase-7 attribution against DW-179 (if both are running we cannot cleanly attribute marginal contribution). Charter for visibility, pin the ex-ante rationale, defer the build-slot to the post-audit weekend-slate ranking. Distinct from DEC-071 (#7 reversal) which IS now-fix because that failure mode is GENUINELY UNMITIGATED today and the literature is unambiguous; here the failure mode is a known-acknowledged placeholder, not an unmitigated catastrophe.

**What was NOT chartered (and why):**
- A now-build of DEC-072: STOP — operator Option 3; tier-weight is a proxy-for-a-proxy + recurring maintenance + would foreclose Phase-7 attribution against DW-179; deferred to post-audit weekend-slate ranking.
- A now-edit of `compute-analyst-revision.ts`: STOP — Signal #1 is §4.4.x CRITICAL; the only proposed contribution-kernel change (DEC-072) is charter-deferred; DEC-055 §c (true-delta sign rule) is UNCHANGED.
- A guess-tune of the decay from 5d → 20d: STOP — silent ROI change explicitly forbidden by ROI guardrails; DW-182 charters the Phase-7 ablation.
- A "we already have per-revision capture" assumption: STOP — verified at HEAD `3c259f07` that the orchestrator persists the Stage-4 z-scored aggregate to `signal_observations`, NOT per-revision rows with analyst identity + forward-outcome-joinable timestamps; DW-178 build confirms at build whether an upstream audit row exists or a new capture is needed.
- A "cut #1" verdict: STOP — Signal #1 is the BEST-CONSTRUCTED of the audit so far (the DEC-055 §c true-delta sign rule alone is a genuine sophistication a naive impl would miss); the improvement menu is dense but no clause cuts the signal.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication; literature citations only (Fang-Yasuda 2009, Loh-Stulz 2011, Clement 1999, Stickel 1992/1995, Mikhail-Walther-Willis 1997/2007, Hong-Kubik 2003, Gleason-Lee 2003, Chan-Jegadeesh-Lakonishok 1996, Brav-Lehavy 2003, Bradshaw-Brown-Huang 2013, Womack 1996, Barber-Lehavy-McNichols-Trueman 2001, Brown-Call-Clement-Sharp 2015).

## #4 Insider transactions — audited 2026-06-30 (HEAD `5abd6588`)

**Verdict:** the audit's SECOND now-fix — a NON-CRITICAL signal-definition mutation chartered as **DEC-073** (PROPOSED, build authorized for the consolidated weekend PR alongside DEC-071). The signal is otherwise SUPERBLY constructed (the 10b5-1-planned-sale exclusion, the careful 5-tier role classifier with title-heuristic + DEF-14A upgrade path DW-093, market-cap normalization, the SEC EDGAR pipeline hardened this session with acceptance-datetime / CIK-to-ticker / dedup/pacing — all feed clean per-Form-4 data) — the construction failure is ONE specific clause: buys and sells are treated SYMMETRICALLY (`sign = +1`/`−1` at `compute-insider.ts:32-36`, identical role-weight × dollar × decay × market-cap both sides) when the literature is unambiguous that insider BUYS carry the edge and insider SELLS earn ~ZERO abnormal (Jeng-Metrick-Zeckhauser 2003 *RES*: purchases ~11.2%/yr abnormal, sales statistically indistinguishable from zero; Lakonishok-Lee 2001 *RFS*: predictive content concentrated in purchases; Seyhun 1998). Symmetric weighting MECHANICALLY DILUTES the buy-concentrated edge — a CFO's $5M post-vest diversification sell receives the same MAGNITUDE as the same CFO's $5M conviction buy, even with the 10b5-1-planned exclusion that already runs (because "discretionary sell" still bundles a heavy non-informational tail: taxes / tuition / divorce / RSU mechanics / option-exercise pre-expiry).

**One-liner:** drop the entire S-side branch — buys-only — at the §4.4.4 compute filter (DEC-073 clause a); the signal becomes a pure insider-BUY-pressure measure; parameter-free, literature-direct, zero free parameters (the operator-decided discipline: don't replace a guess with a costlier guess — the sell-side down-weight β alternative is rejected as the now-fix per clause (b) and becomes the Phase-7 ablation DW-183, MEASURED not GUESSED); the 10b5-1 sub-clause becomes moot (no S-rows survive); typed-absence semantics unchanged (existing `no_qualifying_transactions` literal reused for names with no qualifying purchases); the symmetric variant stands up as a §6.5 shadow alongside the live buys-only so the dropped sell-side's edge in OUR universe is RETROSPECTIVELY MEASURABLE (DW-183) — fully recoverable via a DEC-073 amendment FP with a measured β if sells turn out to carry edge.

**The pre-training urgency (why NOW-FIX, not Phase-7 deferral):** the combiner has NOT yet trained at HEAD `5abd6588`. Shipping buys-only NOW gives the eventual trainer the purest literature-supported signal; deferring means training on the KNOWN-DILUTED symmetric signal, baking the dilution into learned weights, and then needing a retrain to undo it. The urgency window CLOSES at first combiner training fire. This is parallel to DEC-071's "unmitigated now" argument but via a different mechanism (don't-bake-dilution-into-training rather than no-current-veto). NON-CRITICAL signal (§4.3.5 critical-set is #6 + #7 only, per `signal-catalog.ts` and the `missing_critical_signal_{6,7}` excluded-reason enum) → no §4.3.5 amendment, lighter friction than DEC-071.

**The NOT-TIME-SENSITIVE finding (the good news that distinguishes insider from analyst):** MIG-095 (ACT-191) ALREADY persists `owner_cik` on every parsed Form 4 row, and the DEC-058 §h dedup key `(issuer_cik, owner_cik, transaction_date, transaction_seq)` already keys by per-insider identity. Forward per-insider history accrues passively — no urgent capture step required. The Cohen-Malloy-Pomorski 2012 opportunistic/routine classifier (the FRONTIER lever, ~95% of edge concentrates in opportunistic; routine ~0 alpha) is Phase-7-buildable on FREE EDGAR data (3–5yr backfill ETL spec lives in DW-185), NOT vendor-gated, NOT time-sensitive. **The audit's time-sensitive-capture list STAYS AT TWO** (DW-172 PEAD T-0 consensus, DW-178 analyst per-revision capture); insider does NOT join. This is the load-bearing distinction from the analyst case where DW-178 was time-sensitive precisely because the orchestrator was NOT persisting per-revision identity rows.

**Charters:**
- **DEC-073** — "Signal #4 insider buys-only (drop sell-side)" (PROPOSED, build authorized for the weekend bundle). Eight clauses: (a) drop the S-branch at `compute-insider.ts:32-36`; (b) parameter-free, β down-weight is rejected as now-fix and is DW-183 instead; (c) the 10b5-1 sub-clause becomes moot and SIMPLIFIES; (d) NON-CRITICAL, no §4.3.5 amendment; (e) typed-absence unchanged; (f) pre-training urgency justifies now-fix; (g) symmetric shadow variant rides on §6.5 harness for recoverability; (h) decay (DW-184) and CMP classifier (DW-185) are explicitly NOT part of this DEC.
- **DW-183** — Insider buy/sell-asymmetry Phase-7 ablation (3 arms: symmetric shadow / sell-down-weight β grid / buys-only live). The recovery comparator that makes DEC-073 recoverable; depends on DEC-073 build (clauses a + g) + FP-058 + accrued labels.
- **DW-184** — Insider decay-horizon Phase-7 ablation (grid: 14d (live) / 30d / 60d / 90d / 180d half-life). Same shape as DW-182 (analyst decay), MORE SEVERE because insider drift is longer (Lakonishok-Lee 2001: 6–12 months drift; Cohen-Malloy-Pomorski 2012: ~82bp/mo sustained over post-trade year; Ali-Hirshleifer 2017 replication: ~1 year). Pre-data tuning forbidden as silent ROI change.
- **DW-185** — Cohen-Malloy-Pomorski opportunistic-vs-routine classifier (Phase-7 + one-shot free-EDGAR 3–5yr backfill ETL spec). **EXPLICITLY NOT TIME-SENSITIVE** because owner_cik already persists (MIG-095). The frontier lever per literature (~95% of edge in opportunistic subset); composes with DEC-073 (opportunistic-buys-only is the natural composition).

**Why DEC-073 ships now (the asymmetry that distinguishes it from DEC-072 charter-deferral):** unlike DEC-072 (which is a proxy-for-a-proxy with recurring maintenance and would foreclose clean Phase-7 attribution against DW-179), DEC-073 is LITERATURE-DIRECT (drops a clause the literature says carries ~zero abnormal), introduces ZERO free parameters (no β to tune, no tier-map to maintain), and the recoverability is GUARANTEED via the symmetric shadow variant (clause g + DW-183) — if sells turn out to carry edge in our universe, the path back is mechanical. The cost of NOT shipping (training on diluted signal, then retraining) exceeds the cost of shipping (shadow comparator stands up either way). DEC-072's tier-weight has none of those properties.

**What was NOT chartered (and why):**
- A guessed sell-side down-weight β (e.g. β = 0.3 as a "literature-anchored placeholder"): STOP — operator chose Option β (buys-only); a guessed β would be the analyst-tier-weight (DEC-072) anti-pattern (ship a guess + still owe the Phase-7 measurement); DW-183 measures β instead of guessing it.
- A guess-tune of the 14d decay to 60d/90d on intuition: STOP — silent ROI change explicitly forbidden by the ROI guardrails; DW-184 charters the Phase-7 ablation.
- An urgent insider-CIK capture DW (sister to analyst DW-178): STOP — MIG-095 ALREADY persists `owner_cik` (verified); DEC-058 §h dedup key already includes it; the opportunistic/routine accrual happens passively forward; the time-sensitive list stays at two (DW-172 + DW-178).
- A §4.3.5 critical-gate amendment: STOP — Signal #4 is NON-CRITICAL (only #6 + #7 are critical per `signal-catalog.ts`); no amendment required; lighter friction than DEC-071.
- A silent edit to `compute-insider.ts` this turn: STOP — governance-only charter; build is the consolidated weekend bundle alongside DEC-071.
- A "cut #4" verdict: STOP — Signal #4 is otherwise SUPERBLY constructed (5-tier role classifier + 10b5-1 exclusion + market-cap normalization + the EDGAR pipeline hardened this session); the construction failure is ONE specific clause (symmetric sign-handling); the fix is contained.
- A change to the 5-tier role-weight (DEC-058 §c) or the market-cap normalization or the DEC-058 §h dedup key: STOP — out of DEC-073 scope; UNCHANGED.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication; literature citations only (Jeng-Metrick-Zeckhauser 2003 *RES*, Lakonishok-Lee 2001 *RFS*, Seyhun 1998, Cohen-Malloy-Pomorski 2012 *JF*, Ali-Hirshleifer 2017).

## #8 News sentiment (DEC-056 discrete `{−1, 0, +1}` × tier × `exp(−age/24)` over trailing 7d) — audited 2026-06-30 (HEAD `cd3abfd8`)

**Verdict:** spec-correct + the construction is THE BEST anti-phantom discipline in the book (DEC-056 §a discrete sentiment mapping + §e PR-wire exclusion + the articles-present→always-a-value-including-0 distinction vs PEAD's `zero_dispersion` skip + malformed-never-coerced-to-0). NO now-fix, NO vendor-upgrade. One modest forward-only capture lever (DW-186 articleCount, TIME-SENSITIVE) + three Phase-7 charters (DW-187 standalone-vs-combiner decomposition, DW-188 biphasic-kernel ablation, DW-189 vendor-eval explicitly deferred behind DW-187).

**One-liner:** news-sentiment is the audit's WEAKEST standalone signal — small pre-cost effect (Tetlock 2007/2008), TWO-PHASE drift-then-reversion (NOT clean drift; Tetlock's pessimism effect reverses within ~5 trading days), heavily COMMODITIZED post-2010 (Heston-Sinha 2017; Ke-Kelly-Xiu 2019: daily-horizon predictability decayed materially as machine readers shortened the publish-to-discovery gap from hours to seconds) — a combiner-feature (orthogonal narrative channel) NOT standalone alpha, same disposition class as PEAD; BUT the construction (DEC-056 discrete `{−1, 0, +1}` mapping, PR-wire exclusion, zero-vs-skip distinction, malformed-never-coerced-to-0) is the BEST anti-phantom discipline in the book — exactly what a small/noisy/contested edge needs; the discrete mapping is CORRECT not over-conservative (Loughran-McDonald 2011: sign > magnitude for financial text; switching to provider magnitude would import Benzinga calibration noise — instead recover intensity via DW-186 articleCount as a SEPARATE combiner feature, Da-Engelberg-Gao 2011 independent-attention-predictability); NO now-fix, NO vendor-upgrade; the one modest lever is DW-186 articleCount-as-attention-feature; Phase-7 measures its combiner weight (DW-187) + the biphasic-kernel (DW-188).

**STEP-A persistence finding (the disposition pivot for DW-186):** the news kernel `computeNewsSentiment` (compute-news-sentiment.ts:218) DOES compute `meta.articleCount` / `prExcludedCount` / `unmappedPublisherCount`, but the orchestrator path DROPS meta at persist — `captureSignalObservations` (shared/missingness-capture.ts:44-59) persists only `{value, is_present, gics_sector, computed_at, carried_forward}` to `signal_observations`; no meta column. The intermediate `signal_queue_feed_items` table retains raw rows but is sweeper-pruned at `stagingTtlSec = 86400` (24h; news-sentiment-queue-registration.ts:92) → **historical articleCount is NOT recoverable beyond 24h**. → DW-186 joins the TIME-SENSITIVE capture list at THREE (DW-172 PEAD-T0, DW-178 analyst per-revision, DW-186 news articleCount), NOT a cheap surface-it.

**The provider-sentiment bottleneck (verified, second-order):** Polygon `insights[].sentiment` is the Benzinga sentiment field passed through (Polygon's news endpoint is a Benzinga wholesale relationship) — Benzinga is a rules+ML hybrid trained on financial-news prose, NOT academically benchmarked, NOT publicly validated against a held-out set; better than keyword-bag, materially worse than purpose-built (RavenPack ESS, MarketPsych) on agreement-with-human-coders benchmarks. The discrete `{−1, 0, +1}` collapse (DEC-056 §a) is the right defense — Loughran-McDonald 2011 explicitly argue sign is more reliable than magnitude for financial text because magnitude is dominated by stylistic noise. Vendor upgrade is EXPLICITLY DEFERRED behind DW-187 (DW-189): a better classifier × a small/contested edge is still a small/contested edge; mid-5-to-low-6-figure/yr pricing not justified by a combiner-feature-grade signal.

**The 24h decay + the reversion wrinkle:** the current `exp(−age_hours / 24)` is "defensible by accident" — a 24h half-life makes the reversion-phase weight tiny (~6% at day-4), so the current kernel DODGES rather than EXPLOITS the documented reversion. A biphasic kernel (scaled-difference-of-exponentials / sign-flip-at-horizon-T) would EXPLOIT the reversion leg (short past-week's most-positive / long past-week's most-negative) — but the reversion phase MAY HAVE COLLAPSED post-2010 commoditization, so DW-188 is Phase-7 (verify-the-shape-still-exists before fitting), NOT a now-tune.

**Charters:**
- **DW-186** — News articleCount attention-feature CAPTURE (**TIME-SENSITIVE** per STEP-A; 3rd time-sensitive item). Schema-additive (column on `signal_observations` or sidecar `news_attention_observations`); surfaces `articleCount` as a SEPARATE combiner feature distinct from sentiment-direction; recovers the intensity the discrete `{−1, 0, +1}` mapping deliberately discards, WITHOUT trusting Benzinga's magnitude calibration. Anti-completion-theater: no change to the sentiment kernel, the discrete mapping, the PR-wire exclusion, or the existing value.
- **DW-187** — News standalone-vs-combiner-feature decomposition (Phase-7). Direct PEAD-analogue: measure standalone IC + incremental combiner contribution; honest down-weight-or-drop decision if standalone IC ≈ 0 AND incremental contribution dominated by other features. Composes with DW-186 (attention arm).
- **DW-188** — News biphasic-kernel ablation (Phase-7). Tests the drift-then-reversion shape (Tetlock 2007/2008) against the live `exp(−age/24)`; verifies reversion still exists in liquid post-2020 names before fitting. Sequence after DW-187 (the ROI ceiling is conditioned on standalone result).
- **DW-189** — News sentiment-vendor evaluation (charter, EXPLICITLY DEFERRED pending DW-187). Sister to DW-166 / DW-174 / DW-181 — don't procure behind an unmeasured signal.

**What was NOT chartered (and why):**
- A now-fix to the sentiment kernel: STOP — the construction is the BEST anti-phantom discipline in the book; no clause is wrong; the EDGE is the question, not the construction.
- A switch from discrete `{−1, 0, +1}` to provider magnitude: STOP — Loughran-McDonald 2011 sign > magnitude for financial text; switching would import Benzinga's poorly-calibrated probability noise; intensity is recovered orthogonally via DW-186 articleCount instead.
- A now-procure of RavenPack / MarketPsych: STOP — DW-189 explicitly deferred behind DW-187; a better classifier behind a near-zero-standalone-alpha signal is wasted spend; mid-5-to-low-6-figure/yr pricing not justified by a combiner-feature-grade signal.
- A guess-tune of the 24h half-life: STOP — silent ROI change forbidden under the ROI guardrails; DW-188 charters the Phase-7 ablation (and the reversion phase MAY have collapsed post-2010 — verify first).
- A flip of `exp(−age/24)` to a biphasic kernel today on Tetlock-intuition: STOP — pre-data tuning is silent ROI change; the reversion shape must be verified on OUR-universe post-2020 data first (DW-188).
- A "cheap surface-it" disposition for DW-186: STOP — STEP-A verified meta is structurally dropped at persist AND feed_items TTL = 24h; historical attention counts are NOT recoverable → TIME-SENSITIVE capture is the correct framing.
- A "cut #8" verdict: STOP — even a weakest-signal verdict justifies KEEPING the signal as a combiner-feature (orthogonal narrative channel); the down-weight-or-drop decision is DW-187 MEASURED, not GUESSED.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication; literature citations only (Tetlock 2007 *J. Finance*, Tetlock-Saar-Tsechansky-Macskassy 2008 *J. Finance*, Da-Engelberg-Gao 2011 *J. Finance*, Engelberg-Reed-Ringgenberg 2012, Heston-Sinha 2017, Ke-Kelly-Xiu 2019 NBER WP 26186, Loughran-McDonald 2011 *J. Finance*, Hutto-Gilbert 2014).

## #9 Active catalyst (per-event-type weighted intensity over trailing 5 trading days, UNSIGNED) — audited 2026-06-30 (HEAD `cb17a66d`)

**Verdict:** the audit's THIRD now-fix — a FALLBACK-REGIME DEPLOYMENT change chartered as **DEC-074** (PROPOSED, build authorized for the consolidated weekend PR alongside DEC-071 and DEC-073). The signal construction (`compute-active-catalyst.ts`, DEC-057 §a) is UNCHANGED — `raw_signal = Σ catalyst_weight × age_weight` over trailing-5-day events is correct as designed. The defect is at the FALLBACK RANKER LAYER: catalyst is UNSIGNED (always ≥ 0; §4.4.9 "Direction captured by other signals") and the §6.4 additive fallback (`ranker.ts:13` composite formula; `computeComposite` iterates `SIGNAL_IDS_ALL` directly) sums it as if signed, producing a DIRECTION-BLIND positive contribution that pushes every high-event name into the LONG book regardless of whether the underlying event is good or bad news. DEC-074 introduces a separate `SIGNAL_IDS_FALLBACK_SUM` (= `SIGNAL_IDS_ALL` minus catalyst), excluding catalyst from the additive sum ONLY; catalyst stays in `SIGNAL_IDS_ALL` and continues to compute / persist / emit into `combiner_feature_vectors` — the trained combiner reads it unchanged as an interaction feature when FP-058 lands. The audit's META-FINDING (DEC-075) elevates this: 2 of 9 signals (catalyst + reversal — the two most interaction-dependent) are structurally mis-deployed by the additive fallback; the regime is not "graceful degradation" but a regime where correctness-of-construction does not imply correctness-of-contribution.

**One-liner:** Signal #9 catalyst is UNSIGNED salience by design — its economic value is multiplicative (amplify the OTHER signals' direction when an event is fresh), expressible by a trained combiner via tree splits (`catalyst_z > τ AND pead_z > 0 → strong long`; `catalyst_z > τ AND pead_z < 0 → strong short`) but structurally inexpressible by the additive `computeComposite` loop (addition has no `×` operator). VERIFIED LOAD-BEARING FACTS (HEAD `cb17a66d`): (1) composite formula at `ranker.ts:13` is `Σ_critical z_i + Σ_non_critical (is_present ? value : 0)` — pure additive, no interaction; (2) `computeComposite` iterates `SIGNAL_IDS_ALL` DIRECTLY; (3) `active_catalyst_flag` is in `SIGNAL_IDS_NON_CRITICAL` and `raw_signal ≥ 0` by construction; (4) the fallback IS the active ranker (model loader `model_active_artifact_loader_not_wired_pending_3_3b_ii` not wired) AND drives the early live-money window (no trained-combiner-before-live gate on the master plan) → real-capital misallocation in the pre-trained window, not just paper. (5) Per-event records (`active_catalyst_events`) persist at Stage 7 → NOT time-sensitive; time-sensitive-capture list stays at THREE. SALIENCE-PREDICTIVENESS facts (literature, not our-universe): event-study drift is SIGNED by event outcome (Bernard-Thomas 1989 PEAD; Schwert 1996 M&A; Sarkar-de Jong 2006 FDA), so unsigned event PRESENCE carries near-zero unconditional alpha — the drift lives in surprise sign × magnitude, which is PEAD #2's territory not catalyst's; attention/salience literature (Barber-Odean 2008 *RFS*; Da-Engelberg-Gao 2011 *J. Finance*) shows attention-induced buying followed by REVERSAL in retail-heavy names at days-to-weeks horizons — i.e., attention is conditioning-valued, often NEGATIVE if treated additively at relevant horizons. Catalyst is a CONDITIONING VARIABLE, not a signed ranking signal — treating it as a summed ranking signal is a category error. PER-EVENT-TYPE HALF-LIVES (DEC-057 §a): Earnings 48h / M&A 96h / FDA 72h / Regulatory 96h / Guidance 48h / Exec 72h / Analyst 24h / Partnership 36h / Dividend 36h / Splits 24h are defensible PRIORS not measured calibrations; the RELATIVE ordering (M&A > Earnings > Analyst) is literature-defensible (deal-arb horizons > PEAD drift > immediate-pricing of rating moves); exact-hour calibration is Phase-7 (DW-190 folds in the half-life grid).

**THE UNSIGNED-IN-AN-ADDITIVE-RANKER analysis (the existential question):** catalyst is ALWAYS POSITIVE. After cross-sectional z-scoring inside the fallback pipeline, the post-z mean ≈ 0 but the SIGN of each name's z is determined purely by event presence, not by event direction. In the long-short ranker (top = long, bottom = short), an additive positive contribution from catalyst pushes every name with a recent Tier-1 event toward the LONG side equally — Stock A (FDA APPROVAL) gets catalyst z = +1.8 → pushed long; Stock B (FDA REJECTION) gets catalyst z = +1.8 → also pushed long. The signal cannot distinguish A from B; only the OTHER signals (PEAD, news, momentum) carry direction. If those other signals are weaker/noisier than catalyst's contribution in any given name, catalyst DOMINATES and drags B into the long book. This is not hypothetical — it is the mechanical consequence of summing an unsigned salience term with signed alpha terms. The interaction shape catalyst REQUIRES ("amplify the OTHER signals' direction when an event is fresh") is expressible by a trained LightGBM via tree splits but is STRUCTURALLY INEXPRESSIBLE by the additive `computeComposite` loop. In the fallback, catalyst contributes a DIRECTION-BLIND LONG TILT, not a salience amplification. This is the SAME shape as DEC-071's reversal gap (fallback can't express the conditional) but MORE FUNDAMENTAL — reversal had a SIGNED direction (mean-reversion) that could be additively useful when priors were wrong; catalyst has NO direction at all to contribute additively.

**THE META-FINDING (elevated to DEC-075):** this is the SECOND signal where the active additive fallback is structurally incapable of using a signal as designed. The first was reversal (#7, needs interaction to veto falling knives → linearized via DEC-071 gate). The second is catalyst (#9, needs interaction as a salience gate → excluded by DEC-074 because no linearizing gate is available). Two of nine signals — and the TWO MOST INTERACTION-DEPENDENT — are mis-deployed by the additive fallback. That is no longer a per-signal coincidence; it is a REGIME INVARIANT. The §6.4 fallback is NOT graceful degradation — it is a regime where CORRECTNESS OF CONSTRUCTION DOES NOT IMPLY CORRECTNESS OF CONTRIBUTION. DEC-075 names the defect, establishes the additive-valued vs interaction-valued classification gate as a default for every future signal addition (interaction-valued signals EXCLUDED BY DEFAULT until a trained combiner exists), folds in the initial classification audit of all nine signals (7 additive-valued or TBD pending #3; 2 interaction-valued — #7 gated, #9 excluded), and gives FP-058 a concrete acceptance criterion (trained combiner must outperform the exclusion-disciplined fallback on the interaction-valued signals, else it has no measured edge over discipline there).

**Charters:**
- **DEC-074** — "Signal #9 catalyst conditioning-only in the §6.4 additive-fallback regime" (PROPOSED, build authorized for the weekend bundle). The 3rd now-fix. Surgical SUMMATION-SET change in `ranker.ts` (introduce `SIGNAL_IDS_FALLBACK_SUM` = `SIGNAL_IDS_ALL` minus catalyst; `computeComposite` iterates the new set). Catalyst KEPT in `SIGNAL_IDS_ALL` (computed, persisted, emitted, fed to trained-combiner path UNCHANGED). PARAMETER-FREE (exclusion, not tuning). Non-destructive, reversible by construction. Distinct shape from DEC-071 (literature-anchored gate) and DEC-073 (literature-direct asymmetry filter); this one is a DEPLOYMENT-LAYER change, not a signal-construction change.
- **DW-190** — Phase-7 catalyst-as-interaction ablation: catalyst-as-INTERACTION vs catalyst-as-ADDITIVE vs catalyst-EXCLUDED vs catalyst-as-CONDITIONING-GATE; folds in per-event-type half-life grid calibration; records whether catalyst earns its combiner seat at all. NOT time-sensitive (`active_catalyst_events` persist at Stage 7 — recoverable).
- **DEC-075** — "Additive-Fallback Regime: Structural Limits & Pre-Trained-Window Discipline" (PROPOSED, GOVERNANCE-ONLY, NO BUILD). Names the defect (clause a); establishes the additive-vs-interaction classification gate with interaction-valued EXCLUDED BY DEFAULT (clause b); folds in the 9-signal classification audit (clause c — 7 additive / 2 interaction); pins the Phase-7 acceptance criterion (clause d — trained combiner must outperform exclusion-disciplined fallback on interaction-valued signals); scope-limits the rule to classification + default + measurement bar (clause e — does NOT propose retraining the fallback or building an interim interaction-aware ranker); mandates per-signal classification on every future fallback-sum addition (clause f — recurrence prevention).

**What was NOT chartered (and why):**
- A silent edit to `compute-active-catalyst.ts`: STOP — the kernel is correct; the defect is at the deployment layer, not the construction layer; DEC-074 explicitly pins the `ranker.ts`-only seam.
- Removing catalyst from `SIGNAL_IDS_ALL`: STOP — would break the feature vector / §4.3.5 coverage gate / trained-combiner path (all read `SIGNAL_IDS_ALL`); the exclusion is the SUMMATION SET only (DEC-074 clause b).
- Framing DEC-074 as REMOVING the signal: STOP — the signal is KEPT (computed/persisted/emitted/feature-vector-emitted); only fallback summation participation is suspended; the trained combiner uses catalyst as interaction (DEC-074 clauses c, d, g).
- A pre-data half-life tune of DEC-057 §a (e.g., guess-tuning Earnings 48h → 96h): STOP — silent ROI change forbidden; DW-190 charters the Phase-7 grid calibration; relative ordering is defensible-now.
- A sell-off the signal (drop Signal #9 from the catalog): STOP — premature; the catalyst signal has a coherent interaction-feature thesis for the trained combiner; only DW-190's measured null result would justify a drop, on evidence.
- A free-parameter introduction (e.g., a catalyst-down-weight β): STOP — DEC-074 is an EXCLUSION not a tune; introducing a parameter would be the analyst-tier-weight anti-pattern (ship a guess + still owe the Phase-7 measurement).
- Filing the meta-finding as a footnote under #9: STOP — 2/9 signals is a regime invariant, not a per-signal anomaly; elevated to DEC-075 to prevent a 3rd recurrence and to give FP-058 an acceptance bar.
- A fabricated edge/Sharpe number on our universe: STOP — anti-fabrication; literature + architectural reasoning only (Bernard-Thomas 1989 PEAD drift; Schwert 1996 M&A drift; Sarkar-de Jong 2006 FDA event-study; Barber-Odean 2008 *RFS* "All That Glitters"; Da-Engelberg-Gao 2011 *J. Finance* "In Search of Attention"; Savor 2012 news-day decomposition; Tetlock 2010).

## Weekend build list (shadow stand-ups + zero-cost forward actions surfaced by the audit)

The audit is surfacing a cluster of CHEAP, scope-disciplined items that can be stood up THIS WEEKEND to start accruing free comparison data / forward-action data — the PROMOTION decisions stay Phase-7-gated under FP-058. Operator intent (per this turn's framing): finish the full 9-signal audit first, then execute the weekend-buildable set together as one focused build.

| DW | Title | Buildable now? | Why now is cheap | Why promotion is deferred |
|---|---|---|---|---|
| **DW-170** | Momentum signal-level shadow variants (σ-divide, residual-momentum) | YES — shadow stand-up | §6.5 harness exists; raw 12-1 baseline kept; variants compute alongside | Promotion would mutate CRITICAL §4.4.1 → DEC + Phase-7 measurement |
| **DW-171** | PEAD classic-own-firm-SUE σ_proxy variant | **ACCRUAL-GATED** (NOT weekend) — needs ≥ 8 quarters of per-name actual/consensus pairs in `signal_observations` before std is stable | The compute path is cheap once data accrues; shadow stand-up is meaningful only after 8q | Promotion mutates CRITICAL §4.4.6 → DEC + Phase-7 measurement |
| **DW-172** | PEAD T-0 consensus snapshot capture | YES — zero-cost forward action (new table + writer + pre-RTH cron via existing fetcher) | Vendor backfill impossible; cost of NOT starting now is ~3-month delay on Phase-7 walk-down ablation | The walk-down ablation itself is Phase-7 |
| **DW-173** | SI level / DTC as-alpha shadow variant | YES — shadow stand-up | Existing fetcher + existing DTC column (MIG-131) + existing §6.5 harness; the data is already in the system | Promotion mutates CRITICAL Signal #5 → DEC + Phase-7 measurement |
| **DEC-071 + DW-176** | **Reversal gate (news/catalyst) + magnitude cap (3σ trailing-60d) + ungated shadow ride-along** | **YES — CONSTRUCTION FIX (the audit's FIRST actual now-fix, not a measure-later variant)** | Cross-signal read in existing `reversal-orchestrator.ts`; same `as_of` semantics; existing `signal_observations` table; ungated shadow rides on §6.5 harness | This IS the promotion: DEC-071 charters the now-fix because the failure mode is GENUINELY UNMITIGATED today (§6.4 fallback sums additively, no interaction veto; trained combiner months away). Phase-7 (DW-177) ablates the 3σ + lookback parameters retrospectively. |
| **DW-178** | **Analyst per-revision-outcome CAPTURE step** | **YES — TIME-SENSITIVE near-term build candidate (NOT a shadow stand-up; NOT Phase-7)** | One alongside-INSERT per revision into a new `analyst_revision_events` table; tiny volume; no change to live z-scored signal / ranker / PnL | The WEIGHTING that uses it (DW-179 per-analyst accuracy weight) is Phase-7; the CAPTURE itself is time-sensitive because every day without capture = a day of analyst-track-record accrual PERMANENTLY lost (forward stream; no reliable backfill at per-analyst granularity). Sister anti-pattern to DW-172 PEAD T-0 capture. |
| **DEC-072 (charter-deferred)** | Analyst credibility-weight v1.1 — brokerage-tier proxy | **NO — chartered but BUILD DEFERRED to post-audit weekend-slate ranking** (operator Option 3) | Localized multiplier swap at `compute-analyst-revision.ts:208`; tier-map ≤ 50 firms; verified buildable on existing `analystCompany` surfacing | A proxy-for-a-proxy (firm-tier ≈ skill at multiple removes); modest edge vs the LARGE accuracy-weight lever (DW-179); recurring tier-map maintenance; hand-curated GUESS not measured; would foreclose clean Phase-7 attribution against DW-179. Ranked against full 9-signal slate before commit. |
| **DEC-073** | **Insider buys-only (drop sell-side) + symmetric shadow variant ride-along (DW-183 comparator)** | **YES — CONSTRUCTION FIX (the audit's SECOND now-fix, parameter-free, literature-direct)** | One-line filter change at `compute-insider.ts:32-36` (drop the S-branch); 10b5-1 sub-clause is moot (no S-rows survive); typed-absence reuses existing `no_qualifying_transactions` literal; shadow symmetric rides on §6.5 harness; no schema change, no migration, no orchestrator change | Pre-training urgency: shipping NOW gives the eventual combiner the purest literature-supported signal (Jeng-Metrick-Zeckhauser: sells ~ZERO abnormal); deferring bakes the symmetric-dilution into learned weights. Sell-side β down-weight (the parameter-introducing alternative) is rejected as now-fix per DEC-073 clause (b) and is DW-183 instead (MEASURED not GUESSED). NON-CRITICAL signal → no §4.3.5 amendment, lighter friction than DEC-071. |
| **DW-186** | **News articleCount attention-feature CAPTURE (3rd TIME-SENSITIVE; meta discarded at persist, feed_items TTL 24h)** | **YES — TIME-SENSITIVE near-term build candidate (NOT cheap-surface-it; STEP-A verified)** | Schema-additive (column on `signal_observations` or sidecar); kernel already computes `meta.articleCount` — only the persistence path needs threading; no change to sentiment kernel / discrete mapping / PR-wire exclusion / live value | The WEIGHTING (whether `news_attention` earns combiner weight) is DW-187 Phase-7; the CAPTURE is time-sensitive because meta is structurally dropped at `signal_observations` persist AND `signal_queue_feed_items` is sweeper-pruned at TTL 24h → historical attention counts unrecoverable. Sister to DW-172 (PEAD-T0) and DW-178 (analyst per-revision); the time-sensitive-capture list now stands at THREE. |
| **DEC-074** | **Catalyst conditioning-only in §6.4 additive fallback — `SIGNAL_IDS_FALLBACK_SUM` swap in `ranker.ts` (the audit's 3rd now-fix)** | **YES — FALLBACK-REGIME DEPLOYMENT FIX (parameter-free; surgical summation-set change)** | One-file change in `ranker.ts` (introduce `SIGNAL_IDS_FALLBACK_SUM` = `SIGNAL_IDS_ALL` minus catalyst; swap the `computeComposite` iteration target); no migration; no kernel change; no schema change; no parameter; tests verify catalyst is excluded from composite AND remains in feature vector / trained-combiner path | This IS the deployment fix because the failure mode is GENUINELY UNMITIGATED today (§6.4 fallback sums additively with no interaction; trained combiner months away under FP-058) AND drives the early live-money window (no trained-combiner-before-live gate on the master plan); Phase-7 (DW-190) measures catalyst-as-interaction lift retrospectively. Joins DEC-071 + DEC-073 in the weekend bundle as the 3rd now-fix. |
| **DEC-075** | **Additive-Fallback Regime structural rule + 9-signal classification audit (the META-FINDING — governance-only, no build)** | **N/A — governance rule, ZERO CODE** | Pure classification gate (additive-valued vs interaction-valued) + exclusion-by-default for interaction-valued signals + Phase-7 acceptance criterion + recurrence-prevention clause; classification audit folded in (7 additive-valued or TBD pending #3; 2 interaction-valued — #7 gated, #9 excluded) | This is a regime invariant, not a buildable item — it changes the DEFAULT POSTURE for future signal additions and the MEASUREMENT BAR for FP-058. The two behavioral changes are carried by DEC-071 + DEC-074; DEC-075 prevents the 3rd recurrence. |

Sequencing intent: execute the weekend-buildable rows (DW-170 + DW-172 + DW-173 + DW-178 + DW-186 + the DEC-071 construction fix bundled with the DW-176 ungated shadow ride-along + the DEC-073 construction fix bundled with the DW-183 symmetric shadow ride-along + the DEC-074 fallback-regime deployment fix in `ranker.ts` — DW-171 stays accrual-gated; DEC-072 stays charter-deferred; DEC-075 is governance-only; DW-189 vendor-eval stays explicitly deferred behind DW-187) AFTER the remaining audit (#3 options-flow) completes, as one combined scope-disciplined PR per the operator's "review all first, then build together" framing. The audit now has THREE now-fixes (DEC-071 reversal-gate + DEC-073 insider-buys-only + DEC-074 catalyst-conditioning-only) — each literature-unambiguous, each with a cheap surgical seam, each with a recoverable shadow comparator or reversible-by-construction posture. The time-sensitive-capture list still stands at **THREE** (DW-172 PEAD T-0, DW-178 analyst per-revision, DW-186 news articleCount — UNCHANGED; catalyst is NOT time-sensitive because `active_catalyst_events` persist at Stage 7). The META-FINDING (DEC-075) is the single most valuable governance artifact the audit has produced: 2/9 signals are structurally mis-deployed by the additive fallback, that is a regime invariant not a coincidence, the classification gate prevents recurrence, the Phase-7 acceptance bar prevents claim-without-measurement. New weekend-buildable findings from the pending #3 audit will land in this table as they're surfaced.

**Anti-completion-theater note:** these shadow stand-ups are NOT promotions and do NOT change the live ranker / live execution / live PnL — they create the comparator series that Phase-7 (FP-058) requires to make a measured promotion decision instead of a fabricated one.

## Signals pending audit (in order)

- **#3 options-flow**.

*(Updated 2026-06-30 at ACT-357 — list now EMPTY; original line preserved above per Constitution Rule 8 to record the pre-#3-audit state.)*

- **(none — 9-signal audit COMPLETE)**.

## Cross-references

- Deferred-work register: `docs/08-planning/deferred-work-register.md` (DW-169 … DW-190 as of this writing).
- Phase-7 measure-and-lock home: `docs/08-planning/feature-proposals.md` → FP-058.
- Audit-action records: `docs/06-tracking/action-tracker.md` → ACT-350 (ledger creation + #6/#2 findings); ACT-351 (#5 findings + DW-173/174/175 + weekend-build-list section); ACT-352 (#7 findings + DEC-071 now-fix charter + DW-176/177 + weekend-build-list now-fix row); ACT-353 (#1 findings + DEC-072 charter-deferred + DW-178 TIME-SENSITIVE + DW-179/180/181/182); ACT-354 (#4 findings + DEC-073 buys-only NOW-FIX — the audit's 2nd construction fix + DW-183/184/185 + weekend-build-list 2nd-now-fix row); ACT-355 (#8 findings + STEP-A persistence verification + DW-186 TIME-SENSITIVE per STEP-A + DW-187/188 Phase-7 + DW-189 vendor-eval deferred + weekend-build-list DW-186 row + time-sensitive-capture-list now at THREE); **ACT-356 (#9 findings + DEC-074 catalyst-conditioning-only-in-fallback NOW-FIX — the audit's 3rd now-fix + DW-190 Phase-7 catalyst-as-interaction ablation + DEC-075 Additive-Fallback Regime META-FINDING governance rule with 9-signal classification audit + weekend-build-list 3rd-now-fix + governance-rule rows)**.
- Decisions: `docs/decisions/DEC-071-reversal-cross-signal-gate-and-magnitude-cap.md` (the FIRST now-fix charter — PROPOSED, build authorized for the weekend bundle); `docs/decisions/DEC-072-analyst-credibility-weight-brokerage-tier-proxy.md` (the analyst credibility-weight charter — PROPOSED, BUILD DEFERRED to post-audit slate); `docs/decisions/DEC-073-insider-buys-only.md` (the SECOND now-fix charter — PROPOSED, build authorized for the weekend bundle); `docs/decisions/DEC-074-catalyst-conditioning-only-in-fallback.md` (the THIRD now-fix charter — PROPOSED, build authorized for the weekend bundle); `docs/decisions/DEC-075-additive-fallback-regime-structural-limits.md` (the META-FINDING governance rule — PROPOSED, NO BUILD).
- Authoritative signal specs: `docs/04-modules/longshort/design-source/` (CROSSWIND v0.9 — never edit).