# Incidental Findings Log

> **Owner:** Project Lead | **Last Reviewed:** 2026-05-16 | **Status:** Living Document

## Purpose

Cross-cutting incidental findings discovered during planning, review, or execution that:
- Do NOT meet the threshold of a feature proposal or DEC entry
- Are NOT tied to a specific feature module (those go in the module's own INC log if it has one — e.g., `docs/04-modules/longshort/design-source/README.md` for CROSSWIND-specific drift)
- Need to be tracked so they aren't lost between PRs

Each entry must reach one of two terminal states per supervisor protocol §8: **resolved** (fixed and merged) or **logged with disposition** (scheduled / risk / escalated for operator decision). Nothing is just "tracked" — every open INC has a disposition.

## Open Findings

_(no open findings)_

## Resolved Findings

### INC-15 — `permissions` table has no `module` column

| Field | Value |
|---|---|
| **Discovered** | 2026-05-16 |
| **Discovery Context** | Step 4 (Trading Panel Foundation Infrastructure) planning — investigating permission seed pattern for `trading.access` |
| **Severity** | Medium (documentation accuracy; no runtime bug) |
| **Disposition** | Resolved — doc-only fix landed via FP-005 Step 5.0a; DEC-031 sub-point 3 wording amended |

**Finding.** The `permissions` table has schema `(id, key, description, created_at)` — no `module` column. Verified across multiple migrations (all permission INSERTs use only `(key, description)`).

Yet:
- DEC-031 sub-point 3 states: *"Grouping in admin UI achieved via the `module` field on each permission entry."*
- `docs/07-reference/permission-index.md` entries include a `Module: trading-panel` field implying DB-backed metadata.

**Actual mechanism.** `src/pages/admin/AdminPermissionsPage.tsx` groups via `groupByResource()` — splits each permission key at the first dot. `trading.access` groups under resource `trading`, label `Trading`. No `module` field is read from the DB or used in UI. The `module` field in permission-index.md is documentation-only metadata that does not flow to any code or DB column.

**Impact.** Documentation describes a DB column that doesn't exist. Two reasonable resolutions:

1. **Doc-only fix.** Update DEC-031 sub-point 3 and permission-index.md to describe the actual key-prefix grouping mechanism. Cheaper and matches reality.
2. **Schema change.** Add the `module` column to permissions table with appropriate migration, RLS review, and AdminPermissionsPage update to read and group by it. More work but matches DEC-031's stated intent.

**Note for Step 5 (FP-005 long-short).** Same question will apply to `longshort.view` / `longshort.manage` permissions — they will group as resource `longshort` via key-prefix, not by any `module` metadata. INC-15 disposition should be decided BEFORE long-short permissions are seeded, to avoid compounding the drift.

**Related artifacts.** DEC-031 (sub-point 3), permission-index.md, AdminPermissionsPage.tsx (`groupByResource()` function).

**Resolution.** Option 1 (doc-only fix) was selected per FP-005 Step 5.0a / Round 1.1 D1. The fix landed by amending DEC-031 sub-point 3 in `docs/08-planning/approved-decisions.md` to accurately describe the `groupByResource()` mechanism in `src/pages/admin/AdminPermissionsPage.tsx` (first-dot-split of the permission key) and to clarify that the `module:` metadata in `docs/07-reference/permission-index.md` is documentation-only and is not read by code or stored as a DB column. Resolution anchor: this Step 5.0a commit (SHA recorded in the commit message body `inc_15_resolution_sha` field per §22.4).

### INC-17 — Silent auth-context fallback in layout prefetches (M4)

| Field | Value |
|---|---|
| **Discovered** | 2026-05-16 |
| **Discovery Context** | Lovable project review of Step 4 trading panel infrastructure. |
| **Severity** | High (security-relevant: silent permission-loss path indistinguishable from genuine denial) |
| **Disposition** | **Resolved** (2026-05-16) |
| **Resolution** | Layout prefetch queryFns for `get_my_authorization_context` (TradingLayout, AdminLayout) now log to Sentry + console.error in the catch path before returning empty-perms defaults. Empty-perms behavior preserved (fail-closed → AccessDenied) but now visible. Global QueryCache.onError handler added in App.tsx to capture all silently-swallowed query errors going forward. |

### INC-18 — Silent MFA-policy bypass on transient backend failure (N1)

| Field | Value |
|---|---|
| **Discovered** | 2026-05-16 |
| **Discovery Context** | Lovable project review second-read after C1.1 closure. |
| **Severity** | High (DEC-028 enforcement bypass on transient errors — production exposure once `panels.trading = 'required'`) |
| **Disposition** | **Resolved** (2026-05-16) |
| **Resolution** | `RequireMfaForTrading`, `RequireMfaForAdmin`, and `UserLayout`'s self-MFA gate now read `error` from `useMfaPolicy` and fail closed: any policy fetch error → treat enforcement as required → redirect to `/mfa-enroll`. Global QueryCache.onError handler logs the underlying fetch failure to Sentry. DEC-028 enforcement preserved on transient outages. |

### INC-16 — RW-018 test assertions out of sync with implementation (6 pre-existing failures)

| Field | Value |
|---|---|
| **Discovered** | 2026-05-16 |
| **Discovery Context** | C1 fix execution (RW-018 `ApiError` mock re-export). After applying `vi.importActual` pattern, failures dropped from 7 to 6. The 6 remaining failures are pre-existing assertion mismatches, not caused by the fix. |
| **Severity** | Medium (test suite hygiene; RW-018 marked Verified but 6 of its 22 tests do not pass) |
| **Disposition** | **Resolved** (C1.1, 2026-05-16) |
| **Resolution** | Updated 6 stale assertions in `src/test/rw018-sudo-audit-events.test.ts` to match correct implementation behavior per RW-019 (correlation_id mandatory in POST body and round-tripped on error path) and DEC-029 (`logSudoEvent` returns `SudoAuditResult`, not `undefined`). 4 Category-A `.toEqual` blocks extended with `correlation_id: expect.any(String)`. 1 Category-B `.resolves.toBeUndefined()` changed to `.resolves.toMatchObject({ persisted: false })`. 6th failure (uncharacterized in original INC-16 — edge-function source regex `apiError(500…correlationId: ctx.correlationId`) split into two assertions verifying the RW-019 `const correlationId = clientCid ?? ctx.correlationId` derivation and the `apiError(500, …, correlationId)` reference, independent of property-shorthand form. Production code (`src/lib/sudo-audit.ts`, `src/lib/api-client.ts`, `supabase/functions/log-sudo-event/index.ts`) unchanged. Post-fix: 22/22 RW-018 tests pass; 205/205 repo-wide. |

**Finding.** The `vi.importActual` fix resolves the `ApiError` mock issue (1 test restored). Six remaining failures in `src/test/rw018-sudo-audit-events.test.ts` fall into two categories:

**Category A — Success-path POST body assertions (4 tests).** Tests in sections 1 and 3 expect the `apiClient.post` body to contain exactly `{ action, action_key }`, but `logSudoEvent` (implementation-correct per RW-019) sends `{ action, action_key, correlation_id }`. The assertions use `.toEqual()` strict equality, so the extra `correlation_id` field fails the match.

Affected tests:
- `auth.sudo_granted is buffered AND POSTed with the action_key`
- `auth.sensitive_action_performed is buffered AND POSTed`
- `on fresh grant: emits sudo_granted THEN sensitive_action_performed for the same action_key`
- `manual grant flow emits BOTH sudo_granted and sensitive_action_performed once each`

Fix: change `.toEqual({ action, action_key })` to `.toMatchObject({ action, action_key })` or assert the full 3-field body shape.

**Category B — Error-path return-value assertions (2 tests).** Tests in section 6 expect `logSudoEvent` to resolve to `undefined` (`.resolves.toBeUndefined()`), but `logSudoEvent` always returns a `SudoAuditResult` object (implementation-correct per DEC-029). The original `ApiError` mock bug masked this by causing `logSudoEvent` to throw a TypeError on the `instanceof` check, making the promise reject instead of resolve.

Affected tests:
- `logSudoEvent swallows edge-function errors but still buffers the event`
- `isSudoActive is unaffected by audit-write failure`

Fix: change `.resolves.toBeUndefined()` to `.resolves.toBeDefined()` or assert on `result.persisted === false`.

**Impact.** RW-018 is marked Verified in the regression watchlist and action tracker, but 6 of its 22 tests are not reproducible as passing. This is a Constitution Rule 11 issue (evidence not reproducible), though distinct from the `ApiError` mock bug that C1 resolves.

### INC-19 — SECURITY DEFINER functions executable by PUBLIC/anon (H1a)

| Field | Value |
|---|---|
| **Discovered** | 2026-05-16 |
| **Discovery Context** | Lovable project review at HEAD `f13f408` — Supabase linter flagged `0028_anon_security_definer_function_executable` and `0029_authenticated_security_definer_function_executable` on every SECURITY DEFINER function in `public`. By Postgres default, EXECUTE on functions is granted to PUBLIC unless explicitly revoked; for SECURITY DEFINER RBAC helpers (`is_superadmin`, `has_role`, `has_permission`) this created a low-grade anon-enumeration oracle (probe membership without authenticating). |
| **Severity** | Medium (defense-in-depth: no direct privilege escalation since RLS still gates rows, but anon-callable RBAC helpers leak boolean membership signals). |
| **Disposition** | **Resolved** (MIG-036, 2026-05-16) |
| **Resolution** | New migration `20260516113643_18cf3d9a-5369-4596-9d79-fe9e61d0164c.sql` REVOKEs EXECUTE FROM PUBLIC, anon on all **10** SECURITY DEFINER functions in `public` (ground-truth count from `pg_proc` at HEAD `b3c969f` — original prompt estimate of 16 was conservative). GRANTs EXECUTE TO `authenticated` only for `get_my_authorization_context()` — the sole function called from client code via `supabase.rpc(...)` (verified by grep of `src/`). Trigger functions (`accept_invitation_on_confirm`, `handle_new_user`, `handle_new_user_role`, `sync_profile_email`, `rls_auto_enable`), RLS helpers (`has_permission`, `has_role`×2, `is_superadmin`), and server-only paths (RLS helpers also called from edge functions via service-role, which bypasses EXECUTE checks) receive no `authenticated` GRANT. Post-migration `pg_proc` verification confirms `anon_can_execute = false` for all 10 and `authenticated_can_execute = true` only on `get_my_authorization_context`. H1b (0011 `function_search_path_mutable` — 5 WARN linter findings) deferred as separate future PR pending operator authorization. |

---

### INC-20 — ACT-084 v2 scope-leak: Lovable migration-tool path used for superadmin grant during smoke debugging

| Field | Value |
|---|---|
| **Discovered** | 2026-05-24 |
| **Discovery Context** | During FP-006 sub-step 6.4.1 v2 execution, operator authorized a temporary superadmin grant for `tesfayekb@me.com` (UUID `8f8dfd8a-81bb-42f3-bb87-c58e33748b1b`) to enable Option C debugging of the B.3 active 4-RPC smoke cycle. Lovable chose to apply the grant via the Supabase migration tool path (creating `supabase/migrations/20260524041921_d1be05aa-c76a-4289-a863-c16d6926c9c8.sql` + auto-regenerating `src/integrations/supabase/types.ts`) — the EXACT workflow path that Option 3 (operator OOB apply + Lovable verification split-execution) was designed to prevent. The grant itself was operator-authorized; only the delivery path was the violation. |
| **Severity** | Medium (workflow discipline violation: re-introduced the root-cause path that ACT-083b investigation surfaced and ACT-085 will codify as banned for future sub-steps; not a security violation since the grant itself was operator-authorized; not a live-DB drift since the migration applied cleanly and is in `schema_migrations`) |
| **Disposition** | **Open** (two-part follow-up: (a) operator-revokes the temporary grant out-of-band; (b) ACT-085 codifies the path-violation rule formally) |
| **Resolution** | Two-part: |
| **Part (a) — Revoke temporary superadmin grant (operator OOB)** | Operator runs the following SQL via Supabase Dashboard SQL editor against project `sftatlxatbdrotivxcip` to revoke the temporary grant: `DELETE FROM public.user_roles WHERE user_id = '8f8dfd8a-81bb-42f3-bb87-c58e33748b1b' AND role_id = (SELECT id FROM public.roles WHERE key = 'superadmin');` followed by audit log entry: `INSERT INTO public.audit_logs (action, actor_id, target_type, target_id, metadata) VALUES ('rbac.role_revoked', '8f8dfd8a-81bb-42f3-bb87-c58e33748b1b'::uuid, 'user_roles', '8f8dfd8a-81bb-42f3-bb87-c58e33748b1b'::uuid, jsonb_build_object('role_key', 'superadmin', 'reason', 'INC-20 cleanup — ACT-084 v2 temporary smoke-test grant revoked per gate hygiene'));`. After operator confirms revoke, this INC entry status transitions to **Resolved** via append-only correction. |
| **Part (b) — Path-violation root-cause codification (ACT-085 scope)** | ACT-085 (FP-006 sub-step 6.4.1 supervisor protocol amendment) codifies three §22.5 amendments motivated by sub-step 6.4.1's experience: (1) live-DB verification mandatory for CLEAN dispositions touching DB schema/permissions/RPCs/RLS/job_registry; (2) apply-step vs verify-step separation pattern when executor tool capabilities mismatch contract (Option 3 split-execution); (3) **pre-flight live-DB gate pattern as integrity barrier between operator OOB action and executor verification claim**. INC-20 specifically informs item (3): for any one-off DB operation during smoke debugging or capability-gap workaround, supervisor must provide SQL for operator to run out-of-band, NOT direct executor to apply via migration tool. ACT-085 amendment text references this INC entry as motivating evidence. |
| **Disposition path to closure** | (a) operator-revokes grant; this INC entry receives "Resolved (Part a, 2026-05-24)" correction. (b) ACT-085 codifies the workflow rule; this INC entry receives "Resolved (Part b, at ACT-085 SHA)" correction. Both parts complete → INC-20 transitions to **Resolved (full)**. |
| **Live-DB artifact disposition** | The migration file `supabase/migrations/20260524041921_d1be05aa-c76a-4289-a863-c16d6926c9c8.sql` STAYS IN REPO as historical — deleting it would create a "row in schema_migrations references missing file" drift class. The auto-regenerated `src/integrations/supabase/types.ts` changes also stay (legitimate type definitions for feature_flags table that should have been generated earlier anyway). |

#### Resolved (Part a, 2026-05-24)

Operator-confirmed revoke of `tesfayekb@me.com` temporary superadmin grant via Supabase Dashboard SQL editor against project `sftatlxatbdrotivxcip`. Verification query result:

| still_superadmin | latest_migration_version |
| ---------------- | ------------------------ |
| 0                | 20260524041920           |

Expected: `still_superadmin = 0` ✓. Part (a) **RESOLVED**.

#### Resolved (Part b, 2026-05-24, at ACT-085 SHA `<TBD>`)

ADR-004 landed at `docs/04-modules/longshort/design-source/ADR-004-live-db-verification-discipline.md` codifying Amendment 3 ("Executor migration-tool path banned for one-off DB operations during smoke/debugging"). Part (b) **RESOLVED**.

INC-20 transitions to **Resolved (full)** at this SHA.

---

### INC-21: DEC-032 Clause (4) FP-006/FP-007 Dependency-Order Violation

| Field | Value |
|-------|-------|
| **ID** | INC-21 |
| **Date Surfaced** | 2026-05-25 (during ACT-099 post-mortem cycle; immediately before C.1 governance reconciliation) |
| **Classification** | process-defect (framing β per operator calibration); forward-binding fix codified at supervisor-instructions §21.10 (v0.5 → v0.6 amendment) |
| **Surface Story** | At the start of FP-008 Phase 1 universe-construction scoping cycle, supervisor performed sanity-check against feature-proposals.md to identify the next free FP slot. Grep `^### FP-` returned FP-001..FP-005 + `^## FP-006` (heading-depth variance preserved); FP-007 was ABSENT from feature-proposals.md. Cross-reference to DEC-032 clause (4) confirmed FP-007 was reserved on 2026-05-17 as "CI/CD Pipeline Bootstrap" with verbatim language: "FP-007 runs in parallel with FP-005 and is a hard prerequisite for FP-006 entry — FP-006 may NOT begin execution until both FP-005 and FP-007 are closed." FP-006 nonetheless executed and closed (HEAD `13fce9cd` at ACT-098 / 2026-05-25) without FP-007 being authored as an FP entry. |
| **Observation** | The CI/CD scope reserved to FP-007 was nonetheless delivered through FP-006's own sub-steps (6.4 ACT-082 audit-writer trap + 6.10.1 ACT-099 transaction 5 banned-pattern enforcement scripts + workflow extension to 9 gates + docs/banned-patterns.md override registry). The FP-007 wrapper was missing; the FP-007 work was complete. The DEC-032 clause (4) ordering invariant was violated in form but honored in substance: by the time FP-006 closed, the CI/CD scope reserved to FP-007 was substantively delivered. |
| **Why Framing β Not α** | Framing α (informational note; no remediation) would have left the same defect class unprotected against repetition. Framing β (process-defect with forward-binding §21.10 amendment) extends the ADR-003 enforcement-as-scripts-not-prose principle one layer up — from code-pattern enforcement to governance-prerequisite enforcement. Operator calibration confirmed: cost differential is small (~10 lines of supervisor-instruction text + one file op); benefit is structural prevention of the entire defect class. Same cost-benefit ratio as the ADR-003 enforcement-script layer that has paid out twice in the ACT-099 cycle alone (defects #18 + #19 prevented by `ScanState` + string-aware walker patterns). |
| **Disposition** | Resolved-via-process-amendment (β). FP-007 retro-authored at ACT-100 / C.1 (this transaction) in feature-proposals.md with Status `closed (2026-05-25)` at SHA `cd4b8a14` — the SHA where the 9th CI gate landed at ACT-099-cont. PLAN-CI-001-BOOTSTRAP-001 plan section created in master-plan.md (orthogonal to PLAN-TRADING-001 per T6 removability). PLAN-CI-001-BOOTSTRAP-001 closure document created. Supervisor-instructions v0.5 → v0.6 amendment adds §21.10 hard-prerequisite-FP verification gate with machine-checkable artifact requirement: before drafting any execution prompt for an FP whose DEC, master-plan entry, or feature-proposals dependencies field names another FP as hard-prerequisite, supervisor MUST cite in the execution prompt's §22.3 item 2 (Anchor verification) block: (a) the prerequisite FP's feature-proposals.md line number, (b) its current `Status:` value verbatim, and (c) the SHA at which it closed. If any of (a)–(c) cannot be cited, STOP and surface to operator before draft. Recursion clause: applies recursively (a prerequisite's prerequisite must also be verified). Defect class #21 logged forward in supervisor self-defect log: "FP-prerequisite-verification miss; structurally fixed by §21.10 machine-checkable artifact requirement; mirrors ADR-003 enforcement-as-scripts-not-prose pattern at governance layer." Defect class #20 (register-conflation; raised during operator Q3 calibration) also logged forward. |
| **Cross-references** | DEC-032 clause (4) (the prose-DEC that named FP-007 as hard-prerequisite); FP-005 entry §413 item 9 ("CI/CD pipeline configuration for the longshort strategy — FP-007 per Round 1.2 D1.2-4 / F-3" — the FP-005 forward-reference); FP-007 entry in feature-proposals.md (retro-authored at this ACT); PLAN-CI-001-BOOTSTRAP-001 section in master-plan.md (created at this ACT); plan-ci-001-bootstrap-001-closure.md (created at this ACT); ACT-082 (the SHA where Gate 1 + initial 4-gate workflow landed); ACT-099 transaction (the SHA range across which Gates 5-9 + override registry landed); supervisor-instructions v0.6 amendment §21.10 (the forward-binding fix); ADR-003 enforcement-as-scripts-not-prose (the architectural precedent §21.10 extends to governance layer). |
| **Status** | Resolved (β; process-amendment applied) |

**Corroborating evidence (surfaced post-INC-21-authoring at ACT-101 / C.2-lite §21.10 pre-flight):** The FP-006 master-plan Dependencies field already attested verbatim `"FP-007 CI/CD Pipeline Bootstrap closed (hard prerequisite per DEC-032 clause (4))"` at the moment of FP-006 closure (ACT-098 / 2026-05-25), and ACT-098's closure attestation itself ratified the dependency field — meaning the attestation chain was three layers deep (FP-006 master-plan dependency attestation → ACT-098 closure attestation ratifying the dependency → FP-006 closure document publication), all prose, zero machine-check, until ACT-100 §21.10 pre-flight surfaced FP-007's actual entry-absence. §21.10 amendment (v0.6) + §21.10 v0.6.1 (this ACT) close this exact prose-attestation gap mechanically.

---

### INC-22: Feature-Proposal Entry-Format Drift Across Template Generations (FP-005 + FP-006 Stale Status Fields)

| Field | Value |
|-------|-------|
| **ID** | INC-22 |
| **Date Surfaced** | 2026-05-25 (during ACT-101 §21.10 pre-flight; immediately after ACT-100 / C.1 closed and supervisor began drafting FP-008 prerequisite-anchor verification) |
| **Classification** | process-defect (framing β per operator calibration); forward-binding fix codified at supervisor-instructions §21.10 v0.6 → v0.6.1 amendment (three-sub-case expansion) |
| **Surface Story** | Supervisor performed §21.10 pre-flight for FP-008 Phase 1 universe-construction scoping. Per §21.10 (b) requirement, supervisor grepped `\| \*\*Status\*\* \|` against FP-005 + FP-006 to cite verbatim Status values. FP-005 entry at feature-proposals.md line 344 returned `\| **Status** \| \`proposed\` \|` despite FP-005 being closed at ACT-072 on 2026-05-21 (closure document published; master-plan section PLAN-TRADING-001-LONGSHORT-001 status correctly attests `closed`). FP-006 entry at line 455 returned `- **Status:** Approved (governance-authored; execution-pending)` (list-item format, not table-row) despite FP-006 being closed at ACT-098 on 2026-05-25 (closure document published; master-plan section PLAN-TRADING-001-LONGSHORT-002 status correctly attests `closed`). Both FP entries had Status fields that drifted out of sync with their plan-section authoritative status. Additionally surfaced during pre-flight: FP-001 line 98 = `proposed` (stale); FP-002 line 119 = `approved` (stale-ish); FP-003 line 182 = `approved` (stale-ish); FP-004 line 228 = `approved` (factually correct — pattern-type FP, no closure milestone). Path 2-extended-refined adopted: fix FP-005 + FP-006 only; FP-001/002/003 out of scope; FP-004 left as-is. |
| **Observation** | The feature-proposals.md entry template evolved across FP generations. FP-001 through FP-006 were authored under pre-FP-007-template conventions (no explicit `Closure SHA` field; Status fields drifted post-closure because there was no entry-template convention requiring Status-field updates at closure). FP-007 was retro-authored at ACT-100 / C.1 with explicit `Closure SHA` field as part of the §21.10 (c) machine-checkable artifact requirement. The §21.10 pre-flight surfaced that pre-FP-007-template entries are missing the (c) anchor and have stale (b) Status fields. |
| **Why Framing β Not α** | Defect #26 is structurally isomorphic to defect #21 (FP-007 dependency-order violation surfaced at ACT-100 §21.10 pre-flight; resolved via supervisor-instructions §21.10 amendment). Both defects surfaced during pre-flight of a downstream FP; both require supervisor-protocol amendment to mechanically prevent repetition. Treating #26 as α (informational note only) breaks symmetry with #21's resolution and starts the same prose-flexibility erosion ADR-003 was authored to eliminate at the code layer. Cost differential is identical (~5 lines of supervisor-instruction text in either framing); β is strictly dominant on benefit. |
| **Disposition** | Resolved-via-process-amendment (β). **(1) FP-005 + FP-006 entry-format reconciliation:** Status fields corrected from stale values (`proposed` for FP-005; `Approved (governance-authored; execution-pending)` for FP-006) to verbatim `closed (DATE — closure document at PATH; closure ACT-NNN; closure SHA SHORT_SHA)`; `Closure SHA` field added with full 40-char SHA per FP-007 template precedent. **(2) Supervisor-instructions v0.6 → v0.6.1 amendment:** §21.10 expanded with three sub-cases for (c) Closure SHA citation: (i) Milestone FPs with recorded closure SHA (FP-007 forward) — cite (c) Closure SHA verbatim from the FP entry; (ii) Milestone FPs closed under pre-FP-007 template (FP-005, FP-006) — cite (c) closure-document path + closure ACT + closure SHA where reconstructable from git log; (iii) Pattern-type FPs (FP-004) — cite (c) DEC IDs that lock the pattern + DEC statuses (FP-004's DEC-030 + DEC-031 both `active`). All three sub-cases are machine-checkable; no prose-judgment fallback. **(3) Defect class #26 logged forward:** §21.10 (b)(c) anchors must distinguish FP types; pattern-type FPs cite plan-section + DEC IDs + DEC statuses instead of closure SHA. **(4) Defect class #23 logged forward:** §21.10 pre-flight grep template must handle both `\| **Status** \|` (table-row) AND `- **Status:**` (list-item) formats. **(5) Defect class #22 logged forward:** feature-proposal entry-format drift across template generations; mitigation = §21.10 v0.6.1 three-sub-case substitution. **(6) Defect classes #24 + #25 logged forward at this ACT** without separate fixes — #24 (§21.10 recursion must walk to fixed-point — caught by operator Q1 calibration); #25 (CROSSWIND §11.X vs §10.X distinction — phase-plan content lives in §10.X, not §11.X — caught by supervisor §11.1 grep diagnostic). |
| **Cross-references** | §21.10 v0.6 (codified at ACT-100 / C.1 per INC-21 framing β); §21.10 v0.6.1 (codified at this ACT / C.2-lite per INC-22 framing β); INC-21 (defect #21; companion process-defect); FP-005 entry at line 327 (corrected at this ACT); FP-006 entry at line 453 (corrected at this ACT); FP-007 entry at line 434 (template precedent for Closure SHA field); FP-004 entry at line 211 (pattern-type FP; left at `approved` per Path 2-extended-refined); ACT-072 (FP-005 closure); ACT-098 (FP-006 closure); supervisor-instructions v0.6.1 amendment (operator-side artifact at `/mnt/user-data/outputs/supervisor-instructions-v0.6.1-amendment.md`); ADR-003 enforcement-as-scripts-not-prose (the architectural precedent §21.10 + v0.6.1 extends at governance layer). |
| **Status** | Resolved (β; process-amendment applied) |

---

## Naming Conventions

- **INC-N** numbering is sequential, never reused. INC-14 lives in `docs/04-modules/longshort/design-source/README.md` (CROSSWIND-specific path drift); INC-15 onward in this file unless they're long-short-module-specific.
- Each INC has explicit disposition per supervisor protocol §8.
- INCs may originate from operator, AI collaborator (Claude or Cursor), automated tooling, or external review.

### INC-23: CI-INVESTIGATION-01 — strong-evidence.yml Workflow Continuously Red Since FP-007 / ACT-099 + Defect #36 Family Unified Discipline Codification (Path Broad v0.6.4 Amendment)

| Field | Value |
|-------|-------|
| **ID** | INC-23 |
| **Date Surfaced** | 2026-05-26 (operator-surfaced via Lovable GitHub Actions failure email for SHA `252ffc3` showing recurring red runs; second pattern-establishing instance — supervisor Stage 0 SQL phantom column — surfaced same day via operator-attempted execution returning Postgres error 42703) |
| **Classification** | process-defect (framing β per operator calibration — symmetric to INC-22 disposition for defect #26); forward-binding fix codified at operator-side supervisor-instructions v0.6.4 amendment (§22.3 (k) CI-status check + §22.5 (g) executor disposition CI-status confirmation + **§22.3 (l) NEW broad supervisor-side repo-state-reference inline grep-citation requirement**) per Path Broad ruling |
| **Surface Story** | **Part A — CI gate enforcement gap:** Operator surfaced GitHub Actions email indicating strong-evidence.yml workflow failed at SHA `252ffc3` (FP-008 sub-step 8.9 era — ACT-115); pattern recurs on every Lovable commit. Lovable investigation (CI-INVESTIGATION-01) identified 3 TypeScript compile errors entering the tree before FP-007 closure (workflow authored at SHA `4605255` 2026-05-24 00:46:04; errors entered at SHAs `1e4bd29` + `5d5e6de` + `7c9ad7a` between 2026-05-24 10:07 and 2026-05-25 01:50; FP-007 closed at SHA `cd4b8a14` 2026-05-25 04:28:21). Gate 2 (`deno test ... scripts/`) fails at TypeScript compile BEFORE running tests; Gates 3-9 (banned-pattern enforcement) never execute. 53 visible workflow runs across operator screenshots all red; working hypothesis: workflow has NEVER passed. AC-32 attestations in PLAN-TRADING-001-LONGSHORT-003 (FP-008) + AC-32-equivalent in PLAN-TRADING-001-LONGSHORT-002 (FP-006) are mechanically false. **Part B — Defect #36 family recurrence pattern:** Two close-in-time supervisor-side incidents on distinct artifact types: **(B1) AC-38 phantom finding 2026-05-26 / ACT-119 pre-flight:** Supervisor pre-flight surface document Finding 1 claimed "AC-38 missing from master-plan" without grep-verification; operator-corrected; logged at DW-076 as defect #42 candidate. **(B2) `ur.created_at` phantom column 2026-05-26 / Stage 0 OOB SQL drafting:** Supervisor Stage 0 SQL referenced non-existent `user_roles.created_at` column in post-state verification SELECT; operator-caught via Postgres error 42703; supervisor issued corrected SQL acknowledging the defect #36 family recurrence in the correction document itself. Two pattern-establishing instances on distinct artifact types establish defect #36 family as a recurring pattern; v0.6.3 §22.3 (g) Surface-only scoping does NOT cover pre-flight Findings or OOB SQL drafts. Operator-ratified Path Broad codification: v0.6.4 §22.3 (l) NEW broad supervisor-side repo-state-reference inline grep-citation requirement covering all supervisor-issued artifacts. |
| **Observation** | Two structural reasons hid the CI failure for 53 runs: (1) Pre-test compile is invisible by design — Deno type-checks before running tests, so Gate 2 fails at compile, but the CI dashboard shows "Gate 2 failed" looking like a test failure; verbose log requires GitHub Actions sign-in. (2) Supervisor §22.3 item 5 post-commit verification block does NOT include CI-status check; supervisor issued CLEAN dispositions based on repo-content verification alone (file scope, line counts, AC ticks) without confirming workflow runs were green. Executor §22.5 disposition similarly lacked CI-status confirmation. Both gaps codified-fixed at v0.6.4 §22.3 (k) + §22.5 (g). Additionally: defect #36 family recurrences indicate supervisor-side artifact authoring has been trust-based ("I verified this") rather than mechanically auditable ("here is the grep output proving this") for an unknown number of prior artifacts. v0.6.4 §22.3 (l) per Path Broad ruling addresses this prospectively with operator-locked inline-citation refinement: artifact contains the literal grep/read command + output excerpt that proves the repo-state reference, OR the artifact is invalid. Mechanically auditable, not trust-based. |
| **Why Framing β Not α** | Defect #43 is structurally isomorphic to defect #26 (INC-22) and defect #21 (INC-21). All three surfaced as process-defects requiring supervisor-protocol amendment to mechanically prevent recurrence; all three resolved via β framing (codify the rule at instructions level) rather than α framing (informational note only). Cost differential is identical (~one paragraph of supervisor-instruction text per §22.3 (k) + (l) + §22.5 (g)); β strictly dominant on benefit. Operator Path Broad rationale (codified verbatim): "(1) Root cause is shared, not surface-specific — both incidents stem from same failure mode of supervisor authoring artifacts using plausibility/memory instead of mechanical grep. (2) Cost asymmetry favors Broad — marginal cost of §22.3 (l) alongside §22.3 (k) is ~one paragraph; v0.6.5 later costs full amendment cycle plus damage from next instance. (3) Path Narrow's minimal surface argument weaker than it looks — risk of scope-creep governable by tight wording. (4) Hybrid fails evidence test — recurrence already happened twice; conditioning broader rule on future recurrence ignores two past ones." Inline-citation refinement: codified rule requires literal grep/read command + output excerpt in artifact, not separate verification claim — makes discipline mechanically auditable rather than trust-based. |
| **Disposition** | Path Broad (operator-ratified 2026-05-26): **(1) Closure docs frozen** per Option C-corrected. **(2) Stage 0 OOB SQL operator role recovery** complete at this ACT (verified via §22.5.1-equivalent direct user_roles SELECT showing tesfayekb@me.com with `user` + `superadmin` roles; audit row `6b57b462-e61d-4040-bde5-1cefdef46735` durable record). **(3) Stage 0.5 governance discipline codification** (this ACT-120 transaction; DW-077 + INC-23 + plan v13.27 + system-state version bump + bundled placeholder cleanups). **(4) Stage 1 CI-FIX-01** with scope strictly = (a) fix 3 TS error clusters; (b) verify Gates 1-9 execute green end-to-end against current main; (c) backfill `gh run list --workflow=strong-evidence.yml --branch=main --limit=1 --json conclusion` status check in supervisor §22.3 + executor §22.5 per defect #43 codification — NO erratum addenda bundling per operator anti-bundling ruling. **(5) GOV-ERRATA-01** parallel doc-only FP authoring per-affected-FP erratum addenda for FP-005/006/007/008 referencing CI-FIX-01 closure SHA — does NOT block Stage 3 (FP-009). **(6) Stage 2 DEFECT-PLATFORM-01** (panel-switcher + role-assignment UX + admin grant audit). **(7) Stage 3 FP-009** (Phase 1 UI + 7 hardening defects) gated on Stage 1 + Stage 2 CLEAN. **(8) Stage 4 Phase 2 / FP-010+** opens per AC-38 once Stage 3 closes. CI-FIX-01's own closure transaction is the first one to exercise the new defect #43 discipline (validation event for the new rules — including §22.3 (l) inline-citation requirement applied to CI-FIX-01's own §22.3 prompt + §22.5 disposition). |
| **Cross-references** | DW-077 (CI gate enforcement gap + defect #36 family unified discipline forward-binding tracker); supervisor-instructions v0.6.4 (operator-side amendment; §22.3 (k) + §22.3 (l) + §22.5 (g) + defect class #43 entry in §21.10 forward-binding defect classes table); AC-32 in PLAN-TRADING-001-LONGSHORT-003 (FP-008); AC-32-equivalent in PLAN-TRADING-001-LONGSHORT-002 (FP-006); FP-007 closure SHA `cd4b8a14`; FP-008 closure SHA `4ecc4004` + `3b39a04b`; workflow first-authored SHA `4605255`; three TS error trigger SHAs `1e4bd29` + `5d5e6de` + `7c9ad7a`; Lovable investigation report at `/mnt/documents/CI-INVESTIGATION-01-report.md`; pattern-establishing instance #1 = DW-076 (defect #42 candidate; AC-38 phantom finding); pattern-establishing instance #2 = supervisor Stage 0 SQL `ur.created_at` phantom column incident (corrected at `/mnt/user-data/outputs/Stage-0-operator-role-recovery-sql-CORRECTED.md`); INC-21 + INC-22 (predecessor β-framed process-defect resolutions); ADR-003 (enforcement-as-scripts-not-prose architectural precedent §22.3 (l) extends from code-layer to supervisor-artifact-authoring layer); CROSSWIND §2 axiom 2 (external snapshots are primes — the foundational rationale for §22.3 (l) inline-citation refinement). |
| **Stage 0 §22.5.1-equivalent attestation** | Operator role recovery via OOB Dashboard SQL per §22.5.3 (banned executor migration-tool path for one-off operational state); BEGIN/COMMIT atomic transaction landed both INSERTs; verified via direct user_roles SELECT showing 2 rows: `user` role (assigned_by NULL, auto-assigned via `handle_new_user_role` trigger on signup) + `superadmin` role (assigned_by `8f8dfd8a-81bb-42f3-bb87-c58e33748b1b` = self-restore lineage); audit row `6b57b462-e61d-4040-bde5-1cefdef46735` durable record at `audit_logs` action `rbac.emergency_role_restore` created_at 2026-05-27 05:00:27 UTC. Recovery does NOT land in version control per §22.5.3 OOB-SQL discipline. |
| **Status** | Path Broad codification at this ACT-120 (Stage 0.5); CI-FIX-01 (Stage 1) authoring forthcoming; corrective sequencing 8-stage as operator-ratified |

### INC-24: Post-retry HTTP-context loss in 3 sibling constituent fetchers (latent — same shape as ishares bug fixed at FP-008.4 Commit 1.5 failure #1)

| Field | Value |
|-------|-------|
| **ID** | INC-24 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5 failure #1 investigation) |
| **Classification** | latent code defect (not yet test-firing) — **fully resolved across all 4 implicated call sites (1 ishares + 1 polygon-constituent + 1 wikipedia + 2 polygon-enrichment) by FP-008.4 Commit 1.5 + 1.5b + 1.5b-extension** |
| **Surface Story** | While fixing FP-008.4 Commit 1.5 failure #1 (`(6) fetcher throws ConstituentFetchError on non-2xx HTTP` in `ishares-constituent-fetcher_test.ts:67`), root cause traced to the fetcher's `catch` block treating every thrown error as `'network error'` unless `e.name === 'AbortError'`. But `fetchWithTimeoutAndRetry` (per its own vitest contract at `fetch-with-timeout-and-retry.test.ts:71-84`) throws `Error('HTTP <status> <statusText>')` after exhausting retries on 429/5xx. The catch block silently collapses post-retry HTTP context into `'network error'`. **Sibling fetchers with the same catch pattern (verified via grep):** (1) `polygon-constituent-fetcher.ts:110-120`, (2) `wikipedia-constituent-fetcher.ts:~140`, (3) `polygon-enrichment-fetcher.ts:142+201` (two call sites). None of their `_test.ts` suites exercise a 5xx-after-retries path (verified via test grep) — polygon test (4) uses 401 which is non-retryable and returns directly, hitting the `if (!resp.ok)` branch instead. So the defect is latent: no test currently fires it, but production 5xx/429 retry exhaustion silently strips status codes from `ConstituentFetchError` messages, degrading health-monitoring/diagnosability for 3 of 4 fetchers. |
| **Observation** | Scope discipline at FP-008.4 Commit 1.5 limited fix to the single failing test (ishares). The shared-helper contract is correct (the retry helper SHOULD throw on 5xx-after-retries per design); the bug shape lives in each caller's catch block. A clean fix is mechanical (apply the same `isHttpAfterRetries` branch as `ishares-constituent-fetcher.ts:190-208` to all 3 sibling files). Risk of silent propagation if left: production logs/health-monitor lose HTTP status under sustained upstream 5xx. |
| **Disposition** | Commit 1.5b (constituent-ingestion siblings, pre-task-verification-then-replication): `polygon-constituent-fetcher.ts:107-122` applied as **shaped** replication (preserved `on page N` pagination context in the post-retry HTTP-after-retries branch); `wikipedia-constituent-fetcher.ts:153-167` applied as pure **mechanical**; `seeded-membership-fetcher.ts` excluded (no `fetchWithTimeoutAndRetry`; injected seed). Commit 1.5b-extension (enrichment-module sibling, scope-extension after operator-surfaced deferral-quality concern): `polygon-enrichment-fetcher.ts:140-153` (ticker-details) + `:200-212` (aggregates) applied as **shaped** replication (preserved `for ${ticker}` per-ticker context). Choice of in-pass extension over register-tracked deferral was operator-confirmed: trigger condition for register deferral ("Phase 2 §3.3 will touch enrichment module") was plausible-but-uncertain, and fix cost (<10 min) was far below register-tracking overhead with uncertain redemption. Regression test addition for any of the 3 sibling sites deferred per scope discipline (separate work item; the present commits are behavior-preserving correctness fixes bringing all sites into parity with the canonical ishares shape). |
| **Cross-references** | FP-008.4 Commit 1.5 failure #1 canonical fix at `ishares-constituent-fetcher.ts:190-208`; Commit 1.5b sibling fixes at `polygon-constituent-fetcher.ts:107-126` + `wikipedia-constituent-fetcher.ts:153-173`; Commit 1.5b-extension enrichment fixes at `_shared/longshort-universe/enrichment/polygon-enrichment-fetcher.ts:140-160` (ticker-details) + `:198-218` (aggregates); helper contract at `_shared/longshort-universe/shared/fetch-with-timeout.ts:114-146`; helper-contract test at `fetch-with-timeout-and-retry.test.ts:71-84`. |
| **Status** | Fully resolved across all 4 call sites. Regression-test addition for shaped/mechanical post-retry-HTTP path remains a separate deferred work item (not blocking; flagged here for future test-hardening pass). |

### INC-25: §3.3a BMO cutoff one-trading-day-late (real rule defect, financial-critical — fixed at FP-008.4 Commit 1.5 failure #2)

| Field | Value |
|-------|-------|
| **ID** | INC-25 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5 failure #2 three-way reconciliation) |
| **Classification** | real rule defect (not test bug); financial-critical severity; resolved in-pass at this commit |
| **Surface Story** | Test `§3.3a worked example (b): BMO Monday cutoff is prior Wednesday` (`rule-3-3a-earnings-window_test.ts:27`) was failing under end-to-end Deno suite execution. Three-way reconciliation between (1) CROSSWIND_SPEC.md §3.3 worked example (b) verbatim (lines 299-303), (2) test assertion, and (3) implementation `cutoffDate` (`rule-3-3a-earnings-window.ts:57-60`) showed spec and test agreed (cutoff = Wed Apr 29 for BMO Mon May 4) while the implementation produced Thu Apr 30 — one trading day late. Implementation applied `tradingDaysBefore(scheduled, 2)` uniformly to both AMC and BMO; its own docstring (lines 50-54) explicitly justified this as *"same arithmetic — the 'session' containing the news is the same scheduled_date session by spec convention"*, but spec convention is **not** the same: BMO's session is NOT at risk (the print has already arrived by the open). |
| **Observation** | Per spec, the 2-trading-day flat-window is the 2 trading days **immediately preceding the at-risk session**. For AMC the at-risk session IS `scheduled_date` (the print arrives after close, so the AMC session is the last at-risk day). For BMO the at-risk session is the trading day BEFORE `scheduled_date` (the print arrives before open, so by `scheduled_date`'s open the news is already public and the session is no longer "at risk" by this rule's pre-event-window framing). The fix shifts the effective session back by 1 trading day for BMO before applying the standard `tradingDaysBefore(_, 2)` walk. Behavior unchanged for AMC and intraday. **Production-significance**: under the prior implementation, every BMO-flagged earnings event would have held positions one extra trading day deeper into the danger window than spec mandates — direct ROI/risk impact across the entire long-short universe when §3.3a goes live. |
| **Disposition** | Fixed at this commit (`rule-3-3a-earnings-window.ts:cutoffDate`); +30-line docstring with spec citation lines 299-303 verbatim documenting AMC vs BMO arithmetic asymmetry. Test (b) now passes; tests (a) and (c) and intraday/missing-coverage/past-earnings still pass (AMC code path unchanged). `apply-hard-exclusions_test.ts` (6 orchestrator tests) regression-checked: all pass. Rule firing-history reconciliation NOT required because §3.3a refresh job ships `enabled=false` per `job_registry` seed convention (MIG-049 / universe.md line 82) — no production firings to reconcile. |
| **Cross-references** | CROSSWIND_SPEC.md:286 (LOCKED §3.3a row); CROSSWIND_SPEC.md:299-303 (worked examples a/b/c verbatim); fix at `supabase/functions/_shared/longshort-universe/hard-exclusions/rule-3-3a-earnings-window.ts:42-95`; test at `supabase/functions/_shared/longshort-universe/hard-exclusions/rule-3-3a-earnings-window_test.ts:27-33`; FP-008 sub-step 8.3 / ACT-107 (original §3.3a rule landing — defect entered there); universe.md:91 + :104 (rule registry + threshold table). |
| **Status** | Resolved at this commit. Tracking entry preserved for ROI-discipline audit trail (real rule defects in financial-critical paths get explicit INC entries per CROSSWIND §2 axiom + ROI guardrail in project knowledge). |

### INC-26: Asymmetric typed-absence semantics across §3.3 rules — test-fixture completeness hazard (test defect surfaced + fixed at FP-008.4 Commit 1.5 failure #3)

| Field | Value |
|-------|-------|
| **ID** | INC-26 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5 failure #3 three-way reconciliation) |
| **Classification** | test fixture defect (not production rule defect); discoverability hazard for any future §3.3-touching test author |
| **Surface Story** | Two tests in `quarterly-refresh-orchestrator_test.ts` were failing: `happy path → outcome=completed; counts populated; eligible returned + persisted` (line 153 — expected `total_eligible_short=6`, got `0`) and `metrics emitter invoked on outcome=completed with reason arrays` (line 301 — expected `hard_exclusion_reasons.length=0`, got `6`). Three-way reconciliation: (1) §3.3d rule docstring (`rule-3-3d-htb.ts:15-17`) — *"tickers MISSING from `locate_data` are treated as 'no locate' (defensive — better to skip a short than enter one blind). This is NOT a silent default; it is the documented contract."* (2) Type docstring (`hard-exclusions/types.ts:62-67`) — same contract reiterated as typed-absence per §2 axiom 3. (3) Test fixture `makeContext` provided `locate_data: []`. Spec + implementation + type contract all agreed: empty `locate_data` → `htb_no_locate` fires for every constituent → `short_eligible=false`. Test fixture was the outlier; its author had assumed "empty arrays = no rule firings" which is true for §3.3a/b/c/e but **false** for §3.3d. |
| **Observation** | The conceptual hazard is **asymmetric typed-absence polarity** across §3.3 rules: §3.3a (no earnings entry → no firing), §3.3b (no M&A action → no firing), §3.3c (no halt history → no firing), §3.3e (no short-interest record → no firing) — but §3.3d **inverts** the polarity (no locate record → firing). The asymmetry is correct and spec-mandated (positive evidence of borrow availability is required before shorting; missing data is exclusionary, not neutral), but it is a documented contract that lives only inside `rule-3-3d-htb.ts` and `types.ts`. A test-fixture author wiring `exclusionInput` for a happy-path scenario who pattern-matches on the four neutral-absence rules will silently produce an all-shorts-excluded fixture, masking real orchestrator behavior. **Not a production defect** — the rule fires correctly; the fixture was incomplete. But the meta-finding (asymmetric typed-absence is a discoverability hazard, not just a spec quirk) is durable and applies forward to every future §3.3-touching test. |
| **Disposition** | Fixed at this commit: `quarterly-refresh-orchestrator_test.ts:makeContext.exclusionInput.locate_data` now constructs positive locate records (`{ ticker, locate_available: true, borrow_rate_bps: 0 }`) for each polygon ticker, with an inline comment naming the §3.3d typed-absence contract, the §3.3e polarity asymmetry, and a back-reference to INC-26. Forward-binding prophylaxis: comment-in-fixture is the surface; the INC entry is the searchable record. No code change to the rule, the type, or any production path. No regression in the other 14 orchestrator tests (15/15 green) or suite-wide Deno typecheck (exit 0 across 17 _test.ts files). |
| **Cross-references** | §3.3d contract at `supabase/functions/_shared/longshort-universe/hard-exclusions/rule-3-3d-htb.ts:15-17` + `rule-3-3d-htb.ts:34-44` (no-record-firing branch); LocateRecord typed-absence contract at `supabase/functions/_shared/longshort-universe/hard-exclusions/types.ts:62-73`; §3.3e opposite-polarity contract at `rule-3-3e-short-interest.ts:15-18` + `:35` (no-record-no-fire); fixture fix at `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator_test.ts:makeContext`; FP-008 sub-step 8.3 / ACT-107 (original §3.3d rule landing). |
| **Status** | Resolved at this commit. Tracking entry preserved for forward-binding fixture-discipline reference (asymmetric typed-absence is a known discoverability hazard; future §3.3-touching test authors should consult this entry). |

### INC-27: api-conventions CORS contract has no canonical doc source — convention lives only in docstrings + tests (latent + fixed at FP-008.4 Commit 1.5 failure #5)

| Field | Value |
|-------|-------|
| **ID** | INC-27 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5 failure #5 three-way reconciliation) |
| **Classification** | (a) api-critical contract drift (fixed in-pass) + (b) discoverability hazard — no `docs/` source-of-truth document names the `apiSuccess`/`apiError` CORS responsibility contract; it lives only in `handler.ts:161` + `api-error.ts:14,30,34` docstrings + the `shared_test.ts:209-215` test assertion |
| **Surface Story** | Test `apiSuccess returns JSON with CORS headers` (`shared_test.ts:209`) was failing under end-to-end Deno suite execution: `assertExists(res.headers.get("Access-Control-Allow-Origin"))` got `null`. Three-way reconciliation across (1) `apiSuccess` docstring at `handler.ts:161` (*"Build a success JSON response with CORS headers"* — explicit CORS responsibility), (2) sibling `apiError` implementation at `api-error.ts:30,34` (defensive bake-in via `opts?._cors ?? corsHeaders` static fallback + `...headers` spread into response), and (3) the test assertion showed three-of-three converged on "`apiSuccess` should include CORS by default." Implementation at `handler.ts:162-167` was the outlier — returned only `Content-Type: application/json` with no `_cors` parameter and no static fallback. Asymmetry between the two sibling helpers (apiError defensive, apiSuccess bare) is the bug. **Production-significance:** masked-in-practice because every current edge function uses `createHandler` which post-hoc injects CORS at `handler.ts:62-65` over the handler's response; standalone callers (test, any future call site that bypasses the wrapper, any architecture revision that drops the post-hoc injection) emit responses the browser silently rejects. Latent, not yet production-firing — but a class-1 chokepoint defect (api-critical) per the canonical-truth standard. |
| **Observation** | Two findings stacked. **(a) Direct fix:** `apiSuccess` now mirrors `apiError`'s defensive pattern — accepts optional `opts._cors` for dynamic-origin propagation from `createHandler`, falls back to the static `corsHeaders` export from `cors.ts` (the backward-compat export that exists precisely for this case). Same signature shape as `apiError` (`opts?: { ..., _cors?: Record<string, string> }`); same fallback chain (`opts?._cors ?? staticCorsHeaders`); same spread pattern (`{ ...headers, 'Content-Type': 'application/json' }`). No change to `createHandler`'s post-hoc injection (line 62-65 remains; it's now a defense-in-depth overwrite rather than the sole CORS path). **(b) Meta-finding — canonical-source gap:** The api-conventions module has no `docs/04-modules/` or `docs/07-reference/` document naming the CORS responsibility contract. The convention is *only* expressed in two adjacent docstrings (`handler.ts:161`, `api-error.ts:14`) and one test assertion (`shared_test.ts:212`). A future contributor extending the api-helpers (or reading `cors.ts` and wondering why `corsHeaders` exists as a backward-compat export alongside `getCorsHeaders`) has no canonical artifact to consult — they reconstruct the contract from grep. Same shape as INC-26 (test/docstring is the de-facto contract) but lower severity (api-conventions, not financial-critical). |
| **Disposition** | Fix (a) landed at this commit: `handler.ts:162-180` (new docstring naming the apiError-symmetry rationale + cross-referencing INC-27; signature + body extended to mirror apiError); import line 10 extended to also pull `corsHeaders as staticCorsHeaders` from `cors.ts`. Test (`shared_test.ts:209-215`) now passes; all 26 `shared_test.ts` tests green; suite-wide Deno typecheck exit 0 across all 17 `_test.ts` files. Meta-finding (b) tracked here only — no doc creation in-pass per scope discipline; canonical api-conventions doc surface (likely `docs/04-modules/api/` or extension to an existing platform doc) deferred for operator disposition because (i) the convention is now mechanically enforced by the test + by inter-helper symmetry (apiError is the worked example), and (ii) creating a parallel doc when two docstrings + a test already express the contract may be duplication rather than clarification. Recommended disposition: defer to next api-module-touching change (low cost to fold in then; zero cost to defer now). |
| **Cross-references** | Fix at `supabase/functions/_shared/handler.ts:162-180` (apiSuccess) + `handler.ts:10` (import); pattern source at `supabase/functions/_shared/api-error.ts:18-36`; static fallback at `supabase/functions/_shared/cors.ts:67`; canonical wrapper at `handler.ts:29-111` (createHandler) + `:62-65` (post-hoc CORS injection chokepoint); test at `supabase/functions/_shared/shared_test.ts:209-215`; related discoverability-hazard pattern at INC-26 (asymmetric typed-absence across §3.3 rules — same convention-without-canonical-source shape, higher severity). |
| **Status** | Direct fix (a) resolved at this commit. Meta-finding (b) open — awaiting operator disposition on whether to author a canonical api-conventions doc surface or accept docstring-and-test as the de-facto contract. |

### INC-28: Cron-only handler test sentinels lagged the JWT→cron-secret architectural conversion (test-fixture drift across 2 handlers; convention codified at FP-008.4 Commit 1.5c)

| Field | Value |
|-------|-------|
| **ID** | INC-28 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5c three-way reconciliation, Cluster B of the 17-failure full-execution surfacing) |
| **Classification** | test fixture drift (NOT production defect); same class as INC-26 (test/spec/impl three-way reconciliation, fixture is the outlier). Discoverability + convention hazard for any future cron-dispatched edge function. |
| **Surface Story** | Three handler-source-sentinel tests were failing under end-to-end Deno suite execution: (1) `longshort-universe-quarterly-refresh/index_test.ts:(b)` ordering invariant asserting `isFirstTradingDayOfQuarter` appears before `authenticateRequest(req)`; (2) `…/index_test.ts:(c)` asserting `checkPermissionOrThrow` + `'longshort.view'` literal in handler source; (3) `longshort-universe-hard-exclusion-refresh/index_test.ts:(c)` identical assertion shape. Three-way reconciliation across (a) handler docstrings (quarterly L1-15, hard-excl L1-29 — explicit "Cron-only system path"; "JWT auth would fail from pg_cron"; "system-level cron path"), (b) production implementation (both handlers import + call `verifyCronSecret(req)` from `_shared/cron-auth.ts` — real 401-on-missing/wrong-secret check; neither imports nor calls `authenticateRequest` or `checkPermissionOrThrow`), and (c) test sentinels — converged: handlers were deliberately converted JWT-auth → cron-secret-auth in a prior commit (architecturally correct: pg_cron has no user identity, user-identity primitives are conceptually inapplicable), but the test sentinels were not refreshed in the same change. Gate 11's `deno test --no-run` mode (type-check only) concealed the drift across the entire interval until full execution at FP-008.4 Commit 1.5. |
| **Observation** | The conceptual hazard is **test-sentinel-vs-architectural-truth drift on cron-only handlers** — a class-specific instance of the broader execution-layer concealment pattern (parallel to INC-25's type-check layer mechanism and INC-27's wrapper-owns-CORS-so-helper-can-be-bare mental model). Specific finding: cron-dispatched edge functions need a **different** test-sentinel convention than JWT-auth handlers. Asserting `checkPermissionOrThrow` + permission key on a cron-only function is conceptually inapplicable; the correct sentinels are (i) `verifyCronSecret` presence + import from `_shared/cron-auth.ts`, (ii) ABSENCE of `authenticateRequest` / `checkPermissionOrThrow` (positive assertion that the wrong primitive hasn't been re-introduced silently), and (iii) ordering invariant `verifyCronSecret` BEFORE the work-gate (auth-first is correct with cheap cron-secret HMAC; the old JWT-path "gating-first for load-shedding" rationale doesn't apply). The third sentinel is itself an architectural finding worth surfacing: the cron-secret auth conversion enabled the auth-first ordering invariant inversion, but that inversion isn't documented anywhere prior to this entry. |
| **Disposition** | Fixed at this commit. Three test rewrites applied surgically with test-name preservation (git-blame continuity): (1) `longshort-universe-quarterly-refresh/index_test.ts:(b)` — assertion inverted to `verifyCronSecret(req)` BEFORE `isFirstTradingDayOfQuarter(as_of)`; (2) `…/index_test.ts:(c)` — converted to assert `verifyCronSecret` present + cron-auth.ts import present + `checkPermissionOrThrow` and `authenticateRequest` ABSENT (positive-absence-of-wrong-primitive sentinels prevent silent re-introduction of JWT-style authz); (3) `longshort-universe-hard-exclusion-refresh/index_test.ts:(c)` — identical fix shape as (2). Inline comments in all three rewrites name INC-28, the architectural rationale, and the convention. No production handler change (handlers were already correct). No new test added; conversion is contract-shape preservation, not new coverage. |
| **Cross-references** | Handler at `supabase/functions/longshort-universe-quarterly-refresh/index.ts:115` (`verifyCronSecret` call) + `:30` (import); handler at `supabase/functions/longshort-universe-hard-exclusion-refresh/index.ts:95` (`verifyCronSecret` call) + `:31` (import); auth primitive at `supabase/functions/_shared/cron-auth.ts:18-36` (real 401-on-failure check); test rewrites at `longshort-universe-quarterly-refresh/index_test.ts:82-126` and `longshort-universe-hard-exclusion-refresh/index_test.ts:42-74`; related execution-layer concealment findings at INC-25 + INC-27 (subsume into the layered-mechanism finding planned for FP-008.4 close-out summary — INC-28 is the third instance, not a fourth distinct mechanism). |
| **Status** | Resolved at this commit. Forward-binding convention: any new cron-dispatched edge function added to the platform should use INC-28's test-sentinel triad (verifyCronSecret present + import present + user-identity primitives absent + auth-first ordering) as the regression contract. Worth folding into a `docs/04-modules/api/` cron-handler conventions doc when one is authored, or referencing INC-28 inline in a future cron handler's test file. |

### INC-29: Project lacks a separate integration-test convention — 11 live-DB-dependent tests in unit-test naming + auth-js leak-detector tripping in supabase-admin-importing unit tests (systemic; convention codified at FP-008.4 Commit 1.5d)

| Field | Value |
|-------|-------|
| **ID** | INC-29 |
| **Date Surfaced** | 2026-05-30 (FP-008.4 Commit 1.5d Cluster A/C/D triage + classification refinement) |
| **Classification** | systemic test-convention gap (NOT production defect). Two distinct sub-findings stacked: **(a)** Integration tests requiring live Supabase DB + auth state are filed under the same `*_test.ts` naming as pure unit tests — Gate 11's deterministic-execution promise is broken if integration tests run alongside unit tests in the gate; **(b)** Unit tests that import `supabase-admin` (transitive `@supabase/auth-js`) trip Deno's per-test leak detector on the auth-js `startAutoRefresh()` interval, which is a process-lifetime resource, not a per-test leak. Both surfaced together during the 17-failure full-execution triage that strengthened Gate 11 from `--no-run` to full `deno test`. |
| **Surface Story** | Strengthening Gate 11 from type-check-only to full execution surfaced 17 failures (Commit 1.5 frame). Initial triage classified 11 of them as "Cluster C — get-profile DB/auth-dependent integration" and 1 as "Cluster D — log-sudo-event DB-dependent." Re-investigation at Commit 1.5d distinguished the two: **(a) Cluster C IS integration** — `get-profile/index_test.ts` is 13/13 integration (SETUP test creating real users via `supabaseAdmin.auth.admin.createUser`, 11 tests calling deployed function URLs, TEARDOWN deleting users); already wraps the suite-bracket tests in `Deno.test({ ..., sanitizeOps: false, sanitizeResources: false })` (the convention exists locally — proves the auth-js leak is a known artifact of this file's shape). **(b) Cluster D was misclassified** — `log-sudo-event/index_test.ts` uses `installStubFetch` to isolate the handler and never hits a real DB; the failure (only on the leading test, lines 136 + 164 passed) was Deno's leak detector firing on `@supabase/auth-js/startAutoRefresh` from supabase-admin's transitive import (verified via `--trace-leaks`: `te._startAutoRefresh at .../auth-js.mjs:19:24823`). The first stub-fetch test triggers the auth client's process-lifetime auto-refresh; per-test leak detector flags it on that test only. Same root cause as the existing get-profile convention but applied to a different test pattern. |
| **Observation** | Two forward-binding conventions emerge: **(1) Integration-test naming convention** — for tests that fundamentally require live Supabase state (real users, real DB rows, real session JWTs), the project should adopt a `*_integration_test.ts` suffix (or equivalent marker) so Gate 11's deterministic execution can `exclude` them via `deno.json`. Phase 2+ test-strategy refinement should add a separate integration-gate (e.g., Gate 11.5) that runs the integration suite against an ephemeral Supabase project or seeded staging DB. **(2) Auth-js leak-sanitize convention** — every unit test that imports a module which transitively imports `supabase-admin` (any test that ultimately touches `_shared/supabase-admin.ts` even via stubs) must declare `sanitizeOps: false, sanitizeResources: false` on at least the leading test, because `@supabase/auth-js/startAutoRefresh()` registers a `setInterval` for token refresh that Deno's per-test detector flags as uncleared. This is a known supabase-js-under-Deno artifact, not a real production resource leak (it's process-lifetime, cleared on Deno exit). Both conventions are quiet contracts that lived only in `get-profile/index_test.ts`'s local usage before this entry — same shape as INC-27's "convention without canonical source" finding. |
| **Disposition** | Three changes at this commit: **(a)** `supabase/functions/deno.json` `exclude` extended with `get-profile/index_test.ts` (whole-file exclusion — 13/13 are integration; whole-file is the correct unit of exclusion). **(b)** `log-sudo-event/index_test.ts` — all three `Deno.test(name, fn)` calls converted to `Deno.test({ name, sanitizeOps: false, sanitizeResources: false, fn })` form for symmetry + forward protection (would have been per-test sufficient on the leading test only, but applying to all three is the canonical defensive shape — a future contributor reordering tests won't have to re-debug the same leak). **(c)** Cluster A test rewrites in `deactivate-user/index_test.ts` + `reactivate-user/index_test.ts`: CORS preflight assertion converted from wildcard literal (`== "*"`) to structural + positive-absence sentinel (present + non-empty + NOT wildcard) — same INC-28 pattern asserting absence of the wrong primitive to lock the dynamic-origin policy against silent regression to wildcard (which would be a CSRF surface on these admin mutation endpoints — wildcard would be the defect, not the dynamic value). |
| **Cross-references** | Integration test (excluded): `supabase/functions/get-profile/index_test.ts:107-285` (13 tests, SETUP + 11 + TEARDOWN). Exclusion mechanism: `supabase/functions/deno.json:exclude`. Auth-js leak fix: `supabase/functions/log-sudo-event/index_test.ts:104-188` (three tests). Cluster A test rewrites: `supabase/functions/deactivate-user/index_test.ts:CORS-preflight` + `reactivate-user/index_test.ts:CORS-preflight`. CORS policy source-of-truth: `supabase/functions/_shared/cors.ts:44-65` (dynamic-origin allow-list with Lovable preview hosts). Pre-existing local-only application of the auth-js sanitize convention: `supabase/functions/get-profile/index_test.ts:110` + `:285` (SETUP/TEARDOWN brackets). Related convention-without-canonical-source findings: INC-27 (api-conventions CORS contract) + INC-28 (cron-only handler test sentinels) — INC-29 is the third instance; together they justify a future `docs/04-modules/api/` or `docs/03-tests/` conventions surface. |
| **Status** | Resolved at this commit. Forward-binding work registered for `docs/08-planning/deferred-work-register.md`: **(i)** rename `get-profile/index_test.ts` → `get-profile/index_integration_test.ts` + extend `deno.json` `exclude` to pattern `**/*_integration_test.ts` (convention encoding) when next touching the integration test or whenever an additional integration test is added; **(ii)** author Gate 11.5 (integration suite against ephemeral/staging Supabase) as part of Phase 2 test-strategy refinement; **(iii)** fold the auth-js sanitize convention into a `docs/03-tests/edge-function-test-conventions.md` surface when one is authored, or reference INC-29 inline in the next supabase-admin-importing unit test added. |
| **Scope Correction at Commit 1.5i (2026-05-30)** | Initial 1.5d disposition excluded only `get-profile/index_test.ts` based on the then-failing CI surface (Cluster C single-file framing). 1.5h CI surfaced 13 additional failures (`TypeError: Invalid URL: 'undefined/functions/v1/…'`) across 3 more files: `deactivate-user/index_test.ts` (3 tests), `reactivate-user/index_test.ts` (3 tests), `query-audit-logs/index_test.ts` (7 tests). Comprehensive `grep fetch\( \|\${BASE}\|Deno.env.get` survey of all 7 `*_test.ts` files under `supabase/functions/` (excluding `_shared/`) confirms the true integration-test set is **4 files** (the three above + get-profile). Corrected scope: `supabase/functions/deno.json` `exclude` now covers all 4. **Polarity-asymmetry note (explicit, INC-29-binding):** `log-sudo-event/index_test.ts` is **INTENTIONALLY NOT excluded** — it uses a stub URL (`Deno.env.set("SUPABASE_URL", "http://stub.local")` + `installStubFetch`) and is a genuine unit test that stays in Gate 11. Future contributors: handler-directory `index_test.ts` files are NOT uniformly integration tests; classify by `fetch(` + env-var-derived-URL pattern, not by file location. Same discipline shape as `universe.md`'s polarity-asymmetry subsection — name the asymmetry, prevent future pattern-matching errors. Fourth instance of the FP-008.4 enforcement-gap mechanism (alongside DW-079 / DW-080 lineage): each test-discovery-and-exclusion round was based on the previous round's failure surface, not on a comprehensive survey. Resolved at 1.5i via grep-survey-before-exclusion. Forward-binding work for integration-test naming/location convention is now registered as DW-081 (was previously a sub-bullet on this row's Status field). |

### INC-30: Gate 11 type-check coverage gap — edge function entry points without sibling `_test.ts` files are invisible to `deno test --no-run`'s dependency-graph walk (mechanism-class fifth instance; FP-008.4 Commit 2.5)

| Field | Value |
|-------|-------|
| **ID** | INC-30 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 2 — 4 pre-existing TS2353 errors in `supabase/functions/longshort-universe-enrich-and-filter/index.ts` at lines 91, 126, 253, 282 survived 1.5i + Commit 2 CI-green) |
| **Classification** | CI enforcement-gap (NOT production defect). Fifth instance of the FP-008.4 enforcement-gap mechanism class (alongside DW-079 / DW-080 / DW-081 + the 1.5i polarity-asymmetry sub-row on INC-29). Same shape: a gate that nominally enforces a contract actually enforces it only on the subset of files reachable from the discovery seed it walks. |
| **Surface Story** | Commit 2 strengthened the eligibility-coverage write path in `longshort-universe-enrich-and-filter/index.ts` (Step 5.5 — `writeEligibilityCoverage` call after the refresh_log INSERT). During local pre-flight ladder the AI surfaced 4 `TS2353: 'details' does not exist in type` errors against `apiError(...)` call sites at lines 91, 126, 253, 282 — `apiError`'s `opts` type is `{ code?, field?, correlationId?, _cors? }` (per `supabase/functions/_shared/api-error.ts:18-22`); `details` was never a valid key. The errors are pre-existing (not introduced by Commit 2) and the file has lived on `main` with them through the entire FP-008 / FP-008.2 / FP-008.3 / FP-008.4 sequence. **Why Gate 11 missed them**: Gate 11 runs `deno test --no-run` (and post-1.5e full `deno test`) which type-checks the transitive closure reachable from each `*_test.ts` file's import graph. `enrich-and-filter/index.ts` has no sibling `index_test.ts` and is imported by no test under `supabase/functions/` — it is an orphan entry point. The mechanism: **Gate 11 enforces type-check coverage only on files reachable from a test-file import seed; orphan entry points are silently invisible.** Survey at Commit 2.5 enumerated **46 orphan edge function entry points** out of 53 total (`supabase/functions/<name>/index.ts` with no sibling `index_test.ts` / `index.test.ts`); of those 46, **3 currently have type errors** that Gate 11 cannot see: `enrich-and-filter` (4× TS2353 — this commit's target), `longshort-reconciliation-tick` (1× TS2322 — `SupabaseClient` generic identity mismatch between `esm.sh` import and `node_modules` resolved path), `send-signup-nudge` (3× TS2339 — `Property 'user' does not exist on type 'User'`, likely stale `getUser()` response shape). |
| **Observation** | The conceptual hazard is **gate-coverage-via-discovery-seed asymmetry** — a gate that walks a graph from a seed enforces its contract only on the walked subgraph, never on disconnected nodes, and the disconnection is invisible unless a separate audit enumerates the universe. Same shape as DW-081's "test-classification by file location alone breaks when location is uniform across categories" finding and DW-079's "merge gate enforcement was nominal not actual because the gate had been red on main for the duration." Specific finding: every edge function entry point (`supabase/functions/<name>/index.ts`) needs a sibling `_test.ts` file to enter Gate 11's type-check coverage — even if the test file does nothing more than `import "./index.ts"` + a no-op `Deno.test` to anchor the walk. Forward-binding convention: orphan entry points are a CI-coverage defect class, not an authoring-preference. |
| **Disposition** | Commit 2.5 — **scoped fix only** per execution-prompt survey-decision rule ("any non-trivial errors → STOP; fix only enrich-and-filter; rest becomes tracked follow-up under DW-082's umbrella"). Three changes: (1) `enrich-and-filter/index.ts` — 4× `apiError(500, key, { correlationId, details: x.message })` rewritten to `apiError(500, ` + "`${key}: ${x.message}`" + `, { correlationId })` (folds the diagnostic payload into the message string; correlation_id preserved; no API key invented). (2) `enrich-and-filter/index_test.ts` — NEW thin anchor file: `import "./index.ts"` + `assertExists(import.meta.url)` no-op assertion; uses `https://deno.land/std@0.208.0/assert/mod.ts` (matching the version already in use across the longshort-universe `_test.ts` files). The type-check IS the test. (3) DW-082 registered for the remaining 2 orphan files with type errors (`longshort-reconciliation-tick` + `send-signup-nudge`) AND for the broader 46-orphan structural gap (CI lint to fail on orphan entry points). Anti-pattern guards held: no `// @ts-ignore`, no `// deno-lint-ignore`, no `continue-on-error` — errors fixed properly. |
| **Cross-references** | `apiError` signature at `supabase/functions/_shared/api-error.ts:18-22` (the contract that the 4 call sites violated); 4 fix sites at `supabase/functions/longshort-universe-enrich-and-filter/index.ts` lines 88-92, 124-128, 251-255, 280-284 (post-fix line numbers); new anchor at `supabase/functions/longshort-universe-enrich-and-filter/index_test.ts`; survey output at Commit 2.5 closure SHA (46 orphan entry points; 3 with type errors); related FP-008.4 mechanism-class entries DW-079 / DW-080 / DW-081 / INC-29 polarity-asymmetry row; sibling-shape work item DW-082 (this entry's forward-binding companion); `.github/workflows/strong-evidence.yml:Gate 11` step (the gate whose coverage gap this finding documents). |
| **Status** | Resolved for `enrich-and-filter` at Commit 2.5. **Commit 7.5 update:** the Tier-A `longshort-reconciliation-tick` TS2322 half of DW-082 A1 is CLOSED by SupabaseClient specifier unification (`@supabase/supabase-js` canonical import-map path), `@ts-nocheck` removal, handler 403 audit-settling test fix, and Gate 14 recurrence-prevention sentinel; Gate 11 passes 246/246. **Open structurally:** orphan edge function entry points still bypass Gate 11 unless they have sibling tests, and `send-signup-nudge` remains the known ordinary-backlog orphan type error under DW-082 A2. Forward-binding convention: any new edge function added under `supabase/functions/<name>/` MUST land with a sibling `index_test.ts` in the same PR — even if the test is a one-line `import "./index.ts"` + no-op Deno.test anchor. DW-082 Part B codifies the CI lint that will fail on orphan entry points (future orphan-entry gate; Gate 14 now separately covers SupabaseClient specifier recurrence). |

### INC-31: universe_refresh_log.outcome CHECK constraint vs orchestrator writer mismatch — D1 from FP-008.4 Commit 3 pre-investigation survey (resolved at Commit 3)

| Field | Value |
|-------|-------|
| **ID** | INC-31 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 3 pre-investigation survey of the circuit-breaker mechanism) |
| **Classification** | Latent defect in a Tier-A safety mechanism (circuit breaker). Pre-existing. Schema-vs-code contract mismatch — DB-layer CHECK constraint and application-layer enum diverged. |
| **Surface Story** | Commit 3 pre-investigation surveyed the longshort circuit breaker end-to-end. `public.universe_refresh_log.outcome` carries an inline `CHECK (outcome IN ('completed', 'failed', 'partial'))` from MIG-048 (constraint name `universe_refresh_log_outcome_check`). The orchestrator at `supabase/functions/longshort-universe-quarterly-refresh/quarterly-refresh-orchestrator.ts` finalizes a tripped breaker by writing `outcome: 'circuit_breaker_open'` (also present in the `RefreshOutcome` TS union). The DB would reject this value with a 23514 CHECK violation on every real trip and leave a half-finalized row. The breaker has not actually tripped to date because of the separate D5 self-masking race (see DW-082-adjacent survey output and Commit 3.5's scope) — so this constraint mismatch has never manifested at runtime. |
| **Observation** | Conceptual hazard: **type-vs-constraint drift in two-sided enums** — when an application-layer enum and a DB-layer CHECK constraint are authored at different times by different commits, no compiler reconciles them. Same mechanism class as DW-080 (version-pin asymmetry across import surfaces). Specific finding: the constraint widening is a one-line ALTER, but the failure mode it eliminates is silent-until-first-trip. The four-value form `('completed', 'failed', 'partial', 'circuit_breaker_open')` is exactly what the writer needs; NULL is preserved as the in-flight-row convention. |
| **Disposition** | **Resolved at FP-008.4 Commit 3** — `sql/12_universe_refresh_outcome_check_widening.sql` / MIG-056 drops and re-adds the constraint with the 4-value set. Idempotent DROP-then-ADD pattern mirroring `audit_logs_actor_id_fkey` rebuild migration. NULL acceptance preserved (no NOT NULL added). Operator applies via Supabase SQL Editor; live-DB verification per §22.5.1 to follow. D5 / D2 / D3 explicitly out of Commit 3 scope (sequenced to Commits 3.5 / 4). |
| **Cross-references** | MIG-048 (`supabase/migrations/20260525093303_7fe13534-fbe0-4089-9143-bc4ec98ce7d9.sql:15` — original 3-value CHECK); MIG-056 (`sql/12_universe_refresh_outcome_check_widening.sql` — this resolution); orchestrator writer site `supabase/functions/longshort-universe-quarterly-refresh/quarterly-refresh-orchestrator.ts` (`outcome: 'circuit_breaker_open'`); `RefreshOutcome` TS union (same file); Commit 3 pre-investigation survey at the Commit 3 closure SHA; sibling defects D5 (Commit 3.5), D2 + D3 (Commit 4); INC-32 (D6 — duplicate MIG-032 ledger gap); INC-33 (platform job-executor fail-open parallel). |
| **Status** | Resolved at Commit 3 SHA pending operator live-DB apply + §22.5.1 evidence binding. |

### INC-32: MIG-032 duplicate migration (20260513191627) for `circuit_breaker_threshold` ADD COLUMN is not registered in the ledger — D6 from FP-008.4 Commit 3 pre-investigation survey

| Field | Value |
|-------|-------|
| **ID** | INC-32 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 3 pre-investigation survey — surfaced while tracing the platform circuit-breaker schema for D2/D3 fix scope) |
| **Classification** | Documentation/ledger-discipline gap. Harmless at runtime (idempotent `ADD COLUMN IF NOT EXISTS` against an already-present column — the second apply is a no-op). NOT a production defect. Constitution Rule 6 / D5 ledger-entry discipline violation. |
| **Surface Story** | Two timestamp-named migrations in `supabase/migrations/` perform the same `ALTER TABLE public.job_registry ADD COLUMN IF NOT EXISTS circuit_breaker_threshold integer DEFAULT 3` as MIG-032: the canonical MIG-032 entry registered in the ledger, and a sibling timestamped `20260513191627` migration applying the identical statement. The duplicate is idempotent (the `IF NOT EXISTS` guard makes the second apply a no-op against a present column) and has no runtime effect — but it is not registered in `docs/07-reference/database-migration-ledger.md` and violates the D5 "every migration applied → ledger entry in same PR" discipline. |
| **Observation** | Conceptual hazard: **ledger-vs-filesystem drift via implicit-no-op migrations** — idempotency guards make duplicate migrations invisible at apply time, but they remain visible in the migration filesystem and the ledger loses the bijection. Future schema archaeology becomes ambiguous. |
| **Disposition** | Logged-and-deferred. Resolution shape: either (a) register the duplicate as a no-op MIG-NNN ledger entry with a back-reference to MIG-032, or (b) delete the duplicate file if the apply history shows it was never independently applied — operator decision; both are bounded-cost. Not scheduled in Bucket A. |
| **Cross-references** | Canonical MIG-032 entry in `docs/07-reference/database-migration-ledger.md`; duplicate file at `supabase/migrations/20260513191627_*.sql`; Constitution Rule 6 (documentation-in-same-PR discipline); D5 from FP-008.4 Commit 3 pre-investigation. |
| **Status** | Open — ledger-registration gap; non-blocking; not scheduled. |

### INC-33: Platform job-executor circuit breaker exhibits identical fail-open-on-read pattern as longshort breaker D2 — parallel-defect survey output from FP-008.4 Commit 3 pre-investigation

| Field | Value |
|-------|-------|
| **ID** | INC-33 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 3 pre-investigation survey — surfaced when comparing the longshort breaker's `countConsecutiveFailures` to platform breaker reads for shared-pattern propagation) |
| **Classification** | Latent defect in a Tier-A safety mechanism (platform job-executor circuit breaker). Parallel-shape to D2 from the longshort survey. Fail-open-on-read: a read failure prevents the breaker from tripping, allowing operations to proceed under degraded observability. |
| **Surface Story** | Platform job executor at `supabase/functions/_shared/job-executor.ts:128-145` (circuit-breaker read path) exhibits the same shape as the longshort `countConsecutiveFailures` D2 defect: the read of breaker state catches DB errors and returns a value that fails open (proceeds with execution) rather than failing closed (refusing to execute under uncertainty). The longshort survey identified this as D2 and is fixing it in Commit 4; the platform breaker carries the identical pattern but fronts a broader job universe of mixed criticality (financial / compliance / operational classes), so a blanket inversion would cause operational jobs to refuse to run during transient DB hiccups. |
| **Observation** | Conceptual hazard: **shared-pattern propagation in safety mechanisms** — the fail-open-vs-fail-closed decision is per-call-site and per-criticality-class; copying a breaker pattern across modules without re-evaluating the fail direction reproduces the defect at every site. Same mechanism class as DW-079 / DW-080 / DW-081 (pattern that nominally enforces a contract but actually doesn't, at multiple sites). |
| **Disposition** | Logged-and-deferred to DW-083 Part A (this commit's DW entry) — per-job-class audit required before applying Commit 4's inversion pattern to the platform breaker. Financial/compliance classes: fail-closed mandatory. Operational classes: operator decision per class. NOT a blanket fix. Commit 4's longshort fix is the canonical reference pattern. |
| **Cross-references** | `supabase/functions/_shared/job-executor.ts:128-145` (the fail-open read); longshort sibling `countConsecutiveFailures` (D2, fixed in Commit 4); D2 from FP-008.4 Commit 3 pre-investigation survey; DW-083 Part A (deferred fix); `job_registry.class` column (the per-class audit dimension); Constitution: this is the exact phantom-state class the governance model exists to catch. |
| **Status** | Open — deferred to DW-083 Part A; SHOULD-FIX-BEFORE-LIVE for financial/compliance job classes. |

### INC-34: Circuit-breaker self-masking race — orchestrator's in-flight NULL self-row at head of DESC-ordered tail breaks the streak count — D5 from FP-008.4 Commit 3 pre-investigation survey

| Field | Value |
|-------|-------|
| **ID** | INC-34 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 3 pre-investigation survey of the circuit-breaker mechanism — D5; deferred from Commit 3 to Commit 3.5 per sequencing discipline because D1's CHECK widening is the prerequisite to D5 being safely fixable: with D5 fixed the breaker can actually trip, and the first real trip needs D1's widened CHECK to avoid a 23514 violation leaving a half-finalized row) |
| **Classification** | Latent correctness defect in a Tier-A safety mechanism (longshort universe-refresh circuit breaker). The breaker is structurally incapable of tripping in its pre-Commit-3.5 state. Same severity tier as INC-31 (D1) — both gate the breaker actually functioning. |
| **Surface Story** | `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts:78-92` calls `ctx.refreshLogPersister.insertStart(...)` BEFORE `ctx.refreshLogPersister.countConsecutiveFailures(3)`. The `insertStart` impl at `supabase/functions/longshort-universe-quarterly-refresh/index.ts:36-49` writes the in-flight row with `outcome` omitted (lands as NULL). The pre-Commit-3.5 `countConsecutiveFailures` query reads `.select('outcome').order('refresh_started_at', { ascending: false }).limit(N)` with NO `.in(...)` or `IS NOT NULL` filter — so this orchestrator's own freshly-inserted NULL row sits at the head of the DESC tail. The loop's first iteration hits `outcome=null`, falls into `else`, breaks, returns 0. Even if the prior 3 rows were `'failed'`, the count is masked to 0 and the breaker never trips. Identical masking applies to orphan-NULL rows left behind by any prior invocation that crashed before `finalize` patched the terminal outcome. |
| **Observation** | Conceptual hazard: **load-bearing convention without enforcement** — the `NULL = in-flight row` convention is implicit in the insertStart/finalize split, but nothing in the read path acknowledged it. The fix makes the convention explicit (`.in(...)` filter co-located with the sql/12 CHECK value set) AND load-bearing (a regression-sentinel test pins the filter at source level so a future refactor that introduces an `'in_progress'` non-NULL marker fails loudly). Sibling-shape to INC-31 (type-vs-constraint drift in two-sided enums): both are mechanism-class findings where two sides of a contract were authored without a compiler reconciling them — D5 is the read-side analogue of D1's write-side. The stay-tripped semantics decision (circuit_breaker_open counts toward the streak) is the deliberate operator choice over auto-rearm: auto-rearm would mask a persistent upstream failure as a transient one-run cooldown, which is worse than the current never-trips state for a Tier-A trade-gate. |
| **Disposition** | **Resolved at FP-008.4 Commit 3.5** — `countConsecutiveFailures` in `supabase/functions/longshort-universe-quarterly-refresh/index.ts` gains a `.in('outcome', ['completed','failed','partial','circuit_breaker_open'])` filter (excludes NULL self-row AND orphan-NULL rows) and switches the loop predicate from `row.outcome === 'failed'` to `STREAK_FAILURE_OUTCOMES.has(row.outcome as RefreshOutcome)`. `STREAK_FAILURE_OUTCOMES` is exported from `refresh-jobs/types.ts` adjacent to the `RefreshOutcome` union and contains `{'failed','circuit_breaker_open'}` — stay-tripped semantics: a tripped breaker stays tripped on subsequent runs until manually cleared. Five new tests in `quarterly-refresh-orchestrator_test.ts` cover: (1) D5 fix — 3 prior failed (NULL self-row excluded) trips; (2) stay-tripped — prior circuit_breaker_open + 2 failed trips again; (3) clean break — prior completed breaks the streak; (4) source-level convention pin — `.in()` filter + `STREAK_FAILURE_OUTCOMES.has` regression sentinel; (5) set-vs-union-vs-CHECK pin — drift between the TS union, predicate set, and sql/12 CHECK becomes a test failure. D2 fail-open-on-read (return 0 on error) explicitly UNCHANGED in this commit — scoped to Commit 4. D3 optional-method gap explicitly UNCHANGED — the test fake adds the method locally without touching the persister interface. `insertStart` vs `countConsecutiveFailures` ordering UNCHANGED — the NULL self-row is the in-flight signal; the fix is to read past it correctly, not to reorder. |
| **Cross-references** | INC-31 (D1 — sibling write-side type-vs-constraint defect; Commit 3 / MIG-056 resolution); orchestrator call site `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts:78-92`; persister impl site `supabase/functions/longshort-universe-quarterly-refresh/index.ts` (`countConsecutiveFailures`); `RefreshOutcome` union + `STREAK_FAILURE_OUTCOMES` set co-located at `supabase/functions/_shared/longshort-universe/refresh-jobs/types.ts:26-43`; sql/12 CHECK constraint `outcome IN ('completed','failed','partial','circuit_breaker_open')`; D2 / D3 deferred to Commit 4; D4 (manual-clear path RLS-blocked) deferred to DW-083 Part A MUST-FIX-BEFORE-LIVE — until DW-083 Part A closes, a tripped breaker requires raw service-role SQL to clear (acceptable in Phase 1 pre-live; blocking for Phase 2 live trading); FP-008.4 Commit 3.5 closure SHA. |
| **Status** | Resolved at FP-008.4 Commit 3.5 SHA pending CI green on closure SHA. |

### INC-35: Circuit-breaker fail-open-on-read (D2) + optional-method silent-disable (D3) — both inverted to fail-closed at FP-008.4 Commit 4

| Field | Value |
|-------|-------|
| **ID** | INC-35 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 3 pre-investigation survey — surfaced together as the read-path and interface-path symptoms of the same semantic event: "cannot assess the streak"; deferred from Commit 3 to Commit 4 per sequencing discipline because D1 (Commit 3) + D5 (Commit 3.5) are prerequisites to the breaker actually being able to trip, which is the prerequisite to D2/D3's now-live exposure being consequential) |
| **Classification** | Latent correctness defect pair in a Tier-A safety mechanism (longshort universe-refresh circuit breaker). D2: fail-open-on-read — `countConsecutiveFailures` in the production persister caught DB read errors and returned 0, allowing the next refresh to proceed under a degraded (blind) breaker. D3: the method was declared optional on `RefreshLogPersister`; a persister that omitted it silently disabled the breaker via an `? ... : 0` fallback in the orchestrator. Both became consequential the moment Commit 3.5 made the breaker functional. |
| **Surface Story** | D2: `supabase/functions/longshort-universe-quarterly-refresh/index.ts` `countConsecutiveFailures` — `if (error) { console.warn(...); return 0; }`. A transient DB read failure masked any prior failure streak as zero, letting a fresh pipeline run proceed even if the last three attempts had failed. D3: `supabase/functions/_shared/longshort-universe/refresh-jobs/types.ts` `RefreshLogPersister.countConsecutiveFailures?(limit)` — the `?` optional marker plus the orchestrator's `ctx.refreshLogPersister.countConsecutiveFailures ? await ... : 0` guard meant a persister that omitted the method silently turned the breaker off. Together: two paths ("DB read failed" / "method not implemented") that should halt the gate instead both fell through to "no failures, proceed". |
| **Observation** | Conceptual hazard: **fail-direction on a safety-mechanism read MUST match the mechanism's posture, not the read's typical use** — `return 0 on read error` is appropriate for an observability counter (zero means "nothing to report"), but inverts the semantics when the counter front-ends a trade-gate (zero means "no prior failures, proceed"). The §2 axiom ("missing data ⇒ typed-absence path; synthetic numerics pretending completeness = defect") catches this exact class: `0` was a synthetic completeness claim for a read that produced no information. Sibling-shape to INC-33 (platform job-executor breaker exhibits the identical pattern across job classes — deferred to DW-083 Part B for per-class audit; Commit 4 is the canonical reference pattern for the financial/compliance halt direction). The decision between fail-shapes was not code-inferable: (a) trip on read error → one blip stuck-until-raw-SQL (rejected, worse than current given D4 unfixed); (b) throw to handler → breaker permanently blind on persistent read failure (rejected, quiet); (b-prime) finalize as failed and count toward streak → three consecutive read errors converge to a normal breaker trip via the same code path as three real pipeline failures (accepted — operator-confirmed; persistent-read-failure converges to the breaker's own halt-and-surface mechanism rather than a quieter stream of HTTP 500s; consistent with stay-tripped posture from Commit 3.5). |
| **Disposition** | **Resolved at FP-008.4 Commit 4** — D2 (b-prime): the orchestrator wraps `countConsecutiveFailures` in a narrow dedicated try (around only the count call, not the trip block or main pipeline try). A read failure finalizes the in-flight start-row with `outcome: 'failed'` + `failure_reason: 'count_consecutive_failures_read_failed: <err>'` + early-returns; pipeline does not run; finalize targets the same `refresh_id` returned by `insertStart` (patches the in-flight row, does NOT create a second row). Read-error 'failed' rows are streak-eligible per `STREAK_FAILURE_OUTCOMES` (which includes `'failed'`), so three consecutive read failures reach `recentFailures >= 3` on the fourth run and trip the breaker via the standard path. Also eliminates the orphan-NULL-on-read-error case: the start-row is always finalized to a terminal outcome, never left dangling. D3 (x): `countConsecutiveFailures` is now required (non-optional) on `RefreshLogPersister`; the `?` marker was removed; the orchestrator's `? ... : 0` fallback was deleted entirely. A missing implementation is now a TypeScript compile error rather than a runtime fail-open branch. The single test stub that omitted the method (`makePersister()` in `quarterly-refresh-orchestrator_test.ts`) was updated with a 3-line `return 0` stub satisfying the new required signature; the other two implementors (production `makeSupabasePersister` + Commit 3.5 `makeBreakerPersister`) already implemented it. Three new tests in `quarterly-refresh-orchestrator_test.ts`: (1) D2 fail-closed — throwing count → finalize failed, pipeline skipped (sentinel checks on all three fetchers), failure_reason contains both `count_consecutive_failures_read_failed` and the underlying error message, finalize targets the insertStart refresh_id; (2) D2 single-finalize pin — read-error path invokes finalize exactly once (guards against double-finalize collision; pins the four-path mutual-exclusion control flow); (3) D2 converges to breaker trip — three prior 'failed' rows (representing the post-state after three throwing runs) → fourth run trips with outcome 'circuit_breaker_open', proving read-error 'failed' rows are streak-indistinguishable from pipeline-failure 'failed' rows. D4 (manual-clear path RLS-blocked) explicitly UNCHANGED — remains DW-083 Part A MUST-FIX-BEFORE-LIVE; this commit increases its blast radius (a sustained 3-day read outage now trips a breaker that requires raw service-role SQL to clear, acceptable in Phase 1 pre-live, HARD GATE on Phase 2 live signal-stack reliance). Platform job-executor breaker (`job-executor.ts:128-145`) UNCHANGED — remains DW-083 Part B per-class audit; Commit 4 is the canonical fail-closed pattern for the financial/compliance classes. No SQL, no RLS, no clear-RPC, no migration. |
| **Cross-references** | D2/D3 from FP-008.4 Commit 3 pre-investigation survey; orchestrator narrow-try site `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts` (breaker-read section); interface site `supabase/functions/_shared/longshort-universe/refresh-jobs/types.ts` `RefreshLogPersister.countConsecutiveFailures`; production persister `supabase/functions/longshort-universe-quarterly-refresh/index.ts` `makeSupabasePersister` (unchanged — already implemented the method post-3.5); test additions `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator_test.ts` (3 new Deno.test blocks + `makePersister()` stub update + `makeThrowingCountPersister` helper); INC-31 (D1, Commit 3); INC-33 (platform-breaker parallel; DW-083 Part B); INC-34 (D5, Commit 3.5); DW-083 Part A (D4 clear-path MUST-FIX-BEFORE-LIVE); DW-083 Part B (platform-breaker per-class audit using Commit 4 as canonical pattern); `STREAK_FAILURE_OUTCOMES` co-located with `RefreshOutcome` at `refresh-jobs/types.ts`; sql/12 CHECK constraint; FP-008.4 Commit 4 closure SHA. |
| **Status** | Resolved at FP-008.4 Commit 4 SHA pending CI green on closure SHA. |

### INC-36: `longshort_audit_logs` audit-log forgery vector + universe_membership/hard_exclusions garbage-write vectors — three over-broad PERMISSIVE INSERT policies defeated the system-actor-only-writes model; resolved at FP-008.4 Commit 5 / MIG-057

| Field | Value |
|-------|-------|
| **ID** | INC-36 |
| **Date Surfaced** | 2026-06-03 (FP-008.4 Commit 5 pre-investigation survey — surfaced when the "#4 RLS additive-defeat" item, originally framed against the SELECT read overlay, was traced to its actual mechanism on the write side across three tables) |
| **Classification** | **Tier-A security defect** — the most security-critical finding of the FP-008.4 Bucket A pass. The `longshort_audit_logs_insert_policy` PERMISSIVE INSERT policy was `WITH CHECK (true)`, meaning any authenticated user could INSERT arbitrary rows into the per-strategy audit log: any `operator_id`, any `actor`, any `action`, any `target_type`/`target_id`, any `metadata`, any `ip_address`/`user_agent`/`correlation_id`. The governance model (constitution §1 "repo is authoritative artifact"; §8 audit-as-Tier-A-evidence) treats the audit log as ground truth for "what the system did"; a forgeable-by-any-authenticated-user audit table means every audit-evidence-gated decision in the system rests on a machine-only-write property that did not actually hold. Sibling defects on the same write side, lesser but real: `universe_membership_operator_insert` and `hard_exclusions_operator_insert` were both `WITH CHECK (operator_id = auth.uid())` — any authenticated user could inject rows under their own UUID, bypassing the pipeline's refresh_id / firing-rule lineage and poisoning the downstream financial-data tables. |
| **Surface Story** | Three PERMISSIVE INSERT policies, all defeated by the same RLS axiom: PERMISSIVE policies OR-combine, so a single permissive policy whose WITH CHECK evaluates true under any authenticated condition grants the write — and no PERMISSIVE policy can constrain another PERMISSIVE policy. (a) `public.universe_membership` — `universe_membership_operator_insert` `WITH CHECK (operator_id = auth.uid())` allowed any authenticated user to insert universe rows under their own UUID. The companion `universe_membership_operator_read` `USING (operator_id = auth.uid())` SELECT policy is dead-but-harmless (the system actor writes under a synthetic operator_id no authenticated user owns); the longshort_view_read SELECT policy is the real read gate; the read side is intentional additive overlay, not the defeat. (b) `public.hard_exclusions` — identical shape, `hard_exclusions_operator_insert` `WITH CHECK (operator_id = auth.uid())`. (c) `public.longshort_audit_logs` — `longshort_audit_logs_insert_policy` `WITH CHECK (true)`, the forgery vector. All three legitimate writers run under the service role (verified per call site): `universe-membership-persister.ts:57-59`, `hard-exclusions-persister.ts:53-58`, `_shared/strategy-audit.ts:126-137` via `writeStrategyAuditEvent` — all use the shared `supabaseAdmin` client (service-role-keyed per `_shared/supabase-admin.ts:27` `SUPABASE_SERVICE_ROLE_KEY`). Service-role bypasses RLS, so removing the authenticated-role write paths affects zero legitimate writes. |
| **Pre-MIG-057 audit-evidence trust boundary** | The forge-vector was **latent-but-live** from `longshort_audit_logs`' creation through MIG-057 apply. No evidence exists that any authenticated user forged entries during that window. However, by the nature of a forgeable audit log, the audit log itself cannot prove the absence of forgery for its own pre-fix history — pre-MIG-057 entries are consistent with machine actions but not provably exclusive to them. This is an **epistemic boundary on historical evidence**, not a claim that forgery occurred. Operationally, this bounds what pre-MIG-057 audit evidence can be relied on to establish: §8's audit-as-Tier-A-evidence ladder assumes the log is machine-write-only, and for the pre-fix window that assumption did not hold. Post-MIG-057, the RESTRICTIVE deny restores the machine-only property; from MIG-057 apply forward, `longshort_audit_logs` entries are provably machine-written (service-role-only) and can be relied on per §8. Recorded here non-alarmingly because (a) the system was pre-live during the entire pre-fix window — the audit table fronts a strategy that has not run in production trade-execution mode — and (b) the only audit-gated decisions made against this log during the pre-fix window were internal governance/review decisions where the operators authoring the reviews are also the only authenticated users with system access, narrowing the practical forgery exposure to "could an operator have authored a forged entry attributing an action to a different operator," which is bounded by the operator-trust model in any case. The boundary matters for any future replay of pre-MIG-057 audit evidence as ground truth in a context that depends on the machine-only property (e.g., regulatory replay, post-incident attribution) — such use must caveat the pre-MIG-057 window. |
| **Observation** | Conceptual hazard: **PERMISSIVE-only RLS cannot enforce a deny** — the sibling `*_no_direct_write_policy` tables (`kill_switches`, `reconciliation_events`, `universe_eligibility_coverage`, `universe_refresh_log`, `longshort_reconciliation_state`) use PERMISSIVE `FOR ALL USING(false) WITH CHECK(false)`, which works ONLY because no other PERMISSIVE write policy is present. That is a fragile invariant enforced by convention (every future migration author must remember not to add a PERMISSIVE write policy to these tables) rather than by the type system. The RESTRICTIVE construct is the correct primitive for "this command cannot be additively granted": RESTRICTIVE policies AND-combine, so a RESTRICTIVE USING(false) / WITH CHECK(false) denies the command regardless of any present or future PERMISSIVE policy. The fix uses RESTRICTIVE for exactly this reason — it hardens the system-actor-only-writes invariant structurally, not by convention. Sibling-shape to the broader FP-008.4 mechanism-class findings (INC-31, INC-34, INC-35): "two sides of a contract authored without a compiler reconciling them." Here the two sides are "intended posture: only the system actor writes" and "enforced posture: anyone can." The compiler (Postgres RLS combination rules) silently allowed the gap. The sibling `*_no_direct_write_policy` tables deserve the same RESTRICTIVE hardening in a follow-up commit (not in #4 scope; see Disposition); leaving the PERMISSIVE-false-only shape on those tables means the same defect class can recur there if a future migration adds a permissive write policy. |
| **Disposition** | **Resolved at FP-008.4 Commit 5 / MIG-057** (`sql/13_rls_deny_authenticated_writes.sql`). For each of the three tables: dropped the over-broad PERMISSIVE INSERT policy and created three RESTRICTIVE deny-authenticated-write policies scoped per-command (`FOR INSERT WITH CHECK (false)`, `FOR UPDATE USING (false) WITH CHECK (false)`, `FOR DELETE USING (false)`) `TO authenticated`. Scoped per-command (not `FOR ALL`) because RESTRICTIVE AND-combines per command: `FOR ALL USING(false)` would AND against the SELECT read policies and deny all authenticated reads, breaking the dashboards. SELECT policies (`*_longshort_view_read`, `*_operator_read`) intentionally unchanged — the read overlay is the intended additive design per the original sql/ comments. Single transaction (BEGIN/COMMIT) so no observable window where the old PERMISSIVE INSERT is gone but the new RESTRICTIVE deny is not yet present. Idempotent (DROP POLICY IF EXISTS before every CREATE, mirroring sql/11). Posture: **service-role-only writes, no authenticated-role write path even for superadmin** — the three pipeline-write tables are not designed for human hand-editing (membership rows require valid refresh_id lineage, exclusion rows require firing-rule lineage, audit entries must be machine-written to satisfy the §1/§8 ground-truth model). The sanctioned future hand-edit path is an explicit SECURITY DEFINER RPC with superadmin check + audit-log write + explicit reason parameter, mirroring the existing `kill_switch_*` RPC pattern — not an ambient policy. Out of #4 scope but recommended follow-up: harden the five `*_no_direct_write_policy` PERMISSIVE-false tables to the same RESTRICTIVE shape (separate hygiene commit). |
| **Cross-references** | FP-008.4 Commit 5 pre-investigation survey; MIG-057 (`sql/13_rls_deny_authenticated_writes.sql`); database-migration-ledger.md MIG-057 entry; legitimate-writer call sites `_shared/longshort-universe/refresh-jobs/universe-membership-persister.ts:57-59`, `_shared/longshort-universe/refresh-jobs/hard-exclusions-persister.ts:53-58`, `_shared/strategy-audit.ts:126-137` (`writeStrategyAuditEvent`); service-role client provenance `_shared/supabase-admin.ts:27` (`SUPABASE_SERVICE_ROLE_KEY`); sibling `*_no_direct_write_policy` PERMISSIVE-false tables (kill_switches, reconciliation_events, universe_eligibility_coverage, universe_refresh_log, longshort_reconciliation_state) recommended for RESTRICTIVE hardening in a separate commit; INC-31 / INC-34 / INC-35 (mechanism-class siblings — two-sided contracts authored without a compiler reconciling them); constitution §1 (repo as authoritative artifact) + §8 (audit-as-Tier-A-evidence); kill_switch_soft_pause / _hard_pause / _resume RPCs as the canonical SECURITY DEFINER hand-edit pattern; FP-008.4 Commit 5 closure SHA. |
| **Status** | Resolved at FP-008.4 Commit 5 SHA pending CI green on closure SHA + operator out-of-band SQL-Editor apply of `sql/13_rls_deny_authenticated_writes.sql` + §22.5.1 live-DB evidence binding (`pg_policies` snapshot confirming the three over-broad INSERT policies are gone and the nine RESTRICTIVE deny policies are present). |

### INC-37: `universe_membership` re-run misclassified as failure under now-functional breaker — naive `.insert()` throws 23505 on re-run, feeds false-failure signal to Commit 3.5 streak counter — reframing of pre-Phase-2 #5 "INSERT idempotency / prevent duplicates"

| Field | Value |
|-------|-------|
| **ID** | INC-37 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 6 pre-investigation survey of `universe_membership` write path — reframed from the pre-Phase-2 investigation's #5 "INSERT idempotency / prevent duplicates" item once Commit 3.5 made the breaker functional and turned a previously-quiet failure-mode into a Tier-A safety concern) |
| **Classification** | Latent correctness defect in the persistence layer with Tier-A consequence under the post-Commit-3.5 breaker. Pre-3.5 the defect was a quiet misclassification (one failed re-run = one bad audit row); post-3.5 three legitimate re-runs in a row trip the universe-refresh circuit breaker for "we already did this work," halting the trade-gate behind a defect with no real upstream signal. |
| **Surface Story** | `supabase/functions/_shared/longshort-universe/refresh-jobs/universe-membership-persister.ts` calls `supabaseAdmin.from('universe_membership').insert(rowsToInsert)` with no `onConflict`. The table's PK is `(operator_id, ticker, as_of_date)` (MIG-050 Surface 1 Option A; `long_eligible`/`short_eligible` are columns, not key components). A second refresh for the same `as_of_date` (bootstrap re-fire, manual retry, cron edge — all expected operational paths) writes the same PK and throws `23505 unique_violation` on the first overlapping ticker. The orchestrator's outer try/catch finalizes the in-flight refresh-log row as `outcome='failed'` with a duplicate-key error message. Pre-Commit-3.5 this was a quiet annoyance (a bad audit row, no operational consequence — the breaker could not trip). Post-Commit-3.5, `STREAK_FAILURE_OUTCOMES` includes `'failed'`, so three legitimate re-runs in a row reach `recentFailures >= 3` and trip the breaker — halting the trade-gate behind a defect that has zero upstream-data correlate. The pre-Phase-2 investigation framed this as "INSERT idempotency / prevent duplicates," which was directionally right but pointed at the surface (duplicates) rather than the behavior (re-run misclassified as failure). Pre-investigation also confirmed live data has zero duplicates (1678 rows = 1678 distinct keys) — the PK has been doing its job; no cleanup migration is needed. |
| **Observation** | Conceptual hazard: **a safety mechanism's correctness depends on the signal it consumes being truthful** — Commit 3.5 made the breaker capable of tripping, which made every input to the streak counter load-bearing. A persister that throws on a legitimate operational re-pattern feeds the breaker a signal that has no upstream correlate, and the breaker cannot distinguish "real upstream failures" from "the persister doesn't tolerate our normal retry pattern." Same mechanism-class as INC-35 (D2 fail-open-on-read): in both cases the defect is a write/read path producing a `'failed'` outcome for a non-upstream event, polluting the breaker's input set. The fix posture is identical: make the path tolerate the legitimate re-pattern so the breaker sees only real failures. Sibling-shape also to INC-34: load-bearing convention without enforcement — the implicit convention that "refresh-log finalize = real outcome" assumed the persister only finalized on real upstream outcomes, but a naive `.insert()` introduces a path where finalize records `'failed'` for an operational re-pattern. **Reframing-class lesson**: the pre-Phase-2 investigation's surface-framing ("duplicates") missed the actual defect because the surface had not yet been exposed by Commit 3.5; only after the breaker became functional did the real shape ("re-run misclassified as failure") become visible. This is the second item in FP-008.4 (alongside the D2/D3/D5 cluster) where a pre-existing latent defect became consequential the moment a sibling commit made the consuming mechanism functional — a pattern worth carrying forward into future investigations: "what becomes load-bearing the moment this commit lands?" should be a standard pre-investigation lens. |
| **Disposition** | **Resolved at FP-008.4 Commit 6** — `.insert(rowsToInsert)` replaced with `.upsert(rowsToInsert, { onConflict: 'operator_id,ticker,as_of_date', ignoreDuplicates: false })` in `universe-membership-persister.ts`. `ignoreDuplicates: false` selects DO UPDATE (the supabase-js default; pinned explicitly for clarity): `long_eligible` / `short_eligible` / `quarter_label` / `refresh_id` are overwritten with the latest call's values on PK conflict. `refresh_id` becomes a last-writer reference — it points to the most recent refresh whose evaluation produced the row's current state, not the first refresh that ever wrote it; the FK to `universe_refresh_log` remains valid. No schema migration (PK already enforces uniqueness; live data confirms zero duplicates). No SQL changes. Compatible with Commit 5 RESTRICTIVE deny (persister uses `supabaseAdmin` service-role, bypasses RLS) and Commit 2 coverage write (same `(operator_id, as_of_date)` scope, already idempotent on its own PK). Tests: new persister test file `universe-membership-persister_test.ts` with 2 tests (re-run with mutated payload → DO UPDATE fires, last-writer-wins on PK including `refresh_id`; partial overlap → stale-row property pinned at the row level). Orchestrator test file gains 1 test (double `orch.run()` for the same `as_of_date` → both `completed`, no false-failure feed to the breaker). All 26 tests pass (23 baseline + 2 persister + 1 orchestrator). Anti-patterns explicitly NOT used: no DELETE-then-INSERT (would create an empty-universe window observable by the trade-gate), no `ignoreDuplicates: true` (would DO NOTHING — second call's eligibility/refresh_id changes would not land), no DELETE-stale step for tickers dropped from the eligible set between runs (see forward-looking note below), no touch to orchestrator outer try/catch or breaker streak logic (the fix is purely at the persister; making the re-run path not throw in the first place is preferable to making the orchestrator/breaker tolerate it after the fact). **Forward-looking note — out of #5 scope** (Phase 2): stale-row property under UPSERT. If a ticker is eligible in run 1 but NOT in run 2 for the same `as_of_date`, run 1's row remains in `universe_membership` with run 1's `refresh_id`. Consumers that need exact "membership at `as_of_date D` = latest refresh's eligible set" semantics must filter to the latest `refresh_id` on read. If Phase 2 wants this filter implicit, the likely shape is either (a) a `WHERE refresh_id = (SELECT refresh_id FROM universe_refresh_log WHERE as_of_date = D ORDER BY refresh_started_at DESC LIMIT 1)` predicate on all read paths (cheap, decentralized), or (b) a deliberate DELETE-stale step inside a future SECURITY DEFINER RPC that wraps the persist call and the stale-clear in a single transaction (centralized, write-side enforcement, requires a new RPC). Neither is in scope for #5; surfaced here so the consumer audit at Phase-2 entry has the property documented. **Commit-3.5 follow-on framing**: #5 is as much a 3.5 follow-on as a pre-existing item. Without #5, the now-functional breaker is fed false-failure signals by the persister's intolerance of legitimate re-runs, and the operator-visible symptom would be "the breaker trips after we retry a refresh three times, and we can't tell whether the underlying refresh is broken or the breaker is broken." Commit 6 closes that ambiguity. |
| **Cross-references** | FP-008.4 Commit 6 pre-investigation survey; `_shared/longshort-universe/refresh-jobs/universe-membership-persister.ts` (UPSERT call site + semantic comment block); `_shared/longshort-universe/refresh-jobs/universe-membership-persister_test.ts` (new — Tests 1+2); `_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator_test.ts` (Test 3 — double-run smoke); MIG-050 (`universe_membership` PK on `(operator_id, ticker, as_of_date)`); INC-34 / INC-35 (Commit-3.5 / Commit-4 siblings — the mechanism the false-failure signal was feeding); `STREAK_FAILURE_OUTCOMES` in `refresh-jobs/types.ts` (the predicate that made `'failed'` consequential); FP-008.4 Commit 6 closure SHA. |
| **Status** | Resolved at FP-008.4 Commit 6 SHA pending CI green on closure SHA. |

### INC-38: `reconcile()` infrastructure-failure audit hole — loadFn throws escape STEP (a) before STEP (c) writes the `reconciliation_events` row, violating the table's own "every verify_* invocation writes one row" invariant — World-B finding from FP-008.4 Commit 7 pre-investigation (resolved at Commit 7); paired with #9 reconciliation tick-handler fail-loud

| Field | Value |
|-------|-------|
| **ID** | INC-38 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 7 pre-investigation — first round flagged the tick handler's swallow-to-200 pattern as #9, second round reframed the reconcile()-bypass alarm as false, third round read `longshort-reconciliation-lifecycle.ts` directly and identified the actual hole at STEP (a)'s unguarded `await invoke(ts)`) |
| **Classification** | Audit-completeness defect at the canonical reconciliation surface. Infrastructure failures (loadFn throws — broker timeout, DNS, ECONNRESET, service unavailable) were the one outcome class that NEVER reached `reconciliation_events`, leaving operators no canonical record of "the verify could not run." All four other outcome classes (false_positive_within_tolerance / expected_divergence_handled / failure_handled / failure_escalated / system_bug) are recorded because they flow through STEP (c) `writeEventRow` after classification at STEP (b); only infra failures escaped before classification could happen. The lifecycle docstring stated the invariant ("every verify_* invocation writes one row regardless of outcome") that the code did not actually enforce. |
| **Surface Story** | The pre-investigation cycle: (Round 1) the tick handler's three try/catch blocks were observed swallowing every per-verifier outcome into a `results[]` array and unconditionally returning `apiSuccess(...)`, classified as the #9 phantom-success vector. (Round 2) a survey hypothesized that the verify_* wrapper layer might be bypassing `reconcile()` entirely, which would have been a much bigger audit-surface defect — but the three-file check (`verify_buying_power.ts:139-163`, `verify_position.ts:142-167`, `verify_universe_membership.ts:160-185`) confirmed the wrappers ARE thin "build spec → call `reconcile()`" shells. World 1: the audit surface is intact for completed verifies. (Round 3) the one-file read of `longshort-reconciliation-lifecycle.ts` showed that `reconcile()`'s first executable line is `const { expected, observed } = await invoke(ts);` with no surrounding try/catch — so a loadFn throw propagates out of `reconcile()` before STEP (c) writes the row. World B confirmed: infrastructure failures are the one outcome class that never reaches the canonical audit surface. The remediation reuses the existing `system_bug` outcome enum value (infrastructure failure = source unavailable = no comparison possible = structurally a system_bug) so no schema/enum migration is needed. |
| **Observation** | Mechanism-class: **a docstring-stated invariant that the code does not actually enforce is a defect** — the lifecycle's "Throws on / Does NOT throw on" section claimed loadFn rejections re-throw "outside lifecycle classification" but did not say "and the event row is skipped"; readers inferred the invariant from the table-of-rows narrative and assumed the code enforced it. This is the third FP-008.4 instance of "load-bearing convention without enforcement" (after INC-34's CHECK-vs-SET vs INC-37's PK-vs-naive-insert), reinforcing that documented invariants in heavily-rigorous surfaces require either structural enforcement (the closure landed at Commit 7) or an explicit "convention is enforced by ___" pointer in the docstring. Reframing-class lesson: the pre-investigation's middle round was the wrong-shape hypothesis (bypass at the wrapper layer) that, if not checked, would have driven a fix at the wrong site (adding parallel `strategy_audit` events at the tick handler — the audit-writer trap the lifecycle docstring explicitly bans). The discipline of "do not draft the fix until the gating question is resolved by reading source" caught it; this is the second FP-008.4 instance after the #4 RLS read-overlay survey (which found the read overlay was intentional and the real defect was on the write side, not the surface-flagged read side). |
| **Disposition** | **Resolved at FP-008.4 Commit 7.** Three-part fix paired in one commit: **(1)** `supabase/functions/_shared/longshort-reconciliation-lifecycle.ts` — STEP (a)'s `await invoke(ts)` wrapped in try/catch. On throw, `writeEventRow` is invoked with `outcome='system_bug'`, `divergence={infrastructure_failure:true,error:<msg>}`, `expected_value=null`, `observed_value=null`, `notes='infrastructure_failure: <msg>'`, then the original error is re-thrown (caller's fail-loud contract preserved). Event-row-only — state surface intentionally NOT updated on this path (the throw halts the caller; rolling-window/escalation arithmetic applies only to completed verifies with actionable outcomes). Audit-write failure during closure is logged via `console.error` with both the original and write error captured, then the original error still propagates (we do not pretend we recorded what we could not). All 17 verifier wrappers + the cross-check inherit the closure for free — they call `reconcile()` internally. **(2)** `supabase/functions/longshort-reconciliation-tick/index.ts` — #9 fail-loud disposition routing via new exported pure helper `classifyTickDisposition(results)`: any `failure_escalated` / `system_bug` / `infrastructure_failure` outcome → HTTP 500 (`apiError(500, 'reconciliation_tick_escalated')`); otherwise → HTTP 200. Zero `strategy_audit` events emitted — every outcome including infrastructure failures is now in `reconciliation_events` via the closure (the HTTP status IS the disposition signal; emitting parallel strategy-audit events would hit the audit-writer trap the lifecycle docstring bans). NOT-FOR-LIVE-INVOCATION docstring guard added at handler top. **(3)** #8 forward-binding sentinel (Gate 13 — `scripts/check-verify-after-mutation.ts` + `_test.ts`, added to `.github/workflows/strong-evidence.yml`): flags any in-scope file where a `.insert(/.update(/.upsert(/.delete(` precedes a verify_* wrapper invocation (17-name allowlist) or `reconcile(` call; override-annotation escape `// gate-13-allow: post-mutation verify per §7.6 — <reason>` accepted on the verify line or in the immediately-preceding `//` comment block. File-level lexical heuristic (intentionally over-eager — per-file, not control-flow). Current HEAD baseline: 0 violations after annotating the cross-check site at `longshort-universe-quarterly-refresh/index.ts:243` (the .insert() in the same file is `insertStart` writing a bookkeeping row to `universe_refresh_log`, not a strategy-state mutation in the §7.5 sense). #8 lifecycle-ordering itself had no behavioral defect in live code — all live mutator-with-verify sites are already pre-mutation; the sentinel is forward-binding only against Phase-5 order-execution code. **Tests:** 4 new in NEW `_shared/longshort-reconciliation-lifecycle_test.ts` (audit-hole closure behavioral test via globalThis.fetch stub + fake SUPABASE_URL/SERVICE_ROLE_KEY env; state-surface-not-updated invariant test; audit-write-failure-during-closure original-error-still-propagates test; source sentinel pinning the try/catch shape). 6 new in NEW `longshort-reconciliation-tick/index_test.ts` (five disposition cases over `classifyTickDisposition` + source-level no-swallow sentinel; closes the orphan-anchor half of DW-082 A1, now split into A1.a CLOSED + A1.b OPEN). 9 new in `scripts/check-verify-after-mutation_test.ts` (mutation→verify-wrapper flagged; mutation→reconcile() flagged; override-annotation escapes on line and preceding comment block; verify-before-mutation clean; string/comment literal ignored; scope/sanction predicates; clean-baseline assertion). **Out of scope explicitly:** no `strategy_audit`/`audit_logs` events (audit-writer trap); no 6th outcome enum value (system_bug reused); no SQL/schema change; no verifier-wrapper change; no cross-check site change beyond the gate-13-allow annotation; no DW-082 A1.b TS2322 fix (remains MUST-FIX-BEFORE-LIVE); no DW-083 Part B platform-breaker touch. |
| **Cross-references** | FP-008.4 Commit 7 pre-investigation (three-round narrative); DEC-034.1 clause (4) (six-step lifecycle); `reconciliation_events` invariant ("every verify_* invocation writes one row regardless of outcome"); `_shared/longshort-reconciliation-lifecycle.ts` (closure site); `longshort-reconciliation-tick/index.ts` (#9 disposition routing site); `scripts/check-verify-after-mutation.ts` (Gate 13 sentinel); `.github/workflows/strong-evidence.yml` (Gate 13 step); INC-34 / INC-37 (sibling load-bearing-convention-without-enforcement mechanism class); DW-082 A1.a (orphan-anchor half CLOSED at Commit 7) + A1.b (TS2322 half CLOSED at Commit 7.5); DW-083 Part B (platform-breaker per-class audit, untouched); FP-008.4 Commit 7 closure SHA. |
| **Status** | Resolved at FP-008.4 Commit 7 SHA pending CI green on closure SHA. |

### INC-39: Cross-artifact seam — `job_registry.enabled=true` pointing at a mock-backed / NOT-FOR-LIVE handler — `longshort.reconciliation_periodic_sweep` enabled by MIG-045 vs `longshort-reconciliation-tick` marked NOT FOR LIVE INVOCATION at Commit 7; disarmed at FP-008.4 Commit 8 / MIG-058

| Field | Value |
|-------|-------|
| **ID** | INC-39 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 8 pre-investigation — #11 ("tick cron-schedulability + two-invocation rule") was framed as forward-looking work; reading the source showed it was actually a latent live-activation hazard: `job_registry.enabled=true` on a `system_critical`, `*/5 * * * *` scheduled job whose handler is mock-backed and was marked `NOT FOR LIVE INVOCATION` at Commit 7. Live-DB read confirmed `enabled=true` + zero `job_executions` rows — armed but not firing only because scheduler dispatch isn't currently wired.) |
| **Classification** | Cross-artifact-seam mechanism class — the defect lived in the *relationship* between two individually-correct-looking artifacts (registry seed + MIG-045 enablement on one side; handler source + Commit 7 NOT-FOR-LIVE docstring on the other side), not within either artifact in isolation. The repo looked internally consistent — MIG-045 was well-commented ("verifier registry complete, dispatch function exists, can run end-to-end"), the handler's guard was present, Commit 7's hardening was complete — but the *seam* between them encoded a phantom-reconciliation hazard per CROSSWIND §2 axiom 2 (internal derivatives treated as external primes) + §20 (phantom decision throughput). Latent rather than active because pg_cron dispatch wiring is absent in the current environment; the protection was an absence, not a deliberate gate. Same mechanism class as INC-36 (audit-log forgery — three-way seam between RLS `WITH CHECK (true)` + RESTRICTIVE-deny convention + service-role bypass) and INC-37 (PK-vs-naive-insert — seam between `.insert()` semantics + ON CONFLICT idempotency convention + circuit-breaker streak counter). |
| **Surface Story** | Pre-investigation for FP-008.4 Commit 8 / #11. Independent first-pass read of `supabase/functions/longshort-reconciliation-tick/index.ts` source (post Commit 7) noted the migration filename `20260522110000_step_6_3d_activate_reconciliation_periodic_sweep.sql` (MIG-045) and flagged the tension between "activate the reconciliation periodic sweep" and Commit 7's NOT-FOR-LIVE guard. Direct read of MIG-045 confirmed `UPDATE job_registry SET enabled = true WHERE id = 'longshort.reconciliation_periodic_sweep';`. Cross-reference of `_shared/reconciliation_jobs_seed` (`step_6_2_reconciliation_jobs_seed.sql`) confirmed the seed has `schedule='*/5 * * * *'`, `trigger_type='scheduled'`, `class='system_critical'`, `execution_guarantee='exactly_once'`, `concurrency_policy='forbid'`. Live-DB read (operator-run): `SELECT id, enabled, status, schedule, trigger_type, updated_at FROM job_registry WHERE id = 'longshort.reconciliation_periodic_sweep';` returned `enabled=true`, `status=registered`, `schedule='*/5 * * * *'`, `updated_at=2026-05-24 03:57:55.969625+00`. Live-DB read: `SELECT COUNT(*) FROM job_executions WHERE job_id = 'longshort.reconciliation_periodic_sweep';` returned zero — over ~11 days since MIG-045's `updated_at`, the scheduler has never picked up this enabled job (pg_cron dispatch not currently wired in this environment). State assessed as latent loaded gun: `enabled=true` against a mock/NOT-FOR-LIVE handler, disarmed only by absence of dispatch wiring, would fire every 5 minutes the moment dispatch is connected — exactly the §2/§20 phantom-throughput hazard. |
| **Observation** | Mechanism-class: **a financial-safety gate that depends on an absence (no dispatch wiring) is not a gate** — it converts to active hazard the instant the absence is filled, which is *exactly* what bringing pg_cron + job-executor live will do. Asymmetric review surface: MIG-045's reviewer would have needed to read the handler source (`longshort-reconciliation-tick/index.ts`) to catch the contradiction; Commit 7's handler reviewer would have needed to read the registry seed + MIG-045 to catch it; neither review surface naturally crosses the seam. This is the generalizable lesson — load-bearing cross-artifact invariants ("`enabled=true` ⇒ handler is live-ready") that no single-file review can verify need either structural enforcement or a mechanical sentinel that scans across the seam. Same lesson surface as INC-36 (RLS additive defeat — single-table policy review couldn't catch the OR-combination defeat across all PERMISSIVE policies) and INC-37 (idempotency convention — single-call-site review couldn't catch that the streak counter consumes the duplicate-PK failure signal). The generalizable mitigation is a Gate-15-class sentinel scanning for any `enabled=true` + `trigger_type='scheduled'` job in `job_registry` seed/enable migrations whose target handler source contains `NOT FOR LIVE INVOCATION` or a mock-fetcher marker (`MOCK_*_FETCHER`, etc.) — to be authored in #11's second commit alongside the two-invocation liveness rule (the two pair naturally: the liveness rule catches "fires but reconciles nothing real" at runtime; the sentinel catches "armed against a non-live handler" at CI). |
| **Disposition** | **Disarm landed at FP-008.4 Commit 8 / MIG-058.** Idempotent single-row UPDATE: `enabled=false` for `longshort.reconciliation_periodic_sweep` in `public.job_registry`, with `WHERE enabled = true` guard + `DO $$ ... $$` end-state assertion. Live verified post-apply: `enabled=false`, all other registry fields preserved. MIG-045 NOT deleted (forward-only history per D2); MIG-058 supersedes its enablement only. Handler docstring updated to reference MIG-058 explicitly. **Sentinel deferred to Commit 8 second commit** — the cross-artifact `enabled=true`-vs-NOT-FOR-LIVE Gate-15 scanner and the two-invocation liveness rule pair naturally; out of scope for the disarm-only commit per scope discipline. **Re-enable conditions** (all three required, via a future migration explicitly citing MIG-058): (1) real broker fetchers (FP-006 sub-step 6.7 — Alpaca paper) replacing `MOCK_BP_FETCHER` / `MOCK_POSITION_FETCHER`; (2) two-invocation liveness rule live (#11 second commit); (3) explicit re-enable migration confirming (1)+(2). |
| **Cross-references** | MIG-045 (`supabase/migrations/20260522110000_step_6_3d_activate_reconciliation_periodic_sweep.sql`) — the premature enablement superseded; MIG-058 (`supabase/migrations/20260604090759_*.sql`) — the disarm; `supabase/functions/longshort-reconciliation-tick/index.ts` (handler, mock fetchers, NOT-FOR-LIVE docstring, Commit 7 disposition routing); `supabase/functions/_shared/reconciliation_jobs_seed` (`step_6_2_reconciliation_jobs_seed.sql`) — `*/5 * * * *` schedule + `system_critical` class origin; FP-008.4 Commit 7 (#9 fail-loud + reconcile() audit-hole closure + NOT-FOR-LIVE docstring guard); FP-006 sub-step 6.7 (Alpaca paper — real broker fetcher integration, re-enable prerequisite); FP-008.4 Commit 8 pending second commit (#11 two-invocation liveness rule + cross-artifact Gate-15 sentinel); INC-36 + INC-37 (sibling cross-artifact-seam mechanism class instances). |
| **Status** | Disarm resolved at FP-008.4 Commit 8 / MIG-058. Cross-artifact-seam mechanism pattern recorded for the upcoming Gate-15 sentinel (Commit 8 second commit). |

### INC-40: `reconciliation_events` provenance gap — a "real broker, zero positions" event row was data-indistinguishable from a "mock fetcher, returned null" event row; the two-invocation liveness rule (FP-008.4 #11) was unbuildable as a pure SQL query and required a schema column

| Field | Value |
|-------|-------|
| **ID** | INC-40 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 9 pre-investigation survey — surfaced when scoping the two-invocation liveness rule and discovering that `reconciliation_events` carried no field distinguishing mock-fetcher output from real-broker output) |
| **Classification** | Tier-A latent — affects re-enablement safety of `longshort.reconciliation_periodic_sweep` (system_critical job). Disarmed at MIG-058; re-enable gated on this rule per MIG-058's documented conditions. |
| **Surface Story** | The pre-investigation evaluated three candidate definitions of "real verify row" for the liveness predicate: (i) non-zero / non-null result — REJECTED, because `MOCK_POSITION_FETCHER.fetchPosition → null` is indistinguishable from a real broker's "no position open" return, and `MOCK_BP_FETCHER → available_bp: 100000` is a valid live value; flat broker accounts produce mock-like output. (ii) constant-signature detection — REJECTED, brittle (couples detector to mock magic numbers; breaks when mocks change). (iii) provenance-flagged — chosen. The handler knows its own fetcher source at construction time (mock vs live is a property of which fetcher the dispatcher wired, not derived data); the rule's `fetcher_source = 'live'` predicate makes the distinction first-class in the audit surface. The defect class this prevents: a future re-enable that silently degrades to mock-backed dispatch (e.g., a wiring regression at 6.7 that reverts one verifier to its mock fetcher) would otherwise look like "the sweep is running, events are flowing" while being phantom — the same hazard MIG-058 disarmed at the registry layer, but now caught at the data layer if the registry is later re-armed. |
| **Observation** | Mechanism-class sibling to INC-31 / INC-34 / INC-35 / INC-36 — "two sides of a contract authored without a compiler reconciling them." Here the two sides were "what the dispatcher knows" (fetcher provenance — known at module-load time, encoded only in import statements) and "what the audit surface can answer" (which historically had no provenance field). The schema change makes provenance a typed, persisted property; the lifecycle's required `fetcher_source: FetcherSource` parameter makes missing-it a compile-time error (no column default by design — see MIG-059 DEVIATION block). Honest-backfill discipline applied: pre-MIG-059 rows are backfilled to `'unknown'`, NOT retroactively claimed as `'mock'` or `'live'`, mirroring INC-36's epistemic-boundary stance on pre-MIG-057 audit evidence (don't claim a property the historical evidence cannot prove). |
| **Disposition** | **Resolved at FP-008.4 Commit 9 / MIG-059.** Added `reconciliation_events.fetcher_source text NOT NULL CHECK IN ('mock','live','replay','unknown')` (three-step add — nullable → backfill `'unknown'` → NOT NULL + CHECK, deliberately without a default). Lifecycle `reconcile()` gains required `fetcher_source: FetcherSource` parameter threaded to both the normal write and the Commit-7 infrastructure-failure catch write. All 17 verifier wrappers updated to thread the parameter. Tick handler tags all three dispatches `'mock'`; universe quarterly-refresh cross-check tags `'live'`. New `liveness_check` value added to `VerifyCallName` union (DW-069 precedent: non-verify_* reconcile identifier). |
| **Cross-references** | FP-008.4 Commit 9 closure SHA (pending); MIG-058 (the disarm this provenance gap was preventing safe re-enable of); MIG-059 (the resolution); `supabase/functions/_shared/longshort-reconciliation-types.ts` (FetcherSource type + union widening); `supabase/functions/_shared/longshort-reconciliation-lifecycle.ts` (required parameter); `supabase/functions/longshort-reconciliation-liveness-check/index.ts` (the consumer); INC-31 / INC-34 / INC-35 / INC-36 (mechanism-class siblings); INC-36 honest-backfill discipline. |
| **Status** | Resolved at FP-008.4 Commit 9 SHA (pending CI green + §22.5.1 live-DB evidence binding: `information_schema.columns` snapshot confirming `NOT NULL` + no default + four-value CHECK + all existing rows backfilled to `'unknown'`). |

### INC-41: Liveness-check STOP ladder rung (b) — direct alert emission omitted because the repo's alerting model is `system_metrics` push + `alert_configs` threshold evaluated by the job-alert-evaluation job, NOT a synchronous emit helper; deferred as DW-086

| Field | Value |
|-------|-------|
| **ID** | INC-41 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 9 pre-investigation survey — surfaced when scoping the three-rung STOP ladder for the two-invocation liveness rule) |
| **Classification** | Tier-B operational gap — the 2-rung ladder shipped at Commit 9 (`system_bug` event + `enabled=false` halt) covers the auditable-trail + safe-state requirements; the missing rung is paging/alerting velocity. The HTTP 500 the handler returns is the operational signal in the interim (cron-level retry + monitoring infra fires on the 5xx). |
| **Surface Story** | The originally-scoped 3-rung ladder was (a) `reconciliation_events` system_bug row via `reconcile()`, (b) alert emission via shared helper, (c) `job_registry.enabled=false` halt. When scoping (b), the survey found the repo's alerting architecture is two-phase: callers push metrics to `system_metrics`; `alert_configs` rows define thresholds + comparisons + cooldowns; a separate job-alert-evaluation job (Phase 3 monitoring infra) reads metrics + configs and writes `alert_history` rows when thresholds breach. There is no shared `emitAlert()` helper that synchronously writes `alert_history` from a caller (and adding one would bypass the cooldown/configuration model — wrong shape). The correct future shape is a separate commit that (i) pushes a `liveness_check.stop` metric to `system_metrics` from this job and (ii) configures an `alert_configs` row with appropriate severity + threshold. Doing that here would conflate "build the rule" with "extend the alerting pipeline" — separate concerns, separate commits. |
| **Observation** | The 2-rung ladder is strictly safer than the original 3-rung in one dimension: rung (c) immediately removes the unsafe condition (the sweep can no longer fire), so the paging gap is "operators discover the halt via cron-level 5xx monitoring instead of via a paged alert" — bounded delay, not an unsafe state. Documenting the gap here (and as DW-086) preserves the analysis so the future commit lands the correct shape, not an ad-hoc emit helper. |
| **Disposition** | Deferred to DW-086 (push `liveness_check.stop` metric to `system_metrics` + create `alert_configs` row). The 2-rung ladder remains the canonical halt mechanism; rung (b) is paging velocity, not safety. |
| **Cross-references** | FP-008.4 Commit 9 closure SHA (pending); `supabase/functions/longshort-reconciliation-liveness-check/index.ts` (the 2-rung shipped); `system_metrics` + `alert_configs` + `alert_history` (the canonical pipeline); DW-086 (the deferred rung-(b) commit). |
| **Status** | Open — Tier-B gap; tracked at DW-086; not a hard prerequisite to the periodic-sweep re-enable (the re-enable conditions per MIG-058 are real fetchers + liveness rule + explicit re-enable migration; alerting velocity is operationally desirable but not safety-blocking given the registry-disarm rung). |

### INC-42: Commit 9 liveness-check introduced three Gate-2 sentinel hits — two real defects (wall-clock leak + `?? 0` sentinel-coercion on the liveness count), one false-positive (verify-after-mutation on `reconcile()`-encapsulated disarm); resolved at FP-008.4 Commit 9.5

| Field | Value |
|-------|-------|
| **ID** | INC-42 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 9 post-commit Gate-2 sentinel run — caught before merge) |
| **Classification** | Tier-A — two real defects on a financial-path safety mechanism (the periodic-sweep liveness rule itself). Both root-fixed; one false-positive annotated. |
| **Surface Story** | Commit 9's new `longshort-reconciliation-liveness-check/index.ts` tripped three sentinels: (1) `check-wall-clock` flagged `updated_at: new Date().toISOString()` inside `failure_action`'s registry-disarm — a wall-clock read in a financial-path lifecycle that already had an injected clock threaded through `reconcile()` to `ctx.ts`. Banned per DEC-034 clause (4). (2) `check-sentinel-patterns` flagged `live_periodic_sweep_event_count: count ?? 0` on the result of the Supabase `count` query — same fail-direction class as INC-33 / Commit-4 D2: a `null` count (read error / unexpected result) would be silently coerced to `0`, which the liveness predicate would then read as "two consecutive empty windows" → false STOP, halting the sweep on infrastructure noise rather than on real livelessness. (3) `check-verify-after-mutation` (the Gate-13 sentinel built in Commit 7) flagged the `.update({enabled:false})` in `failure_action` paired with the later `await reconcile(...)` call — a false positive: the mutation is *inside* `reconcile()`'s own `failure_action` (STEP e of the lifecycle), executed by `reconcile()` as its halt response, not by a caller mutating financial state before verifying. CROSSWIND §7.5/§7.6's pre-mutation-gate invariant protects positions/orders/lots — not a registry-disarm rung that IS the failure response. |
| **Observation** | Meta-point worth recording: the Gate-13 sentinel we built in Commit 7 caught a pattern in Commit 9's *own* new code on its first run after introduction. The recurrence-prevention is working on subsequent commits as designed — sentinels are not a one-time gate but a continuous floor. The `?? 0` hit is the most instructive: the same fail-direction antipattern (silent zero-coercion on a safety-mechanism count) re-emerged in different code five commits after its first surfacing (INC-33/35), which argues that the sentinel-pattern ban is doing its job — without it, this would have shipped as a latent false-STOP. |
| **Disposition** | **Resolved at FP-008.4 Commit 9.5.** (a) Wall-clock fix: `failure_action`'s `_ctx` widened to `ctx`; `updated_at` now uses `ctx.ts.toISOString()` (the injected clock threaded through `reconcile()`). (b) Sentinel-coercion fix: explicit fail-closed branch — if the count query returns `null`/`undefined`, throw with a descriptive message distinguishing infrastructure failure from a genuine zero; only `count === 0` from a successful query is treated as livelessness. (c) Verify-after-mutation annotation: `// gate-13-allow:` block placed immediately before the `await reconcile(...)` call, citing reconcile()-encapsulated failure_action shape (sibling to Commit 7's cross-check override). No SQL, no live-DB, no other file touched. |
| **Cross-references** | FP-008.4 Commit 9.5 closure SHA (pending); `supabase/functions/longshort-reconciliation-liveness-check/index.ts` (all three changes); INC-33 / INC-35 / Commit-4 D2 (fail-direction-class siblings for the `?? 0` fix); Commit 7 cross-check `gate-13-allow:` (annotation precedent); DEC-034 clauses (2) + (4) (banned-pattern authority). |
| **Status** | Resolved at FP-008.4 Commit 9.5 SHA (pending CI green: Gate 2 green, Gate 11 unchanged 246/0). |

### INC-43: `job_registry.enabled` state drift between migration tree and live DB for the universe jobs (`quarterly_refresh` + 4× `hard_exclusion_refresh_*`) — seed migrations ship them `enabled=false` (ACT-109 / sub-step 8.13 pending), live DB has them `enabled=true`; surfaced during FP-008.4 Commit 10 Gate-15 sentinel parser validation; NOT introduced by Commit 10

| Field | Value |
|-------|-------|
| **ID** | INC-43 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 10 live-DB verification — Gate-15 sentinel's migration-replay parser resolved the 5 universe jobs to `enabled=false` (matching seed migrations 20260525093303 + 20260525103115, both of which explicitly seed `enabled=false` and document `Ships enabled=false; activated at sub-step 8.13` / `ACT-109`); live `SELECT id, enabled FROM job_registry` returned `enabled=true` for `quarterly_refresh` + all 4 hard-exclusion rows). |
| **Classification** | Tier-B governance/audit-trail gap — the migration tree is supposed to be the single source of truth for `job_registry` state per §22.5.1 (every state-change to a financial-system-critical job is a migration with an auditable file). The drift indicates an operator-initiated `enabled=true` flip outside the migration tree (likely via the Supabase dashboard or an ad-hoc SQL run); there is no migration file recording who, when, or why. This is the mechanism-class sibling to INC-39 (cross-artifact-seam) — only here the seam is between "migration tree says X" and "live DB says Y", with no artifact reconciling them. |
| **Surface Story** | Commit 10's Gate-15 sentinel chronologically replays every migration's `INSERT`/`UPDATE` against `public.job_registry` to derive the final state of each job. The parser correctly handled the MIG-044→MIG-045→MIG-058 sweep-enable sequence (3 overlays), but produced `enabled=false` for `longshort.universe.quarterly_refresh` and the 4 `hard_exclusion_refresh_3_3*` rows, while the live DB returns `enabled=true` for all 5. Grepping every migration in `supabase/migrations/*.sql` for these ids found ONLY the seed `INSERT`s (both shipping `enabled=false` per the seed-migration comments — `quarterly_refresh`: "Ships enabled=false; activated at sub-step 8.13"; hard-exclusion-refresh seeds explicitly cite `ACT-109` for activation). No `UPDATE public.job_registry SET enabled = true WHERE id LIKE '%universe%'` exists in the migration tree. The parser is correct; the migration tree is incomplete. |
| **Observation** | Two implications, both bounded: (1) Gate-15 itself is unaffected at the Commit 10 baseline — the universe handlers (`longshort-universe-quarterly-refresh/index.ts` + `longshort-universe-hard-exclusion-refresh/index.ts`) are clean of `NOT FOR LIVE INVOCATION` / `MOCK_*_FETCHER` markers (verified at the pre-investigation survey), so even if Gate-15 read them as enabled=true (matching live), Gate-15 would still pass (P1 doesn't trip; P2 doesn't trip because `handler_path` is populated). The sentinel's correctness as a recurrence-prevention does not depend on which side of the drift it sees. (2) The audit-trail gap is a real governance finding: a future operator looking at the migration tree to answer "when did the universe jobs activate?" gets no answer; the live state is unrecorded. Mechanism-class: same shape as INC-39 (the relationship between two artifacts is unmanaged). The proper fix is a small reconciliation migration that explicitly `UPDATE`s the 5 rows to `enabled=true` with a comment block documenting (i) the date of the original operator flip (per `updated_at`), (ii) the authority (ACT-109 / sub-step 8.13 if that was the trigger; or a backfill-acknowledgment if it predated activation authority), and (iii) the discovery context (this INC). That migration also re-aligns the Gate-15 sentinel's migration-derived state with the live DB — eliminating the drift as a class. |
| **Disposition** | **Open — deferred to a focused reconciliation commit (not in Commit 10's scope).** Rationale: Commit 10 is Tier-B CI-hygiene + scope-locked to the Gate-15 sentinel + `handler_path` column; absorbing a state-reconciliation migration would conflate two governance concerns (sentinel-introduction vs migration-tree-reconciliation) and re-introduce the blast-radius coupling Commits 8 / 9 / 10 were explicitly split to avoid. Commit 10's Gate-15 baseline remains CLEAN regardless of which side of the drift the parser sees (the universe handlers are marker-free). The reconciliation migration is registered as DW-087. |
| **Cross-references** | FP-008.4 Commit 10 closure SHA (pending); DW-087 closure SHA (pending); MIG-062 (the reconciliation migration that recorded the 2026-06-04 10:16:24Z activation per ACT-109 / sub-step 8.13 authority); `supabase/migrations/20260525093303_*.sql` (`quarterly_refresh` seed — explicit `enabled=false` + "activated at sub-step 8.13" comment); `supabase/migrations/20260525103115_*.sql` (hard-exclusion-refresh seeds — explicit `enabled=false` + `ACT-109` activation reference); INC-39 (mechanism-class sibling — unmanaged relationship between two individually-correct artifacts); MIG-058 (the §22.5.1 binding-evidence discipline this finding identifies a gap in); `scripts/check-handler-liveness-markers.ts` (the parser whose correctness surfaced the drift); DW-087 (the deferred reconciliation migration, closed at MIG-062). |
| **Disposition addendum** | **Resolved at MIG-062.** The reconciliation migration recorded the 2026-06-04 10:16:24Z activation event per ACT-109 / sub-step 8.13 authority — using the live `updated_at` as the actual flip timestamp in the comment block, with the `AND enabled = true` WHERE-clause guard ensuring the migration RECORDS the existing state rather than making it happen, and a `DO $$ … RAISE EXCEPTION $$` post-update sanity check catching any drift between INC-43 capture and apply. The Gate-15 sentinel's chronological replay now sees the same `enabled=true` state for these 5 rows as the live DB, eliminating the drift as a class. The seam (migration-tree-vs-live-DB) is now structurally closed for the universe jobs. |
| **Status** | **CLOSED at MIG-062** (DW-087 closure pass, 2026-06-05). Was: Tier-B; tracked at DW-087; not safety-blocking (Gate-15 clean either way; the universe handlers carry no marker patterns); not introduced by Commit 10 (the drift predates Commit 10's work). |

### INC-44: `job_registry` → handler-file linkage was convention-only across three non-uniform conventions plus a script-dispatched job before MIG-060 — recorded as the rationale for the `handler_path` column and Gate-15's secondary P2 (NULL handler) rule

| Field | Value |
|-------|-------|
| **ID** | INC-44 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 Commit 10 pre-investigation survey — surfaced when scoping how the Gate-15 sentinel would JOIN registry rows to handler files; discovered the linkage was implicit across three distinct naming conventions with no enforcement). |
| **Classification** | Tier-B mechanism-class finding (resolved at MIG-060) — the three convention shapes were: (i) platform `<id> → job-<id-with-underscores-dashed>` (e.g., `health_check → job-health-check`); (ii) longshort.x → `longshort-<x>` with non-clean suffixes (`longshort.reconciliation_periodic_sweep → longshort-reconciliation-tick`); (iii) universe 4-rows-share-1-handler via `?rule=` query-param dispatch (`hard_exclusion_refresh_3_3a/b/c/e → longshort-universe-hard-exclusion-refresh`). Plus the script-dispatched `replay_chain` (no edge handler at all). No grep, no compile-time check, and no schema constraint reconciled "what the registry says" with "what file exists on disk" — the linkage was a reader's-mental-model contract. |
| **Surface Story** | A new operator adding a job would have to (a) read three existing seeds to infer which convention applies, (b) guess at the convention break for `tick` vs `liveness-check` (both `longshort.reconciliation_*` but one drops `reconciliation` and adds `tick`, the other keeps `reconciliation` and adds `liveness-check`), and (c) discover the `?rule=` shared-handler pattern only by reading the universe seed's description prose. The Gate-15 sentinel needs an authoritative mapping to JOIN on; deriving it convention-by-convention would re-encode the unenforced convention into the sentinel and break the moment a new shape appeared. |
| **Observation** | The right shape was a typed schema column (the value IS a path; columnize it). MIG-060's `handler_path text CHECK (... ~ '^supabase/functions/[a-z0-9-]+/index\.ts$')` makes the linkage queryable, CHECK-enforceable, and self-documenting. The secondary Gate-15 rule (P2: `enabled=true + trigger_type='scheduled' + handler_path IS NULL` → violation) catches the adjacent seam (an enabled scheduled job that dispatches to nothing — registry-completeness defect); it's clean at baseline because `replay_chain` + control rows are all `trigger_type='manual'`. The `replay_chain` row is intentionally NULL because it has no edge-function handler — it's dispatched by `scripts/replay-pass.ts` / `scripts/replay-run.ts`; representing that as NULL is honest about the registry's scope (NOT "we forgot a handler"). |
| **Disposition** | **Resolved at FP-008.4 Commit 10 / MIG-060.** Column added, all 12 real jobs backfilled (4 universe rows share 1 path), control rows + replay_chain left NULL. Gate-15 P2 codifies "an enabled scheduled job with no handler_path is a defect" so future regressions can't reintroduce the gap silently. |
| **Cross-references** | FP-008.4 Commit 10 closure SHA (pending); MIG-060 (the resolution); `scripts/check-handler-liveness-markers.ts` (the consumer + P2 rule); INC-39 (the parent defect class this column + sentinel pair prevent recurrence of); DW-084 (this commit closes it). |
| **Status** | Resolved at FP-008.4 Commit 10 SHA (pending CI green + §22.5.1 live-DB evidence: 18-row `SELECT` confirms backfill matches spec). |

### INC-45: FP-008.4 #13 framing correction — Wikipedia parser bounds-assertion was scoped as a "close the parser-sanity gap" item, but the survey established it is defense-in-depth on top of an already-loud cross-check; the actual gaps closed were a silent per-row-skip failure mode and the orphan test file

| Field | Value |
|-------|-------|
| **ID** | INC-45 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 #13 pre-investigation survey — the survey looked for a "live financial gap" the bounds assertion would close and found the cross-check already escalates the catastrophic case). |
| **Classification** | Tier-B framing correction + two concrete gaps closed at the same commit (silent per-row-skip forensic blind spot + orphan test file). NOT a live financial defect. |
| **Surface Story** | (1) `parseWikipediaConstituents` (`supabase/functions/_shared/longshort-universe/constituent-ingestion/wikipedia-constituent-fetcher.ts`) was structurally defensive (four `ConstituentFetchError` throws for no-wikitable / no-rows / no-`<th>` / missing-Symbol-column) but had no count-bounds assertion. A structurally-valid parse returning an implausible count (e.g. 40 tickers for sp500 because a Wikipedia layout edit caused most rows to fail the per-row ticker regex) returned the short list with no error. (2) The three per-row `continue` paths (ragged-row, empty-ticker, regex-rejected) were uncounted and silent — there was no on-error breadcrumb to which path drained the list. (3) The fetcher was the orphan of the `constituent-ingestion/` directory: `polygon-constituent-fetcher_test.ts` and `ishares-constituent-fetcher_test.ts` existed; `wikipedia-constituent-fetcher_test.ts` did not. |
| **Framing Correction** | The pre-Phase-2 prompt framed #13 as closing a live gap, but the cross-check (`cross-check-spec.ts` `classify_outcome`, `SURFACE_2_THRESHOLDS.SAFETY_CEILING_SYM_DIFF = 100`) already escalates the 40-vs-500 case to `system_bug` and aborts the refresh — AND Wikipedia is observation-only (`secondary_tickers`); the universe feeds from the operator-seeded primary (`SeededMembershipFetcher`), so a short Wikipedia list cannot silently shrink the universe. The bounds assertion is therefore defense-in-depth: an earlier, more diagnosable fail-fast at the parse boundary ("returned 40, expected 490–520, layout likely changed; skipped 460: 0 ragged, 0 empty, 460 regex-rejected") rather than a downstream vague signal ("jaccard 0.08, system_bug aborted"). Worth doing because the threat (Wikipedia changing its table markup) is genuinely likely over time, and the diagnostic locality + protection for any non-cross-check call site materially improves operability. |
| **Observation** | Defense-in-depth on external-data-parsing paths is the right default when (a) the source's contract is unenforced (HTML scrape, not a typed API), (b) the failure mode is silent (per-row `continue`, no counter), and (c) the downstream backstop's error is diagnostically distant from the actual cause. The skip-counters are a zero-infrastructure forensic improvement (three local ints folded into the throw message) — no new telemetry surface needed, no metrics emit, no orchestrator change. |
| **Disposition** | **Resolved at FP-008.4 #13.** Added `SANITY_BOUNDS: Record<IndexId, {min, max}>` (`sp500: [490, 520]`, `sp400: [385, 415]`; `Record<IndexId, …>` forces exhaustive coverage so a future index addition is a compile-time error without bounds), skip-counters at the three `continue` points, in-parser bounds throw with count + bounds + skip breakdown + "layout likely changed" diagnostic hint, and a 17-case test file (positive in-bounds + bounds boundary + bounds violations + structural negatives + skip-counting behavior + fetcher-wrapper propagation). No change to the cross-check, no change to the universe path, no new telemetry infrastructure. |
| **Cross-references** | FP-008.4 #13 closure SHA (pending); `supabase/functions/_shared/longshort-universe/constituent-ingestion/wikipedia-constituent-fetcher.ts` (the assertion); `…/wikipedia-constituent-fetcher_test.ts` (orphan-closure test file, 17 cases); `…/cross-check-spec.ts` `SURFACE_2_THRESHOLDS.SAFETY_CEILING_SYM_DIFF` (the existing backstop that justified the "defense-in-depth not live gap" framing). |
| **Status** | Resolved at FP-008.4 #13 SHA (pending CI green; 17/17 Deno tests passing locally). |

### INC-46: Universe staleness was unsurfaced across all three universe UI surfaces (LongShortDashboard / UniverseMembershipPage / UniverseRefreshHistoryPage); the quarter-derivation predicate is now triplicated; the three universe UI pages remain test-orphans

| Field | Value |
|-------|-------|
| **ID** | INC-46 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 #16 pre-investigation survey). |
| **Classification** | Three sub-findings bundled under #16's resolution: (a) genuine UI gap — the universe surfaces displayed `as_of_date` + `quarter_label` as raw data with no recency interpretation; (b) Tier-B consolidation debt — the "what quarter is this date in" predicate now exists in three places (`supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts::quarterLabelFor`, `supabase/functions/_shared/longshort-universe/shared/trading-days.ts::quarterOf`, and the new frontend `src/features/longshort/utils/universe-staleness.ts::currentQuarter`); (c) test-orphan note — `LongShortDashboard.tsx`, `UniverseMembershipPage.tsx`, and `UniverseRefreshHistoryPage.tsx` have no `*.test.tsx` files. |
| **Surface Story** | (a) An operator looking at `/trading/longshort` or `/trading/longshort/universe` could read `as-of 2026-04-01` and not know whether that was current (Q2 2026, freshly seeded), aging (early-quarter, awaiting the new refresh), overdue (well into the new quarter and the re-seed missed), or stale (multiple quarters behind — a clearly-missed re-seed loop). The circuit-breaker badge on the history page surfaces individual refresh outcomes but not "where are we now" cumulative state. (b) The frontend cannot import the Deno `_shared` module (different runtime/module system), so duplicating `quarterOf` was the in-scope option; the alternative (a backend RPC / view returning `is_stale`) was rejected as plumbing-heavy for a pure presentation concern. The orchestrator's inline `quarterLabelFor` (which predates `_shared/trading-days.ts::quarterOf`) is the older duplication — independent of #16. (c) Page-render test coverage was already absent (#13 closed the same orphan pattern for the Wikipedia fetcher; the universe UI pages are the next instance). |
| **Observation** | (a) Resolved at #16 — pure-frontend `universe-staleness.ts` util computes the 5-state staleness signal (`bootstrapping` / `current` / `aging` / `overdue` / `stale`) with a refresh-outcome cross-reference cause (`refresh_failing` for `circuit_breaker_open`/`failed`/`partial` outcomes vs `no_recent_attempt` for `completed` of a prior quarter or no row at all). Badge surfaces on the dashboard (primary) + membership page header (secondary), NOT the history page (which answers "what happened" not "where are we"). (b) Triplication is logged here for a future consolidation pass — possible shapes: (i) a shared `@longshort/universe-quarter` TS package that both runtimes import, (ii) a build-step extraction from the frontend file into the backend, or (iii) a backend-computed staleness RPC that removes the need for the frontend copy. Out of #16's pure-frontend scope. (c) Page-render test coverage closure is out of #16 — the testable unit (`universe-staleness.ts`) is covered (Vitest, all 5 states + cause cross-ref + quarter-label parse + boundary math); a future commit can close the page-render orphan gap alongside or independent of FP-008.4. |
| **Disposition** | (a) **Resolved at FP-008.4 #16.** (b) **Open** — consolidation debt, no urgency (the duplication is 4 lines per copy, with consistency comments at each site). (c) **Open** — page-render test orphans, same class as the pre-#13 Wikipedia fetcher gap; logged for future closure. |
| **Cross-references** | FP-008.4 #16 closure SHA (pending); `src/features/longshort/utils/universe-staleness.ts` (the new util); `…/universe-staleness.test.ts` (unit tests); `src/features/longshort/components/LongShortDashboard.tsx` + `src/pages/trading/longshort/UniverseMembershipPage.tsx` (the two surfaces decorated); `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts::quarterLabelFor` + `supabase/functions/_shared/longshort-universe/shared/trading-days.ts::quarterOf` (the two backend predicate copies). |
| **Status** | Sub-finding (a) resolved at FP-008.4 #16 SHA (pending CI green); sub-findings (b) + (c) open as Tier-B follow-ups. |

### INC-47: Four ad-hoc outcome renderers with three different mappings for the same reconciliation enum; the cross-check card rendered `system_bug` + `failure_escalated` as amber-warning (most-severe display-severity misalignment); dead fallback branch + raw snake_case labels + a design-token violation on the cross-check card

| Field | Value |
|-------|-------|
| **ID** | INC-47 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 #17 pre-investigation survey). |
| **Classification** | Five sub-findings bundled under #17's resolution: (a) the **most-severe** UI misalignment — `LongShortDashboard.tsx` cross-check card (~L328) used a binary clean/amber `Badge`, rendering `system_bug` (CROSSWIND §11.0.11 ABORT-class; the Commit-7 infra-failure outcome and Commit-9 liveness-rule STOP outcome) and `failure_escalated` (also ABORT-class) with the same amber colour as `failure_handled` (recoverable, standard procedure); an operator could not visually distinguish a catastrophic outcome from a handled noise event. (b) Mapping inconsistency — the same `reconciliation_outcome` enum rendered three different ways on three surfaces (cross-check card binary, dashboard events list 3-way, `ReconciliationEventsPage` 3-way), with no shared helper. (c) Dead fallback branch — `ReconciliationEventsPage.tsx::outcomeBadge` carried a `return <Badge variant="outline">{outcome}</Badge>` fallback that was unreachable (all 5 enum values are covered by the prior branches) and would have rendered a raw enum string if it ever fired. (d) Raw labels — three of four surfaces rendered the raw snake_case enum string (`false_positive_within_tolerance` verbatim) as operator-facing badge text, while `UniverseRefreshHistoryPage` used human labels (`"Circuit Open"`, `"Completed"`) — an inconsistency that left the same enum looking like two different things across pages. (e) Design-token violation — the cross-check card used custom `bg-warning/10 text-warning border-warning/20` Tailwind classes inline, bypassing the design-system semantic-token rule that all colour usage routes through `StatusBadge` or the design-token-driven `Badge` variants. |
| **Surface Story** | (a) Pre-#17, an operator scanning `/trading/longshort` saw the cross-check card amber-badged for every non-clean outcome — the cross-check could trip on a Wikipedia-vs-Polygon catastrophic sym-diff (Surface 2 Option γ ceiling → `system_bug` → refresh aborted) and the dashboard would render that catastrophe identically to a routine `failure_handled` divergence resolved by standard manual override. The escalation severity lived in `failure_action` + the reconciliation runbook; the UI did not reflect it. (b)+(d) The mapping/label drift was a textbook copy-paste history: each surface implemented its own outcome→badge function, and they drifted as the enum grew (`system_bug` added at Commit 7; `circuit_breaker_open` added at Commit 3) without being audited across all renderers. (c) The dead fallback was a defensive-programming relic from before the enum was exhaustively typed in the row schema; harmless but a tell that the renderer was not exhaustively reasoned about. (e) The custom colour classes pre-date the design-system tokenisation pass; they happened to land on the surface that also had the worst severity-collapse, doubling the regression severity. |
| **Observation** | (a)+(b)+(d)+(e) Resolved at #17 — new `src/features/longshort/utils/outcome-display.ts` exposes exhaustive `Record<ReconciliationOutcome, {label; severity}>` + `Record<RefreshOutcomeKey, {label; severity}>` maps with backend-derived severity tiers (CROSSWIND §11.0.10/11 + DEC-038 clause (2) + FP-008.4 Commit 3.5 `STREAK_FAILURE_OUTCOMES`), and severity→`StatusBadge`-status / severity→`Badge`-variant adapters. All four surfaces (cross-check card, dashboard events list, `ReconciliationEventsPage`, `UniverseRefreshHistoryPage`) now route through the helper: human labels everywhere, severity faithful to the backend everywhere, design-token compliance on the cross-check card via `StatusBadge`. Variant adapter intentionally maps `clean → outline` (not `default`) so clean outcomes read calm rather than high-contrast attention-grabbing — fixes a secondary survey finding that the events list's `clean → default` was visually noisy. (c) Resolved at #17 — the exhaustive `Record<…>` helper guarantees every enum value is handled; the dead fallback was deleted, and a future enum addition would surface as a TypeScript compile error rather than a silent fallthrough (same pattern as #13's `SANITY_BOUNDS` and #16's `STALENESS_BADGE_*` maps). Vitest unit tests (`outcome-display.test.ts`, 16 cases) include dedicated regression assertions: `system_bug → severe` and `circuit_breaker_open → severe` are pinned with explicit `not.toBe('clean')` / `not.toBe('handled')` / `not.toBe('neutral')` guards so the cross-check card's pre-#17 severity-collapse bug can never recur silently. Page-render test orphans (LongShortDashboard / ReconciliationEventsPage / UniverseRefreshHistoryPage) remain — same class as #16's open sub-finding (c); out of #17's pure-presentation scope. |
| **Disposition** | (a)+(b)+(c)+(d)+(e) **Resolved at FP-008.4 #17.** Page-render test-orphan gap remains **Open** (rolled into INC-46 sub-finding (c); not duplicated here). |
| **Cross-references** | FP-008.4 #17 closure SHA (pending); `src/features/longshort/utils/outcome-display.ts` (the new shared util); `…/outcome-display.test.ts` (16 Vitest cases incl. system_bug/failure_escalated/circuit_breaker_open severity regression guards); `src/features/longshort/components/LongShortDashboard.tsx` (cross-check card + events list adopters); `src/pages/trading/longshort/ReconciliationEventsPage.tsx` + `…/UniverseRefreshHistoryPage.tsx` (the two page-level adopters); `src/features/longshort/services/baseline/baseline-query-helpers.ts::ReconciliationOutcome` + `src/features/longshort/utils/universe-staleness.ts::RefreshOutcome` (canonical frontend enum types the helper keys off — no third copy introduced); CROSSWIND §11.0.10 / §11.0.11 + DEC-038 clause (2) + FP-008.4 Commit 3.5 `STREAK_FAILURE_OUTCOMES` (the backend severity sources). |
| **Status** | Resolved at FP-008.4 #17 SHA (pending CI green; 16/16 Vitest tests passing locally). |

### INC-48: FP-008.4 #23 framing correction — "per-ticker enrichment failure attribution" was scoped as 404-only but survey found two silent-skip channels (fetcher 404 + caller try/catch swallow); the caller's catch-all collapse to `kind: 'skipped'` was the larger attribution leak, with INC-24's per-ticker context landing in `console.warn` and nothing structured

| Field | Value |
|-------|-------|
| **ID** | INC-48 |
| **Date Surfaced** | 2026-06-04 (FP-008.4 #23 pre-investigation survey). |
| **Classification** | Five sub-findings bundled under #23's resolution: (a) **scope correction** — the original Bucket A framing was "per-ticker enrichment failure attribution" focused narrowly on `PolygonEnrichmentFetcher.fetchTickerDetails()` returning `null` on HTTP 404 (`polygon-enrichment-fetcher.ts:115`) and the fetcher's silent `continue`; the survey of the live caller (`longshort-universe-enrich-and-filter/index.ts:184-190`) found a second, larger silent-skip channel — the per-ticker worker's `try { … } catch (e) { console.warn(…); return { kind: 'skipped' }; }` collapsed EVERY thrown `ConstituentFetchError` (HTTP 401/5xx after retries, JSON parse, timeout, network) into the same `kind: 'skipped'` bucket as a benign 404, with the per-ticker error context surviving only as a `console.warn` line and nothing structured persisted. A 401 (system-wide auth failure), a 429 (rate-limited), a delisting-404, and a JSON-parse (Polygon API change) are operationally distinct but were indistinguishable in the audit. (b) **same defect class as #13** — silent per-item skip → attribution blind spot, this time on the enrichment path; the fix mirrors #13's "count + identities + reason" pattern with the addition of a persistence field since enrichment-skips need to survive into the refresh log (unlike Wikipedia's parse-time bounds which fail loud in-process). (c) **iShares-source skip silently reducing count** — `polygon-enrichment-fetcher.ts:112` `if (c.source === 'ishares') continue` silently reduced the enriched-set size with no attribution; benign-by-design but completes the attribution shape. (d) **downstream conflation** — `universe_refresh_log` had no field for enrichment-skip counts; `total_constituents_raw - total_post_filters` conflated enrichment-stage drops with filter-stage rejections (a 200-ticker enrichment collapse would appear as "filtering was harsh," wrong attribution). (e) **orchestrator-path-also-affected** — `quarterly-refresh-orchestrator.ts:218` (the planned phase-2 batch-mode call site) also called `enrich()` and silently lost any 404 + iShares skips; the contract change fixes the fetcher boundary for both call sites, but persistence symmetry at the orchestrator's own refresh-log write is out of scope (registered as DW-088). |
| **Surface Story** | Pre-#23: an operator inspecting a refresh that came up suspiciously small (e.g., 850 eligible from a 903-ticker universe) would see `total_constituents_raw=903`, `total_post_filters=850`, `filter_rejection_counts` showing 30 rejections — and 23 tickers' worth of drop unexplained, with no way to distinguish "23 delistings (normal attrition)" from "23 rate-limited fetch errors (operational signal — Polygon issue / our API key issue)" from "23 JSON-parse failures (operational signal — Polygon API shape changed)." `console.warn` lines existed but were edge-function log noise, not structured queryable data. The survey reframed the gap from "404 attribution" to "all enrichment-skip attribution," found the survey-recommended fix (the fetcher-contract `EnrichmentResult { enriched, skipped }` with a closed-set of structural reasons), added the caller's `'fetch_error'` classification for thrown errors, and persisted the aggregate via the new MIG-061 `enrichment_skip_counts` column (mirroring MIG-053's `filter_rejection_counts` shape). |
| **Observation** | Resolved at FP-008.4 #23: (a)+(b)+(d) `UniverseEnrichmentFetcher.enrich()` contract widened from `Promise<EnrichedConstituent[]>` to `Promise<EnrichmentResult>` (new `enrichment/types.ts` exports `EnrichmentResult`, `EnrichmentSkip`, `EnrichmentSkipReason`); fetcher attributes both structural reasons (`'ishares_source'` + `'not_in_polygon_404'`) with ticker identity; caller adds `'fetch_error'` for thrown `ConstituentFetchError` (INC-24's per-ticker context now structurally captured, `console.warn` preserved for runtime debuggability); aggregate persists to `universe_refresh_log.enrichment_skip_counts` (MIG-061, jsonb nullable, no default — `NULL` = pre-MIG-061 untracked per INC-36 epistemic-honesty, `{…}` = post-MIG-061 tracked-zero-vs-populated). Tracked-zero convention: all three reasons appear with explicit `0` when they didn't fire (distinct from untracked-NULL). (c) iShares skip now attributed as `'ishares_source'` (benign-by-design but in the audit). (e) Orchestrator adapter at `quarterly-refresh-orchestrator.ts:218` destructures `enriched` only — mechanical adapter, zero behavior change; persistence symmetry deferred as DW-088. Tests: 12/12 `polygon-enrichment-fetcher_test.ts` passes including new mixed-batch (1 ok + 1 404 + 1 iShares → enriched=1, skipped=2 with correct reasons); 26/26 `quarterly-refresh-orchestrator_test.ts` passes (mock shapes updated, no assertion changes); source-level sentinel pin in `enrich-and-filter/index_test.ts` asserts catch-block `reason: 'fetch_error'` + `enrichment_skip_counts: enrichmentSkipCounts` persistence + tracked-zero initialisation. |
| **Disposition** | Resolved at FP-008.4 #23. **Out-of-scope follow-ups explicitly registered**: DW-088 (orchestrator-side `enrichment_skip_counts` persistence symmetry — the orchestrator now receives `result.skipped` but discards it; persistence symmetry with the bootstrap caller is a separate scope of the same character as DW-087's migration-tree-vs-live-DB symmetry); DW-089 (enrichment-skip sanity-threshold — a 50× spike in `not_in_polygon_404` or `fetch_error` is operationally a signal, not just forensics, but the threshold envelope needs operational data to set; deferred to a future enhancement). **Caller-side behavioral-test orphan**: the worker-pool catch-block lives inside `Deno.serve(createHandler(...))` and is not unit-testable as a pure function without standing up a full Deno.serve + supabase-admin + Polygon HTTP harness; this commit uses a source-level sentinel pin (precedent: FP-008.4 Commit 3.5 STREAK_FAILURE_OUTCOMES sentinel) as the ONE behavioral assertion the new field requires, and rolls the broader caller-behavioral-coverage gap into the same orphan-class as INC-30 / DW-082 (the Gate 11 type-check anchor's broader sibling). |
| **Cross-references** | FP-008.4 #23 closure SHA (pending); MIG-061 (`enrichment_skip_counts jsonb` column add); INC-24 (the per-ticker fetcher-error context that was previously `console.warn`-only); INC-36 (the epistemic-honesty NULL-means-untracked principle reused for pre-MIG-061 rows — same shape as the audit-evidence trust boundary on `longshort_audit_logs` pre-MIG-057); INC-45 (FP-008.4 #13 — the analogous Wikipedia per-row silent-skip attribution gap on the constituent-ingestion path; same defect class, same survey-recommended fix pattern, different persistence model since #13's bounds fail in-process); DW-082 (the broader edge-function-behavioral-test-orphan gap into which the caller's behavioral-coverage gap is rolled); DW-087 (the precedent for the DW-088 follow-up's scope-boundary discipline); DW-088 (orchestrator-side persistence symmetry); DW-089 (enrichment-skip sanity threshold). |
| **Status** | Resolved at FP-008.4 #23 SHA (pending CI green; 38/38 Deno tests passing locally; live-DB §22.5.1 verification post-MIG-061 apply: column shape + zero pre-MIG-061 backfill confirmed). |

### INC-49: GICS sector plumbing was missing through the entire Phase 1 universe-ingestion chain — Wikipedia + iShares parsers discarded the sector column, `UniverseConstituent` had no `gics_sector` field, `universe_membership` had no sector column; surfaced at FP-009 Phase 2.1 pre-investigation survey as the only architectural blocker for the 8-element template; closed at FP-009 Bucket 0 / MIG-063

| Field | Value |
|-------|-------|
| **ID** | INC-49 |
| **Date Surfaced** | 2026-06-05 (FP-009 Phase 2.1 pre-investigation survey). |
| **Classification** | Phase-1-residual-surfaced-by-Phase-2.1 — same shape as FP-008.4 Bucket A was Phase-0B-residual-surfaced-by-Phase-1 (the pattern is: a downstream phase's survey is the first natural test of whether the upstream phase's data plumbing is complete). Specifically: the Wikipedia constituent fetcher's parser located the `Symbol` column by header lookup and emitted ticker only, never reading the `GICS Sector` column that sits in the same table row; the iShares CSV parser located `Ticker` / `Name` / `Asset Class` but ignored `Sector`; the Polygon constituent fetcher does not carry GICS in its reference taxonomy (SIC only); `UniverseConstituent` and `EnrichedConstituent` had no `gics_sector` field; `universe_membership` had no `gics_sector` column. Phase 2.1's within-sector z-score normalization (template element 2, shared across all 9 signal sub-phases) requires sector per universe member — the absence is a hard blocker that would have surfaced mid-implementation as an architectural back-out had the pre-investigation survey not run. Survey-first paid off here: caught at zero cost; absorbed into Phase 2.1 as a prequel bucket (Option A operator-approved) rather than back-out or separate FP. |
| **Surface Story** | Pre-investigation survey for Phase 2.1 (Signal #6, Cross-sectional momentum) needed to confirm the 8-element signal-stack template's shared infrastructure dependencies. Template element 2 (within-sector GICS z-score normalization, per longshort.md §4.4.1) required `universe_member.gics_sector`. Source-code grep across `EnrichedConstituent` / `UniverseConstituent` / all four constituent fetchers / `universe_membership` schema returned zero hits for `gics_sector` or `sector` — the column was parsed-and-discarded at the source layer, never reached the type layer, never reached the persistence layer. Operator decisions: (Q1) Polygon Stocks Advanced tier confirmed; (Q2) backfill policy locked to NULL-forward (no backfill) per MIG-061 / INC-36 epistemic-honesty precedent — historical universe rows are forensic-not-financial; GICS reclassifications happen (2018 Communication Services creation is the canonical case) and a current sector stamped onto a 2017 `as_of_date` is a quiet lie of the MIG-061 class; no Phase 2.1+ consumer needs historical sector; if a future need emerges it gets its own DW + deliberate point-in-time backfill migration; (Q3) FP-009 bucket structure approved as Bucket 0 (this prequel) → A (shared infrastructure) → B (signal-specific) → C (production wiring + observational gate) → D (template-lock closure doc — meta-deliverable per A2). |
| **Observation** | Resolved at FP-009 Bucket 0 / MIG-063: (a) **Wikipedia parser** extended to surface `gics_sector` from the `GICS Sector` header (defense-in-depth: missing header throws `ConstituentFetchError` with `layout likely changed` diagnostic, same shape as the Symbol-column structural throw); per-row empty cell yields `null` (typed-absence per §2 axiom 3). Alternate header `Sector` (older S&P 400 revisions) accepted. (b) **iShares parser** extended to surface `Sector` column verbatim — treated as GICS-equivalent for v1 (taxonomic cross-check between Wikipedia GICS and iShares Sector is a future enhancement, DW-able if and when needed); empty/sentinel cells (`""`, `-`, `--`) → `null`. (c) **Polygon constituent fetcher** emits `gics_sector: null` uniformly (Polygon reference data does not carry GICS — honest typed-absence, NOT a silent default that would mislead z-score consumers downstream). (d) **Seeded-membership fetcher** emits `gics_sector: null` (rehydrator path; reads only ticker from `universe_membership`). (e) **Type contract**: `UniverseConstituent.gics_sector: string \| null` added once at the base; propagates through `EnrichedConstituent` (spread `...c` in the polygon-enrichment-fetcher) and `EligibleConstituent` (extends `EnrichedConstituent`) automatically — no shadow type definitions found. (f) **Persister thread**: single-site change at `quarterly-refresh-orchestrator.ts:249-254` (one new `gics_sector: e.gics_sector` line on the rows-map); `UniverseMembershipPersisterInput.rows[].gics_sector` typed; persister UPSERT includes the column (last-writer-wins, same convention as `refresh_id`). (g) **MIG-063**: additive nullable `text` column, no default, no backfill UPDATE, no CHECK constraint (free-text per source); mirrors MIG-061 verbatim. (h) **Tests**: 4 new Wikipedia (positive verbatim propagation, missing-`GICS Sector`-header structural throw, empty-cell typed-absence, mixed-batch verbatim propagation, "Sector"-alone alternate header), 2 new iShares (sentinel/empty cells → null; CSV without Sector column → uniform null), 1 polygon-constituent assertion (uniform null); all touched test files pass. |
| **Disposition** | Resolved at FP-009 Bucket 0 SHA (pending CI green; live-DB §22.5.1 verification post-MIG-063 apply: column shape + zero pre-MIG-063 backfill confirmed). **Forward-binding observation (not a deferred-work entry)**: the quarterly-refresh orchestrator currently uses Polygon as primary (line 184-191); Polygon emits `gics_sector: null`, so `universe_membership.gics_sector` rows persisted by the quarterly orchestrator will remain NULL in production until a future bucket (likely FP-009 Bucket A's z-score-companion or a separate Phase 2.1 mini-bucket) wires Wikipedia as a sector enrichment step on the primary path. Bucket 0 lands the type + column + persister contract — the data-source-wiring layer is explicitly out of Bucket 0 scope. This is honest plumbing-before-source, NOT a defect: the column shape is the gate that lets Bucket A's z-score module compile against the real contract (z-score tests use synthetic fixtures with sector populated; production population follows when the source-wiring lands). **Anti-pattern guard observations**: backfill explicitly rejected (Q2); CHECK constraint explicitly rejected (taxonomy free-text per source); iShares-vs-Wikipedia sector taxonomic cross-check explicitly out of scope for v1 (future enhancement). |
| **Cross-references** | FP-009 Bucket 0 closure SHA (pending); MIG-063 (`gics_sector text` column add); FP-009 Phase 2.1 pre-investigation survey (the surfacing event); INC-36 (the epistemic-honesty NULL-means-untracked principle reused for pre-MIG-063 rows); MIG-061 (the precedent column-pattern this migration mirrors verbatim — there `jsonb` nullable, here `text` nullable); INC-45 (FP-008.4 #13 — the analogous Wikipedia per-row silent-skip attribution gap on the constituent-ingestion path; same parser, different defect class); `supabase/functions/_shared/longshort-universe-interfaces.ts`; `supabase/functions/_shared/longshort-universe/constituent-ingestion/wikipedia-constituent-fetcher.ts`; `supabase/functions/_shared/longshort-universe/constituent-ingestion/ishares-constituent-fetcher.ts`; `supabase/functions/_shared/longshort-universe/constituent-ingestion/polygon-constituent-fetcher.ts`; `supabase/functions/_shared/longshort-universe/constituent-ingestion/seeded-membership-fetcher.ts`; `supabase/functions/_shared/longshort-universe/refresh-jobs/universe-membership-persister.ts`; `supabase/functions/_shared/longshort-universe/refresh-jobs/quarterly-refresh-orchestrator.ts`; `supabase/functions/_shared/longshort-universe/verify-membership/types.ts` (`UniverseMembershipPersisterInput` row shape). |
| **Status** | Resolved at FP-009 Bucket 0 SHA (pending CI green; live-DB §22.5.1 verification post-MIG-063 apply: column shape + zero pre-MIG-063 backfill confirmed). |
