

### FP-041: Signal #5 — Short Interest Changes (30-day)

### FP-045: Generalized Cursor-Drain Queue-Worker (DEC-047) for Rate-Capped Signals — PEAD Re-Shell + Signal #3 Revival

| Field | Value |
|---|---|
| **ID** | FP-045 (next-free after FP-044; grep-verified at HEAD — only prior mention was FP-044's "Out of Scope" forward-reference, no allocation collision). |
| **Status** | Phase 0 (governance intake — this entry + INC-72 + `ai-failure-modes.md` entries) drafted; Phases 1–4 staged per the operator-authorized phased prompt with a supervisor STOP between phases. |
| **Problem** | INC-72 surfaced the second instance of the rate-bound single-invocation synchronous fan-out anti-pattern: FP-044's `longshort-pead-compute` 504'd at the 150s HTTP wall on its first operator test-fire and its isolate was terminated at ~190s with **zero `signal_compute_log` rows, zero `signal_observations` rows, and zero application log lines** (execution `11a267bc-ee77-4d54-a0ac-1571749f98cb`, 2026-06-09 20:21 UTC). The PEAD orchestrator ships with a vendor-cap arithmetic defect in its header (`pLimitedMap` concurrency-5 claimed "≤10 req/sec... well under the cap" — 10 req/sec = 600/min vs Finnhub's 300/min ceiling; concurrency-based throttling is latency-dependent and unbounded). At the correctly paced 5 rps cap, 1,678 Finnhub calls take 335s (~395s at the operator-measured 4.25 rps effective) — exceeds the 150s HTTP wall outright, approaches/matches the 400s Pro background-worker budget. Same class as FP-043's pre-token-bucket Tradier path (caught pre-production); promoted now to a structural class by this second firing. The DEC-047 cursor-drain queue-worker is the operator-approved resolution shape; this FP implements it once, generalized for both known rate-capped consumers (PEAD on Finnhub 300/min + options-flow on Tradier 120/min — the latter un-parks Signal #3 per FP-043's park clause and closes DW-095). |
| **Resolution shape (per-phase)** | **Phase 0 (this PR scope):** INC-72 record; `ai-failure-modes.md` §12.10 entries (supervisor-side: Phase-2 fresh-clone verification checked purity/enums/guards but not vendor-cap arithmetic, detection one step late at empirical 504; executor-side: rate-comment asserting 600/min "well under" a 300/min cap, gates structurally cannot catch vendor-cap math); FP-045 entry; FP-044 Status forward-fix to `complete-pending-reshell`. Docs only; gate 4; commit; STOP. **Phase 1 — schema:** MIG-082 introduces `signal_queue_runs`/`signal_queue_cursor`/`signal_queue_staging`/`signal_queue_skips` (generalized per `signal_id` — one table set, NOT per-signal tables); RLS per established deny-write/service-role pattern; idempotent; staging TTL handled by the orphan-sweeper rather than a separate cron; §22.5.1 live-DB read. STOP. **Phase 2 — generic engine:** `_shared/longshort-signals/shared/queue-worker/{queue-config,slice-worker,finalizer,orphan-sweeper}.ts` + edge handlers `longshort-queue-init` / `longshort-queue-slice` (serialized across signals so vendor caps never stack) / `longshort-queue-sweeper`. REUSES `token-bucket.ts` (productionClock-routed), `pLimitedMap`, `signal-types.ts`, `persist-signal-compute-log.ts`. No cadence constants (DEC-048). Per-slice audit event = INC-72 instrumentation fix. Pre-flight arithmetic row (calls/slice × rate vs 150s wall) recorded in PR evidence as the new executor discipline. Gates 2+4+11. STOP. **Phase 3 — PEAD consumer (re-shell):** queue-config registration consuming FP-044's `FinnhubEpsEstimateFetcher` + `FinnhubEarningsFetcher` + `computePead` UNTOUCHED. Synchronous-orchestrator path retired from cron entry; FP-044 orchestrator rate-comment defect corrected in same commit (file retained for the per-ticker arm). MIG-083 job_registry rows (DISARMED; DEC-048 interim language; slot non-overlapping). `JOB_ID_TO_SIGNAL_ID` entry + drift sentinels. `function-index.md` + `event-index.md` + `pead.md` addendum + queue-worker module doc (DEC-047 lineage) SAME-PR. §22.5.1. Gates + pre-flight arithmetic. STOP — then operator test-fire of the PEAD queue path validates before Phase 4. **Phase 4 — Signal #3 revival:** queue-config registration consuming FP-043's `TradierOptionsChainFetcher` + `computeOptionsFlow` + per-ticker arm UNTOUCHED. `signal_registry` `options_flow` planned→live (truth-in-telemetry precedent); DW-095 closed (evidence: this FP); DEC-047 status → implemented; FP-043 closure addendum. MIG-084 + §22.5.1. Operator test-fire validates. Closes the rate-capped class. |
| **Scope (Phase 0 — this PR)** | Doc edits only: this FP-045 entry; INC-72 (incidental-findings.md); two §12.10 operational-log entries in `ai-failure-modes.md` (supervisor-side blind-spot + executor-side rate-math defect); FP-044 Status field forward-fix per §22.3(e) to `complete-pending-reshell (orchestration shell superseded by FP-045; compute/fetchers/handlers reused)`. No code; no migration; no reference-index registrations (those land in Phase 2/3 same-PR as the engine + consumer registrations per Rule 6). |
| **Out of Scope** | NO changes to `compute-pead.ts`, `compute-options-flow.ts`, any fetcher logic, `z-score-normalize`, `persist-signal-compute-log` row shape, or `SignalSkipReason` enum (the queue engine maps existing skips, does not invent new ones). NO cron ENABLE on the queue handlers (operator test-fire-then-restore per DEC-040 / DEC-043). NO Signal #1 (`analyst-revisions`) or Signal #8 (`news`) work — those are separate FPs against the same engine once landed. NO `waitUntil` reliance — the queue-worker's design intent is that every invocation completes well inside the 150s wall, removing background-execution variance from the attestation path. NO new RBAC permission (queue-worker reuses `longshort.manage` + `longshort.view`). NO new env-var. |
| **Reference Impact (Phase 0 — this PR)** | None — Phase 0 is docs-only and does not cross module boundaries beyond the governance documents themselves. **(Forward-binding for Phase 2/3/4:** `function-index.md` will gain entries for the queue-worker engine (`queue-config`, `slice-worker`, `finalizer`, `orphan-sweeper`) + 3 edge handlers (`longshort-queue-init` / `longshort-queue-slice` / `longshort-queue-sweeper`); `event-index.md` will gain a per-slice audit event family + per-run lifecycle events; `database-migration-ledger.md` will gain MIG-082 (schema) / MIG-083 (PEAD queue rows) / MIG-084 (options-flow queue rows); `pead.md` will gain a re-shell addendum; `options-flow.md` will gain a revival addendum; a NEW module doc `docs/04-modules/longshort/signals/queue-worker.md` will land in Phase 2 documenting DEC-047 lineage + the per-signal `queue-config` contract.) |
| **Decision ID** | DEC-047 (the cursor-drain queue-worker decision — this FP is its implementation vehicle; status will flip planned→implemented at Phase 4 close). DEC-054 reserved for any generalization decision that diverges from DEC-047 verbatim during implementation (not anticipated; allocation held only for clean addressability if needed). |
| **Cross-references** | INC-72 (the silent-death incident that triggered this FP); FP-044 (the originating synchronous-orchestrator shell being superseded; Status updated this PR); FP-043 (the first instance of the rate-bound fan-out class + the source of the reused `token-bucket.ts` pacer + the parked Signal #3 revived by Phase 4); DEC-047 (the queue-worker architectural decision); DEC-048 (cadence governance — schedules live in `job_registry` only, no cadence constants in code); DEC-053 (split-vendor lock — unchanged); DW-095 (the Signal-#3 deferred-work entry closed by Phase 4); `ai-failure-modes.md` Catalog (potential new #39 entry promotion if Phase 2 reviews surface a third class instance — held for quarterly review). |

### FP-044: Signal #2 — Post-Earnings Announcement Drift (PEAD, 20d half-life) on Finnhub Estimate-1

| Field | Value |
|---|---|
| **ID** | FP-044 (next-free after FP-043; grep-verified at HEAD). |
| **Status** | complete-pending-reshell (orchestration shell superseded by FP-045; compute/fetchers/handlers reused). All three phases landed (Phase 1 ACT-161 fetcher pair; Phase 2 compute + synchronous orchestrator; Phase 3 cron/manual handlers + MIG-081 disarmed `job_registry` row + `signal_registry` planned→live flip + module doc + reference-index registrations). 2026-06-09 20:21 UTC operator test-fire surfaced INC-72: the synchronous-orchestrator cron path 504'd at the 150s wall and the isolate terminated at ~190s with zero persisted rows and zero log lines. Root cause is a vendor-cap × call-count × wall-budget architectural mismatch (`pead-orchestrator.ts` ships with header rate-math claiming 600/min is "well under" Finnhub's 300/min cap), not a code-correctness defect in the compute or fetcher layers. FP-045 generalizes DEC-047's cursor-drain queue-worker for this and the FP-043 sibling class; PEAD's `FinnhubEpsEstimateFetcher` + `FinnhubEarningsFetcher` + `computePead` + z-score path are REUSED UNTOUCHED behind the new orchestration shell. Forward-fix per §22.3(e) — no green-claim revision; the gates and §22.5.1 evidence landed against the orchestrator-of-record at the time. |
| **Problem** | Phase 2.6 deliverable: Signal #2 (post-earnings announcement drift, 20-day decay) per CROSSWIND §4.4.6. The signal requires a per-quarter analyst-consensus row carrying `epsAvg`, `epsHigh`, `epsLow`, `numberAnalysts` for the JUST-REPORTED quarter — needed to compute SUE = `(actual − consensus) / σ_proxy` with the DEC-051 range-proxy denominator and the DEC-052 N≥2 floor. The pre-build vendor audit (ACT-160 / chat-labeled "ACT-164") confirmed FMP `/stable/analyst-estimates?period=quarter` returns ONLY future-quarter rows — every already-reported quarter is gone from the endpoint by the time SUE is computed, so DEC-051 and DEC-052 cannot be satisfied on FMP alone. The earlier DEC-049 single-vendor framing is therefore superseded by DEC-053 (split-vendor: FMP for Signal #1, Finnhub for Signal #2). |
| **Resolution (Phase 1 ONLY in this PR)** | (a) NEW `FinnhubEpsEstimateFetcher` (`_shared/longshort-signals/shared/finnhub-eps-estimate-fetcher.ts`) wrapping `https://finnhub.io/api/v1/stock/eps-estimate?freq=quarterly`. Entitlement-aware (401/403 → `subscription_gated`, 404/empty → `data_unavailable`). Anti-phantom: drops any row missing `epsAvg` / `epsHigh` / `epsLow` / `numberAnalysts`; drops `numberAnalysts<=0` (vendor regression vs zero-coverage 404 distinction). Returns `{period, epsAvg, epsHigh, epsLow, numberAnalysts}` ASC-sorted; consumer filters as_of-relative downstream (no client-side filter in the fetcher — preserves replay-safety). Embedded `verifyFilterHonored` + `verifyFieldsPresent` static helpers. (b) NEW `FinnhubEarningsFetcher` (`_shared/longshort-signals/shared/finnhub-earnings-fetcher.ts`) wrapping `https://finnhub.io/api/v1/stock/earnings` — BARE ARRAY response with defensive type-guard (not a silent cast — a future wrapped-shape vendor regression would otherwise silently empty results). Returns `{period, actual, estimate}` with `actual` typed-null for future quarters. Same status mapping + dual-axis helpers. (c) Pacing decision (recorded in fetcher header): Finnhub 300/min ≫ 839 names; `pLimitedMap` bounded-concurrency only. NO token-bucket — deliberate contrast with FP-043's Tradier path where 120/min was the binding constraint. (d) Dual-axis verify + LOOK-AHEAD GATE re-run live with the actual fetcher classes — operational-window verdict 0/13 drifted → CLEAN; deep-history >4yr drift (13/392) recorded as observability finding, OUTSIDE the §4.4.6 stale-earnings cutoff so it cannot reach production SUE compute. |
| **Resolution (Phase 2 — staged; NOT in this PR)** | NEW pure `compute-pead.ts` (SUE × `exp(−trading_days_since_earnings / 20)`; typed skips `stale_earnings` / `insufficient_analysts` / `zero_dispersion` / `no_earnings_data` / `fetch_error`; reuses `shared/trading-days.ts` for the trading-day calendar). NEW `pead-orchestrator.ts` (universe load → 2-fetcher parallel per-name → per-name compute → within-sector GICS z-score (±3 clip) → persist via `captureSignalObservations` + `persistSignalComputeLog`). Same-commit widen `SignalSkipReason` + update `persist-signal-compute-log_test.ts` exact-match assertions (FP-041 stale-test lesson). signal_id locked: `'pead_sue_20d'`. |
| **Resolution (Phase 3 — staged; NOT in this PR)** | NEW `longshort-pead-compute/index.ts` (cron handler; `verifyCronSecret` + audit envelope) + NEW `longshort-pead-compute-manual/index.ts` (POST + JWT + `longshort.manage` + `parseAsOfDate` + future-date guard). NEW MIG-081 — `job_registry` row `longshort.pead.compute` DISARMED (interim daily-EOD schedule slotted to avoid sibling overlap on UTC 19/20/21/22) + `signal_registry` planned→live flip for the #2 row (cadence string per DEC-048 / MIG-079 truth-in-telemetry precedent; `stale_after_hours` per the §4.4.6 60-trading-day window). NEW `docs/04-modules/longshort/signals/pead.md` (formula verbatim; DEC-051 σ_proxy + `k` statistical basis; DEC-052 floor + small-cap consequence; DEC-053 point-in-time verification finding + the T-0-vs-T-5 conscious approximation; cadence-as-config note). `JOB_ID_TO_SIGNAL_ID` entry + drift sentinel. `function-index.md` + `event-index.md` updates SAME-PR (Rule 6 / defect-#31 discipline). |
| **Scope (Phase 1 — this PR)** | NEW `_shared/longshort-signals/shared/finnhub-eps-estimate-fetcher.ts` (+ `_test.ts` 14 tests). NEW `_shared/longshort-signals/shared/finnhub-earnings-fetcher.ts` (+ `_test.ts` 12 tests). Docs same-PR: this entry; ACT-160 (reconciliation probe outcome — chat-labeled "ACT-164"); ACT-161 (Phase 1 build); DEC-053 (split-vendor lock) + DEC-049 status flipped to `superseded`. No migration (Phase 3). No reference-index registrations (Phase 3 — fetchers do not yet cross module boundaries because the Phase 2 orchestrator that consumes them is not in this PR). No `SignalSkipReason` widening (Phase 2). |
| **Out of Scope** | FMP-side code for Signal #2 (DEC-053 binds the vendor to Finnhub). Phase 2 compute + orchestrator. Phase 3 handlers + MIG-081 + module doc + reference indexes. Cron wiring + enable-flip + DEC-043 attestation (separate operator-run step). Signal #1 (FP-045 future). Signal #8 (DW-095 gate pending). No new RBAC permission (reuses `longshort.manage` + `longshort.view`). No new env-var (`FINNHUB_API_KEY` already registered). |
| **Reference Impact (Phase 1 — this PR)** | None — the new fetchers do not yet cross module boundaries. The reference-index registrations (`function-index.md` entries for both fetchers; `_pattern-vendor-fetcher-filter-honesty.md` Finnhub paths added) land in Phase 3 SAME-PR as the orchestrator that consumes them per Rule 6. **(Forward-binding for Phase 3:** `function-index.md` will gain 5 new entries — both fetchers + compute + orchestrator + (cron + manual) handlers; `event-index.md` will gain 6 audit events — `longshort.pead.compute.{started,completed,failed,manual_triggered,manual_completed,manual_failed}`; `database-migration-ledger.md` will gain MIG-081; `_pattern-vendor-fetcher-filter-honesty.md` will gain Finnhub `/stock/eps-estimate` + `/stock/earnings` to the registered-fetcher pre-flight list.) |
| **Gate evidence (Phase 1 close)** | Gate 2 (wall-clock scanner): CLEAN — 0 wall-clock reads in either fetcher. Gate 4 (ESLint): exit 0, 0 errors / 0 warnings on all four Phase-1 files. Gate 11 (Deno tests): 26/26 passed (14 eps-estimate + 12 earnings; 182ms total). Live LOOK-AHEAD probe: operational-window verdict 0/13 drifted → CLEAN. Dual-axis verify (live): filter HONORED on both endpoints (`symbol=ZZZZZZZZ` → 0 rows); fields POPULATED on AAPL (eps-estimate 41/41 rows with all four numeric fields + n_ge_2; earnings 40/40 with actual + estimate). |
| **Decision ID** | DEC-053 (split-vendor lock; supersedes DEC-049). Preserves DEC-051 + DEC-052 verbatim (the vendor moves; the formula and floor do not). |

### FP-043: Signal #3 — Options Flow Imbalance (5-day, smart-money filtered)

| Field | Value |
|---|---|
| **ID** | FP-043 (next-free after FP-042; grep-verified at HEAD). |
| **Status** | implemented (Phase 1 Tradier fetcher with dual-axis self-checks; Phase 2 compute + orchestrator; Phase 3 chunked coordinator/worker + token-bucket pacer + cron + manual handlers; Phase 4 MIG-078 disarmed `job_registry` seed + `signal_registry` planned→live flip + full doc set). Cron wiring + enable-flip pending separate operator-run DEC-043 attestation. |
| **Problem** | Phase 2.7 deliverable: Signal #3 (options flow imbalance, 5-day, smart-money filtered) per CROSSWIND §4.4.7 is the highest-cost signal in the stack ("~30-50% of daily compute budget"). It introduces a new vendor (Tradier production), the first chunked coordinator/worker architecture under a strict 120 req/min rate cap, and the first signal subject to a dual-axis fetcher discipline (filter-honored + fields-present). |
| **Resolution** | (a) NEW production Tradier fetcher (`tradier-options-chain-fetcher.ts`) wired against `api.tradier.com/v1`, entitlement-aware (401/403 → `subscription_gated`, 404/empty → `data_unavailable`), with embedded `verifyFilterHonored()` + `verifyFieldsPresent()` self-checks (the latter is INC-71's lesson). Tradier-normalisation helper handles the single-result-as-bare-object quirk. (b) NEW pure compute (`compute-options-flow.ts`) — 4-case direction classifier (call-buy-at-ask=+1, put-buy-at-ask=−1, call-sell-at-bid=−1, put-sell-at-bid=+1; sign error inverts the signal — all 4 cases + boundary equalities pinned by tests); smart-money filter (`volume>=100`, `DTE>=7`, `|delta|<=0.65`); 48-hour-half-life exponential decay keyed off `as_of` (no wall-clock); `MIN_QUALIFYING_PRINTS=5` floor; div-by-zero guard. (c) NEW orchestrator + chunk-runner + coordinator: shards the universe into N=6 strides, fans out HTTPS to N parallel workers, each worker paced by a shared token bucket at ~0.28 req/sec (≈ 100 req/min total = 6 × 0.28; ~15% headroom under the 120/min cap; absorbs 429 retries via the fetcher's [1s,2s,4s] backoff). Partial-failure honesty: a failed worker emits `fetch_error` skips for every ticker in its shard rather than silently dropping data (the Signal #4 lesson). (d) NEW `longshort-options-flow-worker` (CRON_SECRET, returns per-shard signals+skips); `longshort-options-flow-compute` (cron coordinator); `longshort-options-flow-compute-manual` (POST + JWT + `longshort.manage`, with `as_of` parsing + future-date guard). (e) Extended `SignalSkipReason` with `no_qualifying_flow` + same-commit `aggregateSkipCounts` seeding + `persist-signal-compute-log_test.ts` exact-match assertion update (FP-041 stale-test lesson applied). (f) Extended `JOB_ID_TO_SIGNAL_ID` + drift sentinel (cross-references `options-flow-orchestrator.ts::SIGNAL_ID`). (g) MIG-078 disarmed `job_registry` seed (schedule `'0 22 * * 1-5'`) + `signal_registry` planned→live flip (status='live', job_registry_id, stale_after_hours=72). signal_id locked: `'options_flow_imbalance_5d'`. |
| **Scope** | NEW `_shared/longshort-signals/shared/tradier-options-chain-fetcher.ts` (+ `_test.ts` 27 tests). NEW `_shared/longshort-signals/options-flow/{compute-options-flow,options-flow-orchestrator,options-flow-chunk-runner,options-flow-coordinator,token-bucket}.ts` (+ tests; 40+ total). NEW `supabase/functions/longshort-options-flow-{worker,compute,compute-manual}/{index.ts,index_test.ts}`. NEW MIG-078 (`job_registry` INSERT disarmed + `signal_registry` UPDATE planned→live). EDIT `_shared/longshort-signals/shared/signal-types.ts` (+`no_qualifying_flow`). EDIT `_shared/persist-signal-compute-log.ts` (+1 seeded skip_counts key) + `_test.ts` (exact-match assertion update). EDIT `_shared/longshort-signals/shared/job-signal-mapping.ts` + `_test.ts` (+1 entry + drift sentinel + sorted-keys assertion update). Docs same-PR: this entry; ACT-157; DEC-045 (Tradier vendor lock); DEC-046 (v1 chain-snapshot approximation; v2 timesales-true deferred); INC-71 (Polygon Options Developer NBBO-absence); `_pattern-vendor-fetcher-filter-honesty.md` (FP-043 extension — `verifyFieldsPresent()` second axis, binding on Signals #1/#2/#8); NEW `docs/04-modules/longshort/signals/options-flow.md`; `database-migration-ledger.md` MIG-078; `function-index.md` (new entries); `event-index.md` (new audit events). |
| **Out of Scope** | Full per-trade 5-day timesales reconstruction (DEC-046 v2 deferred). Polygon Options Developer (disqualified per INC-71). Combiner-stage missingness imputation (Phase 3). Cron wiring / enable-flip / DEC-043 attestation (separate operator step). No new RBAC permission (reuses `longshort.manage` + `longshort.view`). No new env-var (`TRADIER_API_KEY` already registered). |
| **Reference Impact** | database-migration-ledger.md: MIG-078. function-index.md: new entries (Tradier fetcher, compute, orchestrator, chunk-runner, coordinator, token-bucket, 3 edge handlers) + `JOB_ID_TO_SIGNAL_ID` extension. event-index.md: new audit events (`longshort.options_flow.compute.{started,completed,failed,manual_triggered,manual_completed,manual_failed}`). docs/04-modules/longshort/signals/options-flow.md: new. `_pattern-vendor-fetcher-filter-honesty.md`: FP-043 extension section. No new env-vars / configs / npm deps / permissions / routes. |
| **Operational-timing precedent** | The token-bucket pacer must measure real elapsed time. Adopted pattern (reusable, no new ADR/DEC): default the bucket's clock to `productionClock.getWallClockTs().getTime()` from `_shared/longshort-clock.ts` — the SOLE sanctioned wall-clock chokepoint per DEC-034 (4). Token-bucket file therefore contains zero direct wall-clock reads and passes `check-wall-clock.ts` without an `// allow-now-in-business-logic:` override. Future operational pacers (chunked coordinators for Signals #1/#2/#8) inherit this pattern. |
| **Gate evidence (Phase 4 close)** | Gate 2 (wall-clock scanner): CLEAN — 0 violations. Gate 4 (ESLint): 0 errors on changed files. Gate 11 (Deno): all FP-043 tests pass (fetcher 27, compute 24, orchestrator 6, chunk-runner + coordinator + token-bucket + 3 edge handlers + mapping). Live-DB §22.5.1 evidence: `job_registry.longshort.options_flow.compute` row applied (`enabled=false`, `schedule='0 22 * * 1-5'`, `handler_path='supabase/functions/longshort-options-flow-compute/index.ts'`); `signal_registry.options_flow_imbalance_5d` row flipped (`status='live'`, `job_registry_id='longshort.options_flow.compute'`, `stale_after_hours=72`, `planned_phase=NULL`). |

### FP-042: Signal #4 — Insider Transactions (90-day, 14-day decay)

| Field | Value |
|---|---|
| **ID** | FP-042 (next-free after FP-041; grep-verified at HEAD). |
| **Status** | implemented (compute + officer-title classifier + entitlement-aware Form 4 fetcher + orchestrator + cron/manual handlers + MIG-077 disarmed `job_registry` seed + `signal_registry` planned→live flip + Gate-11 Deno tests). Cron wiring + enable-flip pending separate operator-run DEC-043 attestation. |
| **Problem** | Phase 2.4 deliverable: Signal #4 (insider transactions, 90-day, 14-day decay) per CROSSWIND §4.4.4 is the **fourth of nine** signals. It introduces a new external endpoint (Polygon `/stocks/filings/vX/form-4`), the **first weighted-decay aggregator** in the signal stack, and the **first role-classification surface** (officer_title heuristic). The signal is non-critical and SPARSE-by-design — most names have no qualifying insider trades in any given 90-day window, which is the normal state and must NOT degrade the run. |
| **Resolution** | Mirror the proven Signal #5 architecture (FP-041) with the §4.4.4-driven additions: (a) NEW entitlement-aware `PolygonForm4Fetcher` — HTTP 403 → typed `subscription_gated`, 404 → `data_unavailable`, neither throws / neither fabricates; returns BOTH `record_type='transaction'` and `record_type='holding'` rows (compute filters). (b) NEW pure compute module `compute-insider.ts` exporting `filterQualifyingTransactions` (P all; S only where `aff_10b5_one===false`; M/C/A/G excluded; first gate drops holding-only rows), `classifyRoleWeight` (3-tier title-heuristic per DEC-044 — C-suite/President 1.0, NEO-proxy via "Chief X Officer"/EVP/SVP 0.7, 10%+ owner 0.5, generic Section-16 officer 0.4, independent director 0.3; highest-applicable-weight tie-break; `(?<!vice\s)\bpresident\b` guard prevents "Executive Vice President" leaking into tier 1), and `computeInsiderSignal` (Σ shares × price × sign × role_weight × exp(-age/14) / market_cap; `null` on zero qualifying transactions OR non-positive market_cap; replay-deterministic via injected `as_of`). (c) NEW `createInsiderOrchestrator` with THREE parallel side-input fetchers per ticker (form-4 + shares-outstanding + price for market_cap), translating each entitlement-aware unavailability into the matching `SignalSkipReason`; surfaces a new `no_qualifying_transactions` skip when the filter empties the row set (the EXPECTED case for most names). (d) Cron handler `longshort-insider-compute` (verifyCronSecret + productionClock + 3-fetcher context + 3 audit events). (e) Manual sibling `longshort-insider-compute-manual` (POST + JWT + `longshort.manage` + dual audit envelope). (f) MIG-077 disarmed `job_registry` seed with daily-after-close schedule `'0 19 * * 1-5'` + `signal_registry` planned→live flip (status='live', job_registry_id, cadence note, stale_after_hours=48). (g) Extended `SignalSkipReason` with `no_qualifying_transactions` + same-commit `aggregateSkipCounts` seeding + `persist-signal-compute-log_test.ts` exact-match assertion update (FP-041 stale-test lesson applied). (h) Extended `JOB_ID_TO_SIGNAL_ID` + drift sentinel. signal_id locked: `'insider_transactions_90d'`. |
| **Scope** | NEW `_shared/longshort-signals/shared/polygon-form4-fetcher.ts` (+ `_test.ts` 10 tests). NEW `_shared/longshort-signals/insider-transactions/compute-insider.ts` (+ `_test.ts` 26 tests — classifier table + filter + sign/decay/role/market_cap/zero-cap/future-date/determinism). NEW `_shared/longshort-signals/insider-transactions/insider-orchestrator.ts` (+ `_test.ts` 12 tests). NEW `supabase/functions/longshort-insider-compute/{index.ts,index_test.ts}`. NEW `supabase/functions/longshort-insider-compute-manual/{index.ts,index_test.ts}`. NEW MIG-077 (`job_registry` INSERT disarmed + `signal_registry` UPDATE planned→live; applied via `supabase--insert` per FP-041 precedent for data-only changes). EDIT `_shared/longshort-signals/shared/signal-types.ts` (+1 enum member). EDIT `_shared/persist-signal-compute-log.ts` (+1 seeded skip_counts key). EDIT `_shared/persist-signal-compute-log_test.ts` (3 hardcoded objects updated SAME-COMMIT). EDIT `_shared/longshort-signals/shared/job-signal-mapping.ts` + `_test.ts` (+1 entry + drift sentinel). Docs same-PR: this entry; ACT-154; DEC-044 (title-heuristic NEO proxy); DW-093 (DEF-14A deferred enrichment); `database-migration-ledger.md` MIG-077; `function-index.md` (5 new entries + mapping extension); `event-index.md` (6 new audit events); `docs/04-modules/longshort/signals/insider-transactions.md` (new component doc). |
| **Out of Scope** | DEF-14A authoritative NEO enrichment (DW-093, deferred). 30-min intraday polling cadence (spec §4.4.4 notes it; v1 is daily-after-close — noted as future). EDGAR backup fetcher (Polygon entitled; future hardening). Cron wiring / enable-flip / DEC-043 attestation (separate operator step). Touching momentum / reversal / short-interest / combiner / universe / Rankings. No new RBAC permission (reuses `longshort.manage` + `longshort.view`). |
| **Reference Impact** | database-migration-ledger.md: MIG-077. function-index.md: 5 new entries (fetcher, compute, orchestrator, cron handler, manual handler) + JOB_ID_TO_SIGNAL_ID extension. event-index.md: 6 new audit events (`longshort.insider.compute.{started,completed,failed,manual_triggered,manual_completed,manual_failed}`). docs/04-modules/longshort/signals/insider-transactions.md: new. No new env-vars (POLYGON_API_KEY already registered); no new configs; no new npm deps; no new permissions; no new routes. |
| **Decision ID** | DEC-044 (title-heuristic NEO proxy + 3-tier classifier; visible-approximation discipline via `role_tier_source='title_heuristic'` on every observation). |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Pre-build gate (operator-confirmed)** — Live probe of `/stocks/filings/vX/form-4` against the production POLYGON key returned 200 + the documented field set (`transaction_code`, `aff_10b5_one`, `transaction_shares`, `transaction_price_per_share`, `transaction_date`, `transaction_acquired_disposed`, `is_director`, `is_officer`, `is_ten_percent_owner`, `not_subject_to_section_16`, `officer_title`, `record_type`, `security_type`). Probe-confirmed compound title `"CEO AND PRESIDENT"` exists in the wild → drove the word-boundary regex + load-bearing classifier test. The endpoint is entitled on Stocks Advanced; no EDGAR backup needed for v1.

**Closure** — Landed at execution commit. Signal #4 is the fourth of nine live (in the registry sense — code/migration shipped, cron disarmed). 4 live + 5 planned + 1 planned composite. Validation path: operator POST `longshort-insider-compute-manual` with `{ "as_of": "YYYY-MM-DD" }` → expected SPARSE result (most names `no_qualifying_transactions` → `is_present=0` is NORMAL, not failure; a minority carries real weighted-decay z-scores). Cron wiring per DEC-043 is a separate operator step. Authority: ACT-154.

**Addendum (ACT-155, 2026-06-09) — Market-wide Form 4 fetch (CPU-limit fix; Constitution Rule 8 supersession via addendum).** First post-deploy fire died with HTTP 546 `WORKER_RESOURCE_LIMIT` (~2 s edge-isolate CPU budget exceeded ~6 s after boot — NOT memory, NOT wall-time). Root cause: per-ticker fetch across ~839 universe tickers = ~2517 HTTPS round-trips (Form 4 + shares + price ×839) + 839 JSON parses + per-row regex/decay loops — ~3–5× short-interest's CPU footprint. Fix: replace per-ticker Form 4 calls with ONE market-wide paginated call to `/stocks/filings/vX/form-4?transaction_date.gte=…&lte=…` (NO `ticker=`), follow `next_url` cursor (≤50 pages × 1000 rows), group locally by `tickers[0]` (primary-issuer attribution). Live-probe 2026-06-09 confirmed entitlement on the no-ticker variant. Orchestrator restructured to run the existing `filterQualifyingTransactions` per-universe-ticker BEFORE any per-ticker market-cap fetch — tickers with 0 qualifying rows skip `no_qualifying_transactions` without any shares/price HTTPS call. Compute / filter / classifier / z-score / persist BYTE-UNCHANGED. New `PolygonForm4Fetcher.fetchForm4MarketWide` + `Form4Row.tickers?: string[]` + `Form4MarketWideResult` type. The per-ticker `fetchForm4` is retained for the manual single-ticker debug path. +6 fetcher tests (single-page grouping, next_url pagination, 403/404 typed-missing, missing-tickers drop, multi-ticker tickers[0] attribution); 12 orchestrator tests preserved BYTE-IDENTICAL through the mock-shape change. ESLint 0 errors on the 4 changed files; no `any`; no `eslint-disable`; no `Date.now()`; div-by-zero guard preserved. Authority: ACT-155. Pattern becomes the template for the remaining feed-signals (#1 / #2 / #3 / #8) — any per-ticker-across-the-universe fetch would hit the same CPU wall.

**Second addendum (ACT-156, 2026-06-09) — Signal #4 disarmed; Polygon Form 4 endpoint silently ignores filters (INC-70); P2 double-tally fix; EDGAR rebuild deferred (DW-094). Constitution Rule 8 supersession via addendum; original entry + ACT-155 addendum preserved verbatim.**

After the ACT-155 market-wide rewrite, the next fire returned HTTP 200 but with the impossible telemetry `fetch_error: 839` AND `no_qualifying_transactions: 839` simultaneously (1678 skips for 839-ticker universe; `signal_compute_log.run_id='1021808b-4b1c-4b04-bb45-d989d56b5193'`). Investigation surfaced TWO distinct problems:

**P1 (INC-70 — data correctness, severity HIGH).** A 9-variant probe of `/stocks/filings/vX/form-4` against the deployed `POLYGON_API_KEY` returned **byte-identical 1.85 MB / 1000-row firehose responses** for: single-ticker, `ticker.any_of`, comma-list, `ticker.in`, pipe-delimited, repeated-param, market-wide 90-day, AND market-wide 14-day. `ticker=AAPL` returned rows NOT containing AAPL (top buckets: DELL=312, FCNCA=41, SSMR=38). Only `limit=` was honored. The endpoint silently ignores `ticker` (every syntax) AND `transaction_date.*` — Candidates A (multi-ticker), B (date-chunked), C (per-ticker) are ALL impossible at this entitlement tier. The original FP-042 per-ticker fetcher was therefore broken since deploy: it fetched the same firehose 839×, locally filtered by `tickers[0]===ticker` (would yield ~0 rows per name since our universe isn't dominant in the firehose), and the compute layer marked each ticker `no_qualifying_transactions` — perfectly indistinguishable from the EXPECTED sparse profile. **Phantom signal: looked sparse, was actually wrong.** The ACT-155 CPU failure was the lucky trigger that exposed the silent data bug.

**P2 (telemetry double-tally).** The ACT-155 catch block pushed 839 per-ticker `fetch_error` skips on a market-wide throw, then assigned `marketWide = { kind:'rows', rowsByTicker: new Map() }` WITHOUT returning, falling through into the qualifying-filter loop which pushed 839 MORE `no_qualifying_transactions` skips. Two skip classes per ticker, 1678 total — telemetrically impossible.

**This addendum's scope.** (a) **`insider-orchestrator.ts`** — the market-wide-throw catch block now returns `outcome='failed'` with `failure_reason='form4 market-wide fetch failed: <thrown message>'` and `skipped: []`, short-circuiting before the qualifying-filter loop. A run can never again show two skip classes for the same ticker. The `kind:'unavailable'` branch (typed entitlement state — 403/404 — neither a throw nor a fetcher error) is UNCHANGED. (b) **`signal_registry`** — `insider_transactions_90d.status` flipped `'live' → 'planned'` via data UPDATE (no new MIG); `job_registry.longshort.insider.compute.enabled` stays `false`. The non-critical signal rule (§6.5) means the combiner imputes Signal #4's absence and the long-short stack continues to rank on momentum / reversal / short-interest. (c) **FP-042 compute / classifier / filter / z-score code is UNTOUCHED** — it is correct and reused as-is when the EDGAR fetcher lands (DW-094). Only the data-acquisition layer needs replacing. (d) **Docs** — INC-70, DW-094, this addendum, ACT-156, `insider-transactions.md` ACT-156 addendum, NEW `_pattern-vendor-fetcher-filter-honesty.md`.

**Pattern lesson (codified, applies to remaining feed-signals).** Every vendor-endpoint fetcher MUST implement a `verifyFilterHonored()` pre-flight: probe with a filter value that SHOULD return zero rows (e.g. `ticker=ZZZZZZZZ`, far-future date) and assert the response is empty — NOT a firehose. If a should-be-empty query returns data, the filter is silently ignored and client-side filtering will produce wrong data. This would have caught INC-70 on day one. Bound as a §22.3-style pre-flight item for Signals #1 (analyst), #2 (PEAD), #3 (options), #8 (news) — each has its own vendor endpoint whose filters must be probed before trust. Documented at `docs/04-modules/longshort/signals/_pattern-vendor-fetcher-filter-honesty.md`.

**Evidence.** Gate 4 ESLint on changed files — 0 errors, 0 warnings. Gate 4 Vitest — `50 files / 388 tests passed`. Gate 11 Deno scoped to FP-042 surfaces — `70 passed / 0 failed`. Live-DB §22.5.1 confirms `signal_registry.status='planned'` and `job_registry.enabled=false`. **Original FP-042 entry + ACT-155 addendum preserved verbatim per Constitution Rule 8.** Forward-pointer: INC-70 + DW-094 + ACT-156.

| Field | Value |
|---|---|
| **ID** | FP-041 (next-free after FP-040; FP-038 consumed by signal-registry overview). |
| **Status** | implemented (compute + new entitlement-aware fetcher + orchestrator + cron/manual handlers + MIG-076 disarmed `job_registry` seed + signal_registry planned→live flip + Gate-4 tests). Cron wiring + enable-flip pending separate operator-run DEC-043 attestation. |
| **Problem** | Phase 2.3 deliverable: Signal #5 (short-interest changes, 30-day) per CROSSWIND §4.4.3 is the **third of nine** signals and the **first non-price** signal in the stack — it needs a NEW Polygon endpoint, a new TWICE-MONTHLY cadence, and the **first NON-CRITICAL** signal semantics (graceful degradation when data is missing). Without it the combiner sees only price-derived signals (momentum + reversal); adding it introduces the first orthogonal-data signal and exercises the missingness handling path the other 7 non-critical signals will inherit. |
| **Resolution** | Mirror the proven Signal #6 / #7 architecture with the three §4.4.3-driven divergences: (a) NEW pure compute `computeShortInterestChange` returning `-1 × (SI_pct_float[T] - SI_pct_float[T-2_reports])` with `SHORT_INTEREST_MIN_REPORTS=3` (the `-1 ×` negation is LOAD-BEARING — without it this becomes a follow-the-shorts duplicate; the sign-flip pair test pins it). (b) NEW entitlement-aware fetcher `PolygonShortInterestFetcher` — HTTP 403 → typed `subscription_gated`, HTTP 404 → typed `data_unavailable` (NEITHER throws, NEITHER fabricates a zero); 401 / 5xx after retries / parse / timeout throw `SignalComputationError`. (c) `createShortInterestOrchestrator` factory mirroring the reversal orchestrator's 5-step pipeline with `ShortInterestOrchestratorContext` extending `SignalOrchestratorContext` to swap `priceHistory` for `shortInterest`; NON-CRITICAL semantics — `subscription_gated`/`data_unavailable` → typed skip (ticker still ranked); reuses every other shared helper (`pLimitedMap`, `zScoreNormalizeWithinSector`, `captureSignalObservations`, `persistSignalComputeLog`). (d) Cron handler `longshort-short-interest-compute` (verifyCronSecret + productionClock + POLYGON_API_KEY + orchestrator + 3 audit events). (e) Manual sibling `longshort-short-interest-compute-manual` (POST + JWT + `longshort.manage` + dual audit envelope). (f) MIG-076 disarmed `job_registry` seed with TWICE-MONTHLY schedule `'0 21 1,15 * *'` + `signal_registry` planned→live flip (first exercise of the FP-038 template). (g) Extended `SignalSkipReason` enum + `aggregateSkipCounts` with `data_unavailable` + `subscription_gated` (sentinel-stable JSON in `signal_compute_log.skip_counts`). (h) Extended `JOB_ID_TO_SIGNAL_ID` with `'longshort.short_interest.compute' → 'short_interest_change_30d'` so the signal monitor picks up #5 automatically. signal_id locked: `'short_interest_change_30d'`. |
| **Scope** | NEW `_shared/longshort-signals/shared/polygon-short-interest-fetcher.ts` (+ `_test.ts`). NEW `_shared/longshort-signals/short-interest-change/compute-short-interest.ts` (+ `_test.ts`). NEW `_shared/longshort-signals/short-interest-change/short-interest-orchestrator.ts` (+ `_test.ts`). NEW `supabase/functions/longshort-short-interest-compute/{index.ts,index_test.ts}`. NEW `supabase/functions/longshort-short-interest-compute-manual/{index.ts,index_test.ts}`. NEW migration MIG-076 (`job_registry` INSERT disarmed + `signal_registry` UPDATE planned→live). EDIT `_shared/longshort-signals/shared/signal-types.ts` (+ 2 enum members). EDIT `_shared/persist-signal-compute-log.ts` (+ 2 seeded skip_counts keys). EDIT `_shared/longshort-signals/shared/job-signal-mapping.ts` + `_test.ts` (+ 1 entry + 1 cross-ref drift sentinel). Docs same-PR: this entry; ACT-153; `database-migration-ledger.md` MIG-076; `function-index.md` (5 new entries + JOB_ID_TO_SIGNAL_ID extension-point/drift-sentinel update); `event-index.md` (6 new audit events); `docs/04-modules/longshort/signals/short-interest-change.md` (new component doc). |
| **Out of Scope** | FINRA/EDGAR backup fetcher (documented in fetcher header as future hardening; out of FP-041 scope). §3.3e SI > 25% short-book exclusion (lives in existing §3.3 logic). Cron wiring / enable-flip / cron-attributable attestation (separate operator step per DEC-040 + DEC-043). Combiner-stage missingness imputation (Phase 3). Touching momentum / reversal / combiner / universe / Rankings page. New RBAC permission (reuses `longshort.manage` + `longshort.view`). |
| **Reference Impact** | database-migration-ledger.md: MIG-076. function-index.md: 5 new entries (compute, fetcher, orchestrator, cron handler, manual handler) + extension-point update for JOB_ID_TO_SIGNAL_ID. event-index.md: 6 new audit events (`longshort.short_interest.compute.{started,completed,failed,manual_triggered,manual_completed,manual_failed}`). docs/04-modules/longshort/signals/short-interest-change.md: new component doc. No new env-vars (POLYGON_API_KEY already registered); no new configs; no new npm dependencies; no new permissions; no new routes. |
| **Decision ID** | None — twice-monthly cadence is documented in-FP (mirrors universe-quarterly precedent); entitlement-fallback convention (subscription_gated / data_unavailable typed-missing) extends the existing typed-absence discipline; new SignalSkipReason members are additive. No contested decision. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Landed at execution commit. Signal #5 is the third of nine live (in the registry sense — code/migration shipped, cron disarmed). 3 live + 6 planned + 1 planned composite. Validation path: operator POST `longshort-short-interest-compute-manual` with `{ "as_of": "YYYY-MM-DD" }` → either (a) real z-scored short-interest-change observations persist (Stocks Advanced includes short interest), or (b) graceful all-missing degraded outcome with `subscription_gated` count > 0 in `signal_compute_log.skip_counts` — proving the degradation path and flagging that the FINRA backup is needed. Both outcomes are informative; either confirms architecture correctness. Cron wiring per DEC-043 is a separate operator step.

Authority: ACT-153.

#### FP-041 Addendum (revision-fix) — `si_pct_float` derivation (Option A)

| Field | Value |
|---|---|
| **Trigger** | Post-execution manual validation (operator) returned `outcome=completed`, `universe_size=839`, `persisted_count=0`, `insufficient_history=839`, all other skip counts = 0 — entitlement + network + history were fine, but every ticker normalized to zero reports. Read-only investigation traced the cause to `polygon-short-interest-fetcher.ts` `normalizeRow` requiring a `short_percent_of_float` field that Polygon's `/stocks/v1/short-interest` endpoint **does not return**; sample-response field set (verified against live docs) is `ticker / settlement_date / short_interest / avg_daily_volume / days_to_cover`. The anti-phantom guard correctly refused to fabricate a percentage and the failure surfaced cleanly as `insufficient_history` instead of producing garbage z-scores. |
| **Decision** | Option A — derive `si_pct_float = short_interest / share_class_shares_outstanding`, preserving the spec §4.4.3 %-of-float denomination. Option B (`days_to_cover` substitution) was REJECTED — it deviates from §4.4.3 and is inconsistent with §3.3e (also % -of-float) and would require a fresh DEC. The shares-outstanding side input is already available from the SAME Polygon reference endpoint that universe-enrichment uses (`/v3/reference/tickers/{ticker}` returns `share_class_shares_outstanding`); NO new subscription entitlement is introduced. |
| **Conscious approximation (documented per §2 axiom 4)** | The reference endpoint provides ONLY CURRENT shares-outstanding; the signal compares SI at two HISTORICAL report dates (T and T-2 ≈ 30 calendar days). We denominate BOTH historical SI counts with current shares-outstanding. Defensible because shares-outstanding is slow-moving (corporate actions on the order of quarters/years) relative to SI swings (twice-monthly, often orders-of-magnitude); the percentage change is dominated by the SI numerator change. Point-in-time shares-outstanding history (which would account for splits / buybacks shifting the denominator) is not exposed by this endpoint and would require a FINRA + EDGAR cross-source (same backup roadmap as the SI fetcher; intentionally deferred). The approximation is pinned in three places by design: orchestrator file-level comment, `function-index.md` orchestrator row, and `docs/04-modules/longshort/signals/short-interest-change.md`. |
| **Scope** | NEW `_shared/longshort-signals/shared/polygon-shares-outstanding-fetcher.ts` (+ `_test.ts`; 10 tests including missing/zero/negative/NaN → typed-absence; entitlement-aware). EDIT `polygon-short-interest-fetcher.ts` — return shape now `{ report_date, short_interest }` (raw share count); normalizeRow requires finite `short_interest >= 0` and drops missing/negative; zero is VALID (genuinely no shorts). EDIT `short-interest-orchestrator.ts` — context now includes `sharesOutstanding: PolygonSharesOutstandingFetcher`; parallel `Promise.all` fetch per ticker; map raw reports to `ShortInterestReport { si_pct_float }` via derivation; new `missing_shares_outstanding` skip when denominator unavailable; defensive zero-shares re-check inside orchestrator (orchestrator catches divide-by-zero even if fetcher's guard regresses). EDIT `signal-types.ts` — add `missing_shares_outstanding` to `SignalSkipReason` (now 7 members). EDIT `_shared/persist-signal-compute-log.ts` — seed `missing_shares_outstanding: 0` in `aggregateSkipCounts`. EDIT `persist-signal-compute-log_test.ts` — 3 exact-match-assertion updates (stale-test lesson applied). EDIT both handlers (`longshort-short-interest-compute{,-manual}/index.ts`) — construct `PolygonSharesOutstandingFetcher` and pass into ctx (KEEP-IN-SYNC pair). EDIT handler source-sentinel tests — assert `sharesOutstanding` field present. Docs same-PR: this addendum; `function-index.md` (NEW shares-outstanding fetcher entry; UPDATE short-interest fetcher entry + orchestrator entry with derivation + conscious-approximation note + 7-member enum); `docs/04-modules/longshort/signals/short-interest-change.md` (denominator-derivation paragraph + new dataflow diagram + new skip-reason row + dedicated "Conscious approximation" section); ACT-153a (this revision-fix). NO new permissions / routes / migrations / npm dependencies / env-vars. |
| **Anti-pattern guards (Tier A)** | (1) NO fabricated denominator — missing shares → typed `missing_shares_outstanding` skip; (2) DIVIDE-BY-ZERO guard at TWO layers — fetcher rejects zero/negative/NaN/Infinity shares (8 dedicated tests), orchestrator defensively re-checks (test (17) lying-fetcher mock proves it); (3) the spec formula `-1 × (SI%[T] − SI%[T-2])` is preserved — only the SOURCE of `si_pct_float` changes from "phantom field read" to "raw count divided by shares"; (4) NO wall-clock drift — orchestrator timestamps still derive from `as_of`; (5) the conscious-approximation comment is mandatory (the approximation must be VISIBLE — pinned in 3 places). |
| **Verification** | Gate 11 (Deno) scoped to FP-041 + affected shared files: **77 passed / 0 failed** (`polygon-short-interest-fetcher_test.ts` 10/10, `polygon-shares-outstanding-fetcher_test.ts` 10/10, `compute-short-interest_test.ts` 13/13, `short-interest-orchestrator_test.ts` 17/17 — incl. 5 new derivation-path tests, `persist-signal-compute-log_test.ts` 9/9, `longshort-short-interest-compute/index_test.ts` 9/9, `longshort-short-interest-compute-manual/index_test.ts` 9/9). Full Gate 11 (excluding 3 pre-existing broken universe test files unrelated to FP-041): **568 passed / 11 failed** (all 11 failures are pre-existing profile/user-management integration tests requiring a live test Supabase, unrelated to FP-041 — same failures on main). Gate 4 Vitest: **388 passed / 50 files**. Gate 4 ESLint scoped to changed FP-041 files: **0 warnings / 0 errors** (the 15 pre-existing warnings are all in unrelated files on main). No `any`. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure (addendum)** — The compute is now plumbed end-to-end: real `short_interest` and real `share_class_shares_outstanding` → real `si_pct_float` → spec formula → real z-scores. Expected operator-revalidation outcome: `persisted_count ≈ 800+` (z-scored), with `insufficient_history` dropping to only genuinely-short-history names (newly-listed tickers with < 3 SEC reports back to listing date) and `missing_shares_outstanding` capturing the rare reference-endpoint gaps (non-equities, recent delistings). The `signal_registry` coverage column will reflect the real persisted count.

Authority: ACT-153a.


### FP-040: Signal #7 — Short-Term Reversal (1-week)

### FP-038: Signal Registry + "All Signals" Overview

| Field | Value |
|---|---|
| **ID** | FP-038 (the reserved-but-not-yet-consumed signal-registry slot called out by FP-039/FP-040 headers; consumed here per operator approval). |
| **Status** | implemented (MIG-075 + `useSignalRegistry` + `AllSignalsTab` + tab wiring + Gate-4 tests + live-DB §22.5.1 evidence). |
| **Problem** | The Signals hub had per-page surfaces (Rankings, Compute Runs, Coverage) but no single multi-signal overview. Operator-side question "how many of the 9 are live? what's planned next? is anything stale?" required cross-referencing the spec + job_registry + signal_compute_log by hand. No single page told the truth across all signals + the combiner composite. |
| **Resolution** | (1) New index table `public.signal_registry` (MIG-075) with 10 rows: 2 live (`cross_sectional_momentum_12_1` #6, `short_term_reversal_1w` #7) + 7 planned (#1–#5, #8, #9 — `planned_phase` mapped to §4.4 spec ordering Phase 2.3–2.9) + 1 planned composite (Phase 3). §4.4 field values (`display_name`, `spec_ref`, `cadence`, `criticality`) are seeded verbatim from CROSSWIND §4.4.1–§4.4.9. RLS is permission-scoped (`longshort.view`) + 3 RESTRICTIVE deny-writes (per DEC-042 precedent — writes are migration/governance-only). Status is STATIC-seeded — each future signal's FP flips its own row planned → live (no auto-detection in v1). (2) New read-only hook `useSignalRegistry` fetches the registry ordered by `display_order` then joins last-fire + `distinctDates` from `signal_compute_log` for the (bounded) live set; exports `deriveStaleness` (mirrors `longshort-signal-monitor` weekday=36h / Monday=72h thresholds) and `DRIFT_MIN_HISTORY = 30`. (3) New `AllSignalsTab` page: one row per signal + composite, columns `# / signal / spec / cadence / status / last-fire (UTC) / coverage / staleness / drift`. Live rows link to the Rankings tab (the per-signal DETAIL page); planned rows show "—" + a planned-phase badge; composite row shows "Arrives with the combiner (Phase 3)." Drift is a COLUMN with honest states ("Insufficient history" until N ≥ 30 distinct `as_of_date`s), NOT a separate page. (4) `SignalsHubPage` adds the All-Signals tab as the new DEFAULT (the multi-signal index naturally precedes per-signal detail). |
| **Scope** | NEW migration MIG-075 (`signal_registry` + RLS + 10-row seed). NEW `src/features/longshort/hooks/useSignalRegistry.ts`. NEW `src/pages/trading/longshort/signals/AllSignalsTab.tsx`. NEW `src/pages/trading/longshort/signals/__tests__/AllSignalsTab.test.tsx` (5 tests). EDIT `src/pages/trading/longshort/SignalsHubPage.tsx` (register tab + default). Docs same-PR: this entry; ACT-152; `database-migration-ledger.md` MIG-075; `permission-index.md` `longshort.view` "Used by" (+ `signal_registry`); `route-index.md` `/trading/longshort/signals` (page + related tests + functions + implementation + added-by); `component-inventory.md` (new "All-Signals Overview" section). |
| **Out of Scope** | Any change to `RankingsTab` / `ComputeRunsTab` / `CoverageTab` / `SignalDistributionBand`. Any change to compute / orchestrators / crons / `job_registry`. Any new RBAC permission. Any new edge function. Any drift-monitoring page (drift is a column, not a page — explicitly rejected). Any per-signal page (Rankings is signal-generic; registry is the index — explicitly rejected). Any combiner page (composite is one `planned` row until the combiner exists — explicitly rejected). Auto-status-detection (YAGNI; each signal's FP flips its own row). |
| **Reference Impact** | database-migration-ledger.md: MIG-075. permission-index.md: `longshort.view` "Used by" extended with `public.signal_registry`. route-index.md: `/trading/longshort/signals` Page/Related tests/Related functions/Implementation/Added by updated for the All-Signals tab. component-inventory.md: new "Trading / Long-Short Signals — All-Signals Overview (FP-038)" section listing `AllSignalsTab` + `useSignalRegistry` hook module. No new env-vars, configs, npm dependencies, edge functions, audit events, or permissions. |
| **Decision ID** | None — status taxonomy (`live` / `planned` / `deprecated`) is enforced by a DB `CHECK`; permission-scoped RLS rides on DEC-042 precedent; no new contested decision. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Landed at execution commit (HEAD pending). The All-Signals overview is live and is the default Signals-hub tab. Today: 2 of 9 signals live (#6 momentum, #7 reversal) + 7 planned + 1 planned composite. As each future signal lands, its FP flips its registry row `planned → live` in the same migration that arms its compute job, and it appears here + in Rankings automatically. Drift cells will transition from "Insufficient history" to "Available" once a signal accumulates ≥ 30 distinct `as_of_date`s in `signal_compute_log`.

Authority: ACT-152.


| Field | Value |
|---|---|
| **ID** | FP-040 (next-free after FP-039 at HEAD; FP-038 reserved for signal-registry spec — NOT consumed; FP-011..FP-017 burned/reserved per established numbering). |
| **Status** | implemented (code + tests + MIG-074 disarmed seed); cron-wiring + enable-flip pending separate operator-run DEC-043 attestation. |
| **Problem** | Phase 2.2 deliverable: Signal #7 (short-term reversal, 1-week) per CROSSWIND §4.4.2 is the second of nine signals and the first one that deliberately disagrees with momentum (it FADES recent moves; momentum CHASES). Implementing it now provides a momentum/reversal pair in Rankings before the Phase 3 combiner. |
| **Resolution** | Mirror the proven Signal #6 architecture: (a) pure compute `computeReversal` returning `-1 × ((P[T-1]/P[T-6]) - 1)` (the `-1 ×` negation is load-bearing — without it this signal becomes a short-window momentum duplicate); (b) `createReversalOrchestrator` factory matching `createMomentumOrchestrator`'s 5-step pipeline and re-using the shared infra (`pLimitedMap`, `zScoreNormalizeWithinSector`, `PolygonPriceHistoryFetcher`, `captureSignalObservations`, `persistSignalComputeLog`); (c) cron handler `longshort-reversal-compute` (verifyCronSecret + productionClock + POLYGON_API_KEY + orchestrator + 3 audit events); (d) manual sibling `longshort-reversal-compute-manual` (POST + JWT + `longshort.manage` + dual audit envelope); (e) MIG-074 disarmed `job_registry` seed (`enabled=false`). signal_id locked: `'short_term_reversal_1w'`. Bar requirement REVERSAL_MIN_BARS=7; price-history lookback PRICE_HISTORY_LOOKBACK_DAYS=20 calendar days (~14 trading bars, 2× the floor). `job-signal-mapping` extended with `'longshort.reversal.compute' → 'short_term_reversal_1w'` so the existing signal monitor picks the new signal up automatically. |
| **Scope** | NEW: `supabase/functions/_shared/longshort-signals/short-term-reversal/{compute-reversal.ts, compute-reversal_test.ts, reversal-orchestrator.ts, reversal-orchestrator_test.ts}`. NEW: `supabase/functions/longshort-reversal-compute/{index.ts, index_test.ts}`. NEW: `supabase/functions/longshort-reversal-compute-manual/{index.ts, index_test.ts}`. NEW migration MIG-074 (single `INSERT … ON CONFLICT DO NOTHING` into `public.job_registry` — `enabled=false`). EDIT: `supabase/functions/_shared/longshort-signals/shared/job-signal-mapping.ts` (+1 entry) and its `_test.ts` (cross-reference test for reversal SIGNAL_ID + update single-entry guard to two-entry guard). Docs same-PR: this entry, ACT-151, function-index.md (4 new entries + monitor extension-point note), event-index.md (6 new reversal events), database-migration-ledger.md (MIG-074 entry), NEW `docs/04-modules/longshort/signals/short-term-reversal.md`. |
| **Out of Scope** | Cron wiring + enable-flip + cron-attributable attestation (separate operator-run step per DEC-040 + DEC-043). Any change to Signal #6 (momentum), the combiner, the universe, RBAC, audit primitives, façade, trading-navigation, App.tsx routing, or any UI surface. Any fork of shared signal infra. ACT-130 (still reserved for FP-018 Bucket C, already consumed). |
| **Reference Impact** | function-index.md: 4 new entries (compute, orchestrator, cron handler, manual handler) + `job-signal-mapping` extension-point note. event-index.md: 6 new events (`longshort.reversal.compute.{started,completed,failed,manual_triggered,manual_completed,manual_failed}`). database-migration-ledger.md: MIG-074. `docs/04-modules/longshort/signals/short-term-reversal.md` created. No new permissions, configs, env-vars, routes, npm dependencies. No new shared helper (extends an existing one). |
| **Decision ID** | None — the sign-flip negation is documented in-code (compute header) + this FP entry; no DEC needed because §4.4.2 is verbatim spec. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Landed at execution commit (HEAD pending). Compute math, orchestrator, cron + manual handlers, disarmed registry row (MIG-074), and 6 new audit events ship together; cron path remains explicitly UNATTESTED until the separate operator-run wire-and-attest step produces a 200 + cron-attributable `signal_compute_log` row per DEC-043. The manual handler is the immediate verification path.

Authority: ACT-151.

### FP-037: Top-N Selector (20/30/50) on Rankings

| Field | Value |
|---|---|
| **ID** | FP-037 (next-free verified by grep at HEAD — 0 prior references). |
| **Status** | approved (operator-directed forward 2026-06-08 — Tier C; frontend-only, presentational, read-only, Monday-safe). |
| **Problem** | The top/bottom cutoff on the Rankings page is hardcoded to TOP_N = 20 / BOTTOM_N = 20. The operator cannot preview what top-30 or top-50 looks like before 9 signals compete for screen space. |
| **Resolution** | Replace constants with a single `topN` state (default 20), driven by a compact `<Select>` (options 20 / 30 / 50). Long and short cutoffs move together symmetrically (bottomN = topN). All existing usages (slice, `SignalDistributionBand` props, table titles, `startingRank`) read the state value. |
| **Scope** | EDIT: `src/pages/trading/longshort/signals/RankingsTab.tsx` (constants → state + selector; all usages read state). EDIT: `src/pages/trading/longshort/signals/__tests__/RankingsTab.test.tsx` (+1 test — selector changes cutoff to 50, titles update). Docs same-PR: `docs/08-planning/feature-proposals.md` (this entry), `docs/06-tracking/action-tracker.md` ACT-149, `docs/07-reference/component-inventory.md` (RankingsTab description + FP-037 note), `docs/07-reference/route-index.md` (signals hub description + related tests). |
| **Out of Scope** | Any data/query change (server-paginated full list unchanged; only top/bottom CUTOFF changes). Any other tab. Any new dependency. Any edge function, migration, cron, sql/14, signal-math, FP-018 Bucket C surface touch. ACT-130 (still reserved for FP-018 Bucket C). |
| **Reference Impact** | component-inventory.md: RankingsTab description + FP-037 note. route-index.md: signals hub description + related tests update. feature-proposals.md: this entry. action-tracker.md: ACT-149. No new permissions, events, configs, env-vars, migrations, routes, edge functions, or shared helpers. No new npm dependency. |
| **Decision ID** | None — frontend, presentational, within existing UI-design-system discipline. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Landed at execution commit (HEAD pending). Rankings page shows a 20/30/50 selector (default 20) controlling the top/bottom cutoff; titles and distribution-band accents update dynamically; full Gate-4 green. Zero data/query change. Zero Monday/edge/migration touch.

Authority: ACT-149.

---

### FP-045 Phase 2 + Phase 3 Revision Addendum (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-09 |
| **Authority** | Operator REVISION-FIX directive (CI ESLint red post-merge at `b4f4941` and `5396165`); Gate-4 substitution failure logged in `docs/ai-failure-modes.md`. |
| **Phase 2 Disposition** | `closed-with-revision` — Phase 2 commit `b4f4941` shipped 47 `@typescript-eslint/no-explicit-any` errors across 5 new Deno test files (`queue-finalizer_test.ts` 11, `queue-init_test.ts` 6, `queue-slice-worker_test.ts` 10, `queue-sweeper_test.ts` 6, `pead-queue-adapter_test.ts` 14). False-green Gate 4 was produced by running `deno lint` instead of the CI workflow's `npx eslint .`. Production-code surfaces (engine + RPCs + handlers) are untouched by the revision; types-only change in test files. Revision commit (this PR) restores CI green: `npx eslint . → 0 errors / 15 baseline warnings`; `deno test` queue + PEAD suites → 103 passed / 0 failed. |
| **Phase 3 Status (§22.3(e) update)** | Phase 3 commit `5396165` inherited the same Gate-4 substitution and the same 14 errors in `pead-queue-adapter_test.ts`. Status amended to: PEAD registration is functionally complete (registry entry + adapter + enqueue shim + MIG-084 disarmed rows + drift sentinels all green on CI post-revision); the disposition lineage is `landed-with-revision` (Phase 2 + Phase 3 both green at revision SHA below). Operator test-fire of the full queue path remains the validation gate before Phase 4. |
| **Forward-binding Gate-4 discipline** | Gate 4 for this FP and the queue-worker module going forward is the CI workflow's ESLint command verbatim: `npx eslint .` (source: `.github/workflows/strong-evidence.yml`). `deno lint` is supplementary only and is NEVER acceptable as Gate-4 evidence. Every PR's Gate-4 evidence block MUST state the exact command line above its output. Codified in `docs/04-modules/longshort/signals/queue-worker.md` ("Gate-4 discipline" section). `@ts-nocheck` does NOT silence ESLint — typed-mock convention (`unknown` + narrow stubs) is the only acceptable pattern for Deno test files. |
| **Scope (this revision)** | EDIT only: 5 test files (literal `any` → `unknown`; removed obsolete `no-explicit-any` from `deno-lint-ignore-file` directive). Docs same-PR: this addendum; `docs/ai-failure-modes.md` tool-substitution entry; `docs/04-modules/longshort/signals/queue-worker.md` Gate-4 discipline section. ZERO production-code change; ZERO test-behavior change. |
| **Original preserved** | FP-045 entry text remains VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | `docs/ai-failure-modes.md` (tool-substitution entry, this PR date); revision commit SHA pasted into the PR evidence block. |

### FP-045 Phase 3 Validation + Phase 4 Authorization Addendum (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-10 |
| **Authority** | Operator-fired Phase 3 validation test of the full PEAD queue path (init → 9 slices → CAS aggregation barrier → in-process finalizer → `signal_compute_log` + `signal_observations`), 2026-06-10 00:01:32 → 00:10:00 UTC. INVESTIGATION-mode polling + live read-only Finnhub probe series (96-name stratified draw) confirmed the run's evidence body and resolved the only surfaced prediction-vs-observation mismatch (DEC-052 floor attrition) as a vendor-pivot prior-update need, not a code defect. |
| **Phase 3 Disposition** | `validated`. Run `signal_queue_runs.run_id=451b9ee7-9703-429d-97bc-61aeb2697bbc` shipped `outcome='completed'`, `persisted_count=835/839`, 9 slices, CAS-won on slice 9 only, finalizer fired once, zero `slice.failed` / `run.failed` / 429 / stale-heartbeat. Per-slice instrumentation (FP-045 Phase 2 INC-72 fix) verified end-to-end with 9 × `longshort.signal_queue.slice.completed` audit events carrying `{claimed, succeeded, skipped, cas_won, empty}` metadata. Z-distribution: n=835, mean=−0.0156, std=0.8865, min/max=±3.0000 (±3 clip honored), 16 clipped (11 high + 5 low), 1 null (`missing_sector` finalizer-time passthrough). Skip breakdown: `no_recent_earnings=3` (expected — names with >1000 trading days stale per §4.4.6), `zero_dispersion=1` (SATS — vendor ordering anomaly, watch-only per the new note in `docs/04-modules/longshort/signals/pead.md`), `missing_sector=1`, `pead_panel_below_floor=0`. The DEC-052 floor-attrition surprise (predicted ~40-52% small-cap loss per ACT-159 Part A; observed 0) is **NOT a code defect** — adapter row-selection is byte-identical to the FP-044 orchestrator's per-ticker arm; the floor is the first gate in `compute-pead.ts:107-113`; the fetcher preserves `numberAnalysts` verbatim. Causal chain + production-vendor live-probe evidence + Part A dated correction note are recorded in the ACT-159 Addendum (`docs/06-tracking/action-tracker.md`, 2026-06-10). DEC-052 STANDS as a vendor-regression guard; expected attrition on Finnhub Estimate-1 for this universe is ~0, not 40-52%. |
| **Phase 4 Status (forward-binding)** | `authorized-pending-operator-greenlight`. Phase 3 evidence (queue engine validated end-to-end on a live rate-capped consumer; per-slice instrumentation working; CAS race + finalizer idempotency exercised; vendor-cap pacing held with zero 429s) is the validation-gate condition the original FP-045 entry's Phase 4 step bound to. Phase 4 scope unchanged from the original entry: queue-config registration for Signal #3 consuming FP-043's `TradierOptionsChainFetcher` + `computeOptionsFlow` + per-ticker arm UNTOUCHED; `signal_registry.options_flow_imbalance_5d` planned→live (or its existing flipped state if already live); DW-095 closure with this FP as evidence; DEC-047 status → implemented at Phase 4 close; FP-043 closure addendum; MIG-085 (next-free) for the `job_registry` row(s); §22.5.1 live-DB read. Awaits operator greenlight before execution. |
| **Forward-binding Gate-4 discipline** | Unchanged — Gate 4 = `npx eslint .` verbatim (the CI workflow's command, source `.github/workflows/strong-evidence.yml`); `deno lint` remains supplementary diagnostic only. Codified in `docs/04-modules/longshort/signals/queue-worker.md` ("Gate-4 discipline" section); revision-addendum above is the binding precedent. |
| **Original preserved** | FP-045 entry text and the prior Phase 2 + Phase 3 Revision Addendum remain VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | `docs/06-tracking/action-tracker.md` (ACT-159 Addendum, 2026-06-10); `docs/04-modules/longshort/signals/pead.md` (SATS vendor-ordering watch-only note); `docs/ai-failure-modes.md` §12.10 entry `prior-invalidated-by-vendor-pivot` (2026-06-10). |

### FP-045 Phase 4 Closure Addendum — Signal #3 Options-Flow Revival (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-10 |
| **Authority** | Operator EXECUTION-mode greenlight ("Phase 4 — EXECUTE: Signal #3 (options-flow) Revival on the Proven Queue Engine + Stranded-Handler Fix"). Phase 3 validation (run `451b9ee7`) satisfies the validation-gate condition the original FP-045 entry's Phase 4 step bound to. |
| **Phase 4 Disposition** | `landed-pending-operator-validation`. Options-flow is registered as the second queue-worker consumer; the chunked-coordinator architecture (FP-043 Phase 3) is retired in favor of the FP-045 cursor-drain engine. MIG-085 records the metadata flip (`signal_registry.options_flow_imbalance_5d` planned→live; `job_registry.longshort.options_flow.compute` description updated to the queue-shim shape, row preserved per FP-045 §5). The handler set: (a) `longshort-options-flow-compute` gutted to enqueue shim (cron, byte-parallel to PEAD); (b) `longshort-options-flow-compute-manual` gutted to manual init shim (operator JWT + `longshort.manage` + parseAsOfDate + future-date guard preserved); (c) `longshort-pead-compute-manual` FIXED — was stranded importing the synchronous orchestrator (would have 504'd if fired per INC-72), now gutted to manual init shim parallel to the options-flow manual; (d) `longshort-options-flow-worker` deprecated to 410 Gone (handler preserved, FP-043 chunk-runner module remains in tree as the canonical per-ticker semantics pin). Adapter `options-flow-queue-adapter.ts` mirrors `runOptionsFlowChunk`'s per-ticker arm verbatim — no FP-043 compute/fetcher/classifier edits. Registration `options-flow-queue-registration.ts` pins `(signalId='options_flow_imbalance_5d', jobId='longshort.options_flow.compute', ratePerSec=1.7, callsPerName=1, sliceSize=80, heartbeatTimeoutSec=600, stagingTtlSec=86400)` with the wire-call arithmetic row asserted by drift sentinel (`80 × 2 / 1.7 ≈ 94.1s` < 150s wall). DW-095 closed; FP-043 closure addendum appended below. Operator validation test-fire is the next step; combined arm-up (PEAD + options-flow per DEC-040/043) follows. |
| **Pre-flight Tradier authentication probe** | NOT performed at code-time: no in-tree chokepoint exists to probe one Tradier symbol from this loop without standing up disposable infrastructure (the existing fetchers live behind the worker handler that we just 410'd, and the manual handler now enqueues rather than fetching synchronously). The `TRADIER_API_KEY` secret remains configured (verified via the secrets list — `Secret Name: TRADIER_API_KEY` present). 401/403 on entitlement will surface in the operator's validation test-fire IMMEDIATELY at slice 1 as `subscription_gated` skips for every ticker (the engine's typed-skip taxonomy preserves this — no silent zero), NOT as a cron-time failure mode. Recorded honestly per the anti-completion-theater + anti-phantom-default disciplines: the validation test-fire IS the live-probe gate for this consumer. |
| **Arithmetic & gate evidence** | Pre-flight row pinned: `sliceSize=80 × callsPerName=2 (wire) / ratePerSec=1.7 ≈ 94.1s` per slice, ≈37% headroom vs 150s HTTP wall. Full-run estimate: 1,680 wire calls / 1.7 rps ≈ 988s spread across ⌈840/80⌉ = 11 slices ≈ 11 minutes wall-time. Gate 2 (wall-clock scanner): `deno run --allow-read scripts/check-wall-clock.ts` → CLEAN (0 violations on the changed files). Gate 4 (CI ESLint command verbatim): `npx eslint .` → 0 errors / 15 baseline warnings. Gate 11 (Deno tests): all FP-045 Phase-4 suites green (`options-flow-queue-adapter_test.ts` 9 tests; `options-flow-queue-registration_test.ts` 6 tests; gutted handler `_test.ts` suites for `longshort-options-flow-compute` 10, `longshort-options-flow-compute-manual` 8, `longshort-pead-compute-manual` 9, `longshort-options-flow-worker` 4). |
| **Forward-binding Gate-4 discipline** | Unchanged — Gate 4 = `npx eslint .` verbatim (CI workflow command, `.github/workflows/strong-evidence.yml`); `deno lint` remains supplementary diagnostic only. Codified in `docs/04-modules/longshort/signals/queue-worker.md`. |
| **Out of scope** | NO cron arming (combined arm-up is the operator's next step). NO Signal #1 / #8. NO deletion of FP-043 files. NO compute edits. |
| **Original preserved** | FP-045 entry text + the Phase 2 / Phase 3 / Phase 3-validation addendums remain VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | `docs/07-reference/database-migration-ledger.md` (MIG-085); `docs/08-planning/deferred-work-register.md` (DW-095 closure); `docs/08-planning/approved-decisions.md` (DEC-047 status → implemented); `docs/04-modules/longshort/signals/queue-worker.md` (Phase 4 options-flow registration section); `docs/04-modules/longshort/signals/options-flow.md` (LIVE-ON-QUEUE banner); FP-043 Closure Addendum (below). |

### FP-043 Closure Addendum — Orchestration Shell Replaced by FP-045 Phase 4 (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-10 |
| **Authority** | FP-045 Phase 4 closure (this PR) realizes the DW-095 rebuild path declared in the original FP-043 entry's park clause + ACT-158 deferral. |
| **Status Update** | `closed-with-orchestration-replaced`. The original FP-043 "implemented" status (Phase 1 fetcher / Phase 2 compute + orchestrator / Phase 3 chunked coordinator+worker / Phase 4 MIG-078) stands for the COMPUTE deliverable (fetcher + `computeOptionsFlow` + classifier + token-bucket + within-sector z-score). The orchestration SHELL (`runOptionsFlowCoordinator` + `longshort-options-flow-worker` HTTP fan-out) is replaced by the FP-045 cursor-drain queue-worker — the operational availability path the FP-043 park clause anticipated. Module files: `compute-options-flow.ts`, `tradier-options-chain-fetcher.ts`, `token-bucket.ts`, `options-flow-orchestrator.ts`, `options-flow-coordinator.ts`, `options-flow-chunk-runner.ts` ALL REMAIN IN TREE UNCHANGED (FP-043 preservation promise). The chunk-runner stays as the canonical per-ticker semantics pin; `options-flow-queue-adapter.ts` mirrors it verbatim. The single-process orchestrator stays as the unit-replay path for diagnostic small-universe runs. |
| **What changes at the consumer boundary** | (a) `longshort-options-flow-compute` cron handler — gutted to enqueue shim (name + MIG-078 row preserved). (b) `longshort-options-flow-compute-manual` — gutted to manual init shim (auth + parseAsOfDate + future-date guard preserved). (c) `longshort-options-flow-worker` — returns 410 Gone with a structured pointer at the new enqueue paths; handler file preserved per FP-043 promise. |
| **Original preserved** | FP-043 entry text remains VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | FP-045 Phase 4 Closure Addendum (above); MIG-085; DW-095 closure; DEC-047 status flip. |

### FP-039: Cron-Auth Outage Remediation (atomic env-rotate + redeploy + command-reconcile across 6 consumers)

| Field | Value |
|---|---|
| **ID** | FP-039 (FP-038 reserved for signal-registry spec — NOT consumed here; highest live prior FP at HEAD is FP-037). |
| **Status** | Applied + verified (operator-run out-of-band, 2026-06-08). Bucket-C-unblocking. Supersedes FP-019 as a standalone fix (FP-019 was partial — see FP-019 Addendum below). |
| **Problem** | Every cron consumer (jobids 34/35/36/37/48/51) returned 401 Unauthorized on every fire; `cron.job_run_details.status='succeeded'` concealed the failure. Root cause: three concurrent defects — divergent secret values across cron commands (5 jobs on `ee867b97…`, jobid:48 on `076426…`), deployed `CRON_SECRET` env var matching neither, and INC-63 redeploy-required constraint (env binds at deploy). FP-019's partial corrective aligned 5 command-sides but missed jobid:48 and skipped the env+redeploy step — net effect was 0% cron auth success until FP-039. See INC-69 for full surface story. |
| **Resolution (operator-run, applied + verified 2026-06-08)** | One atomic remediation: (1) generated a new canonical `CRON_SECRET` value; (2) updated the Supabase Edge Function secret `CRON_SECRET` to that value; (3) redeployed all 6 consumers so they bind the new env at runtime (`longshort-momentum-compute`, `longshort-universe-quarterly-refresh`, `job-health-check`, `job-alert-evaluation`, `job-metrics-aggregate`, `job-audit-cleanup`); (4) reconciled all 6 cron commands via `cron.schedule(...)` idempotent upserts so every `X-Cron-Secret` header carries the canonical value. Per §22.5.3 plaintext-secret out-of-band path: no MIG allocated. |
| **End-to-end evidence (DEC-043 standard)** | (a) Platform jobs 34/35 returned **200 `{"state":"succeeded","success":true}`** on schedule 2026-06-08 10:02–11:58 UTC (first 200s in the retention window). (b) Momentum jobid:51 test-fired and produced **two cron-attributable `signal_compute_log` rows** — `completed_at` 2026-06-08 11:52:00 and 11:54:00 UTC, `as_of_date` 2026-06-08, `outcome=completed`, `persisted_count=834` (real wall-clock; not the midnight-manual signature). (c) Byte-match attestation for jobid:51: exactly 1 `cron.job` row, `schedule = '0 20 * * 1-5'` byte-identical to `job_registry.schedule`, `active=true`, command carries resolved project ref + real secret (no placeholder literals). |
| **Scope (atomic, all-or-nothing across 6 consumers)** | Env-var rotate + Edge Function redeploy ×6 + `cron.schedule()` reconcile ×6 in one operator session. Treating this as 6 separate fixes would have left a partial-success state similar to FP-019. |
| **Out of Scope** | Warmup jobs 29-33 (405s — pre-existing, separate INC when addressed). CRON_SECRET rotation to a chat-clean value (deferred to DW-091, which is updated-not-closed — see DW-091 addendum). HEAD c443000 reconciliation (unrelated). Any code/migration/schema/RLS change. |
| **Reference Impact** | `docs/06-tracking/incidental-findings.md` (INC-69); `docs/08-planning/approved-decisions.md` (DEC-043); `docs/00-governance/definition-of-done.md` (new DoD checklist item referencing DEC-043); `docs/06-tracking/action-tracker.md` (ACT-150 + ACT-130); `docs/08-planning/deferred-work-register.md` (DW-091 addendum row); `sql/14_longshort_signal_cron_schedule.sql` (header-comment correction — canonical live-verified pattern is now jobid:51, NOT jobid:48). No new permissions, events, configs, env-vars, migrations, routes, edge functions, shared helpers, or npm deps. No MIG (operator-run plaintext-secret out-of-band per §22.5.3). |
| **Decision ID** | DEC-043 (the new scheduled-job-attestation rule motivated by INC-69 — verbatim codification of "enabled ≠ scheduled ≠ authenticated; attestation requires end-to-end 200 + real artifact row"). |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Applied + verified out-of-band by operator 2026-06-08. The 6-consumer cron-auth path is restored end-to-end; first cron-attributable 200s + artifact rows in the project's retention history landed at 10:02–11:58 + 11:52/11:54 UTC. FP-018 Bucket C now closeable per ACT-130 on stronger evidence than originally planned. Forward-binding: DEC-043 is the binding attestation standard going forward; DW-091 remains open with a hard tripwire to close before Phase 8 (small live capital).

Authority: ACT-150 (FP-039/INC-69 governance authoring) + ACT-130 (FP-018 Bucket C closure).

---

### FP-019 Addendum (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-08 |
| **Authority** | INC-69 + FP-039 + DEC-043. |
| **Reframe** | FP-019 is reframed **partial / ineffective as a standalone fix**. (a) FP-019 aligned 5 cron command-sides (jobids 34/35/36/37/51) to send `ee867b97…`, but did NOT touch jobid:48 (`longshort-universe-quarterly-refresh`), which continued to send `076426…`. (b) FP-019 did NOT complete the env-var + Edge Function redeploy step (INC-63 redeploy-required constraint), so the deployed function instances continued to read whatever stale `CRON_SECRET` value they had bound at their last deploy. Net effect: 0% cron auth success until the FP-039 unified remediation. |
| **Original preserved** | The original FP-019 entry, its closure attestation, and its INC-64 cross-reference remain VERBATIM per Constitution Rule 8. This addendum is a forward-pointer ONLY — no silent edit of the original. |
| **Forward-pointer** | See INC-69 (full surface story) + FP-039 (the unified corrective that succeeded) + DEC-043 (the attestation rule motivated by this failure mode). |

---

### FP-009 Addendum (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-08 |
| **Authority** | INC-69 + FP-039 + DEC-043. |
| **Reframe** | FP-009's prior "live and working" attestation for the `longshort-universe-quarterly-refresh` cron path (jobid:48) applies to **MANUAL triggers only**. Live-DB scan of `net._http_response` across the retention window returned **zero 200s** for jobid:48; every `universe_refresh_log` row in the retention window is a MANUAL signature, **never cron-attributable**. The cron auth path for jobid:48 had never worked end-to-end prior to FP-039. |
| **Status post-FP-039** | jobid:48's cron auth is now FIXED by FP-039's unified canonical secret + redeploy. A cron-attributable `universe_refresh_log` row still requires a quarter-start fire (per `0 0 1 1,4,7,10 *` cadence) to confirm end-to-end — this is a follow-up evidence item, NOT yet closed. Flag for the next quarterly boundary verification. |
| **Original preserved** | The original FP-009 attestation remains VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | INC-69 + FP-039 + DEC-043. |

---

### FP-018 Bucket B Addendum (Rule 8 — Forward-Pointer, Original Preserved)

| Field | Value |
|---|---|
| **Addendum Date** | 2026-06-08 |
| **Authority** | INC-69 + FP-039 + ACT-130. |
| **Reframe** | FP-018 Bucket B's momentum cron-fire attestation was **premature**. At Bucket B closure, only manual-midnight `signal_compute_log` rows existed for `longshort.momentum.compute`; no cron-attributable row had ever landed (jobid:51 was returning 401 on every fire per INC-69). The byte-match attestation (`cron.job` row present, `active=true`, schedule byte-identical) was correct as far as it went, but DEC-043 now classifies it as **necessary-but-insufficient** for "cron-attributable fire verified". |
| **Status post-FP-039 / ACT-130** | Bucket C closure (ACT-130) lands the end-to-end evidence (cron-attributable `signal_compute_log` rows at 2026-06-08 11:52/11:54 UTC, real wall-clock, persisted 834) that Bucket B's attestation pre-emptively claimed. The corrected attestation chain is: Bucket B = `cron.job` row + byte-match (scheduling/config evidence per DEC-040); Bucket C = end-to-end 200 + real artifact row (authentication/execution evidence per DEC-043). |
| **Original preserved** | The original Bucket B closure text remains VERBATIM per Constitution Rule 8; this addendum forward-points only. |
| **Forward-pointer** | INC-69 + FP-039 + DEC-043 + ACT-130 (Bucket C closure). |

---

### FP-018 Bucket C — CLOSED (ACT-130, 2026-06-08)

| Field | Value |
|---|---|
| **Closure Date** | 2026-06-08 |
| **Closure Authority** | ACT-130 (reserved ID now consumed). |
| **Evidence (DEC-043 end-to-end standard)** | (a) **Cron-attributable `signal_compute_log` rows for jobid:51** — `completed_at` 2026-06-08 11:52:00 UTC and 11:54:00 UTC, `as_of_date` 2026-06-08, `outcome=completed`, `persisted_count=834`. These are real wall-clock timestamps (NOT the midnight-manual signature of every prior row). (b) **Byte-match attestation for jobid:51** — exactly 1 `cron.job` row; `schedule = '0 20 * * 1-5'` byte-identical to `job_registry.schedule` for `longshort.momentum.compute` (`enabled=true`); `active=true`; command carries resolved project ref + real secret (no placeholder literals). (c) **Authentication evidence** — `net._http_response` for jobid:51 returned 200 post-FP-039 (first 200 in the retention window). |
| **Comparison to originally-planned closure** | Originally planned: passive observation at 2026-06-08 20:00 UTC of a single cron-attributable `signal_compute_log` row. **Actually delivered: stronger evidence** — two cron-attributable rows from a controlled test-fire, plus the unified cross-consumer 200s confirming the auth path is restored across all 6 cron consumers (not just jobid:51). The test-fire path also surfaced INC-69, which a passive 20:00 observation would NOT have caught (the concealing `cron.job_run_details.status='succeeded'` metric would have read green against a still-broken auth path). |
| **FP-009 phase-gate re-check** | Per the original Bucket C definition, the FP-009 phase-gate re-check is OWED — the `master-plan.md` phase-gate checkbox that was reverted at INC-62 surfacing time is eligible for re-tick on this evidence. Phase-gate update is a separate action (per ACT-012 phase-gate protocol) and is left to the next phase-boundary sweep. |
| **Forward-pointer** | INC-69 (the outage discovered during this closure's test-fire); FP-039 (the corrective that enabled this closure); DEC-043 (the new attestation standard this closure satisfies). |

Authority: ACT-130.

### FP-037: Top-N Selector (20/30/50) on Rankings

| Field | Value |
|---|---|
| **ID** | FP-037 (next-free verified by grep at HEAD — 0 prior references). |
| **Status** | approved (operator-directed forward 2026-06-08 — Tier C; frontend-only, presentational, read-only, Monday-safe). |
| **Problem** | The top/bottom cutoff on the Rankings page is hardcoded to TOP_N = 20 / BOTTOM_N = 20. The operator cannot preview what top-30 or top-50 looks like before 9 signals compete for screen space. |
| **Resolution** | Replace constants with a single `topN` state (default 20), driven by a compact `<Select>` (options 20 / 30 / 50). Long and short cutoffs move together symmetrically (bottomN = topN). All existing usages (slice, `SignalDistributionBand` props, table titles, `startingRank`) read the state value. |
| **Scope** | EDIT: `src/pages/trading/longshort/signals/RankingsTab.tsx` (constants → state + selector; all usages read state). EDIT: `src/pages/trading/longshort/signals/__tests__/RankingsTab.test.tsx` (+1 test — selector changes cutoff to 50, titles update). Docs same-PR: `docs/08-planning/feature-proposals.md` (this entry), `docs/06-tracking/action-tracker.md` ACT-149, `docs/07-reference/component-inventory.md` (RankingsTab description + FP-037 note), `docs/07-reference/route-index.md` (signals hub description + related tests). |
| **Out of Scope** | Any data/query change (server-paginated full list unchanged; only top/bottom CUTOFF changes). Any other tab. Any new dependency. Any edge function, migration, cron, sql/14, signal-math, FP-018 Bucket C surface touch. ACT-130 (still reserved for FP-018 Bucket C). |
| **Reference Impact** | component-inventory.md: RankingsTab description + FP-037 note. route-index.md: signals hub description + related tests update. feature-proposals.md: this entry. action-tracker.md: ACT-149. No new permissions, events, configs, env-vars, migrations, routes, edge functions, or shared helpers. No new npm dependency. |
| **Decision ID** | None — frontend, presentational, within existing UI-design-system discipline. |
| **Reviewed By** | Operator |
| **Review Date** | 2026-06-08 |

**Closure** — Landed at execution commit (HEAD pending). Rankings page shows a 20/30/50 selector (default 20) controlling the top/bottom cutoff; titles and distribution-band accents update dynamically; full Gate-4 green. Zero data/query change. Zero Monday/edge/migration touch.

Authority: ACT-149.
